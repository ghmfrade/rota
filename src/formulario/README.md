# formulario/

Formulário: edita a operação de um Autos, chama OSRM, calcula e congela; exporta JSON de operação + PDF operacional (Spec 04). Depende apenas de `shared/`; nunca importa de `comparador/` — a comunicação entre ferramentas é só por arquivo JSON (RN-097).
