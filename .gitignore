# Configuracao local/pessoal do Claude Code (caminhos e permissoes por maquina) —
# nunca versionar; cada ambiente (Windows local, Codespaces) tem o seu.
.claude/settings.local.json

# Dependencias e build do Next.js (criados na TASK-001)
node_modules/
.next/
.next-e2e-controlado/
out/
dist/
coverage/
next-env.d.ts
*.tsbuildinfo

# Artefatos de teste (Playwright)
test-results/
playwright-report/

# Ambiente e logs
.env
.env.local
*.log

# Corridas do orquestrador (docs-dev/20): ledger, sinais e relatorio sao registro
# de execucao efemero, como o ultimo-test-all.log — nunca fonte de verdade.
docs-dev/20-ORQUESTRACAO/corridas/

# Sistema operacional
.DS_Store
Thumbs.db

certificates