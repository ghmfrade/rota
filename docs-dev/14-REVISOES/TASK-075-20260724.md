# Revisão da TASK-075

**Revisor:** Claude (revisão de aderência, `/revisar-aderencia`)
**Data:** 2026-07-24
**Commit/branch revisado:** `cf13b46` (branch `redesign`), único commit da entrega. Working tree limpo no momento da revisão.

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

No fluxo "novo" da etapa Identificação, escolher um Autos no dropdown deixa de comitar a identidade na hora: passa a popular um candidato local (`codigoCandidato`, estado de componente, nunca elevado à sessão) exibido numa pré-visualização (código, denominação da linha, empresa, tipo, selo `operante`/não operante), trocável livremente. O botão "Confirmar Autos" é o único gesto que cria o documento e congela `codigo`/`empresa` (Spec 04 §5). Autos `operante: true` reforça, na própria pré-visualização, a recomendação de carregar o JSON vigente (Spec 04 §3.1). Arquivos: `src/formulario/identificacao/pre-visualizacao.ts` (novo — `preVisualizacaoDeAutos`/`identidadeDeAutosEstatico`, funções puras), `src/formulario/identificacao/identificacao.tsx`, `src/formulario/identificacao/index.ts` (barrel), e ajuste do passo de confirmação em `testes/e2e/identificacao.spec.ts`, `servicos.spec.ts`, `etapa-itinerarios.spec.ts`, mais teste unitário novo `testes/unitarios/formulario/identificacao-pre-visualizacao.test.ts`.

## Regras RN verificadas

- RN-016 (identificação vem das listas estáticas) — atendida. `preVisualizacaoDeAutos`/`identidadeDeAutosEstatico` seguem lendo exclusivamente `ListasAutosEmpresas`; nenhum dado novo inventado. Testada em `identificacao-pre-visualizacao.test.ts`.
- RN-023/DEC-034 (troca de tipo reconverte Serviços, nunca bloqueia) — atendida, inalterada pela task (fora de escopo declarado); E2E `identificacao.spec.ts` continua cobrindo o caminho após a confirmação.
- RN-096/NEG-004 (nada de persistência indevida; pendências/estado efêmero) — atendida e reforçada: o candidato da pré-visualização é estado local do componente (`useState` em `identificacao.tsx:66-68`), nem sessão nem contrato — mais efêmero ainda do que a identidade de sessão que a regra já cobria.
- RN-011 (`autos.status` proposta/vigente) — atendida: `identidadeDeAutosEstatico` (`pre-visualizacao.ts:47-54`) comita `status: "proposta"`, testado explicitamente.
- RN-008..015 (contrato JSON fechado, sem campo novo) — atendida: `denominacao_linha` (só das listas estáticas) é usada exclusivamente na pré-visualização (`PreVisualizacaoAutos`) e comprovadamente **não** entra em `IdentidadeAutos` — teste dedicado (`identidade não tem denominacaoLinha nem denominacao_linha`, chaves exatas `["codigo","empresa","status","tipo"]`).

## Specs verificadas

- Spec 04 §5 — aderente: código/empresa não editáveis após a confirmação (E2E prova ausência de `campo-codigo`/seletor antes de confirmar e presença travada depois); tipo segue editável.
- Spec 04 §3.1 (parte final) — aderente: reforço de carregar o JSON vigente exibido quando `operante: true`, na pré-visualização (evidência: `data-testid="reforco-operante"`, E2E `identificacao.spec.ts:136`).
- DEC-064 — aderente ponto a ponto: candidato trocável livremente sem efeito colateral, "Confirmar Autos" como ato único de criação, reforço `operante` na pré-visualização. Nenhum desvio da decisão registrada em `10-DECISION_LOG.md`.

## Pontos corretos

- Separação limpa entre função pura de exibição (`preVisualizacaoDeAutos`) e função pura de commit (`identidadeDeAutosEstatico`), cada uma testada isoladamente, inclusive o caso negativo de código inexistente (`undefined`, sem exceção).
- Estado do candidato mantido fora da sessão — reforça a garantia de RN-096 em vez de apenas preservá-la.
- Testes cobrem exatamente o ponto crítico de contrato (RN-008..015): prova negativa de que `denominacao_linha`/`denominacaoLinha` não vazam para `IdentidadeAutos`.
- Uso do design system aderente (DEC-050): `Botao variante="primario"`, `Painel tom="informativo" elevacao="plana"`, `Selo tom="sucesso"/"neutro"` — todos props existentes de `shared/ui` (`botao.tsx`, `painel.tsx`, `selo.tsx`), sem `style=` inline.
- `data-testid`/`aria-*` existentes preservados (`seletor-autos`, `campo-codigo`, `campo-empresa`, `select-tipo`, `selo-status-identificacao`); testids novos (`previa-autos`, `confirmar-autos`, `selo-situacao-previa`, `reforco-operante`) só na superfície nova.
- E2E dos três specs afetados atualizados de forma deliberada (passo extra de `confirmar-autos`), sem enfraquecer as asserções pré-existentes — pelo contrário, o teste principal de `identificacao.spec.ts` ficou mais rigoroso (agora prova a ausência de comprometimento antes de confirmar e a troca de candidato B→A antes de confirmar).

## Problemas encontrados

Nenhum.

## Violações de escopo

Nenhuma. O "Fora de escopo" da task (modo carregado inalterado; editabilidade do tipo preservada; nenhum campo novo de contrato; sem desfazer confirmação) foi respeitado integralmente — conferido linha a linha no diff.

## Testes avaliados

- cobertura das RN da task: sim — RN-016, RN-011, RN-008..015 (contrato) com teste unitário direto; RN-023/RN-096 preservadas e exercitadas pelos E2E existentes/atualizados.
- casos inválidos testados: sim — código inexistente (`undefined` nos dois helpers), "Confirmar Autos" desabilitado sem candidato, ausência do seletor após confirmar.
- regressão de UUID: N/A (task não toca import/export/cópia de entidades).
- OSRM mockado: N/A (etapa Identificação não chama OSRM; E2E de rota mockam PNG/OSRM apenas onde a etapa de itinerário é exercitada incidentalmente).
- suíte executada com resultado: **verde** — log canônico reutilizado (não repetido nesta revisão). `npm run test:all:verificar` confirmou: executor `Claude`, fingerprint `e312675c3e6c3c32261f79ffa53446bc561b072f86cfbdcbd0cc4697a92fe1ca`, working tree `498d7fa6397724af2122bdced82f79ddd9749373d9277b227d3e2a3884f91763` (mesmo da árvore revisada; `git status` limpo). Log mostra 72 E2E + suíte unitária em `APROVADO`, incluindo os três novos casos de `identificacao.spec.ts` (pré-visualização, reforço `operante`, troca de tipo pós-confirmação). Adicionalmente, `npm run typecheck` e `npm run lint` rodados nesta revisão: ambos limpos, sem erros.

## Checklist 07

Resultado: 27 itens aplicáveis OK, 51 N/A (task de UI local que não toca JSON/domínio/Comparador/roteamento), 0 violados.

- Escopo: todos os 6 itens OK (task pertence ao ROTA; não introduz workflow; não cria status além da exceção RN-011 — o `status: "proposta"` já existia, task só desloca *quando* ele é atribuído; sem persistência servidor; escopo exato; nenhuma regra inventada — tudo cita DEC-064/Spec 04 §5/§3.1/RN-016).
- JSON (contrato): N/A em sua maioria (nenhum campo novo, nenhuma UUID envolvida nesta etapa) — item "nenhum campo novo sem alterar Spec 02" OK (confirmado pelo teste que prova `denominacao_linha` fora do contrato comitado).
- Domínio: N/A (task não toca Seção/Local/Serviço/viagem/feriado).
- Comparador: N/A.
- Roteamento: N/A (Identificação não chama OSRM).
- UI/PDF: aplicáveis OK — design system (`shared/ui`, sem `style=` inline) e `data-testid`/`aria-*` preservados; demais itens (nomes de Seção, offsets, matrizes, PDF) N/A.
- Testes: todos os aplicáveis OK — RN com teste, casos inválidos, fixtures reais (`carregarListasAutosEmpresas`) reutilizadas, verificação honesta (log reutilizado e validado via `test:all:verificar`, working tree e fingerprint conferidos nesta revisão), porta dedicada liberada (log mostra encerramento do PID próprio 6788).

## Pendências

Nenhuma.

## Decisão

**Aprovado.** A entrega implementa exatamente o que a DEC-064 decidiu, sem extrapolar o "Fora de escopo" declarado na task, com testes que cobrem tanto o caminho feliz quanto os casos inválidos e o ponto de maior risco de contrato (vazamento de `denominacao_linha` para o documento). A suíte canônica está verde no mesmo working tree revisado, e typecheck/lint (rodados nesta revisão) não acusam nada. Nenhuma condição de merge.
