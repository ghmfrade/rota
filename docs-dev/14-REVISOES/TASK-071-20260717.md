# Revisão da TASK-071

**Revisor:** Claude (Opus 4.8), apoio de `/revisar-aderencia`
**Data:** 2026-07-17
**Commit/branch revisado:** `02396af` (branch `redesign`) — "Implementa TASK-071: pontos de rota da sessão sobrevivem a um recálculo que falha"

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

A lista de pontos de rota em edição deixou de ser derivada de `estadoAtual.rota.pontos_de_rota` (que desaparece na transição para `sem-rota`) e passou a viver em estado de sessão próprio, `pontosDeRotaEmEdicao` (DEC-058). A leitura passa por `pontosDeRotaDoItinerario` ([estado-itinerarios.ts:47](../../src/formulario/itinerarios/estado-itinerarios.ts#L47)): a entrada de sessão manda, e na ausência dela cai no eco congelado `rota.pontos_de_rota` do documento (reedição fiel, sem OSRM). Os gestos de parada/ponto comitam a lista no MESMO commit síncrono das paradas ([etapa-itinerarios.tsx:319-328](../../src/formulario/itinerarios/etapa-itinerarios.tsx#L319-L328)), de modo que ela sobrevive a uma falha de recálculo. Arquivos: `sessao.ts`, `estado-itinerarios.ts`, `etapa-itinerarios.tsx`, `index.ts`, + testes unitário e E2E.

## Regras RN verificadas

- **RN-048** (falha bloqueante sem degradação silenciosa) — **atendida**. A lista de pontos vive em sessão e não é apagada pela transição a `sem-rota`; a última rota válida do documento não é sobrescrita na falha (`aplicarNovasParadas` só grava rota em `situacao === "recalculada"`, [etapa-itinerarios.tsx:341-346,363](../../src/formulario/itinerarios/etapa-itinerarios.tsx#L341-L346)). Verificada por E2E (fail→sobrevive→success) e por leitura do código.
- **RN-052 / RN-015** (abrir JSON não chama OSRM; leitores não recalculam) — **atendida**. `pontosDeRotaDoItinerario` sem entrada de sessão faz leitura pura do eco `rota.pontos_de_rota`, sem requisição. Teste unitário "sem entrada na sessão, cai no eco... sem OSRM" ([itinerarios-estado.test.ts:75-85](../../testes/unitarios/formulario/itinerarios-estado.test.ts#L75-L85)).
- **RN-042** (ponto de rota sem identidade) — **atendida**. Continua `{apos_parada_ordem, latitude, longitude}`, sem `uuid`; cópia por spread `[...pontosParaReaplicar]`. Nada de trecho/tarifa/uuid introduzido.
- **RN-096 / NEG-004** (sessão efêmera; nada gravado antes da exportação) — **atendida**. `pontosDeRotaEmEdicao` é estado de sessão, no padrão de `paradasEmEdicao`/`estadosRotaViva`; não entra no JSON. `rota.pontos_de_rota` gravado segue sendo o eco do último recálculo bem-sucedido.

## Specs verificadas

- **Spec 03 §3.6.2** (reedição fiel — pontos reaplicados ao recalcular) — **aderente**. A lista é reaplicada em `dispararRecalculo` via `pontosParaReaplicar`; a URL final leva `waypoints=` (E2E). Ao abrir JSON, nasce do eco do arquivo.
- **Spec 02 §10.4** (`rota.pontos_de_rota` = eco congelado) — **aderente**. Contrato/schema inalterados; nenhum arquivo de contrato/schema/JSON tocado (confirmado no diff).
- **Spec 04 §7.3** (OSRM demo sem SLA; falha corriqueira) — **aderente**. É exatamente o soluço transitório que a DEC-058 endereça.

## Pontos corretos

- Fonte da verdade em edição separada do resultado do cálculo, exatamente como a DEC-058 pede: gestos/mapa/sub-lista leem de `pontosDeRotaAtual` (agora sessão), que é passado ao `EditorMapaItinerario` ([etapa-itinerarios.tsx:587](../../src/formulario/itinerarios/etapa-itinerarios.tsx#L587)); markers e `sub-lista-pontos-de-rota` desenham desse prop incondicionalmente, então sobrevivem em `sem-rota`.
- Commit síncrono conjunto (paradas + pontos) antes do `await` do recálculo — a lista já está persistida quando a falha chega.
- Fallback lazy para o eco do documento em vez de hidratação eager na abertura: funcionalmente equivalente e mais enxuto; cobre o bug anterior à TASK-063 (importar JSON com pontos + falha no recálculo seguinte).
- Comentários citam Spec/RN/DEC corretas; nada de regra inventada.

## Problemas encontrados

Nenhum problema bloqueante. Observações menores (não-bloqueantes) na seção Pendências.

## Violações de escopo

Nenhuma. Não há mudança de contrato/schema, não se cria ponto em `sem-rota`, não há aviso de UI de "pontos preservados" (corretamente ausente — a DEC-058 não o pede), e a re-ancoragem foi deixada para a TASK-066. `posicionar-ponto-de-rota.ts`/`intercalar-pontos-de-rota.ts` reusados sem alteração.

## Testes avaliados

- **cobertura das RN da task:** sim. Prioridade sessão×eco, fallback sem OSRM, lista vazia da sessão prevalecendo, e Serviço/sentido inexistente (unitários); sobrevivência à falha + reaplicação com `waypoints=` (E2E).
- **casos inválidos testados:** sim — 2 casos `[inválido]` no unitário (lista vazia prevalece; uuid inexistente → `[]` sem exceção) e o cenário `sem-rota` no E2E.
- **regressão de UUID:** N/A (ponto de rota não tem uuid — RN-042; a task não toca import/export/cópia de uuid).
- **OSRM mockado:** sim — `page.route` intercepta `router.project-osrm.org` alternando `Ok`/`NoRoute`; nenhum teste depende do serviço real.
- **suíte executada com resultado:**
  - `npm run typecheck` — **verde**.
  - `npm run lint` — **verde**.
  - `npx vitest run` — **verde** (81 arquivos, 960 testes).
  - E2E TASK-071 isolado (`-g "TASK-071"`) — **verde** (passou em execução isolada e na re-execução com retry).
  - E2E arquivo `etapa-itinerarios.spec.ts` completo — **flaky no ambiente**: 1ª execução falhou em 8 testes, todas no helper compartilhado `abrirEtapaVolta`; re-execução `--workers=1 --retries=1` passou 8 e falhou 1 (o "composer REAL DEC-046", num `painel-pendencias` de timing). Falha um teste **diferente** a cada corrida, sempre em pontos genéricos de setup/timing e em testes **não tocados** por esta task — ruído de infra pré-existente, não regressão da TASK-071 (cujo teste é estável).

## Checklist 07

Percorrido item a item: **13 itens ok, 39 N/A, 0 violados.**

- Escopo: pertence ao ROTA, sem workflow, sem status de pedido, sem persistência servidor, só o escopo da task, sem regra inventada — **ok** (6/6).
- JSON: contrato só com dados de operação, autossuficiente, sem campo novo (nenhum arquivo de schema tocado), dados congelados não recalculados por leitor — **ok**; UUID preservada, R$, paradas Seção/Local, km/segundos — **N/A** (task não altera esses eixos).
- Domínio: ponto de rota não tratado como parada/Seção/Local (sem uuid/trecho/tarifa) — **ok**; demais **N/A**.
- Roteamento: OSRM indisponível bloqueia sem fallback, abrir JSON não chama OSRM — **ok**; distância/mensagens/waypoints inalterados — **N/A**.
- UI/PDF: `data-testid`/`aria-*` preservados (E2E reusa os seletores existentes) — **ok**; demais itens de PDF/matriz/nomes — **N/A**.
- Testes: RN com teste, casos inválidos, OSRM mockado, fixtures canônicas (`documentoExemploMinimo`), verificações executadas e reportadas — **ok**; regressão UUID — **N/A**.

## Pendências

- **Não-bloqueante:** o "Testes esperados" da task lista um teste de **integração** (Vitest, `congelada→falha→ok`, asserindo a `rota` gravada com os pontos) e um **snapshot de contrato** do `rota.pontos_de_rota` exportado, além do E2E. Entregou-se o E2E (que cobre o cenário fim-a-fim, com `waypoints=` na URL final) + unitários da função de fonte, sem os dois testes na camada Vitest de integração/snapshot. As RN da task ficam cobertas; fica como possível reforço futuro, não condição de merge.
- **Pré-existente (não desta task):** a flakiness do arquivo `etapa-itinerarios.spec.ts` no ambiente atual (falhas rotativas em `abrirEtapaVolta`/`painel-pendencias`) merece um follow-up de estabilização de E2E, independente da TASK-071.

## Decisão

**Aprovado.** Escopo exato conforme o "Objetivo" e o "Fora de escopo"; todas as RN da task (RN-042/048/052/015/096, NEG-004) atendidas e verificadas por testes; contrato/schema JSON inalterados; `data-testid`/`aria-*` intocados; typecheck, lint e a suíte unitária inteira (960 testes) verdes; o teste E2E da própria task é estável. As duas pendências são não-bloqueantes: uma é reforço de camada de teste sobre um cenário já coberto fim-a-fim, a outra é flakiness pré-existente de E2E não atribuível a esta task. Nenhuma violação de RN Alta, NEG-xxx ou escopo. Liberado para avançar à próxima task (TASK-066, que depende desta).
