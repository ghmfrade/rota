# Revisão da TASK-014

**Revisor:** Claude (Opus 4.8), apoiando revisão humana
**Data:** 2026-07-09
**Commit/branch revisado:** working tree sobre `master` (3255ba4), implementação da TASK-014

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Casca do Formulário (Spec 04 §4): container `AplicacaoFormulario` que decide entre a tela inicial (TASK-013) e o editor por etapas; `LayoutFormulario` com cabeçalho persistente, stepper de 7 etapas livremente navegáveis e painel de pendências vivo (`PainelPendencias` + coletor puro `coletarPendencias`). A `TelaInicial` foi refatorada para elevar o estado terminal por callbacks. Etapas são placeholders (conteúdo é TASK-015+). Unit (11 casos) + E2E (4 casos) cobrindo navegação livre e pendência clicável; regressão de TASK-013 preservada.

## Regras RN verificadas

- **RN-078 (Alta) — atendida (parte "estrutura", como manda o backlog).** Painel com taxonomia bloqueante × alerta e navegação por clique à etapa de origem (`etapaAlvo`); o gate real de exportação é TASK-032/007 (matriz `03`: "painel de pendências + export", INT+E2E — o INT de export é da task de exportação). Coberto por unit + E2E.
- **RN-011 (Alta) — atendida.** O selo exibe apenas `autos.status` existente (`layout-formulario.tsx` `dadosCabecalho`); nenhum status de processo criado. No modo "novo" não há selo (identidade ainda indefinida).
- **RN-004 (Alta) — atendida.** A casca apenas **lê** `sessao.documento.autos.*`, sem mutar nem reindexar; o caminho de import que preserva UUIDs (TASK-006) está intocado — round-trip existente segue verde.
- **RN-096 / NEG-009 (Alta) — atendida.** Estado só em memória (React `useState`); nenhuma persistência de servidor; smoke "sem requisição externa" continua verde.
- **RN-095 + NEG-004 (Alta) — atendida.** Pendências são de **validação**, recomputadas da sessão a cada render (`coletarPendencias` puro), jamais persistidas nem gravadas no JSON; teste unitário afirma explicitamente a não persistência. Sem status/etapa de processo.

## Specs verificadas

- **Spec 04 §4 — aderente.** 7 etapas com rótulos e ordem literais; navegação livre (nenhuma trava); cabeçalho persistente em todas as etapas (verificado no E2E percorrendo as 7).
- **Spec 04 §11 — aderente (subconjunto conhecível).** Duas listas (bloqueantes × alertas), itens clicáveis para a etapa de origem; o único alerta de §11 computável neste ponto ("documento criado do zero") está implementado; demais fontes documentadas como TODO por task.
- **Spec 04 §16 decisão 1 — aderente.** "Navegação por etapas não travada" honrada; só a exportação (task futura) é bloqueada.
- **Spec 04 §3.1/§3.2 — aderente (regressão).** Mensagens obrigatórias migraram para a casca com testids/textos idênticos; E2E de TASK-013 verde.
- **DEC-033 — aderente.** Alerta do-zero navega para a etapa Revisão.

## Pontos corretos

- Coleta de pendências isolada como função pura e testável, desacoplada da UI (fronteira `docs-dev/13`: `formulario/` só depende de `shared/`).
- Refatoração da `TelaInicial` mínima e limpa: estados terminais removidos, sem código morto nem testids duplicados (diff conferido).
- Placeholders honestos por etapa; Resumo operacional (§10/TASK-031) e mapa (§7/TASK-020) deliberadamente fora, sem stubs falsos.
- Inferências controladas explicitadas (cabeçalho "a definir" no modo novo; destino do alerta) e a única ambiguidade real formalizada em DEC-033 antes de codar.

## Problemas encontrados

Nenhum de severidade bloqueante ou média.

- (Informativo) Em modo "carregado", os alertas técnicos de importação (350 m/tipificação — RN-091) aparecem na mensagem de abertura, não no painel de pendências. É coerente com a Spec 04 §11 (que não os lista entre seus alertas) e §3.1 item 5; a consolidação definitiva de todas as pendências de §11 é da TASK-032. Sem ação nesta task.

## Violações de escopo

Nenhuma. O container e a refatoração da `TelaInicial` são a integração mínima necessária (entrada → editor). Nada do "Fora de escopo" (conteúdo de etapas, mapa, contadores, gate real de exportação) foi antecipado.

## Testes avaliados

- **cobertura das RN da task:** sim — RN-078 (estrutura) coberta por unit (`coletarPendencias`, `ETAPAS`) e E2E (navegação livre + pendência clicável).
- **casos inválidos testados:** sim — "carregado não gera pendência" (não inventar bloqueantes), "nenhuma pendência é bloqueante neste estágio" (evita falso gate), pureza/não persistência.
- **regressão de UUID:** N/A — a task consome o documento importado sem tocar import/export/cópia; round-trip existente permanece verde.
- **OSRM mockado:** N/A — nenhum caminho desta task chama OSRM.
- **suíte executada com resultado:** verde — typecheck OK; lint OK; `vitest` 399/399; Playwright 10/10; `next build` (export estático) OK.

## Checklist 07

- **Escopo (6):** 6 ok.
- **JSON (9):** RN-004 ok, RN-015 ok; 7 N/A (não toca o JSON).
- **Domínio (9):** 9 N/A (task de layout).
- **Comparador (7):** 7 N/A.
- **Roteamento (5):** 5 N/A.
- **UI/PDF (6):** RN-078 ok (estrutura); 5 N/A (PDF/matriz/horário/`Cidade - Nome`/aviso SEI — conteúdo de etapas futuras).
- **Testes (6):** 4 ok; 2 N/A (regressão UUID; OSRM mock).
- **Total:** 13 ok, 35 N/A, 0 violados.

## Pendências

- Follow-up (não bloqueia merge): TASK-032 consolida as demais pendências de §11 e decide o encaminhamento dos alertas técnicos de importação; o gate real de exportação contra pendências (RN-078 parte "export") entra em TASK-007/032.
- Nenhuma Q-xxx aberta (Q-014 decidida em DEC-033).

## Decisão

**Aprovado.** Escopo exato (casca + painel, sem antecipar etapas), RN da task atendidas com testes (válidos e inválidos), checklist sem itens violados e todas as verificações verdes. As lacunas apontadas são explicitamente de tasks posteriores e coerentes com o backlog ("RN-078 (estrutura)"). Sem condições de merge.
