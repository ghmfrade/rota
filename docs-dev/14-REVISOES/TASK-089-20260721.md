# Revisão da TASK-089

**Revisor:** Codex
**Data:** 2026-07-21
**Commit/branch revisado:** correção absorvida pelo commit `37502b4` (TASK-068) / `redesign` em `d43883e`

## Resultado

- [ ] Aprovado
- [x] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

A asserção do E2E de reordenação passou a procurar somente `pendencia-item` com
`data-severidade="bloqueante"`, em vez de exigir painel vazio
(`testes/e2e/etapa-itinerarios.spec.ts:117`). A mudança foi implementada dentro
do commit da TASK-068, sem commit próprio da TASK-089. As demais asserções do
cenário e o caso inválido `NoRoute` permaneceram ativos.

## Regras RN verificadas

- RN-078 — **atendida**: o caminho de sucesso exige zero pendências bloqueantes,
  sem confundir o alerta legítimo de feriados com bloqueio
  (`testes/e2e/etapa-itinerarios.spec.ts:117`).
- RN-071 — **atendida**: o teste tolera a presença do alerta não bloqueante; o
  produto continua emitindo `severidade: "alerta"` para grade de feriados vazia
  (`src/formulario/pendencias/pendencias.ts:200`).
- RN-048 — **atendida**: a asserção não foi removida nem trocada por contagem
  fixa de alertas, e o cenário vizinho continua exigindo pendência bloqueante
  quando o OSRM mockado devolve `NoRoute`
  (`testes/e2e/etapa-itinerarios.spec.ts:126`).
- RN-052 — **atendida**: o cenário preserva a prova de que reordenar recalcula,
  atualiza a ordem e recompõe a descrição com o envelope OSRM mockado
  (`testes/e2e/etapa-itinerarios.spec.ts:71`).

## Specs verificadas

- Spec 03 §9.3 — **aderente**: grade de feriados vazia continua válida e não
  torna o E2E de recálculo vermelho.
- Spec 04 §11 — **aderente**: a asserção respeita a taxonomia entre erros
  bloqueantes e alertas.
- Spec 04 §12 — **aderente**: a distinção aferida é a mesma usada pelo gate, no
  qual alertas não bloqueiam.

## Pontos corretos

- O filtro é semântico e estável: seleciona `data-severidade="bloqueante"`, sem
  se acoplar à quantidade ou ao texto de alertas
  (`testes/e2e/etapa-itinerarios.spec.ts:119`).
- Permaneceram as provas da nova ordem das Paradas, da descrição contendo
  `Via Reordenada 1` e da ausência de `mensagem-sem-rota`
  (`testes/e2e/etapa-itinerarios.spec.ts:105`).
- O caso inválido continua encontrando a pendência de rota e confirmando sua
  severidade bloqueante (`testes/e2e/etapa-itinerarios.spec.ts:140`).
- A varredura de `pendencia-item` em `testes/e2e/` encontrou somente esta
  asserção por severidade, o caso inválido por texto/severidade e o alerta
  específico de documento novo; nenhuma outra asserção exige contagem total
  zero do painel.
- O fixture canônico `testes/fixtures/carregar-multi-servico.json` não foi
  alterado pelo commit que introduziu a correção.

## Problemas encontrados

- **Baixa — rastreabilidade/processo:** a correção foi incluída no commit
  `37502b4 Implementa TASK-068`, sem commit `Implementa TASK-089`. Por isso, o
  `git show --stat 37502b4` contém arquivos de `src/` pertencentes à TASK-068 e
  não satisfaz literalmente o critério documental da TASK-089 de que o stat da
  task contenha somente testes. O hunk que realiza a TASK-089 é restrito a
  `testes/e2e/etapa-itinerarios.spec.ts:117`, não altera produção nem fixture e
  está correto. A absorção fica formalizada por este parecer e pela atualização
  de `docs-dev/19-STATUS_EXECUCAO.md`; não há correção de código a fazer.

## Violações de escopo

- Nenhuma no recorte funcional da TASK-089. A coexistência com alterações da
  TASK-068 no mesmo commit é a ressalva de rastreabilidade descrita acima, não
  uma ampliação do hunk da TASK-089.

## Testes avaliados

- cobertura das RN da task: **sim** — sucesso sem bloqueante, recálculo e caso
  inválido `NoRoute` com bloqueante.
- casos inválidos testados: **sim** — o cenário vizinho usa OSRM mockado com
  `NoRoute` e exige `data-severidade="bloqueante"`.
- regressão de UUID: **N/A** — nenhum contrato, importação, exportação ou cópia
  foi alterado.
- OSRM mockado: **sim** — ambos os cenários interceptam
  `https://router.project-osrm.org/**`; nenhum serviço externo real é usado.
- verificação direcionada: o cenário corrigido passou **3/3** com
  `--repeat-each=3`, `--workers=1` e servidor de produção controlado próprio.
- verificações complementares: `npm run typecheck` e `npm run lint` — **código
  0**. O build controlado da suíte canônica também ficou verde.
- suíte canônica: o log anterior foi corretamente rejeitado por E2E com código
  1. A revisão executou uma única vez
  `npm run test:all:log -- --executor=Codex-revisao`; resultado **90 arquivos /
  1.083 testes unitários aprovados e 58 E2E aprovados**, incluindo o cenário
  corrigido e o `NoRoute`. `npm run test:all:verificar` confirmou executor
  `Codex-revisao`, fingerprint
  `032dfe88d074fcf9ff60092907dfdda1292517d923303ce99b47ce13681e0f5c` e
  working tree
  `00c21aa501d19b4a9ade6b12db96274b9a6725e300e14f1fda33b4dc1336f57e`.
- tentativa direcionada descartada: a primeira execução avulsa 3× usou
  `next dev` e falhou porque o Next bloqueou HMR cross-origin em `127.0.0.1`;
  não foi tratada como evidência. Os PIDs próprios foram encerrados e a mesma
  repetição foi refeita no modo controlado, passando 3/3.

## Checklist 07

Resultado: **19 itens atendidos, 34 N/A, 0 violados**.

### Escopo

- [x] A alteração pertence ao ROTA, não ao SEI (RN-095).
- [x] Não introduz workflow (RN-095).
- [x] Não cria status de pedido (RN-011).
- [x] Não cria persistência transacional (RN-096).
- [x] O hunk funcional implementa apenas a TASK-089; a coabitação no commit da
  TASK-068 está registrada como ressalva.
- [x] Nenhuma regra foi inventada; o filtro decorre das RN-071/078 e das Specs
  03 §9.3 e 04 §11/§12.

### JSON (contrato)

- [N/A] Dados de operação no JSON — contrato não tocado.
- [N/A] JSON autossuficiente — contrato/leitores não tocados.
- [N/A] Campo novo — nenhum arquivo de schema ou produção integra o hunk.
- [N/A] Preservação de UUID na importação/edição (RN-004).
- [N/A] UUID nova para entidade nova/cópia (RN-002/007).
- [N/A] XOR de Parada (RN-033).
- [N/A] Valores monetários no JSON (RN-013).
- [N/A] Unidades/arredondamento (RN-014/050).
- [N/A] Dados congelados em leitores (RN-015).

### Domínio

- [N/A] Separação Seção/Local (RN-025/031).
- [N/A] Ponto de rota distinto de Parada (RN-042).
- [N/A] Identidade do Serviço (RN-001).
- [N/A] `numero_n` como identidade (RN-006).
- [N/A] Extremos como Seções (RN-035).
- [N/A] Conjunto de Seções Ida/Volta (RN-030).
- [N/A] Tipificação (RN-019..022).
- [N/A] Estratificação de Viagem (RN-061).
- [N/A] Feriado fora das contagens (RN-069).

### Comparador

- [N/A] Comparação por UUID (RN-081).
- [N/A] Taxonomia de alterações (RN-081..083).
- [N/A] Comparativo fora do JSON e sem mutação (RN-080/097).
- [N/A] `status`/datas fora do diff (RN-012).
- [N/A] Bloqueio de Autos diferentes (RN-084).
- [N/A] Comparação estável de rota (RN-087).
- [N/A] Comparador sem OSRM/persistência (RN-080).

### Roteamento

- [x] Falha `NoRoute` permanece bloqueante, sem fallback (RN-048).
- [N/A] Distância exclusivamente do OSRM (RN-047/050) — cálculo não tocado.
- [N/A] Pontos de rota condicionando o traçado (RN-041..043) — não tocados.
- [x] Editar/reordenar continua recalculando (RN-052).
- [x] Mensagem de `NoRoute` permanece operacional e sem tarifa (RN-049).

### UI/PDF

- [N/A] Padrão de nomes de Seção (RN-076).
- [N/A] Horários/offsets (RN-067/076).
- [N/A] Matrizes triangulares/sem R$ (RN-076).
- [N/A] Locais fora de grade/descrição/PDF (RN-031/044/076).
- [N/A] Aviso SEI no PDF (RN-077).
- [x] Bloqueantes e alertas permanecem distinguidos para o gate (RN-078).
- [N/A] Design system (DEC-050) — produção/UI não tocada.
- [x] `data-testid` existentes foram preservados; o filtro usa atributo já
  existente (DEC-050).

### Testes

- [x] As RN aferidas possuem cobertura compatível com a matriz 03.
- [x] O caso inválido `NoRoute` foi testado.
- [N/A] Regressão específica de UUID — task não toca import/export/cópia.
- [x] O OSRM está mockado em todos os cenários envolvidos.
- [x] O fixture canônico foi reutilizado e permaneceu inalterado.
- [x] Verificações direcionadas, typecheck, lint e build aplicável foram
  executados e reportados honestamente.
- [x] A evidência final canônica tem marcador, códigos 0, fingerprint atual e
  identidade do mesmo working tree.
- [x] O log anterior inválido foi rejeitado e a revisão executou o comando
  canônico uma única vez.
- [x] O executor canônico limpou seu servidor na porta 3100; as execuções
  direcionadas encerraram somente os próprios PIDs e não reutilizaram servidor
  alheio.

## Pendências

- Nenhuma condição de código ou merge: a implementação funcional está correta.
- A condição administrativa era registrar formalmente que a TASK-089 foi
  absorvida pela TASK-068; este parecer e a atualização simultânea de
  `docs-dev/19-STATUS_EXECUCAO.md` a cumprem.
- Q-xxx abertas: nenhuma.

## Decisão

**Aprovado com ressalvas.** A TASK-089 está funcionalmente concluída: o E2E
afere ausência de bloqueante, preserva as garantias do caminho de sucesso,
continua distinguindo `NoRoute`, passa repetidamente e a suíte canônica está
verde. A única ressalva é histórica e documental — a implementação foi absorvida
pelo commit da TASK-068 sem commit próprio. Como a absorção fica registrada e
não há violação de RN Alta, NEG-xxx ou código a corrigir, a TASK-089 deve sair da
lista de pendentes e não deve ser reimplementada.
