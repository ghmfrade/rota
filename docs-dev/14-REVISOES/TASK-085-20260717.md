# Revisão da TASK-085

**Revisor:** Claude (revisão assistida, `/revisar-aderencia`)
**Data:** 2026-07-17
**Commit/branch revisado:** `redesign` — diff de working tree (não commitado ainda): `src/formulario/exportacao/gate-exportacao.ts`, `src/formulario/exportacao/tela-exportacao.tsx`, `src/formulario/layout/layout-formulario.tsx`, `testes/unitarios/formulario/gate-exportacao.test.ts`, `testes/unitarios/formulario/tela-exportacao.test.tsx`, `testes/unitarios/formulario/layout-formulario-shell.test.tsx`.

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

`avaliarGateExportacao` (`gate-exportacao.ts`) passa a rodar `esquemaDocumentoOperacao.safeParse` diretamente (em vez de `validarParaExportacao`, que fazia o mesmo `safeParse` só que descartava o resultado numa mensagem genérica) e devolve cada `issue` traduzida em `Pendencia` operacional no novo campo `errosEstruturais`, com mensagem dedicada para RN-018/026/030/034/035/036/039/057 e um fallback genérico (nunca cita uuid/RN/caminho JSON) para o resto. `tela-exportacao.tsx` lista esses motivos (`pendenciasBloqueantes` + `errosEstruturais`) num `<ul data-testid="exportacao-motivos-bloqueio">`; `layout-formulario.tsx` mescla os mesmos `errosEstruturais` às pendências da casca, fazendo-os aparecer em "Erros bloqueantes" na Revisão (via `tela-revisao.tsx`, inalterado — já filtra por `severidade` e navega por `etapaAlvo` genericamente).

## Regras RN verificadas

- RN-078 (exportação bloqueada com pendências, e o usuário precisa ver quais) — atendida: motivos agora aparecem nas duas telas; teste de regressão garante que a lista deixa de vir vazia (`gate-exportacao.test.ts`, `layout-formulario-shell.test.tsx`, `tela-exportacao.test.tsx`).
- RN-076 (rótulo `Cidade - Nome` da Seção) — atendida: `rotuloSecao` usa `${secao.municipio} - ${secao.nome}` (`gate-exportacao.ts:94-96`); teste confere `"Guarujá - Terminal Extra"`.
- RN-018/026/030/034/035/036/039/057 — não são regras novas desta task (a checagem cruzada já existia em `coletarViolacoesEstruturais`, `src/shared/contrato/validacoes-estruturais.ts`, e já era executada por `esquemaDocumentoOperacao.safeParse` — confirmado lendo `src/shared/contrato/index.ts:8-18`, que faz `esquemaDocumentoOperacaoBase.superRefine` chamando `coletarViolacoesEstruturais`). A task só traduz a mensagem já produzida; nenhuma violação passou a ser detectada ou deixou de ser — verificado comparando `exportar-documento.ts:78-90` (`validarParaExportacao`, código anterior, mesmo `safeParse`) com a nova `coletarErrosEstruturais` (`gate-exportacao.ts:220-233`).
- RN-004 (UUIDs) — N/A: task não toca import/export/cópia de entidade, só leitura para exibição de mensagem.

## Specs verificadas

- Spec 04 §11 (duas listas, item navegável) — aderente: `tela-revisao.tsx` já filtrava por `severidade`/navegava por `etapaAlvo`; os novos itens usam a mesma estrutura sem exigir mudança nesse componente.
- Spec 04 §14 (tom operacional, nunca "estrutura interna") — aderente nos casos dedicados (RN-018/026/030/034/035/036/039/057): mensagens citam Seção/Serviço/sentido em linguagem de negócio. No fallback genérico, a mensagem é neutra ("Há um problema nos dados de ... Revise a etapa ...") — cumpre "nunca expõe `[RN-xxx]` cru" (testado em `gate-exportacao.test.ts`, caso "uuid de Viagem duplicada"), embora seja menos específica que as mensagens dedicadas (aceitável — o próprio "Fora de escopo" da task previu o modelo misto).
- Spec 02 §14 (violações estruturais, fonte das mensagens) — aderente, mesma fonte (`coletarViolacoesEstruturais`) já usada antes.

## Pontos corretos

- Fonte única de verdade: `layout-formulario.tsx` consome o mesmo `avaliarGateExportacao` que a etapa Exportação usa para os botões, em vez de recomputar violações por conta própria — evita a divergência que o "Riscos" da task pedia para evitar.
- Nenhuma mudança de contrato JSON, nenhuma regra nova inventada — a task só transporta mensagem já calculada.
- Regressão coberta explicitamente: teste com Seção órfã confirma que a Revisão deixa de mostrar "Nenhum erro bloqueante" (o bug relatado).
- `data-testid`/`aria-*` existentes preservados; só testids novos foram adicionados (`exportacao-motivos-bloqueio`).

## Problemas encontrados

Nenhum.

## Violações de escopo

Nenhuma. A alteração de código toca só os arquivos previstos em "Arquivos prováveis" da task (`gate-exportacao.ts`, `tela-exportacao.tsx`) mais `layout-formulario.tsx` (necessário para a Revisão, citado no objetivo/critérios de aceite). Não introduz workflow, não muda contrato JSON, não decide "quais" validações bloqueiam.

Nota: o working tree também traz alterações não commitadas em `docs-dev/06-BACKLOG_INICIAL.md`, `docs-dev/10-DECISION_LOG.md` e `docs-dev/16-OPEN_QUESTIONS.md` que **não pertencem** a esta task — são o registro da TASK-083/DEC-068/Q-048 (ponto de rota órfão) e da própria TASK-085/TASK-084 recém-abertas no backlog. Não afetam o veredito desta revisão (docs, não código; nenhum código de TASK-083/084 está presente neste diff), mas ficam como pendência de organização de commit: ao comitar a implementação da TASK-085, isolar esses arquivos do commit ou tratá-los num commit à parte de planejamento, para não misturar "abre tasks no backlog" com "implementa TASK-085" no mesmo commit de implementação.

## Testes avaliados

- cobertura das RN da task: sim — RN-078 (regressão + caminho liberado inalterado) e RN-076 (rótulo) testadas.
- casos inválidos testados: sim — Seção órfã (RN-018), itinerário sem viagem (RN-039), paradas insuficientes (RN-034), uuid duplicada sem tradução dedicada (fallback, RN-005).
- regressão de UUID: N/A (task não toca import/export/cópia).
- OSRM mockado: N/A (task não chama roteamento).
- suíte executada com resultado: verde — `npm run typecheck` (sem erros), `npm run lint` (sem erros), `npm test` → **82 arquivos, 981 testes, todos passando** (rodado nesta revisão).

## Checklist 07

Percorrido item a item; aplicáveis a esta task, todos ok:
- Escopo: ok (RN-095/096/011 N/A — task é só camada de apresentação/gate; escopo exato; nenhuma regra inventada).
- JSON (contrato): N/A em quase tudo (task não toca contrato) — confirmado que nenhum campo novo foi adicionado ao schema.
- Domínio: N/A (task não mexe em entidades do modelo).
- Comparador: N/A.
- Roteamento: N/A.
- UI/PDF: RN-076 (rótulo `Cidade - Nome`) ok; RN-078 (bloqueio com pendências) ok; design system (`shared/ui`, `Painel`, sem `style=` inline) ok — reaproveita `Painel`/classes Tailwind já usadas no mesmo arquivo; `data-testid`/`aria-*` existentes preservados, ok.
- Testes: ok em todos os itens aplicáveis (ver seção acima).

Nenhum item aplicável violado.

## Pendências

- Nenhuma Q-xxx aberta por esta task.
- Organização de commit: ver nota em "Violações de escopo" sobre separar os arquivos de backlog/decisão de TASK-083 do commit de implementação da TASK-085.

## Decisão

Aprovado sem ressalvas: escopo exato (só o bug de feedback do gate, sem mexer em quais validações bloqueiam), toda RN citada tem teste, checklist limpo, suíte completa verde (981 testes), typecheck e lint limpos. Verificado que a fonte de violações estruturais não mudou (mesma `coletarViolacoesEstruturais`/`esquemaDocumentoOperacao` de antes) — a task só torna visível o que já bloqueava, sem alterar cobertura de validação.
