# Revisão da TASK-104

**Revisor:** Codex
**Data:** 2026-07-30
**Commit/branch revisado:** branch `redesign`; implementação `9adf7d9`

## Resultado

- [ ] Aprovado
- [x] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

A implementação adiciona o CRUD e os filtros de Tabelas excepcionais por
Serviço em `src/formulario/viagens/`, integra o painel à etapa Viagens e cobre
os fluxos válidos e inválidos com testes unitários e E2E. A grade excepcional,
as contagens, PDF e Comparador permanecem fora desta entrega.

## Regras RN verificadas

- RN-002 — atendida: a criação usa `crypto.randomUUID()` e os testes injetam
  UUIDs distintas para provar a identidade das entidades novas
  (`src/formulario/viagens/tabelas-excepcionais.ts:35`;
  `testes/unitarios/formulario/viagens-tabelas-excepcionais.test.ts:30`).
- RN-003/RN-004 — atendidas: editar a descrição recompõe somente a tabela
  personalizada alvo e preserva sua UUID
  (`src/formulario/viagens/tabelas-excepcionais.ts:81`;
  `testes/unitarios/formulario/viagens-tabelas-excepcionais.test.ts:106`).
- RN-098 — atendida no código: tipos canônicos são únicos, `personalizado`
  pode repetir, sua descrição é obrigatória e descrições não são mantidas nos
  tipos canônicos (`src/formulario/viagens/tabelas-excepcionais.ts:35`;
  `testes/unitarios/formulario/viagens-tabelas-excepcionais.test.ts:30`).
- RN-099 — atendida no caminho de remoção: a tabela referenciada não é
  removida, e nenhuma Viagem é apagada ou convertida para a grade comum
  (`src/formulario/viagens/tabelas-excepcionais.ts:120`;
  `src/formulario/viagens/tabelas-excepcionais.ts:137`;
  `testes/unitarios/formulario/viagens-tabelas-excepcionais.test.ts:207`).
- RN-095/RN-096 — atendidas: a UI manipula somente o estado client-side do
  Serviço, sem workflow, campo de processo, backend ou persistência
  transacional (`src/formulario/viagens/etapa-viagens.tsx:295`).

## Specs verificadas

- Spec 02 §6.1 — aderente: entidade, tipos, descrição condicional,
  cardinalidade canônica e ausência de verificação de sobreposição foram
  preservados.
- Spec 02 §11/§12 — aderente: a remoção preserva a referência enquanto houver
  Viagens associadas; criação e edição respeitam identidade estável.
- Spec 04 §8.5 — aderente ao escopo da TASK-104: criação por tipo, edição do
  nome personalizado e filtro por tipo/descrição foram entregues; a grade e a
  semeadura permanecem para a TASK-105.

## Pontos corretos

- O motor puro não muta o Serviço de entrada nos caminhos de criação, edição
  e remoção.
- A cardinalidade é local ao Serviço selecionado; o E2E alterna entre dois
  Serviços e prova que as tabelas não vazam entre eles
  (`testes/e2e/etapa-viagens.spec.ts:1122`).
- A edição existe somente para tabelas `personalizado` e preserva a UUID.
- O filtro combina tipo fechado e busca textual case-insensitive da descrição,
  sem introduzir datas, meses ou sobreposição.
- A remoção vazia exige confirmação; a remoção referenciada é bloqueada antes
  do diálogo e informa a quantidade de Viagens associadas
  (`src/formulario/viagens/painel-tabelas-excepcionais.tsx:94`;
  `testes/e2e/etapa-viagens.spec.ts:1208`).
- A UI reutiliza `Botao`, `Campo`, `Dialogo`, `Painel`, `Select` e `Tabela` de
  `shared/ui`, sem `style=` inline, cores avulsas ou alteração de seletores
  existentes.
- Os E2E interceptam o endpoint público do OSRM e comprovam zero chamadas nos
  dois cenários da task.

## Problemas encontrados

1. **Ressalva impeditiva de merge — fundamento normativo ainda não
   versionado.** O bloqueio de remoção implementado em
   `src/formulario/viagens/tabelas-excepcionais.ts:134` cita a DEC-098, mas
   Q-076 e DEC-098 existem somente como alterações não commitadas em
   `docs-dev/16-OPEN_QUESTIONS.md` e `docs-dev/10-DECISION_LOG.md`. Assim, o
   commit `9adf7d9` depende de uma decisão que ainda não integra o histórico da
   branch. A regularização deve preservar separação das alterações alheias
   atualmente misturadas no working tree.
2. **Ressalva de rastreabilidade — derivados exigidos pela DEC-098 não foram
   atualizados.** O próprio texto pendente da decisão determina incorporar a
   política de remoção à RN-098/RN-099 em
   `docs-dev/01-RULE_INDEX.md` e à rastreabilidade correspondente em
   `docs-dev/03-TRACEABILITY_MATRIX.md`; nenhum desses arquivos foi alterado.
   A implementação e os testes estão corretos, mas a cadeia
   decisão → RN → teste permanece incompleta.

## Violações de escopo

- Nenhuma. Não foi implementada grade excepcional, semeadura, contagem, PDF,
  Comparador ou verificação de sobreposição.

## Testes avaliados

- cobertura das RN da task: sim — RN-002/003/004/098 e o caminho de
  integridade referencial da RN-099.
- casos inválidos testados: sim — segunda tabela canônica, personalizado sem
  nome, edição inválida e remoção com Viagem associada.
- regressão de UUID (se aplicável): sim — UUID nova na criação e preservada na
  edição.
- OSRM mockado: sim — interceptado nos E2E, com zero chamadas.
- suíte executada com resultado: verde — `npm run test:all:verificar`
  reutilizou o log válido do executor `Codex`, fingerprint
  `2f4af73122dcba60f593f1436d62e7092615c6387f79596229ee97b9daa83b54`
  e identidade do working tree
  `498d7fa6397724af2122bdced82f79ddd9749373d9277b227d3e2a3884f91763`;
  1.334 unitários e 96 E2E, códigos 0. A revisão não repetiu a suíte.
- verificações complementares: `npm run typecheck` e `npm run build` verdes;
  `npm run lint` com 0 erros e 1 aviso preexistente em
  `testes/e2e/etapa-viagens.spec.ts:592`, fora do diff da TASK-104.

## Checklist 07

Resultado: **26 itens conformes, 27 N/A, 0 violados**. As duas ressalvas
documentais acima são condições externas aos 53 itens técnicos do checklist.

### Escopo

- [x] A alteração pertence ao ROTA, não ao SEI (RN-095).
- [x] Não introduz workflow.
- [x] Não cria status de pedido.
- [x] Não cria persistência transacional (RN-096).
- [x] Implementa somente o objetivo da TASK-104.
- [x] O comportamento tem fonte na Spec 02 §6.1, Spec 04 §8.5 e DEC-098.

### JSON (contrato)

- [x] O JSON continua contendo somente dados de operação.
- [x] O documento continua autossuficiente.
- [x] Nenhum campo novo foi adicionado.
- [x] UUIDs existentes são preservadas na edição (RN-004).
- [x] Tabelas novas recebem UUID nova (RN-002).
- [N/A] Paradas não foram tocadas.
- [x] Nenhum valor monetário foi introduzido.
- [N/A] Distâncias, durações e arredondamento não foram tocados.
- [N/A] Dados congelados e leitores não foram tocados.

### Domínio

- [N/A] Seção e Local não foram tocados.
- [N/A] Pontos de rota não foram tocados.
- [x] Serviço mantém identidade por UUID.
- [x] `numero_n` não é usado como identidade.
- [N/A] Extremos de itinerário não foram tocados.
- [N/A] Conjunto de Seções de Ida/Volta não foi tocado.
- [N/A] Tipificação não foi tocada.
- [x] Viagens associadas mantêm dia e grade sem conversão (RN-061/RN-099).
- [N/A] Contagens e rótulo da semana padrão estão fora do escopo.

### Comparador

- [N/A] Os sete itens não se aplicam; o Comparador não foi tocado.

### Roteamento

- [N/A] Os cinco itens não se aplicam; nenhum cálculo de rota foi tocado.

### UI/PDF

- [N/A] Nomes de Seção, horários, matrizes, Locais, PDF e exportação não foram
  tocados.
- [x] UI aderente ao design system e aos componentes de `shared/ui`.
- [x] Seletores existentes foram preservados e novos controles têm rótulos
  acessíveis.

### Testes

- [x] As RN implementadas possuem testes.
- [x] Casos inválidos foram testados.
- [x] Criação e edição têm regressão de UUID.
- [x] E2E interceptam o OSRM e comprovam ausência de chamada.
- [x] A fixture canônica `carregar-multi-servico.json` foi reutilizada.
- [x] Testes direcionados, typecheck, lint e build foram verificados.
- [x] A evidência final tem marcador aprovado, códigos 0, fingerprint atual e
  identidade do mesmo working tree.
- [x] O log válido foi reutilizado sem repetir a suíte.
- [x] O log confirma limpeza da árvore do PID próprio do servidor E2E.

## Pendências

- Versionar Q-076 e DEC-098 em commit documental coerente, sem arrastar
  alterações de outras tasks.
- Atualizar RN-098/RN-099 no `01-RULE_INDEX.md` e a matriz `03` com a política
  de remoção e seus testes.
- Reavaliar a TASK-104 após a regularização; até lá, não marcá-la como
  concluída nem avançar para a TASK-105.

## Decisão

**Aprovado com ressalvas impeditivas de merge.** O código e os testes atendem
ao escopo e às regras de criticidade alta verificadas, e todas as verificações
técnicas estão verdes. O ciclo ainda não pode ser encerrado porque a decisão
que autoriza o bloqueio de remoção não está versionada e a rastreabilidade
normativa exigida por essa decisão permanece incompleta. A TASK-104 continua
na fila até a regularização documental e uma reavaliação.
