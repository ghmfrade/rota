# Revisão da TASK-020

**Revisor:** Claude (Opus 4.8), apoiando revisão humana
**Data:** 2026-07-10
**Commit/branch revisado:** `e527cd9` sobre `master` — "Implementa TASK-020: Mapa base MapLibre/OSM"

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Mapa base client-side (Spec 01 §8 — OSM/MapLibre), primitiva reutilizável por Formulário e Comparador em `src/shared/mapa/`: partes puras (`config.ts` URL de tiles configurável, `estilo.ts` style raster OSM, `geometria.ts` linhas→GeoJSON, `captura.ts` canvas→imagem) + componente React `mapa.tsx` (`"use client"`) com marcadores arrastáveis, desenho de rota (LineString), clique e handle de captura. `maplibre-gl@^5` adicionado; harness `src/app/mapa-demo/page.tsx` para o E2E. Testes: 4 suítes unitárias (15 casos) + 1 smoke E2E.

## Regras RN verificadas

- **RN-074** (parte "imagem do mapa por captura do próprio canvas") — **atendida**. Mapa criado com `canvasContextAttributes.preserveDrawingBuffer: true` ([mapa.tsx:127](../../src/shared/mapa/mapa.tsx#L127)); `capturarImagemMapa` devolve o `toDataURL` do canvas e lança sem canvas ([captura.ts:19-31](../../src/shared/mapa/captura.ts#L19-L31)). Testada em unit (PNG/JPEG + 2 casos inválidos) e no E2E (data-URI real). As demais partes da RN-074 (estrutura/conteúdo do PDF) são da TASK-033.
- **RN-097** (arquitetural) — **atendida**. Componente em `shared/mapa`, dependente só de `maplibre-gl`/React; sem import de `formulario/` ou `comparador/`. Não há barrel `src/shared/index.ts` que arraste a UI para os testes de node (verificado); só o harness importa `@/shared/mapa`.

## Specs verificadas

- **Spec 01 §8** — aderente: "Mapa e tiles client-side (OSM/MapLibre); imagem do mapa no PDF por captura do próprio canvas". Lib fixada (MapLibre) usada; captura de canvas suportada; nada guardado no servidor (harness usa só `useState` local).
- **Spec 04 §6/§7** — aderente ao que cabe a esta task: o componente expõe as capacidades que as etapas de itinerário (§7) vão orquestrar (marcador, arrasto, clique, LineString). A orquestração (Seção/Local/itinerário/tabela lateral) é explicitamente das TASK-017/018/019 — não implementada aqui.
- **docs-dev/13** — aderente: MapLibre GL como lib de mapa; URL de tiles configurável (espelha o padrão de URL base do OSRM); nenhuma dependência que exija servidor próprio.

## Pontos corretos

- **Fronteira de contrato intacta:** nenhum campo/JSON tocado; sem R$, sem status, sem persistência (NEG-004/007/009 respeitados).
- **SSR/prerender seguro:** `maplibre-gl` entra por `import()` dinâmico dentro de `useEffect`; `npm run build` prerenderiza `/mapa-demo` como estático sem tocar WebGL — o risco nº 1 da análise foi fechado com evidência.
- **Partes puras isoladas e testáveis sem WebGL** (config/estilo/geometria/captura), com o componente fino por cima.
- **URL de tiles configurável** (`NEXT_PUBLIC_TILES_URL`, default OSM) marcada como inferência controlada e **bem ancorada em DEC-029** ("mantém a URL configurável para permitir migração futura sem refatoração") — não é regra nova inventada.
- **E2E não depende de rede externa:** tiles interceptados com PNG local (mesma disciplina do mock de OSRM); nenhuma chamada a OSRM nesta task.
- Correção do `beforeId` no `addLayer` (rota acima dos tiles) e do PNG do stub (sem erros de decode no E2E) feitas dentro da task.

## Problemas encontrados

Nenhum bloqueante. Observações menores (não violam RN Alta nem NEG):

1. **Harness `/mapa-demo` é uma rota real no build estático.** Necessária agora para dar ponto de montagem ao E2E (o mapa só é integrado a uma etapa na TASK-017). Inerte (sem dados/persistência), declarada temporária no cabeçalho do arquivo. Não é resolvível *dentro* desta task sem quebrar o E2E → vira **follow-up** para a TASK-017, não condição de merge (ver Pendências).
2. **Import futuro de `@/shared/mapa` em teste de node quebraria** (o índice reexporta `mapa.tsx`, que importa o CSS do MapLibre). Mitigado e documentado no `src/shared/README.md`; os testes desta task importam os submódulos puros diretamente. Ponteiro para quem escrever testes de node no futuro.

## Violações de escopo

Nenhuma sobre o núcleo. O componente não implementa OSRM, regra dos 350 m, derivação de município nem lógica de Seção/Local/Parada — tudo deixado para as tasks posteriores. Única adição fora do módulo puro é o harness `mapa-demo` (infra de teste, ver Problemas item 1).

## Testes avaliados

- **Cobertura das RN da task:** sim — RN-074 (captura) em unit + E2E; partes puras (config/estilo/geometria) cobertas.
- **Casos inválidos testados:** sim — `urlDeTiles` valor em branco → default; `linhasParaGeoJson` linha < 2 pontos descartada; `capturarImagemMapa` sem canvas / canvas sem `toDataURL` → lança.
- **Regressão de UUID:** N/A — task não toca import/export/cópia de entidades.
- **OSRM mockado:** N/A — nenhuma chamada a OSRM; E2E intercepta tiles (sem depender de rede).
- **Suíte executada:** **verde** — `typecheck` OK, `lint` OK (0 warnings), `npm test` **455/455** (15 novos), `npx playwright test mapa-base` **1/1**, `npm run build` OK (`/mapa-demo` estático).

## Checklist 07

- **Escopo (6):** 6 OK (pertence ao ROTA; sem workflow; sem status além de `autos.status`; sem persistência de servidor; só o escopo — o harness é infra de teste declarada; nenhuma regra inventada — config de tiles citando DEC-029).
- **JSON (9):** N/A (nenhum contrato tocado; sem R$; sem campo novo).
- **Domínio (9):** N/A (nenhuma entidade de domínio tocada).
- **Comparador (7):** N/A.
- **Roteamento (5):** N/A (nenhuma chamada a OSRM).
- **UI/PDF (6):** RN-074 (captura de canvas) OK; demais itens de conteúdo de PDF N/A (TASK-033).
- **Testes (6):** 5 OK, 1 N/A (OSRM).
- **Resultado:** **0 violados**, ~12 OK, ~40 N/A.

## Pendências

- **Follow-up (TASK-017):** remover ou reaproveitar o harness `src/app/mapa-demo/` quando a etapa de itinerário passar a montar o mapa de verdade; o E2E de mapa deve então apontar para o ponto de montagem real.
- **Nota para testes futuros:** importar submódulos puros de `shared/mapa` (não o índice) em ambiente node.
- **Q-xxx:** nenhuma aberta. A questão do provedor de tiles em produção **não foi registrada como Q**, por decisão explícita do responsável nesta conversa; fica coberta pelo mecanismo de URL configurável (DEC-029, por analogia).

## Decisão

**Aprovado.** Escopo exato (primitiva de mapa), RN-074 e RN-097 atendidas com teste, contrato JSON intacto, nenhuma dependência de OSRM/rede nos testes, checklist sem itens violados, suíte verde (455 unit + 1 E2E) e build estático confirmando o isolamento do WebGL do SSR.

A única adição fora do núcleo puro (`/mapa-demo`) é infraestrutura de teste necessária e declarada temporária; por não ser resolvível dentro desta task, é registrada como follow-up da TASK-017 e não como condição de merge. Próxima task pode prosseguir (`docs-dev/00` §3).
