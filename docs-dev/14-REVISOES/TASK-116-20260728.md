# Revisão da TASK-116

**Revisor:** Codex
**Data:** 2026-07-28
**Commit/branch revisado:** `92414d2` + correção `213626d` / `redesign`

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

A entrega mantém a digitação de horário como rascunho local em
`CampoHorarioGrade`: `onChange` não cria nem edita Viagem, `Enter`/`Tab`
validam e confirmam, e `blur` descarta o rascunho. A correção `213626d`
também restaura `HH:MM` quando o usuário confirma uma grafia equivalente ao
valor persistido e acrescenta as regressões que faltavam.

Arquivos revisados:

- `src/formulario/viagens/campo-horario-grade.tsx`
- `src/formulario/viagens/etapa-viagens.tsx`
- `src/formulario/viagens/horario-relogio.ts`
- `testes/unitarios/formulario/campo-horario-grade.test.tsx`
- `testes/unitarios/formulario/etapa-viagens-foco.test.tsx`
- `testes/e2e/etapa-viagens.spec.ts`

## Regras RN verificadas

- **RN-061 — atendida.** A confirmação cria uma única Viagem no dia e na
  grade correntes; os discriminadores não foram alterados
  (`src/formulario/viagens/etapa-viagens.tsx:221`).
- **RN-063 — atendida.** A criação continua delegada ao motor que gera
  `horarios_paradas` completo; a edição fora de ordem continua recusada
  (`src/formulario/viagens/etapa-viagens.tsx:307`).
- **RN-064 — atendida.** A sugestão inicial só é disparada por
  `aoConfirmarCriacao`, depois da confirmação explícita
  (`src/formulario/viagens/etapa-viagens.tsx:221`).
- **RN-065 — atendida.** A redistribuição continua restrita à confirmação de
  passante e à Viagem editada (`src/formulario/viagens/etapa-viagens.tsx:307`).
- **RN-067 — atendida.** O campo recebe horário de relógio, mantém o rascunho
  local e normaliza a exibição final inclusive quando o valor é semanticamente
  equivalente (`src/formulario/viagens/campo-horario-grade.tsx:46`).
- **RN-095/RN-096 — atendidas.** Não há workflow, status de pedido,
  persistência transacional ou servidor.

## Specs verificadas

- **Spec 04 §2, itens 4–5 — aderente.** O usuário digita horário de relógio e
  os offsets permanecem internos.
- **Spec 04 §8.1 — aderente.** O valor confirmado é exibido em `HH:MM`,
  inclusive no caminho equivalente `0800` → `08:00`.
- **Spec 04 §8.2 — aderente.** Criação, sugestão inicial, redistribuição e
  bloqueio fora de ordem só ocorrem após confirmação válida.
- **Spec 02 §11/§11.1 — aderente.** O contrato não mudou;
  `horario_saida`/`offset_horario` continuam em `HH:MM:SS`.
- **Spec 03 §8.1/§8.2 — aderente.** Acúmulo, âncoras, interpolação, tail e
  monotonicidade não foram alterados.

## Pontos corretos

- `onChange` só aceita o alfabeto do campo e atualiza `rascunho`; não chama
  `aoConfirmar` (`src/formulario/viagens/campo-horario-grade.tsx:95`).
- `Enter`/`Tab` validam antes de navegar; valor inválido impede navegação e
  mantém foco/rascunho (`src/formulario/viagens/campo-horario-grade.tsx:65`).
- `blur` descarta sem confirmar e restaura o último valor persistido
  (`src/formulario/viagens/campo-horario-grade.tsx:101`).
- O caminho de valor equivalente chama `definirRascunho(normalizado)` sem
  editar novamente a Viagem (`src/formulario/viagens/campo-horario-grade.tsx:51`).
- O E2E digita `10:32` caractere a caractere, comprova ausência de criação
  intermediária/`01:03` e só então confirma
  (`testes/e2e/etapa-viagens.spec.ts:74`).
- As regressões exercem valor equivalente e parcial `10:3` com `Enter` e
  `Tab`, tanto no componente quanto na grade real
  (`testes/unitarios/formulario/campo-horario-grade.test.tsx:122`;
  `testes/e2e/etapa-viagens.spec.ts:113`).
- A navegação continua seguindo a UUID após criação/reordenação
  (`src/formulario/viagens/etapa-viagens.tsx:447`).

## Problemas encontrados

- Nenhum.

## Violações de escopo

- Nenhuma. O diff acumulado da task se limita ao campo compartilhado, à
  composição necessária com navegação/foco e aos testes de regressão.

## Consequências sobre outras tasks

- **TASK-105:** a grade excepcional ainda não é renderizada no arquivo real
  (`src/formulario/viagens/etapa-viagens.tsx:861`). A herança em runtime nessa
  futura superfície permanece **não verificada nesta revisão**; o E2E da
  TASK-105 deve exercê-la quando a grade existir.
- **TASK-106/TASK-115:** os destinos continuam calculados por
  `destinoNavegacaoGrade` e o foco pendente continua associado à UUID
  (`src/formulario/viagens/etapa-viagens.tsx:447`). As regressões E2E desses
  caminhos passaram.
- Não houve alteração da fronteira Formulário/Comparador/`shared` nem do
  contrato JSON.

## Testes avaliados

- **cobertura das RN da task:** sim — criação, edição, sugestão, passante,
  formato final e confirmação explícita estão cobertos.
- **casos inválidos testados:** sim — parcial `10:3`, horário impossível e
  passante fora de ordem.
- **regressão de UUID:** N/A; a task não toca importação/exportação/cópia.
- **OSRM mockado:** sim — o E2E intercepta o endpoint e confirma zero chamadas.
- **fixture canônica:** sim — `testes/fixtures/carregar-multi-servico.json`.
- **verificações:** `git diff 92414d2^..213626d --check`,
  `npm run lint` e `npm run typecheck`, todos verdes; o build verde consta na
  evidência canônica.
- **suíte executada com resultado:** verde. `npm run test:all:verificar`
  confirmou executor `Codex`, 96 arquivos/1.294 testes unitários, 86 E2E,
  build e códigos 0; fingerprint
  `886d8c53df61afac014e764264151832a28e8de4d625a541870398280b1aaae3` e
  identidade
  `498d7fa6397724af2122bdced82f79ddd9749373d9277b227d3e2a3884f91763`.
- **reutilização da evidência:** log válido reutilizado; a suíte pesada não foi
  repetida. O log registra a porta 3100 e a árvore do PID próprio encerrada.

## Checklist 07

Resultado: **24 itens OK, 29 N/A, 0 violados**.

### Escopo

| Item | Resultado | Evidência |
|---|---|---|
| Alteração pertence ao ROTA, não ao SEI (RN-095) | OK | Correção da grade de Viagens. |
| Não introduz workflow (RN-095) | OK | Nenhum estado administrativo. |
| Não cria status de pedido (RN-011) | OK | Contrato intocado. |
| Não cria persistência transacional (RN-096) | OK | Estado local client-side. |
| Implementa apenas o escopo da task | OK | Campo, composição e testes. |
| Nenhuma regra nova inventada | OK | TASK-116 e Specs 02/03/04. |

### JSON (contrato)

| Item | Resultado | Evidência |
|---|---|---|
| JSON só com dados de operação (RN-010) | OK | Nenhum campo novo. |
| JSON autossuficiente (RN-009) | OK | Nenhum recurso externo novo. |
| Nenhum campo sem Spec 02 | OK | Schema intocado. |
| UUIDs existentes preservadas (RN-004) | OK | Edição mantém a identidade. |
| Entidades novas recebem UUID nova (RN-002/007) | OK | Criação permanece no motor existente. |
| XOR de Parada (RN-033) | N/A | Paradas intocadas. |
| Sem R$ (RN-013) | OK | Nenhum valor monetário. |
| Unidades/arredondamento (RN-014/050) | N/A | Fora do escopo. |
| Congelados não recalculados por leitores (RN-015) | N/A | Leitores intocados. |

### Domínio

| Item | Resultado | Evidência |
|---|---|---|
| Seção e Local separados (RN-025/031) | N/A | Entidades intocadas. |
| Ponto de rota não vira Parada (RN-042) | N/A | Fora do diff. |
| Serviço mantém UUID (RN-001) | N/A | Serviço intocado. |
| `numero_n` não é identidade (RN-006) | N/A | Nenhuma lógica por número. |
| Extremos continuam Seções (RN-035) | N/A | Estrutura intocada. |
| Ida/Volta compartilham Seções (RN-030) | N/A | Estrutura intocada. |
| Tipificação respeitada (RN-019..022) | N/A | Fora do escopo. |
| Viagem estratificada (RN-061) | OK | Confirmação mantém dia e grade. |
| Feriado fora das contagens (RN-069) | N/A | Contagens intocadas. |

### Comparador

| Item | Resultado | Evidência |
|---|---|---|
| UUID como base (RN-081) | N/A | Comparador fora do diff. |
| Taxonomia de diff (RN-081..083) | N/A | Comparador fora do diff. |
| Comparativo não gravado (RN-080/097) | N/A | Comparador fora do diff. |
| Status/datas fora do diff (RN-012) | N/A | Comparador fora do diff. |
| Autos diferentes bloqueados (RN-084) | N/A | Comparador fora do diff. |
| Rota por sinais estáveis (RN-087) | N/A | Comparador fora do diff. |
| Sem OSRM/persistência no Comparador (RN-080) | N/A | Comparador fora do diff. |

### Roteamento

| Item | Resultado | Evidência |
|---|---|---|
| OSRM indisponível bloqueia rota inválida (RN-048) | N/A | Roteamento intocado. |
| Distância vem do OSRM (RN-047/050) | N/A | Distâncias intocadas. |
| Pontos de rota só condicionam traçado (RN-041..043) | N/A | Fora do diff. |
| Abrir JSON não chama OSRM (RN-052) | N/A | Importação intocada. |
| Mensagens §14 sem tarifa (RN-049) | N/A | Mensagens intocadas. |

### UI/PDF

| Item | Resultado | Evidência |
|---|---|---|
| Nome `Cidade - Nome da Seção` (RN-076) | N/A | Rótulos intocados. |
| Horário de relógio; offsets ocultos (RN-067/076) | OK | Rascunho local e saída `HH:MM`. |
| Matrizes triangulares sem R$ (RN-076) | N/A | Matrizes fora do diff. |
| Locais fora da grade principal (RN-031/044/076) | N/A | Linhas intocadas. |
| Aviso SEI no PDF (RN-077) | N/A | PDF fora do diff. |
| Gate de exportação (RN-078) | N/A | Exportação fora do diff. |
| Design system (DEC-050) | OK | `Campo` compartilhado; sem `style=`. |
| `data-testid`/`aria-*` preservados (DEC-050) | OK | Atributos existentes intactos. |

### Testes

| Item | Resultado | Evidência |
|---|---|---|
| Toda RN/regra implementada possui teste | OK | Unitários e E2E cobrem os critérios da task. |
| Casos inválidos testados | OK | `10:3`, `24:60` e fora de ordem. |
| Regressão de UUID quando aplicável | N/A | Não toca import/export/cópia. |
| OSRM mockado | OK | Endpoint interceptado; zero chamadas. |
| Fixtures canônicas reutilizadas | OK | `carregar-multi-servico.json`. |
| Direcionados/typecheck/lint/build reportados | OK | Diff check, lint, typecheck e build verdes. |
| Evidência final canônica única e válida | OK | Marcador, códigos, fingerprint e identidade confirmados. |
| Revisão reutilizou log ou executou uma vez | OK | Log válido reutilizado. |
| Porta/PIDs isolados e encerrados | OK | Porta 3100; PID próprio encerrado. |

## Pendências

- Nenhuma condição de merge.
- Nenhuma Q-xxx aberta.
- O E2E da futura grade excepcional permanece atribuído à TASK-105, sem impedir
  a conclusão da TASK-116.

## Decisão

**Aprovado.** A correção fecha a violação de RN-067 da primeira revisão, os
critérios de aceite estão cobertos por regressões válidas e inválidas, o escopo
permanece exato e todas as verificações estão verdes. A TASK-116 pode sair da
fila de pendentes.
