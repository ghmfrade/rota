# Revisão da TASK-083

**Revisor:** Codex
**Data:** 2026-07-20
**Commit/branch revisado:** `a12db0c` (`redesign`)

## Resultado

- [ ] Aprovado
- [ ] Aprovado com ressalvas
- [x] Reprovado

## Resumo da entrega

A entrega altera `reancorarPontosDeRota` para filtrar pontos órfãos após a remoção
de uma Parada de extremo e adiciona um aviso efêmero por Serviço/sentido em
`EtapaItinerarios`. Foram acrescentados testes puros para os dois extremos, a
pós-condição da RN-042 e um recálculo com OSRM mockado para a remoção da última
Parada.

## Regras RN verificadas

- **RN-042 — atendida na lógica, cobertura incompleta na UI.** A remoção filtra
  resultados fora de `[1, paradas.length - 1]` em
  `src/formulario/roteamento/reancorar-pontos-de-rota.ts:126`; os testes cobrem
  primeiro extremo, último extremo, remoção do meio e pós-condição em
  `testes/unitarios/formulario/roteamento-reancorar-pontos-de-rota.test.ts:70` e
  `testes/unitarios/formulario/roteamento-reancorar-pontos-de-rota.test.ts:129`.
  O aviso exigido pela DEC-068, porém, não é exercitado por teste.
- **RN-048 — atendida.** A defesa em profundidade para ponto importado inválido
  permanece em `src/formulario/roteamento/cliente-osrm.ts:54` e é coberta em
  `testes/unitarios/formulario/itinerarios-estado.test.ts:248`. O caminho normal
  novo recalcula com OSRM mockado em
  `testes/unitarios/formulario/itinerarios-estado.test.ts:361`.
- **RN-041 — atendida.** O recálculo depois do descarte usa apenas as Paradas
  restantes e o teste obtém rota recalculada sem criar trecho por ponto de rota
  (`testes/unitarios/formulario/itinerarios-estado.test.ts:361`).
- **RN-004..007 — N/A.** Não há importação, cópia, criação ou troca de identidade.
- **RN-010/RN-011 — atendidas.** Nenhum campo foi acrescentado ao contrato; o
  aviso fica em estado React efêmero (`src/formulario/itinerarios/etapa-itinerarios.tsx:125`).
- **RN-095/RN-096 — atendidas.** Não há workflow nem persistência transacional.

## Specs verificadas

- **Spec 02 §10.4 — aderente.** A função impede que a remoção devolva
  `apos_parada_ordem` fora do intervalo e preserva os demais pontos.
- **Spec 03 §3.6/§3.6.1/§3.6.2 — aderente.** O ponto continua sendo somente
  condicionante de traçado, sem identidade e sem criar Parada ou trecho.
- **DEC-068 — implementação aderente, verificação incompleta.** O descarte e o
  aviso efêmero foram implementados, mas o aviso não foi comprovado por teste da etapa.

## Pontos corretos

- A filtragem usa a pós-condição da RN-042, sem tentar detectar extremos por uma
  regra paralela (`src/formulario/roteamento/reancorar-pontos-de-rota.ts:126`).
- Os testes puros cobrem remoção da primeira e da última Parada, preservação dos
  demais pontos, remoção do meio e limites da pós-condição.
- O teste de recálculo usa `fetchFn` mockado e confirma chamada única ao OSRM,
  sem depender do serviço público
  (`testes/unitarios/formulario/itinerarios-estado.test.ts:361`).
- O aviso usa `role="status"`, é efêmero e não entra nas pendências bloqueantes
  nem no JSON (`src/formulario/itinerarios/etapa-itinerarios.tsx:691`).
- O diff está restrito aos quatro arquivos previstos pela task; a rede da
  RN-048 e o contrato JSON não foram alterados.

## Problemas encontrados

1. **Alta — critério de aceite sem teste (RN-042/DEC-068).** Não existe teste que
   renderize `EtapaItinerarios`, remova uma Parada de extremo e confirme
   `aviso-ponto-de-rota-descartado`, nem que prove que esse aviso não entra nas
   pendências bloqueantes. A busca pelo `data-testid` encontra somente a
   implementação em `src/formulario/itinerarios/etapa-itinerarios.tsx:696`. O
   teste chamado de integração em
   `testes/unitarios/formulario/itinerarios-estado.test.ts:361` compõe diretamente
   as funções puras e não exercita o estado `descartePontoDeRotaMapa`, a tabela
   lateral ou a renderização do aviso. Pelo template de revisão, ausência de
   teste da regra implementada reprova a entrega.
2. **Alta — suíte completa vermelha.** `npm test -- --run` terminou com 19
   suítes `.tsx` sem importar (`Cannot find module '/@fs/C:/Projetos/ROTA/...'`):
   65 arquivos e 879 testes passaram, mas o processo saiu com código 1. A causa
   observada é de resolução de caminho no ambiente de execução e não uma
   asserção funcional da TASK-083; ainda assim, o protocolo proíbe registrar a
   verificação como aprovada enquanto falhar.

## Violações de escopo

- Nenhuma.

## Testes avaliados

- cobertura das RN da task: **não completa** — descarte, limites e recálculo
  cobertos; exibição e caráter não bloqueante do aviso sem teste;
- casos inválidos testados: **sim** — ponto cru fora do intervalo continua
  resultando em `sem-rota`, sem chamar o OSRM;
- regressão de UUID (se aplicável): **N/A**;
- OSRM mockado: **sim**;
- suíte executada com resultado: **vermelho** — suíte completa: 19 arquivos
  falharam na importação, 65 passaram, 879 testes passaram; testes alterados
  isolados: 2 arquivos, 31 testes, todos verdes; `npm run typecheck` e
  `npm run lint`: verdes.

## Checklist 07

**Resultado: 16 itens ok, 32 N/A, 2 violados.**

### Escopo

- RN-095, sem gestão do SEI: **OK**.
- RN-095, sem workflow: **OK**.
- RN-011, sem status de pedido: **OK**.
- RN-096, sem persistência transacional: **OK**.
- somente escopo da task: **OK**.
- nenhuma regra inventada: **OK** — comportamento vem da DEC-068.

### JSON (contrato)

- somente dados de operação: **OK**.
- autossuficiência de leitores: **N/A**.
- nenhum campo novo: **OK**.
- preservação de UUID importada: **N/A**.
- UUID nova para entidade nova/cópia: **N/A**.
- XOR Seção/Local: **N/A**.
- nenhum valor monetário: **OK**.
- unidades e half-up: **N/A**.
- dados congelados não recalculados por leitores: **N/A**.

### Domínio

- Seção e Local separados: **N/A**.
- ponto de rota não tratado como entidade/Parada/trecho: **OK**.
- identidade de Serviço por UUID: **N/A**.
- `numero_n` fora da identidade: **N/A**.
- extremos como Seções: **N/A**.
- conjunto de Seções Ida/Volta: **N/A**.
- tipificação: **N/A**.
- estratificação de Viagem: **N/A**.
- feriado fora das contagens: **N/A**.

### Comparador

- Os sete itens de comparação, diff, `status`, Autos, rota estável e ausência de
  OSRM/persistência: **N/A**.

### Roteamento

- falha do OSRM sem fallback: **OK** — defesa da RN-048 preservada.
- distância exclusivamente do OSRM: **N/A** — caminho existente não alterado.
- pontos somente condicionam traçado e preservam contagem: **OK**.
- abrir JSON sem OSRM: **N/A**.
- mensagens de erro da Spec 04 §14: **N/A**.

### UI/PDF

- nomes de Seção: **N/A**.
- horários/offsets: **N/A**.
- matrizes: **N/A**.
- Locais no PDF/grade: **N/A**.
- aviso SEI no PDF: **N/A**.
- bloqueio de exportação: **N/A**.
- design system: **OK** — token de classe, sem `style=` inline.
- seletores e `aria-*` existentes preservados: **OK**.

### Testes

- toda regra implementada possui teste: **VIOLADO** — aviso não testado.
- casos inválidos: **OK**.
- regressão UUID: **N/A**.
- OSRM mockado: **OK**.
- fixtures canônicas: **N/A** — nenhum JSON de contrato novo foi criado.
- verificações executadas e verdes: **VIOLADO** — suíte completa saiu com código 1.

## Pendências

- Acrescentar teste de composição/UI que execute a remoção de primeira e/ou
  última Parada pela etapa, confirme o aviso e demonstre que ele não é uma
  pendência bloqueante.
- Corrigir ou contornar de modo reproduzível a resolução `/@fs/C:/Projetos/ROTA`
  das suítes `.tsx` e executar novamente a suíte completa com saída verde.
- Rodar uma nova revisão de aderência depois das correções; até lá a TASK-083
  permanece pendente e não deve avançar como concluída.

## Decisão

**Reprovado.** A implementação de domínio está aderente e os testes diretamente
alterados passam, mas há um critério de aceite de UI sem teste e a verificação
completa está vermelha. Ambos são critérios explícitos de reprovação do template.

