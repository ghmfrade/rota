# Revisão da TASK-092

**Revisor:** Claude (Opus 4.8), revisão de aderência em conversa separada da implementação
**Data:** 2026-07-22
**Commit/branch revisado:** `8e33b09` (branch `redesign`)

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Ajuste de apresentação da coluna "Mover" da tabela lateral de itinerários: os dois contêineres das setas ↑/↓ passaram de `flex flex-wrap gap-1` para `flex flex-nowrap gap-1` (`src/formulario/itinerarios/etapa-itinerarios.tsx:916` para ponto de rota e `:1032` para Parada), com comentário citando TASK-092/DEC-073. No mesmo ciclo entraram três testes de integração em `testes/unitarios/formulario/etapa-itinerarios.test.tsx`: guarda do `flex-nowrap` nos dois contêineres (`:265-281`), desseleção sobre linha de Local extremo preservando o estado vermelho (`:893-938`, follow-up herdado da 064/091) e composição das quatro colunas/vocabulário de Tipo/`aria-label` do "X" (`:940-995`, follow-up da 091). Nenhum outro arquivo tocado.

## Regras RN verificadas

- **RN-025** — atendida com teste. Coluna Tipo exibe `Seção` (`etapa-itinerarios.tsx:985`); o teste-guarda afere o vocabulário nas linhas de Seção (`etapa-itinerarios.test.tsx:976,978`). Nenhuma mudança de modelo.
- **RN-031** — atendida com teste. `Local de parada` na coluna Tipo (`etapa-itinerarios.tsx:985`), aferido em `etapa-itinerarios.test.tsx:977`; sem promoção nem papel novo.
- **RN-042** — atendida com teste. Linha de ponto de rota segue `ponto-rota-item` (nunca `parada-item`), nome "Ponto de Rota N (lat, long)", célula Tipo vazia (`etapa-itinerarios.tsx:885,893,911`); o teste-guarda afere a sequência de `data-testid` das linhas, a célula Tipo vazia e o `aria-label="Remover Ponto de Rota 1"` (`etapa-itinerarios.test.tsx:965-971,982,989-991`).
- **RN-035 + DEC-070** — atendida com teste. A desseleção (segundo clique) remove só `aria-current`/`bg-azul-100` e preserva `data-estado="local-extremo"`, `text-erro` e `aria-invalid` do alvo `parada-local-extremo` (`etapa-itinerarios.tsx:996,1006-1010,1018-1019`; teste em `etapa-itinerarios.test.tsx:920-931`), com contagem de chamadas do fetch mockado provando zero OSRM (`:911,932`).
- **RN-096** — atendida. Só classes CSS e testes; nada persistido, nenhum arquivo de `shared/contrato` no diff, seleção/desseleção segue estado de UI efêmero.

## Specs verificadas

- **Spec 04 §7/§7.3** — aderente. A tabela lateral mantém estrutura, handlers (`moverParada`, `moverPontoDeRotaNaTabela`, `removerParadaNaTabela`, `aoRemoverPontoDeRota`) e regras de desabilitar intocados; só a decoração do contêiner das setas mudou.
- **doc 18 §3/§5 (DEC-050/073, vinculante)** — aderente. `flex-nowrap` é utilitário de layout no chamador, sem `style=` inline, sem tocar `Tabela`/`Botao` nem tokens; devolve a linha à altura compacta que a DEC-073 (densidade `compacta`) pretendia. O risco de overflow apontado pela task é absorvido pelo wrapper `overflow-x-auto` da `Tabela` (`src/shared/ui/tabela.tsx:52`), verificado nesta revisão.

## Pontos corretos

- Escopo cirúrgico: o diff de `8e33b09` contém exatamente os dois arquivos previstos em "Arquivos prováveis"; os dois únicos pontos de código alterados são os contêineres nomeados na task (`:913-916` e `:1029-1032` no estado atual).
- Os demais `flex flex-wrap` do app (viagens, matrizes, layout, tela inicial etc.) permanecem intactos — nenhum "aproveitamento" fora da coluna Mover (grep de `flex-wrap|flex-nowrap` em `src/` conferido nesta revisão).
- O teste do critério 1 cobre os dois tipos de linha (Parada e ponto de rota) e afere tanto a presença de `flex-nowrap` quanto a ausência de `flex-wrap` (caso inválido da task).
- O teste de desseleção segue o padrão do toggle existente e reutiliza `montarSessaoNaEtapa`/`paradaDeLocal`/`paradaDeSecao`/fixture canônica `documentoExemploMinimo` (`etapa-itinerarios.test.tsx:22-23,158,205`), com OSRM mockado globalmente em `beforeEach` (`:229-230`).
- O teste de composição fixa os quatro cabeçalhos por igualdade estrita (`["Cidade - Nome", "Tipo", "Mover", "Remover"]`), a ordem de travessia (ponto de rota após a Parada de ordem 1) e o vocabulário completo da coluna Tipo — fecha o problema 1 do parecer da 091.
- Nenhum `data-testid`/`aria-*`/handler alterado (diff só de `className` + comentários); E2E intocados e verdes (62 aprovados no log canônico).
- Os três testes novos batem com o delta da suíte: 1124 → 1127 testes.

## Problemas encontrados

1. **(Baixa — processo/rastreabilidade)** No momento da revisão, a própria definição da TASK-092 (`docs-dev/06-BACKLOG_INICIAL.md`, +91 linhas) e o registro de criação em `docs-dev/19-STATUS_EXECUCAO.md` estavam **não commitados**: o commit de implementação `8e33b09` entrou no histórico referenciando uma task cuja entrada de backlog ainda estava só no working tree (mesma classe do problema 3 do parecer da 091). **Resolvido nesta revisão:** os dois arquivos entram no commit deste parecer, fechando o ciclo com a task no histórico.

## Violações de escopo

- Nenhuma. Nada do "Fora de escopo" foi tocado: decisões da DEC-073 (colunas, vocabulário, densidade, tamanho de botões, precedência de canais), tokens, doc 18, handlers, `data-testid`/`aria-*`, sincronização tabela↔mapa e contrato JSON permanecem intactos — apenas testados, não alterados.

## Testes avaliados

- cobertura das RN da task: **sim** — RN-025/031/042 no teste de composição; RN-035/DEC-070 no teste de desseleção; critério 1 no teste de `flex-nowrap`.
- casos inválidos testados: **sim** — wrap reintroduzido (`not.toContain("flex-wrap")`), desseleção que apagasse `data-estado`/`text-erro`/`aria-invalid`, regressão de coluna/vocabulário/`aria-label` (igualdade estrita).
- regressão de UUID: **N/A** (import/export/cópia não tocados).
- OSRM mockado: **sim** — `vi.stubGlobal("fetch", respostaOsrmMock())` em `beforeEach` (`etapa-itinerarios.test.tsx:229-230`); o teste de desseleção prova zero chamadas adicionais por contagem (`:911,932`).
- suíte executada com resultado: **verde, log canônico reutilizado** — `npm run test:all:verificar`: "Log canônico válido. Executor: Claude. Fingerprint: `1bbd0e3c6656760df1c837a580d5d020c55829b00e94d76ee3e0e4a6a56f0fff`. Working tree: `00c21aa501d19b4a9ade6b12db96274b9a6725e300e14f1fda33b4dc1336f57e`." Conteúdo do log: Vitest **92 arquivos / 1127 testes aprovados**; Playwright **62 aprovados**; códigos 0/0; "Resultado geral: APROVADO"; limpeza do PID próprio registrada (`taskkill /PID 24464`). A suíte **não** foi repetida nesta revisão. Adicionalmente, `npm run typecheck` e `npm run lint` executados nesta revisão: limpos.

## Checklist 07

**Resultado: 21 itens ok, 32 N/A, 0 violados.**

- **Escopo (6/6 ok):** pertence ao ROTA; sem workflow; sem status de pedido; sem persistência indevida; só o escopo da task (diff cirúrgico); nenhuma regra inventada — vocabulário e formato vêm da DEC-073, aqui apenas testados.
- **JSON (9 N/A):** contrato intocado; nenhum campo, UUID, parada ou valor alterado.
- **Domínio (3 ok, 6 N/A):** Seção/Local seguem entidades separadas (vocabulário testado); ponto de rota não tratado como Parada (RN-042, testado); extremos-Seção (RN-035) — o estado de erro da ocorrência inválida é preservado pela desseleção, com teste. Demais itens não tocados.
- **Comparador (7 N/A).**
- **Roteamento (1 ok, 4 N/A):** nenhuma chamada OSRM nos gestos testados (contagem do mock); falha/conversão/mensagens/invariantes não tocadas.
- **UI/PDF (3 ok, 5 N/A):** `Cidade - Nome` (RN-076) aferido no cabeçalho e nos rótulos; design system aderente (utilitário de layout, sem `style=` inline, componentes/tokens intocados); `data-testid`/`aria-*` preservados (diff só de classes; E2E verdes sem alterar seletores). Offsets/matrizes/aviso SEI/exportação: N/A.
- **Testes (8 ok, 1 N/A):** RN da task com teste; casos inválidos testados; OSRM mockado; fixtures canônicas reutilizadas; typecheck/lint executados e limpos; evidência única canônica válida reutilizada sem repetir a suíte; porta/PIDs limpos no log. UUID: N/A.

## Pendências

- Nenhuma condição de merge. O problema 1 (backlog/status não commitados) é resolvido no próprio commit deste parecer.
- Consequências sobre outras tasks, verificadas nesta revisão: os follow-ups do parecer da 091 (problemas 1 e 2, o segundo herdado da ressalva 2 da 064) estão **fechados** pelos dois testes-guarda lidos nesta revisão; a condição da 091 (commitar o registro da DEC-073) foi cumprida em `a4130a6`. A próxima task do Grupo B volta a ser a **TASK-070** (ponteiro; conteúdo não verificado nesta revisão).
- Nenhuma Q-xxx aberta por esta revisão.

## Decisão

**Aprovado.** Escopo exato (dois contêineres, dois arquivos), critérios de aceite 1–5 todos cobertos por teste ou por evidência canônica (altura da linha aferida pelo proxy de classe `flex-nowrap`, exatamente como os "Testes esperados" da task prescrevem), RN-025/031/035/042/096 atendidas com testes diretos, zero OSRM provado por contagem, nenhum seletor/handler alterado, suíte canônica verde (1127 unitários + 62 E2E, log válido reutilizado), typecheck e lint limpos. Nenhuma RN Alta ou NEG-xxx violada; checklist sem itens violados. O único achado (registro da task não commitado) é de processo e se resolve no commit deste parecer, que inclui `docs-dev/06` e `docs-dev/19`.
