# Revisão da TASK-103

**Revisor:** Codex
**Data:** 2026-07-27
**Commit/branch revisado:** `99a64c5` / `redesign`

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

O módulo puro `src/shared/contagens/contagens.ts` passou a considerar na semana
padrão somente Viagens da grade comum: `viagem_feriado = false` e
`tabela_excepcional_uuid = null`. O mesmo módulo fornece a fonte única do novo
rótulo, consumida pelas telas de Serviços, Resumo operacional e pela legenda da
grade de feriados. Testes unitários e E2E cobrem fórmula, invariância e exibição.

## Regras RN verificadas

- RN-069 — **atendida**. A porta única `viagensSemanaPadrao` aplica os dois
  predicados em `src/shared/contagens/contagens.ts:51`; `viagensSemana`,
  contagens por Serviço/Autos e estratificação por faixa passam por essa porta
  (`src/shared/contagens/contagens.ts:60`, `src/shared/contagens/contagens.ts:128`,
  `src/shared/contagens/contagens.ts:194`). O caso literal 5 comuns + 2
  excepcionais + 1 feriado e a invariância global/por faixa estão em
  `testes/unitarios/contagens/contagens.test.ts:119` e
  `testes/unitarios/contagens/contagens.test.ts:247`.
- RN-099 — **atendida no recorte da TASK-103**. Viagens com
  `tabela_excepcional_uuid` não nula são excluídas das contagens em
  `src/shared/contagens/contagens.ts:51`; a tela de Serviços comprova a mesma
  invariância em `testes/unitarios/formulario/servicos-contadores.test.tsx:103`.
  CRUD, grade excepcional, cópia e diff permanecem fora desta task.
- RN-072 — **preservada**. As fórmulas de viagens e opções continuam centralizadas
  em `src/shared/contagens/contagens.ts:60` e
  `src/shared/contagens/contagens.ts:128`; apenas o conjunto de Viagens de entrada
  foi corrigido conforme a definição atual de semana padrão.
- NEG-018 — **atendida conforme a RN-069 vigente**. Feriado e operação
  excepcional não entram nas contagens; o rótulo atualizado tem fonte única em
  `src/shared/contagens/contagens.ts:31`.

## Specs verificadas

- Spec 03 §9.2 — **aderente**: somente a grade comum entra nas contagens; a
  invariância a feriado e operação excepcional está coberta.
- Spec 03 §9.4 — **aderente**: `viagens_semana` conta objetos de Viagem comuns, e
  as opções de deslocamento continuam derivadas dessa quantidade.
- Spec 04 §10 — **aderente**: Resumo operacional e Serviços exibem o rótulo
  completo pela constante compartilhada
  (`src/formulario/resumo/resumo-operacional.tsx:53` e
  `src/formulario/servicos/servicos.tsx:385`).
- Spec 04 §13.3 — **aderente no artefato reutilizável**: a fonte compartilhada
  contém literalmente “semana padrão (sem feriados nem operação excepcional)”.
  A geração do PDF permanece fora do escopo e ainda não foi verificada nesta
  revisão.

## Pontos corretos

- O filtro foi alterado em uma única porta de domínio, sem duplicar a fórmula em
  componentes.
- A constante `ROTULO_SEMANA_PADRAO` é a única ocorrência do rótulo em código de
  produção; não restou o literal antigo em `src/`.
- A invariância foi provada tanto nas contagens totais quanto nas contagens por
  faixa de horário.
- A UI da etapa Serviços possui teste de integração contra uma Viagem
  excepcional realista, associada a uma Tabela excepcional.
- O diff está restrito ao módulo de contagens, aos consumidores do rótulo e aos
  testes correspondentes; contrato JSON, UUID, PDF e Comparador não foram
  alterados.

## Problemas encontrados

- Nenhum.

## Violações de escopo

- Nenhuma. CRUD/grade excepcional, PDF e Comparador não foram implementados.

## Testes avaliados

- cobertura das RN da task: **sim** — RN-069 e o recorte de RN-099 foram
  cobertos por fórmula dirigida, invariância de documento, estratificação por
  faixa, integração da tela e conteúdo do rótulo.
- casos inválidos testados: **N/A** — cálculo puro, conforme a própria task; os
  casos não contáveis (feriado, excepcional e sentido ausente) foram exercitados.
- regressão de UUID (se aplicável): **N/A** — importação, exportação, edição e
  cópia não foram tocadas.
- OSRM mockado: **N/A** — a task não toca roteamento; a suíte E2E não dependeu do
  OSRM público.
- suíte executada com resultado: **verde** — log canônico reutilizado após
  `npm run test:all:verificar`: executor `Codex`, 94 arquivos/1.255 testes
  unitários aprovados, 73 E2E aprovados, build estático aprovado, códigos 0 e
  limpeza da árvore do PID próprio confirmada. Fingerprint
  `23722e05a4bf659fd276132d90ad1aa19e4f408a383ee4a05f0c7412ed3a8b8b`;
  identidade do working tree
  `498d7fa6397724af2122bdced82f79ddd9749373d9277b227d3e2a3884f91763`.
  A revisão não repetiu a suíte. `npm run lint` e `npm run typecheck` também
  terminaram com código 0.

## Checklist 07

Resultado: **16 itens OK, 37 N/A, 0 violados**.

### Escopo

- [OK] A alteração pertence ao ROTA, e não ao SEI (RN-095).
- [OK] Não introduz workflow (RN-095).
- [OK] Não cria status de pedido (RN-011).
- [OK] Não cria persistência transacional (RN-096).
- [OK] Implementa apenas o escopo da TASK-103.
- [OK] Não inventa regra; fórmula e rótulo vêm das Specs 03 §9.2/§9.4 e
  04 §10/§13.3.

### JSON (contrato)

- [N/A] Dados de operação no JSON (RN-010) — contrato não alterado.
- [N/A] JSON autossuficiente (RN-009) — contrato/leitores não alterados.
- [N/A] Campo novo sem Spec 02 — nenhum campo criado.
- [N/A] Preservação de UUID na importação/edição (RN-004) — fluxo não tocado.
- [N/A] UUID nova para entidade nova/cópia (RN-002/RN-007) — fluxo não tocado.
- [N/A] XOR de Parada (RN-033) — não tocado.
- [N/A] Valores monetários no JSON (RN-013) — não tocado.
- [N/A] Unidades/arredondamento (RN-014/RN-050) — não tocados.
- [N/A] Dados congelados (RN-015) — não tocados.

### Domínio

- [N/A] Separação Seção × Local (RN-025/RN-031).
- [N/A] Ponto de rota (RN-042).
- [N/A] Identidade de Serviço por UUID (RN-001).
- [N/A] `numero_n` como identidade (RN-006).
- [N/A] Extremos como Seções (RN-035).
- [N/A] Conjunto de Seções Ida/Volta (RN-030).
- [N/A] Tipificação (RN-019..022).
- [OK] Viagem permanece estratificada; o cálculo lê
  `viagem_feriado`/`tabela_excepcional_uuid` sem alterar o modelo (RN-061).
- [OK] Feriado e operação excepcional ficam fora das contagens, com o rótulo
  vigente da RN-069. O literal antigo no item do checklist está superado pela
  Spec 04 §10/§13.3 e pela própria RN-069 atualizada.

### Comparador

- [N/A] Casamento por UUID (RN-081).
- [N/A] Taxonomia de entidade (RN-081..083).
- [N/A] Imutabilidade das entradas (RN-080/RN-097).
- [N/A] Exclusão de status/datas do diff (RN-012).
- [N/A] Bloqueio de Autos diferentes (RN-084).
- [N/A] Comparação de rota (RN-087).
- [N/A] Comparador sem OSRM/persistência (RN-080).

### Roteamento

- [N/A] Falha OSRM bloqueante (RN-048).
- [N/A] Distância vinda do OSRM (RN-047/RN-050).
- [N/A] Invariante de pontos de rota/trechos (RN-041..043).
- [N/A] Abertura sem OSRM (RN-052).
- [N/A] Mensagens de erro (RN-049).

### UI/PDF

- [N/A] Padrão de nomes de Seção (RN-076).
- [N/A] Horários de relógio/offsets (RN-067/RN-076).
- [N/A] Matrizes (RN-076).
- [N/A] Locais fora da grade/PDF principal (RN-031/RN-044/RN-076).
- [N/A] Aviso SEI no PDF (RN-077).
- [N/A] Bloqueio de exportação (RN-078).
- [OK] UI aderente ao design system; houve apenas troca de texto/importação de
  constante, sem `style=` ou componente cru novo (DEC-050).
- [OK] `data-testid` e `aria-*` foram preservados; E2E passou sem mudança de
  seletores (DEC-050).

### Testes

- [OK] RN-069/RN-099 possuem testes dirigidos e de invariância (matriz 03).
- [N/A] Caso inválido de domínio — cálculo puro; cenários não contáveis e
  sentido ausente foram testados.
- [N/A] Regressão de UUID — import/export/cópia não tocados.
- [N/A] Mock OSRM — roteamento não participa desta task.
- [OK] Fixtures canônicas foram reutilizadas nos testes de integração; o helper
  sintético existente foi estendido somente com os campos do schema vigente.
- [OK] Testes direcionados integram a suíte válida; lint, typecheck e build
  foram confirmados verdes.
- [OK] Uma única evidência final canônica tem marcador, códigos 0, fingerprint
  atual e identidade do mesmo working tree.
- [OK] A revisão reutilizou o log válido após `test:all:verificar`, sem repetir
  a suíte.
- [OK] O log confirma porta dedicada, encerramento da árvore do PID próprio e
  resultado geral aprovado.

## Pendências

- Nenhuma condição de merge e nenhuma Q-xxx aberta pela TASK-103.
- Ponteiros de sequenciamento, sem afirmar implementação ainda inexistente:
  TASK-034 deve consumir o rótulo no PDF; TASK-036/037/039 devem reutilizar o
  módulo/rótulo no Comparador. Essas superfícies permanecem não verificadas
  nesta revisão.

## Decisão

**Aprovado.** A entrega corresponde exatamente ao objetivo, atende as regras de
criticidade alta RN-069/RN-099 no recorte da task, preserva RN-072, possui testes
dirigidos e de invariância e passou por todas as verificações aplicáveis. Não há
violação de escopo, NEG-xxx, contrato JSON ou condição pendente para merge.
