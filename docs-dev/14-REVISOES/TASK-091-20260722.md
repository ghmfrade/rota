# Revisão da TASK-091

**Revisor:** Claude (Opus 4.8), revisão de aderência em conversa separada da implementação
**Data:** 2026-07-22
**Commit/branch revisado:** `f1eca46` (branch `redesign`)

## Resultado

- [ ] Aprovado
- [x] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Redesenho de apresentação da tabela lateral unificada da etapa de itinerários: quatro colunas explícitas (`Cidade - Nome` · Tipo · Mover · Remover) na mesma linha por item, "X" compacto de remover com `aria-label`, setas compactas, densidade e zebra de maior contraste como prop nova `densidade="compacta"` de `Tabela` e prop nova `tamanho="compacto"` de `Botao`. Arquivos: `src/formulario/itinerarios/etapa-itinerarios.tsx`, `src/shared/ui/tabela.tsx`, `src/shared/ui/botao.tsx`, `docs-dev/18-DESIGN_SYSTEM.md` e testes em `testes/unitarios/shared-ui/{botao,tabela}.test.tsx` + ajuste de rótulo em `testes/unitarios/formulario/etapa-itinerarios.test.tsx`. Nenhum dado, handler ou regra de montagem/rota mudou.

## Regras RN verificadas

- **RN-025** — atendida. O rótulo de Seção continua vindo de `nomeExibicaoSecao` (`etapa-itinerarios.tsx:976`), padrão `Cidade - Nome`; a coluna "Tipo" exibe `Seção` (`etapa-itinerarios.tsx:982`). Nenhuma mudança de modelo.
- **RN-031** — atendida. Local distinguido na coluna Tipo como `Local de parada` (`etapa-itinerarios.tsx:982`), rótulo via `nomeExibicaoLocal`; sem promoção nem papel novo.
- **RN-042** — atendida com teste. Ponto de rota segue item discriminado: `data-testid="ponto-rota-item"`/`data-tipo="ponto-de-rota"` (`etapa-itinerarios.tsx:893-894`), nunca `parada-item`; coluna de nome exibe "Ponto de Rota N (lat, long)" (`etapa-itinerarios.tsx:885,904-910`), célula Tipo vazia (`etapa-itinerarios.tsx:911`, DEC-073 item 1); sem `Cidade - Nome`. Teste: `testes/unitarios/formulario/etapa-itinerarios.test.tsx:245-259` (linha não é `parada-item`, texto não contém "Santos -").
- **RN-096** — atendida. Redecoração é estado de UI puro: nenhum campo novo, nenhuma persistência, nenhum toque em import/export ou contrato JSON (diff de `f1eca46` não contém arquivos de `shared/contrato`).

## Specs verificadas

- **Spec 04 §7/§7.3** — aderente. A tabela lateral segue listando Seções, Locais e pontos de rota na ordem da travessia, com reordenar (setas → `moverParada`/`moverPontoDeRotaNaTabela`, `etapa-itinerarios.tsx:926-946,1032-1041`) e remover (`removerParadaNaTabela`/`aoRemoverPontoDeRota`, `etapa-itinerarios.tsx:958,1053`) preservados. A intercalação na tabela única é a DEC-060 (anterior a esta task), não regressão.
- **Spec 02 §5** — aderente. Convenção `Cidade - Nome` no cabeçalho e nos rótulos de Seção; nenhum campo do JSON tocado.
- **Spec 02 §10.1/§10.4** — aderente. Ordem de travessia e ancoragem intocadas; a task não altera `apos_parada_ordem` nem os motores de reancoragem.
- **doc 18 §2/§3/§4 (DEC-050, vinculante)** — aderente. Variação **por prop** (`densidade` em `tabela.tsx:11-15`, `tamanho` em `botao.tsx:14-19`), nunca por `className` do chamador; token global de zebra intocado (densidade `padrao` mantém `cinza-50` — `tabela.tsx:31-34`); doc 18 atualizado no mesmo ciclo (§3 `Botao`/`Tabela` e §5 nova entrada da tabela lateral), como a DEC-073 exigia.

## Pontos corretos

- Os cinco itens da **DEC-073** implementados fielmente: (1) Tipo só para Seção/Local, célula vazia para ponto de rota; (2) "Ponto de Rota N (lat, long)" com "R" maiúsculo; (3) "X" como `Botao` `fantasma`+`compacto` com `aria-label="Remover …"`; (4) zebra `cinza-100` + `py-1.5` só na variante `compacta`; (5) precedência de canais — zebra/hover excluem a linha `aria-current="true"` nas **duas** densidades (`tabela.tsx:25-38`), seleção `bg-azul-100` vence sempre; erro em texto/borda (`etapa-itinerarios.tsx:1003-1007`) e ciano em texto (`etapa-itinerarios.tsx:900`) em canais físicos distintos.
- Todos os `data-testid` preservados (`parada-remover`, `remover-ponto-rota`, `parada-mover-cima/-baixo`, `ponto-rota-mover-cima/-baixo`, `tabela-paradas`, `parada-item`, `ponto-rota-item`, `parada-rotulo`, `parada-local-extremo`) — E2E verdes sem alterar seletores (`testes/e2e/etapa-itinerarios.spec.ts:876,884` usa `getByTestId("parada-remover")`).
- Regras de desabilitar das setas intactas (extremos em `etapa-itinerarios.tsx:1031,1040`; `podeMoverPontoDeRotaNaLista` em `918-925,936-943`).
- Testes novos das variantes: `botao.test.tsx:106-131` (padrão sem prop; compacto não altera variante/handlers) e `tabela.test.tsx:79-122` (default não emite classes da compacta; compacta preserva a exclusão `:not([aria-current="true"])` — o mecanismo da precedência seleção>zebra).
- Nenhum `style=` inline introduzido; nenhuma outra tabela do app afetada (default `padrao` inalterado).
- Comentários de código citando DEC-073/doc 18 §4 nos pontos de decisão.

## Problemas encontrados

1. **(Baixa — lacuna de teste direcionado; task "Testes esperados")** A task pede teste de integração de que "`EtapaItinerarios` renderiza as quatro colunas por item". Não há asserção dos quatro cabeçalhos (`Cidade - Nome`/`Tipo`/`Mover`/`Remover`), do vocabulário da coluna Tipo (`Seção`/`Local de parada`/célula vazia) nem do `aria-label` do "X" em `etapa-itinerarios.test.tsx` — a única mudança lá foi o caso do rótulo (`:258`). Os critérios de aceite 1–3 estão satisfeitos no código (verificado nesta revisão) e indiretamente pela suíte verde, mas sem teste-guarda próprio; uma regressão de coluna/vocabulário passaria despercebida.
2. **(Baixa — follow-up herdado da 064 não absorvido)** A revisão da TASK-064 (`14-REVISOES/TASK-064-20260722.md:45,75`) recomendou absorver na bateria de composição da 091 o teste "desselecionar Local extremo preserva o estado vermelho". Existe teste de **seleção** sobre a linha inválida (`etapa-itinerarios.test.tsx:857-871`) e de toggle em linha comum (`:757`), mas a desseleção sobre a linha inválida segue sem teste direto. Era recomendação, não condição — permanece follow-up aberto.
3. **(Processo — rastreabilidade)** O registro da **DEC-073** (que resolve a Q-052 e desbloqueia esta task) está **não commitado** no working tree (`docs-dev/10-DECISION_LOG.md` e `docs-dev/16-OPEN_QUESTIONS.md` modificados, sem commit). A implementação `f1eca46` entrou no histórico dependendo de uma decisão cujo registro ainda está fora dele. Condição: commitar o registro da DEC-073.

## Violações de escopo

- Nenhuma. Os arquivos tocados são exatamente os "arquivos prováveis" da task (menos `globals.css`, dispensado pela solução via classes utilitárias no componente) mais `botao.tsx`, cuja prop `tamanho` é exigida pela DEC-073 item 3 combinada com doc 18 §4 (variação por prop). Nenhum motor, handler, contrato ou marcador de mapa alterado; `docs/specs/**` intocado.

## Testes avaliados

- cobertura das RN da task: **RN-042 sim** (`etapa-itinerarios.test.tsx:245-259`); RN-025/031/096 são de apresentação nesta task e estão cobertas indiretamente (rotulagem existente + ausência de persistência); lacuna direcionada das quatro colunas/vocabulário → problema 1.
- casos inválidos testados: **sim** — guarda de seletor do "X"/setas (contagens em `:252-254,261`), ponto de rota sem `parada-item` (`:245-250`), default de `Tabela` sem classes da compacta (`tabela.test.tsx:79-99`).
- regressão de UUID: **N/A** (import/export/cópia não tocados).
- OSRM mockado: **sim** — `vi.stubGlobal("fetch", respostaOsrmMock())` em `beforeEach` (`etapa-itinerarios.test.tsx:229-231`); testes de seleção provam zero chamadas extras (`:760-770`).
- suíte executada com resultado: **verde, log canônico reutilizado** — `npm run test:all:verificar`: "Log canônico válido. Executor: Claude. Fingerprint: `fcdc8ded7f17c0de11654d33e4b4a8b70e5eb98906252169751629c95e1a3f5d`. Working tree: `00c21aa501d19b4a9ade6b12db96274b9a6725e300e14f1fda33b4dc1336f57e`." Conteúdo do log: Vitest **92 arquivos / 1124 testes aprovados**; Playwright **60 aprovados, 3 flaky (aprovados em retry)**; códigos 0/0; "Resultado geral: APROVADO"; limpeza do PID próprio registrada. A suíte **não** foi repetida nesta revisão (regra da evidência canônica). Adicionalmente, `npm run typecheck` e `npm run lint` executados nesta revisão: limpos.

## Checklist 07

**Resultado: 21 itens ok, 22 N/A, 0 violados** (as lacunas de teste entram como ressalvas, não violação de item).

- **Escopo (6/6 ok):** pertence ao ROTA; sem workflow; sem status de pedido; sem persistência indevida; só o escopo da task; nenhuma regra inventada — vocabulário e formato vêm da DEC-073.
- **JSON (9 N/A):** contrato intocado no diff; nenhum campo, UUID, parada ou valor alterado.
- **Domínio (2 ok, 7 N/A):** Seção/Local seguem entidades separadas (rótulos distintos, sem promoção); ponto de rota não tratado como Parada (RN-042, com teste). Demais itens não tocados.
- **Comparador (7 N/A).**
- **Roteamento (2 ok, 3 N/A):** pontos de rota seguem só condicionando o traçado (handlers preservados); redecoração não chama OSRM (testes contam chamadas do mock). Falha/mensagens/conversão não tocadas.
- **UI/PDF (4 ok, 4 N/A):** padrão `Cidade - Nome` (RN-076) mantido; design system aderente (DEC-050 — variantes por prop, tokens, sem `style=` inline, doc 18 atualizado no ciclo); `data-testid`/`aria-*` preservados (E2E verdes sem alterar seletores). Offsets/matrizes/aviso SEI/exportação: N/A.
- **Testes (6 ok, 1 parcial, 1 N/A):** RN-042 com teste; casos inválidos testados; OSRM mockado; fixtures canônicas reutilizadas; evidência única canônica válida e reutilizada; porta/PIDs limpos no log. Parcial: teste direcionado das quatro colunas/vocabulário ausente (problema 1). UUID: N/A.

## Pendências

- **Condição (antes do merge do ciclo):** commitar o registro da DEC-073 (`docs-dev/10-DECISION_LOG.md` + `docs-dev/16-OPEN_QUESTIONS.md`), hoje só no working tree (problema 3).
- **Follow-up (não bloqueia):** teste de integração das quatro colunas + vocabulário de Tipo + `aria-label` do "X" (problema 1) e teste de desseleção sobre Local extremo herdado da 064 (problema 2) — cabem juntos num único teste-guarda da bateria de composição.
- Nenhuma Q-xxx aberta por esta revisão; a Q-052 está decidida (DEC-073).

## Decisão

**Aprovado com ressalvas.** A entrega implementa exatamente os cinco pontos da DEC-073, preserva todos os seletores/handlers/regras de desabilitar, não toca contrato JSON nem specs, respeita doc 18 §4 (variação por prop, token global intocado) e mantém os quatro sinais visuais em canais físicos distintos com a precedência seleção>zebra garantida por seletor testado. Suíte canônica verde (log válido reutilizado), typecheck e lint limpos. Nenhuma RN Alta ou NEG-xxx violada; escopo exato.

Ressalvas: (1) o registro da DEC-073 precisa entrar no histórico — condição do ciclo, trivial; (2) a lacuna do teste direcionado das quatro colunas/vocabulário e (3) o follow-up de desseleção herdado da 064 viram follow-ups explícitos, recomendados como um único teste-guarda de composição. Nada disso compromete o comportamento entregue.
