# Revisão da TASK-058

**Revisor:** Claude (Opus 4.8), sob supervisão de Guilherme Frade
**Data:** 2026-07-16
**Commit/branch revisado:** `5dca75b` (branch `redesign`) — "Implementa TASK-058: Ressalvas pendentes das revisões TASK-053/TASK-057"

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Fecha os seis itens pontuais listados na task (ressalvas das revisões `TASK-053-20260715.md` e `TASK-057-20260715.md` que não foram cobertas por outra task): (1) UUID da fixture `viagemFeriado()` trocado para um UUIDv4 literal válido (`testes/unitarios/formulario/servicos-contadores.test.tsx:18-24`); (2) `data-testid="rotulo-semana-padrao"` renomeado só no lado novo, para `servicos-rotulo-semana-padrao` (`src/formulario/servicos/servicos.tsx:348`), com o E2E ajustado (`testes/e2e/servicos.spec.ts:141-144`) e o testid preexistente do resumo intocado; (3) o literal `ROTULO_SEMANA_PADRAO` ganhou fonte única em `src/shared/contagens/contagens.ts:28-31`, consumido por `servicos.tsx` e `resumo-operacional.tsx`; (4) a soma `viagensSemana(undefined) + viagensSemana(undefined)` virou a constante `{ ida: 0, volta: 0, total: 0 }` (`servicos.tsx:149-153`); (5) os estados de erro/carregamento da Identificação (`erroListas`/`!listas`) foram envolvidos em `Painel` (`src/formulario/identificacao/identificacao.tsx:154-172`), preservando os `data-testid`; (6) a "Origem" da RN-069 em `docs-dev/01-RULE_INDEX.md:510` passou a citar também a Spec 04 §6. Sete arquivos tocados, 33 inserções / 27 remoções.

## Regras RN verificadas

- **RN-001/RN-010** (UUIDv4 válido) — **atendida**. O novo `uuid` da fixture, `"dddddddd-0000-4000-8000-dddddddddddd"`, casa com `REGEX_UUID_V4` de `src/shared/contrato/esquema.ts:9-10` (grupo de versão `4000`, grupo de variante `8000` — ambos válidos). O documento montado no teste da RN-069 agora sobreviveria a `esquemaDocumentoOperacao`, ao contrário da fixture anterior (`${base.uuid}-feriado`).
- **RN-069/NEG-018** (feriado fora das contagens; rótulo obrigatório) — **atendida**. A unificação do literal em `shared/contagens` não altera nenhum cálculo — é só a localização da constante — e o teste de regressão (dois documentos que só diferem na grade de feriados) continua verde (`servicos-contadores.test.tsx:54-92`).
- **RN-072** (contagem vive no módulo puro) — **atendida**. A simplificação do caminho "em construção" para a constante `0` não introduz cálculo na etapa: o comentário em `servicos.tsx:64-68` documenta que é o mesmo resultado de `viagensSemana(undefined)`, e a guarda `if (!itinerario) return 0` permanece em `contagens.ts:57` — conferido nesta revisão.
- **DEC-052** (rótulo "Tipo do Autos" permanece) — **atendida**. `identificacao.tsx:245` mantém `rotulo="Tipo do Autos"` sem alteração; o diff desta task só envolve os dois primeiros retornos de `SeletorDeAutos` em `Painel`, sem tocar `IdentidadeEditavel`.
- **DEC-050/doc 18** (design system, superfícies em `Painel`) — **atendida**. Os dois estados novos usam o componente `Painel` padrão (sem props), consistente com o terceiro retorno de `SeletorDeAutos`, que já usava `Painel` antes desta task.

## Specs verificadas

- **Spec 04 §6** (Serviços) — confirmado como a seção correta para a Origem da RN-069 acrescentada no RULE_INDEX (`docs/specs/04-formulario-ux-pdf.md:97` é o cabeçalho "6. Serviços").
- **Spec 04 §5** (Identificação) — aderente; a task não altera a regra de campo somente-leitura/editável, só a superfície visual dos estados de carregamento/erro.

## Pontos corretos

- Os seis itens da task foram resolvidos exatamente como prescrito, sem extrapolar: nenhum item toca `shared/contagens` além de mover a constante (permitido explicitamente pelo texto da task), nenhum toca o rótulo "Tipo do Autos" (DEC-052), nenhum recalcula regra.
- O testid renomeado é só o novo (`servicos.tsx`); o do resumo (`resumo-operacional.tsx:53`) ficou intocado, e `testes/e2e/resumo-operacional.spec.ts:22` (que usava `getByTestId("rotulo-semana-padrao")` sem `.first()`) segue seguro porque não há mais colisão de nome — verificado com `grep` nos dois arquivos e confirmado pela suíte E2E rodada nesta revisão.
- A fixture UUID nova segue a mesma convenção de literais determinísticos já usada no mesmo arquivo (`"cccccccc-0000-4000-8000-cccccccccccc"` em `servicos-contadores.test.tsx:105`), em vez de introduzir um padrão novo.
- Os comentários que documentam as decisões (`servicos.tsx:64-68`, `contagens.ts:28-30`, `resumo-operacional.tsx:20-22`) citam a RN/DEC de origem em vez de narrar o óbvio.

## Problemas encontrados

Nenhum. Os seis itens da task foram verificados individualmente nesta revisão (leitura dos arquivos + regex/testes), sem desvio.

## Violações de escopo

Nenhuma. Conferido contra o "Fora de escopo" da task:

- `src/shared/contagens/` — tocado apenas para acrescentar a constante `ROTULO_SEMANA_PADRAO` (opção que a própria task sugere textualmente); nenhuma função de cálculo alterada — conferido em `contagens.ts` nesta revisão.
- Rótulo "Tipo do Autos" — **inalterado** (`identificacao.tsx:245`), conforme DEC-052.
- `docs/specs/**` — não tocado.
- Ressalvas de aparência já roteadas para a TASK-056 (marca "(sem itinerário ainda)", superfície da `Tabela`, aviso aninhado) — não tocadas aqui.
- Ressalva de layout da TASK-055 (tabela lateral / mapa único) — não tocada; é a TASK-060.
- Nenhuma mudança de comportamento, mensagem ou contrato JSON.

## Testes avaliados

- **cobertura das RN da task:** sim — RN-001/RN-010 (fixture agora válida, `servicos-contadores.test.tsx:18-24`), RN-069 (regressão feriado, `:54-92`), rótulo único (`:125-147` + `resumo-operacional.spec.ts:22`).
- **casos inválidos testados:** N/A — task de correção pontual (fixture, testid, literal, superfície, doc), sem regra nova para testar caminho inválido.
- **regressão de UUID:** sim, no sentido estrito do item 1 — a fixture corrigida agora é UUIDv4 válido; a task não toca import/export/cópia, então RN-004/007 (preservação de UUID) são N/A.
- **OSRM mockado:** N/A — nenhum teste desta entrega toca roteamento.
- **suíte executada com resultado, nesta revisão:**
  - `npm run typecheck` → sem erros.
  - `npm run lint` → sem erros.
  - `npm test` → **67 arquivos, 842 testes, 842 passed** (28,4s).
  - `npx playwright test servicos identificacao resumo-operacional` → **13 passed** (18,3s) — cobre os três arquivos afetados pela task.
  - `npm run build` → export estático concluído sem erros.

## Checklist 07

Percorrido item a item.

- **Escopo** (6/6 ok): não é SEI; sem workflow; sem status novo; sem persistência; escopo exato (os seis itens da task, nada além); nenhuma regra nova — cada correção cita a ressalva de origem (TASK-057 P1-P4, TASK-053 P3) ou a DEC/RN correspondente.
- **JSON** (2 ok, 7 N/A): só dado de operação; nenhum campo novo. N/A: os demais itens (autossuficiência, UUID em import/cópia, Parada, R$, km/segundos, dados congelados) — a task não toca esses caminhos.
- **Domínio** (2 ok, 7 N/A): RN-069 ok (feriado fora da contagem, rótulo único); `numero_n` não tocado. N/A: demais itens do bloco.
- **Comparador** (7 N/A): nenhum arquivo de `comparador/` tocado.
- **Roteamento** (5 N/A): sem OSRM.
- **UI/PDF** (2 ok, 6 N/A): design system ok (`Painel` de `shared/ui`, sem `style=` inline); `data-testid`/`aria-*` existentes preservados, com a única renomeação sendo a deliberada do item 2 (testid novo, não preexistente do resumo). N/A: padrão de nome de Seção, horários/offsets, matrizes, Locais, aviso SEI, bloqueio de exportação.
- **Testes** (3 ok, 3 N/A): RN da task com teste ok; fixtures canônicas reutilizadas — agora válidas contra o schema (item 1 fechado) ok; verificações executadas e reportadas honestamente ok. N/A: casos inválidos (task de correção pontual, sem regra nova), regressão de UUID em import/export/cópia, mock de OSRM.

Resultado: **17 itens ok, 32 N/A, 0 violados.**

## Pendências

Nenhuma. Os itens 1-6 da task foram fechados e verificados nesta revisão; nenhuma Q-xxx nova aberta.

## Decisão

**Aprovado.** Os seis itens pontuais da task — todos ressalvas já formalizadas em revisões anteriores (`TASK-053-20260715.md`, `TASK-057-20260715.md`) — foram resolvidos exatamente como prescrito: fixture UUIDv4 válida, testid desambiguado só no lado novo, literal com fonte única em `shared/contagens`, simplificação de estilo sem mudar regra, estados de erro/carregamento da Identificação em `Painel` com `data-testid` intacto, e a Origem da RN-069 no RULE_INDEX atualizada para citar a Spec 04 §6. Nenhuma violação de escopo, nenhuma RN de criticidade Alta nem NEG-xxx em risco, checklist 07 limpo, e a suíte completa (typecheck, lint, 842 testes unitários, os 13 E2E dos três arquivos afetados, e o build de export estático) verde nesta revisão.
