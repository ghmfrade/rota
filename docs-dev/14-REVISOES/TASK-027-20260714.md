# Revisão da TASK-027

**Revisor:** Claude (Opus 4.8), apoiado pelo prompt `/revisar-aderencia`
**Data:** 2026-07-14
**Commit/branch revisado:** `f3e29b3` (branch `master`) — "Implementa TASK-027: Editor da matriz de seccionamento"

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Editor da `matriz_seccionamento` por Serviço na etapa **Matrizes** (Spec 04 §9.2).
Três camadas: (a) algoritmos puros de sugestão em [sugestoes-seccionamento.ts](../../src/formulario/matrizes/sugestoes-seccionamento.ts)
(os dois modos da Spec 03 §6); (b) operações imutáveis de edição em
[edicao-seccionamento.ts](../../src/formulario/matrizes/edicao-seccionamento.ts)
(habilitar/desabilitar/editar/aplicar em lote); (c) UI da grade triangular em
[etapa-matrizes.tsx](../../src/formulario/matrizes/etapa-matrizes.tsx), ligada ao
shell em [layout-formulario.tsx](../../src/formulario/layout/layout-formulario.tsx).
Dois helpers de [calculo-matriz-distancias.ts](../../src/formulario/matrizes/calculo-matriz-distancias.ts)
(`secoesAtendidas`, `chaveParNaoDirecional`) foram exportados para reuso, sem mudança de comportamento.
24 testes unitários novos + 5 cenários E2E.

## Regras RN verificadas

- **RN-058** (par com `distancia_km` confirmada; sugestão não persistida como tal) — **atendida.** `habilitarPar` grava o valor sugerido no momento do clique e a célula fica editável ([edicao-seccionamento.ts:36-62](../../src/formulario/matrizes/edicao-seccionamento.ts#L36-L62)); o JSON guarda o valor final. Testes `habilitarPar` e `definirDistancia`.
- **RN-059** (par não-direcional, sem duplicata, `a≠b`, presente em `matriz_distancias` do Serviço) — **atendida.** Rejeição de `secao_a==secao_b` e de par ausente em `matriz_distancias`; idempotência via `chaveParNaoDirecional` ([edicao-seccionamento.ts:42-54](../../src/formulario/matrizes/edicao-seccionamento.ts#L42-L54)). Testes `[inválido] secoes-iguais` e `[inválido] par-ausente-em-matriz-distancias`.
- **RN-060** (dois botões de sugestão em lote; último acionado/edição manual vale) — **atendida.** `aplicarSugestaoEmLote` recomputa e sobrescreve todos os pares habilitados ([edicao-seccionamento.ts:105-117](../../src/formulario/matrizes/edicao-seccionamento.ts#L105-L117)). Teste "último modo acionado vale".
- **RN-076** (formato triangular, "X" na diagonal, `Cidade - Nome`, km, sem R$) — **atendida.** Grade triangular inferior com "X" na diagonal, cabeçalhos via `nomeExibicaoSecao` (`municipio - nome`, [fluxos-secao.ts:31-33](../../src/formulario/secoes/fluxos-secao.ts#L31-L33)), input em km, sem valores em R$. E2E "triangular inferior … sem R$" (assert `body not.toContainText("R$")`).
- **RN-004** (UUIDs preservadas) — **atendida.** Operações compõem/removem entradas por referência às UUIDs existentes; nenhuma UUID é gerada nem alterada. Teste "RN-004: não altera nenhuma UUID de Seção referenciada".
- **RN-013** (sem R$) — **atendida.** Só km; nenhum campo/label monetário.

## Specs verificadas

- **Spec 04 §9.2** — aderente: clique alterna habilitar/desabilitar; habilitar preenche com a sugestão ativa; dois botões de sugestão em lote; edição manual da célula. Evidência: [etapa-matrizes.tsx:94-124, 156-247](../../src/formulario/matrizes/etapa-matrizes.tsx#L94-L124).
- **Spec 03 §6.1–§6.4** — aderente: `sugerirMenorDistancia` = mínimo do `valor_adotado_de_distancia` entre todos os Serviços que atendem o par; `sugerirDistanciaDoServico` = valor do próprio Serviço; sem conversão de unidade; caso "par só do Serviço" faz os dois coincidirem. Evidência: [sugestoes-seccionamento.ts](../../src/formulario/matrizes/sugestoes-seccionamento.ts) + testes de empate e de par exclusivo.
- **Spec 02 §9** — aderente: `matriz_seccionamento` como array de `ParSecao` (`secao_a_uuid`, `secao_b_uuid`, `distancia_km`), default `[]`, sem campo novo no contrato.

## Pontos corretos

- Separação limpa cálculo puro / edição pura / UI; funções de edição imutáveis (devolvem array novo).
- Reuso de `chaveParNaoDirecional`/`secoesAtendidas` garante que seccionamento e distâncias usam a **mesma** ordenação de linhas/colunas — sem divergência de grade.
- A grade só renderiza células para Seções atendidas; como `matriz_distancias` cobre todas as combinações (RN-054), todo par exibido é habilitável — o check defensivo de RN-059 nunca barra o caminho da UI.
- Seletor de Serviço usa `s.uuid` como `value` (identidade), `numero_n` só como rótulo — consistente com RN-006.
- Casos inválidos e de borda testados (par igual, par ausente, no-op de desabilitar/editar, nenhum Serviço atende → `undefined` sem inventar valor).

## Problemas encontrados

Nenhum bloqueante.

## Violações de escopo

Nenhuma. A entrega cobre exatamente o shell da etapa Matrizes + a matriz de **seccionamento** (objetivo da task). O display somente-leitura da matriz de **distâncias** (§9.1) é deixado explicitamente para a TASK-048 (comentário em [etapa-matrizes.tsx:24-26](../../src/formulario/matrizes/etapa-matrizes.tsx#L24-L26)) — pendência apenas apontada, não implementada aqui. Contagens/pares compráveis ficam para a TASK-031 (não tocadas nesta task).

## Testes avaliados

- **cobertura das RN da task:** sim — RN-058/059/060 e o formato de RN-076 têm teste unitário e/ou E2E; ambos os algoritmos de sugestão cobertos (empate, par exclusivo do Serviço, sem conversão de unidade).
- **casos inválidos testados:** sim (seções iguais, par ausente em `matriz_distancias`, no-op de desabilitar/editar, nenhum candidato → `undefined`).
- **regressão de UUID:** sim — teste dedicado confirma preservação (RN-004).
- **OSRM mockado:** N/A na suíte unitária; o E2E `route(...osrm...).abort()` confirma que **nenhuma** chamada OSRM é disparada (a etapa lê matriz congelada) — coerente com a fronteira da task.
- **suíte executada com resultado:**
  - `npm run typecheck` — **verde**.
  - `npm run lint` — **verde**.
  - `npx vitest run` — **verde** (47 arquivos, 661 testes; os 24 novos de seccionamento inclusos).
  - E2E Playwright (`etapa-matrizes.spec.ts`) — **não executado nesta revisão** (requer navegador instalado); o arquivo foi lido e cobre o fluxo "—" → habilitar → editar → reaplicar sugestão, sem R$ e sem OSRM.

## Checklist 07

Percorrido item a item. Resultado: itens aplicáveis **ok**; demais **N/A**. Nenhum item violado. Destaques:

- Escopo: pertence ao ROTA (não SEI); sem workflow, sem status de pedido, sem persistência transacional; só o escopo da task; nenhuma regra inventada (a única inferência — modo de sugestão default — está **marcada como inferência controlada** no código, [etapa-matrizes.tsx:47-56](../../src/formulario/matrizes/etapa-matrizes.tsx#L47-L56)). **ok.**
- JSON: sem campo novo (ParSecao já existe na Spec 02 §9); default `[]`; UUIDs preservadas; sem R$; distâncias em km. **ok.**
- UI/PDF: `Cidade - Nome`; matriz triangular inferior em km sem R$; sem offsets. **ok.** (Aviso SEI / exportação bloqueada = N/A, são de outras etapas.)
- Testes: regra RN com teste; casos inválidos; regressão de UUID; fixtures canônicas (`documentoBidirecionalMultiServico`, `carregar-multi-servico.json`) reutilizadas. **ok.**

## Pendências

- **E2E não executado nesta revisão** — rodar `npm run test:e2e` (com `npx playwright install chromium`) antes/depois do merge para fechar a verificação de tela. Não bloqueia: a lógica está coberta por unitários verdes e o arquivo E2E foi lido e é aderente.
- **Inferência controlada do "modo ativo" default** (`menor-distancia`): a Spec 04 §9.2 nomeia "sugestão ativa" sem fechar o mecanismo do estado inicial. Está documentada como inferência controlada e é sempre sobrescrevível manualmente (RN-058). Se o domínio quiser fechar o comportamento, cabe uma Q-xxx — mas não é lacuna que impeça o merge.
- Sem Q-xxx aberta bloqueando a task.

## Decisão

**Aprovado.** Escopo exato (shell + matriz de seccionamento), RN-058/059/060/076/004/013 atendidas e cobertas por teste, checklist 07 limpo, typecheck/lint/unitários verdes (661 testes). A única inferência (modo de sugestão default) está corretamente marcada como controlada e não inventa regra de negócio. Recomenda-se apenas rodar a suíte E2E antes do merge como fechamento de verificação — follow-up, não condição bloqueante.
