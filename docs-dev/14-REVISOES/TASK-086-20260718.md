# Revisão da TASK-086

**Revisor:** Claude (revisão assistida)
**Data:** 2026-07-18
**Commit/branch revisado:** 21de457 (branch `redesign`)

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Adiciona `diagnostico?: string` opcional a `Pendencia` (`src/formulario/pendencias/pendencias.ts`); `coletarErrosEstruturais` (`src/formulario/exportacao/gate-exportacao.ts`) monta o diagnóstico (RN extraída da tag `[RN-xxx]` da mensagem crua do schema + `issue.path.join(".")` + mensagem crua) e `avaliarGateExportacao` emite `console.debug` por erro estrutural quando `NODE_ENV !== "production"`. `tela-exportacao.tsx` e `tela-revisao.tsx` renderizam `data-diagnostico` no `<li>`/botão do item de bloqueio. Testes novos em `gate-exportacao.test.ts`, `tela-exportacao.test.tsx` e `tela-revisao.test.tsx`.

## Regras RN verificadas

- RN-078 — atendida (gate de exportação intocado em sua lógica de bloqueio; task só acrescenta trilha técnica).
- RN-076 / Spec 04 §14 — atendida: `mensagem` operacional não foi alterada (`montarDiagnostico` é função nova, separada de `mensagemOperacional`); testes-guarda confirmam `textContent`/`mensagem` sem `[RN-\d+]`.
- RN-004/007 (UUID) — N/A, task não toca import/export/cópia de entidades.

## Specs verificadas

- Spec 04 §14 — aderente: diagnóstico técnico só em atributo (`data-diagnostico`) e `console.debug`, nunca em texto visível. Verificado em `tela-exportacao.test.tsx:184-199` e `tela-revisao.test.tsx:86-109` (`textContent`/`item!.textContent` sem `[RN-\d+]`).
- Spec 04 §11 (duas listas de Revisão/Exportação) — estrutura preservada, só ganhou atributo.

## Pontos corretos

- `diagnostico` é estritamente opcional e `coletarPendencias` (pendências vivas) permanece sem preenchê-lo — confirmado por teste de regressão-guarda dedicado (`gate-exportacao.test.ts`, "pendência viva... continua sem diagnostico").
- `console.debug` guardado por `process.env.NODE_ENV !== "production"`, com teste cobrindo os dois ambientes via `vi.stubEnv`.
- Atributo `data-diagnostico` só aparece quando `diagnostico` está definido (comportamento nativo do React para prop `undefined`), com teste explícito do caso ausente em ambas as telas.
- `data-testid`/`aria-*` existentes preservados — nenhum seletor alterado nos componentes.
- Extração de RN reaproveita o mesmo padrão (`/^\[RN-(\d+)\]/`) já usado por `mensagemOperacional`, sem duplicar regra de negócio nova.

## Problemas encontrados

Nenhum.

## Violações de escopo

Nenhuma — `pendencias.ts` só ganhou o campo opcional; `coletarPendencias` e o contrato JSON (`esquemaDocumentoOperacao`) não foram tocados; nenhum painel de DEV ou gate por `NODE_ENV` na UI foi criado.

## Testes avaliados

- cobertura das RN da task: sim (RN-018 com tradução dedicada e RN-005 via fallback, ambos com diagnóstico verificado)
- casos inválidos testados: sim (log em produção não deve disparar; item sem `diagnostico` não recebe atributo; pendência viva sem diagnóstico)
- regressão de UUID: N/A
- OSRM mockado: N/A (task não toca roteamento)
- suíte executada com resultado: verde — `npx vitest run` nos 3 arquivos afetados: 3 arquivos, 29 testes, todos passando; suíte completa `npm test`: 83 arquivos, 993 testes, todos passando; `npm run typecheck` e `npm run lint` sem erros.

## Checklist 07

39 itens aplicáveis avaliados, 0 violados, demais N/A (task não toca JSON/domínio/comparador/roteamento além do citado). Destaques:

- Escopo: aderente em todos os itens — sem workflow, sem status novo, sem persistência, apenas o escopo da task.
- JSON (contrato): N/A em quase todos — nenhum campo novo no JSON exportado; `diagnostico` vive só em `Pendencia` (estrutura de apresentação em memória), não no `DocumentoOperacao`.
- UI: `data-testid`/`aria-*` preservados (ok); design system N/A (nenhum componente visual novo, só atributo).
- Testes: RN implementada com teste (ok); casos inválidos testados (ok); fixtures reutilizadas (`documentoExemploMinimo`, `SESSAO_CARREGADA`) (ok); verificações executadas e reportadas (ok).

## Pendências

Nenhuma.

## Decisão

Aprovado sem ressalvas: a task cumpre exatamente o objetivo e os 5 critérios de aceite, respeita o "Fora de escopo" (mensagem operacional intocada, sem campo novo no contrato JSON, sem painel de DEV), e toda regra tem teste incluindo os casos inválidos e a regressão-guarda de que `coletarPendencias` permanece sem `diagnostico`. Suíte completa, typecheck e lint verdes.
