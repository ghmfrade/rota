# Revisão da TASK-074

**Revisor:** Claude (Opus 4.8), apoiado no protocolo `docs-dev/07` e `docs-dev/14`
**Data:** 2026-07-17
**Commit/branch revisado:** branch `redesign`, **working tree não commitado** (base HEAD `6f69b62`). A implementação da TASK-074 nunca chegou a virar commit — os arquivos estão pendentes na árvore de trabalho.

## Resultado

- [ ] Aprovado
- [x] Aprovado com ressalvas
- [ ] Reprovado

O **código da TASK-074 é correto e aderente** (extração fiel, RN preservadas, testes verdes). A ressalva é **bloqueante e não é de mérito do código**: o working tree está **contaminado por mudanças da TASK-080** (implementada em paralelo, ao vivo), entrelaçadas no mesmo arquivo `etapa-itinerarios.tsx` e num arquivo à parte (`promocao-servico.ts`). Um commit de TASK-074 feito agora, por `git add` do arquivo inteiro, **arrastaria a TASK-080 junto** — violação de escopo. A separação cirúrgica é condição de merge.

## Resumo da entrega

Reorganização de layout da etapa "Seções, Locais e Itinerários" (Spec 04 §7/§7.1; doc 18 §5): o painel "Reutilizar Seção existente" saiu de dentro do `EditorMapaItinerario` (abaixo do mapa) e virou o componente novo `PainelReusoSecao`, renderizado na coluna lateral **acima** da tabela de paradas; a tabela ganhou wrapper com rolagem própria atrelada à altura do mapa. Arquivos da task: **criados** `src/formulario/itinerarios/painel-reuso-secao.tsx`, `src/formulario/itinerarios/altura-mapa.ts`, `testes/unitarios/formulario/painel-reuso-secao.test.tsx`; **alterados** `editor-mapa-itinerario.tsx`, `etapa-itinerarios.tsx` (parcialmente — ver contaminação), `index.ts`.

## Análise da influência entre tasks (pedido explícito do responsável)

### 1. TASK-073 — SEM influência nociva (limpa)

A TASK-073 (tela inicial, já commitada em `f06a003`/`6f7fde9`) **não capturou nenhum arquivo da 074**: seus commits tocaram só `tela-inicial.tsx`, `botao.tsx`, `painel.tsx`, `18-DESIGN_SYSTEM.md` e seus testes. Separação de fronteira correta.

Ela alterou dois componentes de `shared/ui` que o `PainelReusoSecao` (074) consome, mas ambas as mudanças são **puramente aditivas e retrocompatíveis**, verificadas no diff `git diff 7621a25 HEAD`:
- `Painel` (`src/shared/ui/painel.tsx`): ganhou prop **opcional** `interativo?: boolean` (default `false`). O `PainelReusoSecao` usa `<Painel>` sem essa prop → comportamento idêntico ao anterior.
- `Botao` (`src/shared/ui/botao.tsx`): trocou `ComponentPropsWithoutRef` por `ComponentProps` (passa a **aceitar** `ref`, sem exigir). Nenhum efeito sobre `<Botao variante="secundario">` do painel.

**Conclusão:** a 073 não gerou problema para a 074.

### 2. TASK-080 — CONTAMINAÇÃO ATIVA (bloqueante para o commit)

Durante esta própria revisão o working tree **mudou** (no primeiro `git status` da revisão `promocao-servico.ts` não constava modificado; minutos depois, sim) — há edição concorrente da **TASK-080** (backlog `docs-dev/06` linha 2392: "correção, 2026-07-17 — remove o gate `modo === 'novo'` da promoção") acontecendo na mesma árvore. Suas mudanças estão **misturadas** com as da 074:

- `src/formulario/itinerarios/promocao-servico.ts` — **arquivo 100% da TASK-080**, modificado no working tree: adiciona `promoverServicoNaSessao` e move para lá as helpers `paradasPorSentidoDoServico`/`estadosPorSentidoDoServico`. O comentário no próprio código diz "generalizada aos dois modos pela TASK-080". **Não pertence à 074.**
- `src/formulario/itinerarios/etapa-itinerarios.tsx` — contém, no mesmo arquivo, **duas tasks**:
  - **074 (correto):** import + render de `PainelReusoSecao`, `ALTURA_MAPA_CLASSE_LG`, wrapper de rolagem.
  - **080 (intruso):** remoção das duas helpers (linhas ~94-124 do HEAD), troca do import `promoverServico` → `promoverServicoNaSessao`, e substituição do bloco de promoção inline (~386-433 do HEAD) por `proxima = promoverServicoNaSessao(proxima, linhaAtual.servicoUuid)` (linha ~373 atual).

**Isto não é defeito funcional da 074** — as duas mudanças são ortogonais (layout do reuso × lógica de promoção) e a árvore combinada está internamente consistente (typecheck/testes verdes na sessão de implementação). Mas é um **defeito de fronteira de commit**: a 074 não pode ser commitada sem cirurgia, sob pena de varrer a 080 (não revisada) para dentro do commit `Implementa TASK-074`.

**Efeito colateral sobre o relatório de implementação:** as validações reportadas na "Implementação concluída" (typecheck limpo, 942 testes verdes) rodaram sobre a **árvore combinada 074+080**, não isolam a 074. Não invalidam o mérito da 074, mas não são prova isolada.

## Regras RN verificadas

- **RN-025 / RN-026** (Seção compartilhada; contribuição de geoloc por Serviço/sentido) — **atendidas**. `PainelReusoSecao` chama `contribuirParaSecaoExistente` (motor intacto, `fluxos-secao.ts` não tocado). Teste confirma que a contribuição do Serviço corrente entra em `secao.servicos[]` com a geoloc do sentido.
- **RN-027** (350 m por centroide cumulativo) — **atendida**. Motor preservado; teste de caso inválido reproduz recusa real (contribuição bidirecional que empurra ponto já aceito para fora do limiar), sem `aoAtualizarSecao`.
- **RN-076** (`Cidade - Nome da Seção`) — **atendida**. Dropdown usa `nomeExibicaoSecao`; teste verifica o rótulo `Cidade Demo - Terminal Demo`.
- **RN-052** (recálculo ao vivo ao alterar itinerário) — **atendida e é o ponto crítico de regressão**: `PainelReusoSecao` recebe `aoAtualizarSecao={aoCriarOuAtualizarSecao}` (`etapa-itinerarios.tsx:656`), **o mesmo handler de antes da extração** → o reuso continua inserindo a Parada e disparando o recálculo. Fio preservado.

## Specs verificadas

- **Spec 04 §7.1** (reutilização de Seções) — **aderente**. Oferta todas as Seções do documento (filtro por Serviço é Q-043/TASK-077, fora de escopo — não antecipado); pontos como opções de posição quando > 1; lançamento direto quando há 1 ponto; recusa de 350 m com a mensagem literal. Comportamento inalterado.
- **Spec 04 §7** + **doc 18 §5** ("tabela lateral acompanha na mesma linha visual") — **aderente**. Coluna presa a `lg:h-[60vh]`; tabela em wrapper `overflow-y-auto`. Referência de altura única (`altura-mapa.ts`), atacando o risco de dessincronia apontado na task.

## Pontos corretos

- Extração fiel: `PainelReusoSecao` reproduz o bloco original 1:1 (mesmo motor, mesmos testids `reuso-secoes`/`select-secao-reuso`/`select-ponto-reuso`/`confirmar-reuso`).
- `editor-mapa-itinerario.tsx`: remoção limpa do bloco/estado/handler de reuso e dos imports órfãos (`Select`, `contribuirParaSecaoExistente`, `nomeExibicaoSecao`, `pontosOfertadosParaReuso`); `mensagemSecao` corretamente mantido (ainda serve criação/arrasto).
- Testid novo `mensagem-recusa-reuso` (inferência controlada, documentada): **aditivo**, não altera testid existente. Verificado que nenhum E2E afirma a recusa do reuso da etapa via `mensagem-recusa-secao` (só as demo pages `editor-secoes-350m.spec.ts`/`editor-locais-350m.spec.ts`, componentes intocados). Doc 18 §6.5 respeitado.
- `altura-mapa.ts` mantém as duas classes literais (`h-[60vh]`, `lg:h-[60vh]`) com nota correta sobre a varredura textual do Tailwind — sem isso o utilitário responsivo não seria gerado.

## Problemas encontrados

(ordenados por severidade)

1. **[Bloqueante — fronteira de commit] Contaminação por TASK-080 no working tree.** `promocao-servico.ts` (todo da 080) e o refactor de promoção dentro de `etapa-itinerarios.tsx` (da 080) estão misturados às mudanças da 074. Não é defeito de RN nem de escopo *do código escrito para a 074*, mas impede um commit limpo. **Condição de merge**: separar (ver Pendências).
2. **[Menor — verificação] E2E da etapa de itinerários não executado isoladamente.** Na sessão de implementação a porta 3000 estava ocupada por outro `next dev` concorrente; e a árvore está contaminada, então rodar agora não isolaria a 074. Precisa rodar sobre a árvore já separada.

## Violações de escopo

- **No código autoral da TASK-074: nenhuma.** As edições que a 074 fez são exatamente layout (reuso reposicionado + rolagem). Não inventou regra, não tocou contrato JSON, não tocou motor de reuso/350 m.
- **No working tree entregue: presente, porém de origem externa (TASK-080)** — ver Problema 1. A responsabilidade é de higiene de commit, não de conteúdo da 074.

## Testes avaliados

- Cobertura das RN da task: **sim** — `painel-reuso-secao.test.tsx` cobre RN-025/026 (contribuição), RN-027 (recusa 350 m), RN-076 (rótulo).
- Casos inválidos testados: **sim** — recusa de 350 m (bidirecional que estoura o cluster), fora de SP, e ausência de Seção selecionada (botão não renderiza).
- Regressão de UUID: **N/A** — task não toca import/export/cópia; motor de Seção intacto.
- OSRM mockado: **N/A** — o painel de reuso não chama OSRM (o recálculo disparado por `aoAtualizarSecao` é da etapa, coberto pelos E2E existentes).
- Suíte executada: na implementação, `vitest run` **942/942 verde**, typecheck e lint limpos — **mas sobre a árvore combinada 074+080** (não isola a 074). Reexecução isolada pendente após separação.

## Checklist 07

- **Escopo:** 074 pertence ao ROTA (não SEI); sem workflow; sem status; sem persistência; decisões citam Spec 04 §7.1 / RN. Item "implementa apenas o escopo" — **OK para o código da 074**, mas o *working tree* carrega a 080 (ressalva 1). ✓ com ressalva.
- **JSON (contrato):** N/A — nenhum campo, nenhuma UUID, nenhum recálculo de congelado tocado. ✓
- **Domínio:** Seção/Local seguem separados; `numero_n` não usado como chave; RN-030/035 não afetadas. ✓
- **Comparador / Roteamento:** N/A (task de UI do Formulário; sem OSRM direto). ✓
- **UI/PDF:** `Cidade - Nome da Seção` (RN-076) preservado; `data-testid`/`aria-*` existentes intocados (novo testid aditivo); componentes de `shared/ui`; sem `style=` inline. **DEC-050 aderente.** ✓
- **Testes:** RN da task com teste, casos inválidos presentes, suíte reportada honestamente (com a ressalva de que rodou sobre árvore combinada). ✓ com ressalva.

Resultado: **~24 itens OK, ~restante N/A, 0 violados no código da 074**; 2 ressalvas operacionais (contaminação + E2E).

## Pendências

**Condição de merge (bloqueante):**

1. **Separar a TASK-080 do commit da 074.** Commitar em `Implementa TASK-074` **apenas**:
   - `src/formulario/itinerarios/painel-reuso-secao.tsx` (novo)
   - `src/formulario/itinerarios/altura-mapa.ts` (novo)
   - `testes/unitarios/formulario/painel-reuso-secao.test.tsx` (novo)
   - `src/formulario/itinerarios/index.ts` (só o export do painel)
   - `src/formulario/itinerarios/editor-mapa-itinerario.tsx` (só a extração/altura)
   - `src/formulario/itinerarios/etapa-itinerarios.tsx` — **apenas os hunks da 074** (imports de `PainelReusoSecao`/`ALTURA_MAPA_CLASSE_LG`, o render do painel na coluna, o wrapper de rolagem). **Excluir** os hunks da 080 (remoção das helpers, troca `promoverServico`→`promoverServicoNaSessao`, refactor do bloco de promoção) via `git add -p`.
   - **Não** incluir `src/formulario/itinerarios/promocao-servico.ts` (é da 080).
2. Rodar `npm run typecheck` **após** a separação: como o `etapa-itinerarios.tsx` da 074 pura ainda referencia `promoverServico` + helpers inline (versão HEAD), e a 080 pura vive no seu próprio ramo, garantir que a versão commitada da 074 seja consistente com o `promocao-servico.ts` do HEAD (não o modificado). Se a separação por hunks deixar o arquivo inconsistente, reconstruir `etapa-itinerarios.tsx` a partir do HEAD aplicando só as mudanças da 074.
3. Rodar `npm run test:e2e` (ou ao menos `etapa-itinerarios.spec.ts`) sobre a árvore já separada, com a porta 3000 livre, para confirmar visualmente que nenhum seletor quebrou e a rolagem funciona.

**Follow-up (não bloqueante):**

4. A TASK-080 é uma task real aberta e legítima; suas mudanças (`promocao-servico.ts` + refactor da promoção) devem seguir seu **próprio** ciclo `/implementar-task` + `/revisar-aderencia`, sem carona no commit da 074.

## Decisão

**Aprovado com ressalvas.** O código autoral da TASK-074 é escopo-exato, aderente à Spec 04 §7/§7.1 e ao doc 18 §5, preserva RN-025/026/027/052/076 e tem testes com casos inválidos — mereceria "aprovado" isolado. O que impede o "aprovado" pleno é **higiene de commit**: o working tree entregue mistura a TASK-080 (implementada em paralelo por engano), e um commit ingênuo violaria o princípio "uma task por vez" (`docs-dev/04` §7). Nenhuma RN de criticidade Alta nem NEG-xxx é violada pelo código da 074, o que afasta a reprovação.

Sobre a pergunta do responsável — **houve influência, e ela pode gerar problema**: a da **TASK-073 é inofensiva** (mudanças aditivas em `shared/ui`, fronteira de arquivos limpa); a da **TASK-080 é o risco real** — não corrompeu o comportamento da 074, mas contaminou a fronteira de commit e precisa ser separada antes do merge. Feita a separação (pendências 1-3), a 074 fica limpa.
