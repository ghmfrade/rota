# Revisão da TASK-114

**Revisor:** Codex
**Data:** 2026-07-28
**Commit/branch revisado:** `13e583b` / `redesign`

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

A entrega mantém as ações “Apagar viagem” e “Restaurar sugestão” dentro da
última coluna da grade, com espaço interno reservado para não cobrir o horário,
sem alterar a posição usada de segunda a sábado. O E2E acrescentado afere a
geometria e os efeitos das ações em domingo nas grades comum e de feriados.

Arquivos de código e teste revisados:

- `src/formulario/viagens/etapa-viagens.tsx`
- `testes/e2e/etapa-viagens.spec.ts`

## Regras RN verificadas

- **RN-061 — atendida.** A mudança é exclusivamente de posicionamento. O E2E
  apaga a Viagem de domingo e confirma que a Viagem de segunda permanece
  (`testes/e2e/etapa-viagens.spec.ts:562-566`).
- **RN-066 — atendida.** A ação de domingo restaura somente a Viagem acionada
  (`testes/e2e/etapa-viagens.spec.ts:556-560`); o motor preserva identidade,
  saída, dia e feriado, com regressão unitária explícita
  (`testes/unitarios/formulario/viagens-redistribuicao.test.ts:202-220`).
- **RN-067 — atendida.** O campo de horário permanece focável e editável em
  `HH:MM`, sem exposição de `offset_horario`
  (`testes/e2e/etapa-viagens.spec.ts:553-557`).
- **RN-095/RN-096 — atendidas.** O diff não introduz workflow, estado de
  pedido, persistência ou servidor; limita-se à apresentação client-side.

## Specs verificadas

- **Spec 04 §8.1 — aderente.** A estrutura Seções × SEG…DOM é preservada; a
  detecção usa o último elemento de `DIAS_SEMANA`
  (`src/formulario/viagens/etapa-viagens.tsx:445`).
- **Spec 04 §8.2 — aderente.** “Restaurar sugestão” continua sendo ação por
  Viagem e mantém o efeito existente.
- **Spec 04 §8.3 — aderente.** “Apagar viagem inteira” continua removendo uma
  única Viagem do dia escolhido.
- **Design system §3/§6 — aderente.** O wrapper `overflow-x-auto` de `Tabela`
  permanece intacto (`src/shared/ui/tabela.tsx:52`); a posição usa utilitários
  Tailwind e os botões continuam usando `Botao`, sem `style=` inline.

## Pontos corretos

- Domingo recebe espaço interno na célula de partida e posicionamento
  `right-0`; os demais dias conservam `left-full`
  (`src/formulario/viagens/etapa-viagens.tsx:454,537`).
- Os `data-testid` e os `aria-label` das duas ações foram preservados
  (`src/formulario/viagens/etapa-viagens.tsx:544-555`).
- O teste compara os dois botões com as bordas esquerda e direita da área
  visível e mantém X acima de ↻
  (`testes/e2e/etapa-viagens.spec.ts:538-551`).
- O horário de domingo continua focável; restaurar e apagar permanecem
  clicáveis e produzem os efeitos esperados
  (`testes/e2e/etapa-viagens.spec.ts:553-566`).
- A grade de feriados repete a aferição da borda direita
  (`testes/e2e/etapa-viagens.spec.ts:568-584`).
- A regressão existente conserva, em segunda, posição externa, ordem e
  dimensões dos botões (`testes/e2e/etapa-viagens.spec.ts:281-306`).
- A rota pública do OSRM é interceptada, e o cenário confirma zero chamadas
  (`testes/e2e/etapa-viagens.spec.ts:521-525,586`).

## Problemas encontrados

- Nenhum problema bloqueante ou ressalva funcional.

## Violações de escopo

- Nenhuma. O diff não altera o efeito das ações, o componente `Tabela`, os
  demais flutuantes, contrato JSON, PDF, Comparador ou contagens.

## Consequências sobre outras tasks

- Os flutuantes de inserção relativa e cópia continuam em canais físicos
  próprios dentro da mesma célula; os arquivos reais foram conferidos em
  `src/formulario/viagens/etapa-viagens.tsx:503-589,627-657`, e seus E2E
  existentes permaneceram verdes.
- TASK-108..112 são apenas ponteiros de trabalho futuro nesta revisão; o
  comportamento de implementações futuras não foi verificado.
- Não houve mudança nas fronteiras entre Formulário, Comparador e `shared`,
  nem no contrato visual compartilhado de `Tabela`.

## Testes avaliados

- **cobertura das RN da task:** sim — RN-061, RN-066 e RN-067 cobertas pelo
  novo E2E e pelas regressões unitárias herdadas.
- **casos inválidos testados:** sim — nenhum trecho dos botões pode ultrapassar
  a área visível; o horário não fica impedido; UUID inexistente no motor de
  apagar permanece coberto
  (`testes/unitarios/formulario/viagens-copias-grade.test.ts:160-163`).
- **regressão de UUID:** N/A para importação/exportação/cópia; a preservação de
  UUID no reset continua coberta pelo teste unitário da RN-066.
- **OSRM mockado:** sim — o endpoint público é interceptado e não é chamado.
- **fixture canônica:** sim — o E2E reutiliza `multiServico`, carregada da
  fixture canônica do fluxo.
- **verificações leves:** `git diff --check HEAD^ HEAD`, `npm run lint`,
  `npm run typecheck` e `npm run build` executados nesta revisão, todos verdes.
- **suíte executada com resultado:** verde. `npm run test:all:verificar`
  confirmou o log do executor `humano`: 95 arquivos/1.282 testes unitários,
  80 E2E aprovados e 1 E2E flaky aprovado no retry, códigos finais 0,
  fingerprint
  `19c40a1e3b5ccbfafbf5af902b4d2ffdc35949406933ac2029bc993182a33320`
  e identidade do working tree
  `00c21aa501d19b4a9ade6b12db96274b9a6725e300e14f1fda33b4dc1336f57e`.
- **reutilização da evidência:** o verificador passou; conforme o protocolo, a
  suíte completa não foi repetida.

## Checklist 07

Resultado: **23 itens OK, 30 N/A, 0 violados**.

### Escopo

| Item | Resultado | Evidência |
|---|---|---|
| Alteração pertence ao ROTA, não ao SEI (RN-095) | OK | Correção visual da grade de Viagens. |
| Não introduz workflow (RN-095) | OK | Nenhum estado administrativo. |
| Não cria status de pedido (RN-011) | OK | Contrato intocado. |
| Não cria persistência transacional (RN-096) | OK | Mudança client-side de classes. |
| Implementa apenas o escopo da task | OK | Diff restrito aos dois arquivos previstos. |
| Nenhuma regra nova inventada | OK | Comportamento fundamentado na Spec 04 §8.1–§8.3. |

### JSON (contrato)

| Item | Resultado | Evidência |
|---|---|---|
| JSON só com dados de operação (RN-010) | OK | Nenhum campo novo. |
| JSON autossuficiente (RN-009) | OK | Nenhum recurso externo novo. |
| Nenhum campo sem Spec 02 | OK | Schema não foi alterado. |
| UUIDs existentes preservadas (RN-004) | OK | Reset preserva UUID; regressão unitária verde. |
| Entidades novas recebem UUID nova (RN-002/007) | N/A | A task não cria nem copia entidades. |
| XOR de Parada (RN-033) | N/A | Referências de Parada intocadas. |
| Sem R$ (RN-013) | OK | Nenhum valor monetário. |
| Unidades/arredondamento (RN-014/050) | N/A | Fora do escopo. |
| Congelados não recalculados por leitores (RN-015) | N/A | Nenhum leitor alterado. |

### Domínio

| Item | Resultado | Evidência |
|---|---|---|
| Seção e Local separados (RN-025/031) | N/A | Entidades intocadas. |
| Ponto de rota não vira Parada (RN-042) | N/A | Fora do diff. |
| Serviço mantém UUID (RN-001) | N/A | Serviço não é alterado. |
| `numero_n` não é identidade (RN-006) | N/A | Nenhuma lógica por número. |
| Extremos continuam Seções (RN-035) | N/A | Itinerário intocado. |
| Ida/Volta compartilham Seções (RN-030) | N/A | Estrutura intocada. |
| Tipificação respeitada (RN-019..022) | N/A | Fora do escopo. |
| Viagem estratificada (RN-061) | OK | Ações continuam por UUID/dia; domingo isolado no E2E. |
| Feriado fora das contagens (RN-069) | N/A | Contagens intocadas. |

### Comparador

| Item | Resultado | Evidência |
|---|---|---|
| UUID como base (RN-081) | N/A | Comparador fora do diff. |
| Taxonomia de diff (RN-081..083) | N/A | Comparador fora do diff. |
| Comparativo não gravado (RN-080/097) | N/A | Comparador fora do diff. |
| Status/datas fora do diff (RN-012) | N/A | Comparador fora do diff. |
| Autos diferentes bloqueados (RN-084) | N/A | Comparador fora do diff. |
| Rota por sinais estáveis (RN-087) | N/A | Comparador fora do diff. |
| Sem OSRM/persistência no Comparador (RN-080) | N/A | Comparador fora do diff. |

### Roteamento

| Item | Resultado | Evidência |
|---|---|---|
| OSRM indisponível bloqueia rota inválida (RN-048) | N/A | Roteamento intocado. |
| Distância vem do OSRM (RN-047/050) | N/A | Distâncias intocadas. |
| Pontos de rota só condicionam traçado (RN-041..043) | N/A | Fora do diff. |
| Abrir JSON não chama OSRM (RN-052) | N/A | Importação/roteamento intocados. |
| Mensagens §14 sem tarifa (RN-049) | N/A | Mensagens de roteamento intocadas. |

### UI/PDF

| Item | Resultado | Evidência |
|---|---|---|
| Nome `Cidade - Nome da Seção` (RN-076) | N/A | Rótulos de Seção intocados. |
| Horário de relógio; offsets ocultos (RN-067/076) | OK | Campo continua em HH:MM e focável. |
| Matrizes triangulares sem R$ (RN-076) | N/A | Matrizes fora do diff. |
| Locais fora da grade principal (RN-031/044/076) | N/A | Montagem de linhas intocada. |
| Aviso SEI no PDF (RN-077) | N/A | PDF fora do diff. |
| Gate de exportação (RN-078) | N/A | Exportação fora do diff. |
| Design system (DEC-050) | OK | `Botao`, tokens e utilitários oficiais; sem `style=`. |
| `data-testid`/`aria-*` preservados (DEC-050) | OK | Seletores e rótulos existentes intactos. |

### Testes

| Item | Resultado | Evidência |
|---|---|---|
| Toda RN/regra implementada possui teste | OK | Novo E2E e regressões RN-061/066/067 verdes. |
| Casos inválidos testados | OK | Limites, não cobertura do horário e remoção por UUID. |
| Regressão de UUID quando aplicável | N/A | Não toca importação/exportação/cópia. |
| OSRM mockado | OK | Endpoint público interceptado. |
| Fixtures canônicas reutilizadas | OK | `multiServico` canônica. |
| Direcionados/typecheck/lint/build reportados | OK | Diff check, lint, typecheck, build e suíte verdes. |
| Evidência final canônica única e válida | OK | Verificador confirmou marcador, códigos e fingerprint. |
| Revisão reutilizou log ou executou uma vez | OK | Log válido reutilizado; suíte não repetida. |
| Porta/PIDs isolados e encerrados | OK | Log registra encerramento da árvore do PID próprio. |

## Pendências

- Nenhuma condição funcional ou de merge remanescente para a TASK-114.

## Decisão

**Aprovado.** A entrega corrige o recorte das ações em domingo sem alterar o
comportamento, os seletores acessíveis, a posição das demais colunas ou o
contrato compartilhado de `Tabela`. O escopo, as RN aplicáveis, os requisitos
negativos e o design system permanecem aderentes, e todas as verificações
exigidas estão verdes.
