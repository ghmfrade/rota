# Revisão da TASK-053

**Revisor:** Claude (Opus 4.8), a pedido de lucasreis199-lgtm
**Data:** 2026-07-15
**Commit/branch revisado:** `9904b9c` — "Implementa TASK-053: Etapa Identificação com shared/ui" (branch `redesign`)

## Resultado

- [ ] Aprovado
- [x] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Um único arquivo tocado: `src/formulario/identificacao/identificacao.tsx` (+87/−69). Os `<select>` crus viram `Select` de `shared/ui` (rótulo via `MolduraControle`), as duas superfícies da etapa viram `Painel`, o status vira `Selo tom="azul"` e o aviso de reconversão (DEC-034) vira `Painel tom="informativo"` com `role="status"` preservado. Nenhuma alteração de comportamento, de handler ou de contrato JSON. Nenhum teste novo (task de restilização — "Testes esperados: E2E existentes; varredura de `style=`/hex no diff").

## Regras RN verificadas

- **RN-016** (identidade vem das listas estáticas) — **atendida.** `aoSelecionarAutos` (`identificacao.tsx:77-90`) está byte-a-byte igual ao anterior: mesma busca em `listas.autos`, mesmo `nomeDaEmpresa`, mesmo `status: "proposta"`. Coberta por `testes/e2e/identificacao.spec.ts:79` — verde.
- **RN-023 / DEC-034** (troca de tipo reconverte com aviso, nunca bloqueia) — **atendida.** `aoTrocarTipo` (`identificacao.tsx:92-120`) inalterado; o aviso continua condicionado a `reconversao.length > 0` e continua sem qualquer bloqueio. Coberta por `identificacao.spec.ts:51` e `:109` — verdes.
- **RN-004** (UUIDs preservadas) — **não aplicável ao diff, e verificado que continua assim:** o arquivo não importa, não exporta e não copia entidade; não há `crypto.randomUUID` nem manipulação de `uuid` em nenhuma das 300 linhas.
- **RN-011** (`status` só proposta/vigente, não editado aqui) — **atendida.** O status segue somente-leitura: virou `Selo`, que renderiza `<span>` (`selo.tsx:36`) — não há controle editável. Comentário `identificacao.tsx:260` preservado.
- **RN-076** (nomenclatura oficial nos rótulos) — **atendida com observação** (ver problema 1): rótulos usam "Autos", "Serviços", "Tipo do Autos" — nomenclatura oficial, nada traduzido/renomeado.
- **RN-095/RN-096** (sem workflow, sem persistência) — **atendidas.** Nenhum estado novo, nenhuma escrita, nenhum campo de fluxo.

## Specs verificadas

- **Spec 04 §5** (tabela de campos da Identificação) — **aderente.** Confrontei linha a linha:
  - _Código do Autos / Empresa — "não editável após criado o documento"_: continuam `<span>` de texto (`identificacao.tsx:226,233`). **Verificado no navegador:** `etapa-identificacao` tem 0 elementos `role="textbox"` (asserção de `identificacao.spec.ts:48`, verde).
  - _Tipo — "editável a qualquer momento"_: `Select` funcional, `selectOption` verde nos 2 testes de troca.
  - _Status — "exibido como selo"_: agora é literalmente o `Selo` do doc 18 §3 — a entrega **aproxima** o código da spec, que já dizia "selo" quando o código tinha um `<span>` pelado.
  - _Data — somente exibição_: `<span data-testid="campo-data">` preservado.
- **doc 18 §3** (contratos de `Painel`/`Select`/`Selo`) — **aderente.** `tom="informativo"` para aviso é exatamente o que §3 prescreve ("borda `azul-300`, fundo `azul-50`, texto `azul-900` — avisos").
- **doc 18 §6.1/§6.3** (sem `style=`, sem hex/px avulso) — **aderente, varrido:** `grep "style="` → 0 ocorrências; `grep -E "#[0-9a-fA-F]{3,6}\b|[0-9]+px"` → 0 ocorrências.
- **doc 18 §6.4** (variação de aparência por prop, nunca por `className`) — **aderente, e este é o ponto alto da entrega** (ver Pontos corretos 2).
- **doc 18 §7** ("mudança de comportamento nunca entra por task de design") — aderente; ver problema 1 para o único texto visível que mudou.

## Pontos corretos

1. **`data-testid`/`role`/`aria-*` intocáveis — provado por diff de atributos, não por impressão.** Extraí o conjunto de atributos antes (`9904b9c^`) e depois: **idênticos** — os 11 `data-testid`, `role="alert"`, `role="status"` e a única ocorrência de `sr-only`. Os 4 E2E da etapa passam sem alterar seletores, e a suíte E2E completa (47 testes) passa.

2. **A armadilha herdada da TASK-052 foi evitada — e eu medi.** A pendência 3 do parecer da TASK-052 (`14-REVISOES/TASK-052-20260715.md:105`) avisava que sobrescrever as classes fixas do `Painel` via `className` falharia em silêncio nas TASK-053..056. Esta entrega **não** cai nisso: usa `tom="informativo"` (prop) e `Selo tom="azul"` (prop), e reserva `className` para espaçamento (`mt-4`, `mb-3`) e tipografia (`text-sm`) — exatamente o que doc 18 §6.4 libera. Verificado no navegador, não por leitura: `selo-status-identificacao` renderiza `background-color: rgb(219, 234, 254)` = `#dbeafe` = token `--color-azul-100` (doc 18 §2). O tom pretendido é o tom real.

3. **Não forçar `Campo` em Código/Empresa foi a decisão certa, não uma omissão.** O resumo da task cita `Campo`/`Botao`, que não aparecem no diff. Está correto: a Spec 04 §5 exige Código/Empresa **não editáveis**, e envolvê-los em `Campo` injetaria um `<input>` — quebrando a asserção `getByRole("textbox")).toHaveCount(0)` e, pior, contradizendo a spec. `Botao` não se aplica (a etapa não tem ação). Ausência justificada pela spec.

4. **Consistência de tom com o shell, conferida no código.** `Selo tom="azul"` para o status (`identificacao.tsx:261`) bate com `layout-formulario.tsx:147`, que usa `Selo tom="azul"` para o mesmo `status` no cabeçalho persistente. Mesmo dado, mesmo tom — sem drift entre etapa e shell.

5. **Corrigiu de graça uma invalidez de HTML que existia antes.** No código anterior, o `<div role="status">` do aviso ficava **dentro** do `<dl>`, misturado a `dt`/`dd` — modelo de conteúdo inválido (`<dl>` aceita grupos `dt`/`dd` **ou** `div`, não a mistura com um `div` avulso ao final). Agora o `<dl>` contém só `<div>`s de grupo e o aviso é irmão do `<dl>` dentro do `Painel`. Estrutura válida, sem que a task pedisse.

6. **Nome acessível do `select-tipo` preservado apesar da troca de mecanismo.** O rótulo saiu de `<label>` envolvente + `<span class="sr-only">` para `htmlFor`/`id` gerado por `useId`. Risco real de regressão de a11y — **verificado no navegador**: `label[for]` resolve e `toHaveAccessibleName("Tipo do Autos")` passa. Mesmo nome de antes.

## Problemas encontrados

1. **[Baixa — ressalva] O rótulo visível do tipo mudou de "Tipo" para "Tipo do Autos"** (`identificacao.tsx:238,241`).
   O `<dt>Tipo</dt>`, antes visível, virou `<dt className="sr-only">Tipo</dt>`, e quem passou a rotular visivelmente o controle é o `rotulo="Tipo do Autos"` do `Select` (antes `sr-only`). **Medido no navegador:** o `label[for="_r_0_"]` tem `textContent: "Tipo do Autos"` e `position !== "absolute"` (isto é, visível). A tabela da Spec 04 §5 nomeia o campo **"Tipo"**.
   Por que é só Baixa: "Tipo do Autos" é nomenclatura oficial (RN-076), espelha "Código do Autos" da mesma tela, e o nome **acessível** não mudou. Não é mensagem da Spec 04 §14. Mas é o único texto visível alterado por uma task cujo princípio (doc 18 §1.2) é "comportamento intocável", então fica registrado em vez de passar batido.
   *Correção sugerida (opcional):* `rotulo="Tipo"` + remover o `<dt>` sr-only redundante — alinha com a spec e elimina a duplicação "Tipo" + "Tipo do Autos" lida por leitor de tela.

2. **[Baixa — observação] `Painel` dentro de `Painel`: moldura e sombra duplicadas** (`identificacao.tsx:278` dentro de `:220`).
   O aviso de reconversão é um `Painel tom="informativo"` aninhado no `Painel` da identidade. Ambos renderizam `<section>` com `border` + `rounded-painel` + `shadow-sombra-2` (`painel.tsx:45,53-56`). Resultado: duas superfícies elevadas empilhadas — `sombra-2` sobre `sombra-2`.
   Não é violação: doc 18 §3 manda usar `tom="informativo"` para avisos e não proíbe aninhamento, e o `tom` é aplicado por prop (correto). É risco de **drift visual**, o mesmo tipo de item que a TASK-052 tratou como follow-up.
   *Follow-up sugerido:* escopo natural da varredura de consistência da **TASK-056**, que já prevê revisar o padrão de avisos do bloco. Se a dúvida for real (aviso aninhado deve ou não carregar sombra própria?), vira Q-xxx — não decisão desta revisão.

3. **[Baixa — observação] Os estados de erro e carregamento ficaram fora de qualquer `Painel`** (`identificacao.tsx:156,164`).
   `erroListas` e `!listas` renderizam `<p>` solto com `text-sm text-erro` / `text-sm text-cinza-500`, sem superfície, enquanto o estado carregado é um `Painel`. A etapa "muda de moldura" conforme o estado.
   Não é violação de doc 18 §6.2 — a enumeração vinculante é "botão/campo/select/painel/selo/tabela/tooltip"; `<p>` não está nela, e §6.4 libera `className` para tipografia (`text-sm`). Os tons usados são tokens do §2 (`--color-erro` para erro, `cinza-500` para auxiliar), corretos. É consistência visual, não aderência.

4. **[Informativo] Uma linha fora da convenção de formatação do próprio arquivo** (`identificacao.tsx:156`, 91 colunas).
   O resto do arquivo é quebrado em 80 colunas no estilo Prettier; essa linha nova não. **Não é achado acionável:** Prettier não é dependência nem script deste projeto (`package.json` não o lista; `npx` teve de baixá-lo), e `layout-formulario.tsx` também não passaria — ou seja, não há padrão automatizado sendo violado. Registro só para não parecer que passou despercebido.

**Sobre o critério de aceite "zero elemento cru estilizado":** a etapa mantém `<p>`, `<dl>`, `<dt>`, `<dd>` e `<ul>` com utilitários Tailwind. Considero **atendido**, pelo precedente já firmado no parecer da TASK-052 (`TASK-052-20260715.md:56`): a enumeração vinculante de doc 18 §6.2 é "botão/campo/select/painel/selo/tabela/tooltip", e §6.4 declara `className` livre para "posição, espaçamento, largura, `text-sm`". Não existe componente de parágrafo/lista em `shared/ui` (conferido: `botao`, `campo`, `carimbo`, `moldura-carimbo`, `moldura-controle`, `painel`, `select`, `selo`, `tabela`, `tooltip`) — exigir literalidade aqui pediria um componente que a TASK-050 não criou. Além disso, `text-xs`/`cinza-500` para texto auxiliar e `text-sm`/`cinza-700` para corpo são exatamente a escala do doc 18 §2.

## Violações de escopo

**Nenhuma.** O diff toca um único arquivo, `src/formulario/identificacao/identificacao.tsx`, que é literalmente o alvo da task. Nada do "Fora de escopo" foi tocado: nenhuma outra etapa, nenhuma regra de identificação alterada, nenhum componente de `shared/ui` modificado (o diff não os toca — só os importa), nenhum arquivo de `data/`, contrato ou teste. Nenhum "aproveitei para melhorar".

## Testes avaliados

- **cobertura das RN da task:** sim — RN-016 e RN-023/DEC-034, as duas RN citadas pela task, têm E2E dedicado em `testes/e2e/identificacao.spec.ts` (4 testes, cobrindo modo carregado e modo novo). Nenhuma RN foi alterada, então nenhum teste novo era devido; a task pediu explicitamente "E2E existentes" e é isso que se aplica.
- **casos inválidos testados:** parcial — a troca de tipo é exercitada nos dois sentidos (com e sem Serviços a reconverter, `:51` e `:109`). **Lacuna pré-existente, não introduzida por esta task:** o ramo `erroListas` (`identificacao.tsx:154-160`) não tem E2E; o `data-testid="erro-listas-identificacao"` não é exercido por nenhum teste do repositório (grep em `testes/`). A entrega não piorou isso — o ramo já existia sem cobertura.
- **regressão de UUID (se aplicável):** N/A — o arquivo não toca import/export/cópia.
- **OSRM mockado:** N/A — nenhuma chamada de rede; nenhuma introduzida.
- **suíte executada com resultado:** **verde**, tudo rodado nesta revisão:
  - `npm run typecheck` → sem erros.
  - `npm run lint` → sem erros.
  - `npm test` → **Test Files 66 passed (66) · Tests 837 passed (837)**, 33.88s.
  - `npx playwright test` (suíte E2E completa) → **47 passed**, 1.3m.
  - `npm run build` (export estático) → sucesso, todas as rotas prerenderizadas como estáticas.
  - Varredura do diff: `style=` → 0; hex/px avulso → 0.

## Checklist 07

**Resultado: 11 itens ok, 39 N/A, 0 violados** (de 50 itens).

- **Escopo (6/6 ok):** pertence ao ROTA; sem workflow; sem status além de proposta/vigente; sem persistência; escopo exato; nenhuma regra inventada (toda escolha visual cita doc 18 §2/§3).
- **JSON (9 N/A):** o diff não toca contrato, schema, import/export nem qualquer campo.
- **Domínio (9 N/A):** nenhuma entidade de domínio tocada.
- **Comparador (7 N/A):** módulo não tocado.
- **Roteamento (5 N/A):** nenhuma chamada OSRM, nenhuma geo.
- **UI/PDF (2 ok, 6 N/A):**
  - _"UI aderente ao design system (DEC-050)"_ — **ok:** componentes de `shared/ui`, tokens do `@theme`, zero `style=`, zero hex/px, variação por prop (§6.4).
  - _"`data-testid` e `aria-*` existentes preservados"_ — **ok:** conjunto de atributos idêntico ao commit anterior; 47 E2E verdes sem alterar seletores.
  - N/A: nomes de Seção, offsets, matrizes, Locais, aviso SEI no PDF, bloqueio de exportação — nada disso existe nesta etapa.
- **Testes (3 ok, 3 N/A):** RN da task cobertas por E2E verde; fixtures canônicas reutilizadas (`tela-inicial-carregar-valido.json`); verificações executadas e reportadas acima com saída real. N/A: casos inválidos de regra nova (não há regra nova), regressão de UUID, mock de OSRM.

## Pendências

1. **Nenhuma condição de merge.** As três ressalvas são de severidade Baixa, não violam RN de criticidade Alta nem NEG-xxx, e nenhuma bloqueia a task seguinte.
2. **Follow-up (opcional, problema 1):** alinhar o rótulo visível a "Tipo" (Spec 04 §5) e remover o `<dt>` sr-only redundante. Uma linha; sem risco de regressão (o nome acessível já está verificado e não depende do `dt`).
3. **Follow-up para a TASK-056 (problema 2):** padrão de aviso aninhado em `Painel` — decidir se `sombra-2` sobre `sombra-2` é o pretendido. Já cabe no "Fora de escopo" desta task e dentro da varredura de consistência que a TASK-056 prevê.
4. **Lacuna de teste pré-existente (não desta task):** `erro-listas-identificacao` segue sem E2E. Registro como ponteiro; corrigi-lo aqui seria violação do "uma task por vez".
5. **Consequência sobre outras tasks:** esta entrega é o **primeiro consumidor de `Painel`/`Select`/`Selo` numa etapa interna** e valida na prática a correção que a TASK-052 introduziu (variantes por prop). O padrão que ela estabelece — `tom`/`variante` por prop, `className` só para espaçamento/tipografia — é o que **TASK-054 e TASK-055** devem repetir. *Restrição de afirmação: li nesta revisão `painel.tsx`, `select.tsx`, `selo.tsx`, `moldura-controle.tsx`, `layout-formulario.tsx:143-151` e o parecer da TASK-052; **não** inspecionei o código das TASK-054/055/056, que ainda não foram implementadas.*

## Decisão

**Aprovado com ressalvas.**

A entrega faz exatamente o que a task pediu, e faz bem: um arquivo, zero mudança de comportamento, os 11 `data-testid` e os `role`/`aria-*` provados idênticos por diff de atributos, e a suíte inteira verde (837 unitários, 47 E2E, build estático, typecheck, lint). O mérito principal é ter escapado da armadilha que o parecer da TASK-052 deixou registrada para justamente esta task: a variação de aparência vem de prop, e eu confirmei no navegador que o tom pretendido é o tom renderizado (`azul-100` = `#dbeafe`), em vez de aceitar a leitura do código. Duas decisões merecem destaque por serem sutis e corretas — não usar `Campo` em Código/Empresa (que injetaria um `<input>` proibido pela Spec 04 §5) e preservar o nome acessível do `select-tipo` apesar de trocar o mecanismo de rotulagem, também verificado no navegador.

As ressalvas são todas Baixa e nenhuma é condição de merge: o rótulo visível "Tipo" → "Tipo do Autos" (nomenclatura oficial, nome acessível intacto, mas divergente da tabela da Spec 04 §5), a moldura dupla do aviso aninhado, e a inconsistência de superfície nos estados de erro/carregamento. Registro-as porque são exatamente o tipo de desvio cosmético que se acumula em silêncio ao longo de um bloco de redesign de seis tasks — e porque a TASK-056 já tem varredura de consistência prevista, que é onde 2 e 3 se resolvem naturalmente.

Não há violação de RN Alta, de NEG-xxx nem de escopo. **Liberado para a TASK-054.**
