# Revisão da TASK-064

**Revisor:** Claude (Opus 4.8) — `/revisar-aderencia`, conversa separada da implementação
**Data:** 2026-07-22
**Commit/branch revisado:** branch `redesign` — `d6a5fcf` (implementação), `50f840b` (prioridade de clique entre marcadores sobrepostos) e `7a3084f` (zebra × seleção; encontrado **não commitado** no working tree ao iniciar esta revisão e commitado durante ela — ver problema 1)

## Resultado

- [ ] Aprovado
- [x] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Sincronização bidirecional de seleção tabela↔mapa da etapa de itinerários (Spec 04 §7): estado efêmero `{chave, origem}` em `etapa-itinerarios.tsx`, chaves compartilhadas em `selecao-itinerario.ts` (reusam literalmente o `id` dos marcadores: `secao-<uuid>`, `local-<uuid>`, `ponto-rota-<indice>`), props `selecionado`/`aoSelecionar` na primitiva `shared/mapa/mapa.tsx` (outline via classe `.marcador-mapa--selecionado`), realce de linha com `aria-current` + `bg-azul-100`, scroll-into-view somente quando a seleção nasce no mapa, toggle e descarte da seleção na troca de Serviço/sentido. Dois fixes na sequência: z-index por classe (Seção > Local > ponto de rota, hierarquia DEC-069) para prioridade de clique entre marcadores sobrepostos, e exclusão da linha `aria-current="true"` do zebra/hover da `<Tabela>` compartilhada.

## Regras RN verificadas

- **RN-096** (nada persistido no servidor; exportar é o salvar) — **atendida**: seleção é `useState` local, nunca entra na sessão/documento; testes provam zero chamadas de rede na seleção ([etapa-itinerarios.test.tsx](../../testes/unitarios/formulario/etapa-itinerarios.test.tsx) e E2E com contador de OSRM em 0).
- **RN-042** (ponto de rota não é entidade: sem uuid) — **atendida**: chave posicional `ponto-rota-<indice>` ([selecao-itinerario.ts:18-20](../../src/formulario/itinerarios/selecao-itinerario.ts#L18-L20)), nenhum uuid ou campo novo criado.
- **RN-035 / DEC-070** (Local extremo inválido; erro contextual preservado) — **atendida por composição**: seleção usa canal distinto (fundo `bg-azul-100` / outline) do erro (`text-erro`/`border-erro`/borda do marcador); teste de coexistência na mesma linha (`aria-current="true"` + `data-estado="local-extremo"` + `aria-invalid` simultâneos) e na mesma classList do marcador (`--selecionado` + `--invalido`).
- **RN-076** (rótulo `Cidade - Nome da Seção` na tabela) — **atendida (preservada)**: a renderização do rótulo não foi tocada pelo diff; E2E confirma o texto da linha.
- **RN-025..036** (Seção/Local/Parada sem mudança de modelo) — **atendidas (preservadas)**: o diff não altera schema, validação ou montagem; só UI de realce.
- **RN-052** (abrir JSON não chama OSRM) — **atendida**: seleção não dispara recálculo; testes contam chamadas de `fetch`/rotas OSRM antes e depois.

## Specs verificadas

- **Spec 04 §7** ("tabela lateral … sincronizada com o mapa (selecionar na tabela destaca no mapa e vice-versa)") — **aderente**: os dois sentidos implementados e testados (unitário, integração e E2E), inclusive sobre a lista unificada da TASK-079 (paradas **e** pontos de rota selecionáveis).
- **Spec 04 §7.3 item 6** (clique sobre a linha cria vértice) — **aderente com arbitragem documentada**: clique que acerta um **marcador** agora prioriza seleção (`stopPropagation` opt-in em [mapa.tsx:471-484](../../src/shared/mapa/mapa.tsx#L471-L484)); clique na linha fora de marcador segue criando vértice (E2E do gesto continua verde, adaptado com zoom para expor a linha).
- **doc 18 §87 / DEC-054** (mapa em destaque + tabela lateral) — **aderente**: layout intocado; a task só adiciona o vínculo de seleção.

## Pontos corretos

- Chave de seleção única por construção (mesmo `id` do marcador), eliminando duas fontes de verdade — com teste garantindo não-colisão Seção×Local de mesmo uuid.
- Scroll-into-view condicionado à origem "mapa" (nunca "pula" sob o cursor no clique da própria linha), exatamente como o backlog de 2026-07-22 exige; E2E com `toBeInViewport`.
- Troca de Serviço/sentido descarta a seleção (reset durante o render, sem `useEffect` em cascata) — testado.
- Clique em botões de ação da linha (mover/remover) não altera a seleção — testado.
- Opt-in retrocompatível na primitiva do mapa: marcador sem `aoSelecionar` não muda de comportamento — testado nos dois níveis.
- Casos inválidos cobertos: chave sem marcador no sentido atual, marcador sem handler, ausência de seleção.
- Fix de z-index coerente com a hierarquia visual já decidida (DEC-069), independente da ordem de criação dos marcadores, com E2E que reproduz o cenário real (ponto de rota criado ao vivo sobre Seção).

## Problemas encontrados

1. **(Média — rastreabilidade, resolvida nesta revisão)** O fix zebra×seleção ([tabela.tsx:17-21](../../src/shared/ui/tabela.tsx#L17-L21) + E2E + unitário) estava **não commitado** no working tree ao iniciar a revisão, embora a evidência canônica (`ultimo-test-all.log`) fingerprintasse o tree **com** ele. Sem esse fix, o critério de aceite 1 falha visualmente nas linhas pares (seleção correta no estado, invisível na tela). Commitado durante a revisão como `7a3084f`, preservando a validade do log (conteúdo do tree inalterado).
2. **(Baixa — lacuna de teste)** O caso inválido do backlog "desselecionar preserva integralmente o estado vermelho" (Local extremo) não tem teste direto: há teste de coexistência ao **selecionar** e teste de toggle em linha comum, mas não a desseleção sobre a linha inválida. Estruturalmente seguro (classes aditivas concatenadas, canais independentes), mas segue como follow-up — encaixa naturalmente na bateria de composição da TASK-091/Q-052.
3. **(Observação — fronteira com Q-052)** A composição zebra×seleção é território nomeado pela **Q-052** (aberta, bloqueia a TASK-091). O fix `7a3084f` decide **apenas** que zebra/hover cedem na linha selecionada — não muda contraste, token global, colunas nem densidade — e era necessário para o próprio critério de aceite desta task. A Q-052 permanece aberta e a TASK-091 herda a obrigação de compor sobre este realce (já registrado no backlog da 091).
4. **(Observação)** Dois E2E pré-existentes do gesto de ponto de rota foram adaptados (zoom antes do clique na linha) porque o marcador agora captura clique próprio. **Seletores e testids intactos** — a adaptação é de interação, decorrente da mudança comportamental legítima da Spec 04 §7, e está comentada nos próprios testes.

## Violações de escopo

- Nenhuma. O toque em `shared/ui/tabela.tsx` é correção mínima exigida pelos critérios de aceite 1 e 3 da própria task (seleção perceptível), não antecipação do redesenho da TASK-091 (ver problema 3). Nenhuma mudança em contrato JSON, OSRM, montagem ou criação/edição de paradas.

## Testes avaliados

- cobertura das RN da task: **sim** — RN-096 (zero rede/persistência), RN-042 (chave posicional), RN-035/DEC-070 (composição), nos níveis unitário (mapa, editor, chave), integração (etapa) e E2E.
- casos inválidos testados: **sim** — 4 testes `[inválido]` + chave sem marcador no sentido; lacuna pontual do problema 2.
- regressão de UUID: **N/A** — não toca import/export/cópia.
- OSRM mockado: **sim** — `page.route` abortando/fulfillando no E2E, `fetch` stubado nos unitários; nenhum teste depende do serviço real.
- suíte executada com resultado: **verde** — log canônico reutilizado conforme protocolo: `npm run test:all:verificar` → "Log canônico válido. Executor: Codex. Fingerprint: `1deafcd47a1c1592053f807bc1448d7989b57e77cecc67e8f49a022eeeb5cf25`. Working tree: `00c21aa501d19b4a9ade6b12db96274b9a6725e300e14f1fda33b4dc1336f57e`." Suíte **não** repetida. Typecheck e lint reexecutados nesta revisão: limpos.

## Checklist 07

**Resultado: 30 itens ok, 24 N/A, 0 violados.**

- **Escopo (6/6 ok):** só ROTA; sem workflow; sem status de pedido; sem persistência (RN-096 testado); escopo exato (ver "Violações de escopo"); nenhuma regra inventada — as arbitragens (prioridade de clique, z-index) citam Spec 04 §7/DEC-069 e estão documentadas como inferência controlada no código.
- **JSON (9 — ok por preservação):** o diff não toca schema, campos, UUIDs, unidades nem dados congelados; contrato inalterado.
- **Domínio (9 — ok por preservação):** Seção/Local separados; ponto de rota sem uuid (RN-042 — chave posicional); identidade por UUID nas chaves de seleção; `numero_n` ausente da lógica; extremos/RN-030/tipificação/viagens intocados.
- **Comparador (7 — N/A):** módulo não tocado.
- **Roteamento (5):** seleção não chama OSRM (testado, ok); RN-047/048/041-043/052 preservados (ok); mensagens §14 N/A.
- **UI/PDF (9):** RN-076 rótulo preservado (ok); design system ok — tokens `azul-100`/`--color-azul-700`/`sombra-*`, classes em `globals.css`, sem `style=` inline; `data-testid`/`aria-*` existentes preservados, `aria-current` aditivo (ok); horários/matrizes/PDF N/A.
- **Testes (9):** RN com teste ok; inválidos ok; UUID N/A; OSRM mock ok; fixtures canônicas reutilizadas ok; verificações direcionadas + typecheck/lint ok; evidência canônica única verificada ok; log reutilizado sem repetir a suíte ok; porta/PIDs validados pelo verificador ok.

## Pendências

- **Follow-up (não bloqueia merge):** teste direto de "desselecionar Local extremo preserva o estado vermelho" (problema 2) — sugerido absorver na bateria de composição da TASK-091.
- **Q-052 permanece aberta** — a TASK-091 (bloqueada) compõe zebra/colunas/densidade **sobre** o realce desta task; o fix `7a3084f` não a decide.
- Nenhuma Q-xxx nova necessária.

## Decisão

**Aprovado com ressalvas.** A sincronização bidirecional pedida pela Spec 04 §7 está completa, testada nos três níveis com OSRM sempre mockado, compõe com o erro contextual da DEC-070 sem mascará-lo, não persiste nada (RN-096) e não toca o contrato JSON. Nenhuma RN Alta ou NEG-xxx violada; escopo exato.

Ressalvas: (1) parte da entrega (fix zebra×seleção) estava fora do histórico ao iniciar a revisão — **condição cumprida no próprio ciclo** com o commit `7a3084f`; (2) a lacuna de teste do problema 2 vira follow-up explícito, recomendado à TASK-091. Com a condição 1 cumprida, não há impedimento de merge.
