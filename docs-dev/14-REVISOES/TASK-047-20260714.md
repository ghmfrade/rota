# Revisão da TASK-047

**Revisor:** Claude Opus 4.8 (revisão de aderência)
**Data:** 2026-07-14
**Commit/branch revisado:** `2f21111` (master) — "Implementa TASK-047: Feedback ao usuário do motivo de o recálculo/rota não ocorrer"

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Superficializa, na etapa "Seções, Locais e Itinerários" (`src/formulario/itinerarios/etapa-itinerarios.tsx`), as `ViolacaoMontagem` (RN-034/035/036) que `aplicarNovasParadas` antes descartava com `if (!resultado.ok) return`. Um estado local efêmero (`violacoesMontagemMapa`, chaveado por `chaveItinerario`) é gravado na recusa e limpo no próximo sucesso; a UI renderiza um bloco `role="alert"` (`avisos-montagem-invalida`) com as mensagens da tentativa corrente. Nenhuma mudança em `motor-montagem.ts`, `estado-itinerarios.ts`, contrato JSON ou write-back. Testes: 3 casos unitários reforçados com asserção de texto da mensagem + 1 novo (RN-049, sem "tarifa"/"R$") em `itinerarios-motor-montagem.test.ts`; 2 cenários E2E novos em `etapa-itinerarios.spec.ts` (RN-034 ao reduzir a 1 parada; RN-035 ao criar Local no extremo, com o aviso sumindo ao corrigir).

## Regras RN verificadas

- **RN-034** — atendida. Mensagem "O itinerário precisa de ao menos 2 paradas" (já existente em `motor-montagem.ts:138`) agora chega à tela; testado em unitário (asserção de texto) e E2E (reduzir a 1 parada).
- **RN-035** — atendida. Mensagens de extremo (`motor-montagem.ts:146`/`156`) exibidas; testado em unitário e E2E (criar Local via mapa no extremo).
- **RN-036** — atendida. Mensagem de geolocalização ausente (`motor-montagem.ts:169`/`178`) exibida; testado em unitário (texto "geolocalização de Volta").
- **RN-048** — atendida, sem regressão. O write-back (`documentoComItinerarioAtualizado`) só ocorre no ramo `resultado.ok === true` com `situacao === "recalculada"` (`etapa-itinerarios.tsx:296-309`, inalterado por esta task); a captura de violações acontece ANTES desse ramo e faz `return` sem tocar o documento. E2E prova que a última rota válida (`descricao-texto`) permanece após uma tentativa inválida subsequente.
- **RN-049** — atendida. Nenhuma mensagem exibida cita tarifa/R$: verificado por regex em teste unitário dedicado e por asserção no E2E.
- **RN-052/RN-015** — sem regressão. `resolverParadasRota` (motor, inalterado) continua recusando ANTES de montar `ParadaRota[]`/chamar `recalcularItinerario`; o E2E de RN-034 mede o contador de chamadas OSRM e confirma que a tentativa inválida não gera chamada nova.
- **Fronteira com §11 (pendências)** — confirmada por leitura de `src/formulario/pendencias/pendencias.ts:110-114`: `coletarPendencias` só lê `itinerario.estadoRota.situacao` (de `ItinerarioAoVivo`, alimentado por `estadosRotaViva`/rota congelada). O novo estado `violacoesMontagemMapa` é local ao componente, nunca entra em `ItinerarioAoVivo` nem em `sessao` — não gera pendência prematura para itinerário em WIP, como a task exigia.

## Specs verificadas

- **Spec 04 §7.3** — aderente: "erro → categorias de §14" já cobria a falha do OSRM (TASK-022, inalterada); esta task cobre o caso irmão (montagem estruturalmente incompleta) que a spec não nomeia explicitamente mas que a revisão da TASK-019 identificou como lacuna de UX. Tratado como inferência controlada, textos ancorados em Spec 02 §10.1 (já existentes, não inventados).
- **Spec 04 §11** — aderente: verificado que o novo aviso não interfere no painel de pendências (`pendencias.ts`, ver acima) — só itinerário completo sem rota gera pendência bloqueante.
- **Spec 02 §10.1** — aderente: os textos exibidos (mínimo 2 paradas, extremos Seção, geolocalização do sentido) são os já validados na revisão da TASK-019, sem alteração de redação além do que já existia.

## Pontos corretos

- Escopo estritamente cirúrgico: `git show --stat` confirma 3 arquivos tocados (a etapa + os dois arquivos de teste), nenhum arquivo de `motor-montagem.ts`, `estado-itinerarios.ts`, `sessao.ts` ou schema alterado.
- Limpeza do aviso na recomputação bem-sucedida usa atualização funcional de `setState` (`(mapa) => ...`), evitando corrida com o `await` que atravessa o ciclo de render — mesmo padrão de cautela já usado no arquivo para `sessaoRef`.
- Teste E2E de RN-034 mede o **contador** de chamadas OSRM (não um booleano single-shot) — decisão correta, já que o cenário precisa de uma chamada real bem-sucedida no passo intermediário (2 paradas válidas) antes da tentativa inválida final; um mock que abortasse tudo (como a primeira versão do teste, corrigida durante a implementação) teria confundido "falha de rede" com "montagem incompleta".
- Novo teste unitário de RN-049 cobre duas combinações de violação simultânea (não só uma), reduzindo a chance de uma mensagem futura vazar menção a tarifa sem quebrar o teste.

## Problemas encontrados

Nenhum.

## Violações de escopo

Nenhuma. O "Fora de escopo" da task (mudar quando/como o recálculo ocorre; gate de exportação da TASK-032; pendência bloqueante de rota ausente da TASK-044; contrato JSON) foi respeitado — `motor-montagem.ts`, `estado-itinerarios.ts`, `pendencias.ts` e o schema não foram tocados (confirmado por `git show --stat` e por leitura de `pendencias.ts` nesta revisão). A opção "sinalizar a divergência tabela×documento" do "e/ou" da task não foi implementada — dentro do esperado, já que o critério de aceite resumido da task pede o motivo da recusa, não necessariamente as duas frentes.

## Testes avaliados

- **Cobertura das RN da task:** sim — RN-034/035/036 (mensagem exibida, unit + E2E) e RN-048/049 (write-back preservado, sem tarifa) todos com teste.
- **Casos inválidos testados:** sim — <2 paradas, Local no extremo (criação via mapa), duas violações simultâneas para o teste de RN-049.
- **Regressão de UUID:** N/A — task não toca import/export/cópia.
- **OSRM mockado:** sim — unitário não chama rede (função pura); E2E via `page.route` para `router.project-osrm.org`, com contador de chamadas explícito no teste de RN-034.
- **Fixtures reutilizadas:** sim — `carregar-multi-servico.json`, mesma fixture da suíte já existente da TASK-019.
- **Suíte executada:** verde — `npm test` 618/618 (era 617; +1 teste novo); `npx playwright test` 31/31 (era 29; +2 cenários); `npm run typecheck` e `npm run lint` limpos. Resultados conferidos nesta revisão executando os mesmos comandos novamente (não apenas citados do resumo da implementação).

## Checklist 07

- **Escopo (6/6 ok):** ROTA não-SEI; sem workflow/status/persistência nova; escopo exato; nenhuma regra inventada (textos reusam `validarMontagem` existente).
- **JSON (9/9 N/A ou ok):** nenhum campo/contrato tocado; RN-004/007/033/013/014/050/015 N/A ou inalteradas (write-back gate confirmado intacto).
- **Domínio (9/9 N/A):** nenhuma entidade/regra de domínio alterada.
- **Comparador (7/7 N/A):** módulo não tocado (RN-097 respeitada — só `formulario/itinerarios`).
- **Roteamento (5/5 ok):** RN-048/052/049 confirmadas com teste; RN-047/050/041-043 N/A (motor inalterado).
- **UI/PDF:** "mensagens seguem Spec 04 §14 e não mencionam tarifa" (RN-049) — ok, testado; demais itens (nomes Cidade-Nome, offsets, matrizes, PDF, exportação bloqueada) N/A, não tocados por esta task.
- **Testes (6/6 ok):** cobertura RN, casos inválidos, OSRM mockado, fixtures reutilizadas, verificações executadas e reportadas honestamente (nesta revisão, re-executadas).

**Nenhum item aplicável violado.**

## Pendências

- Nenhuma. Task sem "Perguntas em aberto" na entrada; nenhuma ambiguidade nova surgiu na implementação.
- A opção não escolhida do "e/ou" do resumo da task (sinalizar divergência tabela×documento) não é uma pendência formal — não era critério de aceite obrigatório; se o responsável do domínio quiser essa frente adicional, cabe uma task nova, não um follow-up desta revisão.

## Decisão

**Aprovado.** Escopo exato (3 arquivos, todos dentro do previsto na análise), as três RN centrais da task (034/035/036) mais RN-048/049 estão implementadas e testadas em unitário e E2E, com o contador de chamadas OSRM provando que a tentativa inválida não dispara rede nem apaga a última rota válida. Verificado nesta revisão — não só citado do resumo da implementação — que o novo estado local não vaza para `coletarPendencias` (leitura de `pendencias.ts`) nem para o contrato/write-back (leitura do gate em `etapa-itinerarios.tsx`). Suíte completa reexecutada e verde (618 unitários, 31 E2E, typecheck e lint limpos). Sem violação de RN, sem violação de escopo, sem ressalva pendente.
