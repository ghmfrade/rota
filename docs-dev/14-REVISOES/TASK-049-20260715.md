# Revisão da TASK-049

**Revisor:** Claude (Opus) — revisão autônoma DEC-050 f
**Data:** 2026-07-15
**Commit/branch revisado:** `e4150c6` (branch `redesign`) — "Implementa TASK-049: Fundação do design system: Tailwind CSS 4 + tokens"

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Funda a base do design system (doc 18 §1–§2 / DEC-050 b). Instala `tailwindcss@4` e `@tailwindcss/postcss` (devDependencies), cria `postcss.config.mjs` com o plugin do Tailwind e reescreve `src/app/globals.css`: `@import "tailwindcss"`, bloco `@theme` com os tokens do doc 18 §2 (paleta azul/cinza, semânticas, raios, sombras 1–3), transições em `:root`, base tipográfica em `@layer base` e bloco `prefers-reduced-motion`. O utilitário `sr-only` passa a ser emitido de verdade (Preflight/utilitário nativo do Tailwind). Diff restrito a `package.json`, `package-lock.json`, `postcss.config.mjs` e `src/app/globals.css` — nenhuma tela, componente, `data-testid`, `aria-*`, contrato ou comportamento tocado.

## Regras RN verificadas

- **RN-095** (Alta) — *atendida*. A alteração é 100% de aparência/build no ROTA; nada de SEI, workflow, status de pedido ou manifestação. Só config de estilo e tokens de CSS.
- **RN-096** (Alta) — *atendida*. Tailwind 4 processa em build (plugin PostCSS); `npm run build` gera export estático em `out/` sem servidor, ORM, fila ou endpoint de escrita. Guardrail "nenhuma dependência que exija servidor próprio" intacto (DEC-050 b) — todas as novas deps são `devDependencies`.
- **RN-011** (Alta) — *atendida (por não-ação)*. Nenhum status de pedido introduzido; JSON não tocado.
- **RN-076 / RN-010..015** — *N/A*. A task não toca contrato JSON, rótulos de tela nem PDF (só fundação de estilo).

## Specs verificadas

- **Spec 04 §2 (princípios de UX)** — *aderente*. Base sóbria/institucional: paleta azul/cinza, sombras sutis, transições curtas, respeito a `prefers-reduced-motion`. Nenhuma mudança de estrutura ou comportamento de tela.
- **doc 18 §2 (tabela de tokens)** — *aderente, conferido hex a hex* (ver "Pontos corretos").
- **doc 18 §6.5 (`sr-only` existe de verdade)** — *aderente* (ver "Pontos corretos").
- **DEC-050 (a)(b)** — *aderente*. Stack de estilo Tailwind 4 via `@theme` em `globals.css`, processamento em build; governança do doc 18 respeitada.

## Pontos corretos

- **Tokens do doc 18 §2 batem hex a hex.** Conferência integral de `src/app/globals.css:3-42` contra a tabela do doc 18 §2: `azul-50 #eff6ff`, `azul-100 #dbeafe`, `azul-300 #93c5fd`, `azul-600 #2563eb`, `azul-700 #1d4ed8`, `azul-900 #1e3a5f`, `cinza-50 #f8fafc`, `cinza-100 #f1f5f9`, `cinza-200 #e2e8f0`, `cinza-400 #94a3b8`, `cinza-500 #64748b`, `cinza-700 #334155`, `cinza-900 #0f172a`, `erro #dc2626`, `alerta #d97706`, `sucesso #16a34a`, `radius-painel 12px`, `radius-controle 8px`, `sombra-1 0 1px 2px rgb(15 23 42 / 0.06)`, `sombra-2 0 2px 8px rgb(15 23 42 / 0.10)`, `sombra-3 0 8px 24px rgb(15 23 42 / 0.16)`, `transicao-rapida 150ms cubic-bezier(0.2, 0, 0, 1)`, `transicao-media 250ms cubic-bezier(0.2, 0, 0, 1)`. Todos os 23 conferem; nenhum a mais, nenhum divergente.
- **`sr-only` emitido de verdade.** No CSS exportado (`out/_next/static/chunks/*.css`): `.sr-only{clip-path:inset(50%);white-space:nowrap;border-width:0;width:1px;height:1px;margin:-1px;padding:0;position:absolute;overflow:hidden}` — visually-hidden real (posiciona fora do fluxo, colapsa dimensões, esconde overflow), satisfazendo o critério de aceite e o doc 18 §6.5. A classe é referenciada em `src/formulario/viagens/etapa-viagens.tsx:351` e `src/formulario/identificacao/identificacao.tsx:232`, que antes ficavam sem definição.
- **Utilitários dos tokens gerados.** Nomenclatura `--color-*`/`--radius-*`/`--shadow-*` no `@theme` faz o Tailwind 4 emitir os utilitários (`bg-azul-600`, `text-cinza-700`, `rounded-painel`, `shadow-sombra-2` etc.), atendendo "tokens disponíveis como utilitários". Confirmado grep dos tokens no CSS emitido.
- **`prefers-reduced-motion` respeitado** (`globals.css:54-63`), como exige o doc 18 §2 (desliga transform/animação).
- **Escopo cirúrgico.** Diff limitado a config + `globals.css`; nenhum `.tsx`, nenhuma tela, nenhum seletor — a não-regressão de `data-testid`/`aria-*` é estrutural.

## Problemas encontrados

Nenhum bloqueante.

- **(Informativo, não impeditivo)** As transições (`--transicao-rapida`/`--transicao-media`) foram declaradas em `:root` (`globals.css:39-41`), não dentro do `@theme`. Ficam disponíveis como custom properties (`var(--transicao-rapida)`), mas — ao contrário de cores/raios/sombras — não geram utilitário Tailwind próprio. É escolha defensável (não há namespace de transição no `@theme` do Tailwind 4 que produza utilitário útil aqui) e não afeta este escopo; fica o registro para a TASK-050 consumir os valores via `var(...)` e não esperar uma classe `duration-*` nomeada.

## Violações de escopo

Nenhuma. O "Fora de escopo" da task (componentes `shared/ui`, restilização de tela, `data/`, contrato, comportamento) foi respeitado: o diff não cria `shared/ui`, não estiliza tela alguma e não toca nenhum arquivo de componente. Todos os artefatos do "Fora de escopo" ficaram intactos.

## Testes avaliados

- **Cobertura das RN da task:** N/A no sentido de teste unitário próprio — a task é fundação de estilo/config (RN-095/096 são de não-ação; não há regra de negócio nova a cobrir). A verificação é feita por build/lint/typecheck e pela não-regressão da suíte, como o próprio "Testes esperados" da task define.
- **Casos inválidos testados:** N/A (sem lógica nova).
- **Regressão de UUID:** N/A (não toca import/export/cópia).
- **OSRM mockado:** N/A (não toca roteamento).
- **Fixtures canônicas:** N/A.
- **Suíte executada (verificada por mim, não só reportada):**
  - `npm run typecheck` → OK (sem erros).
  - `npm run lint` → OK (sem avisos).
  - `npm test` (Vitest) → **765 passed (55 arquivos)**.
  - `npm run build` (export estático) → **OK**, 8/8 páginas geradas em `out/`.
  - `npm run test:e2e` (Playwright) → **47 passed** — nenhum seletor alterado; o Preflight/reset do Tailwind não regride nenhuma asserção de E2E.

## Checklist 07

- **Escopo (6 itens):** 6 ok — pertence ao ROTA e não ao SEI (RN-095); sem workflow; sem status de pedido além da exceção (RN-011); sem persistência de servidor (RN-096, Tailwind em build); só o escopo da task (nada aproveitado — sem `shared/ui`, sem restilização); nenhuma regra inventada (tudo ancorado em doc 18 §2 / DEC-050).
- **JSON (9 itens):** N/A — a task não toca o contrato JSON.
- **Domínio (10 itens):** N/A — a task não toca entidades de domínio.
- **Comparador (7 itens):** N/A.
- **Roteamento (5 itens):** N/A — sem OSRM; abrir/navegar segue sem rede.
- **UI/PDF (8 itens, inclui os 2 novos de DEC-050):**
  - RN-076 (nomenclatura/formatos), offsets, matrizes, Locais, aviso SEI, exportação bloqueada → **N/A** (task não estiliza tela nem toca PDF).
  - **UI aderente ao design system (DEC-050)** → **ok**: tokens via `@theme` conforme doc 18 §2; sem `style=` inline; nenhum hex/px avulso fora do `@theme` (única fonte dos valores). Base do sistema estabelecida corretamente.
  - **`data-testid`/`aria-*` preservados; E2E sem alterar seletores (DEC-050)** → **ok**: nenhum `.tsx` tocado; 47 E2E verdes sem mexer em seletor.
- **Testes (6 itens):** verificações executadas e reportadas honestamente (typecheck/lint/765 unit/build/47 E2E, todos verdes) → ok; demais itens (RN com teste, casos inválidos, regressão UUID, OSRM mock, fixtures) → N/A pela natureza da task.

**Resultado: todos os itens aplicáveis ok (incluindo os 2 novos de UI/DEC-050); nenhum violado.**

## Pendências

- Nenhuma condição de merge.
- Follow-up (não-bloqueante): a TASK-050 deve consumir `--transicao-rapida`/`--transicao-media` via `var(...)` (não há utilitário nomeado gerado para elas).
- Q-xxx abertas: nenhuma.

## Decisão

**Aprovado.** Escopo cirúrgico e exatamente o previsto (fundação Tailwind 4 + tokens do doc 18 §2 + `sr-only` real), sem tocar tela, componente, contrato ou comportamento — "Fora de escopo" integralmente respeitado. Os tokens conferem hex a hex com o doc 18 §2 (23/23) e são emitidos como utilitários; o `sr-only` aplica visually-hidden de verdade no CSS exportado, satisfazendo o critério de aceite e o doc 18 §6.5. O desvio reportado pelo implementador (não definir `sr-only` manualmente porque o Tailwind já o emite) é legítimo e comprovado no bundle. Guardrails RN-095/096 intactos (tudo em build, deps de dev). Todas as verificações verdes por conferência própria: typecheck, lint, 765 unitários, build estático e 47 E2E. A única observação (transições em `:root` vs `@theme`) é informativa e não afeta este escopo.
