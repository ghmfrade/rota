# Revisão da TASK-098

**Revisor:** Codex (revisão de aderência)
**Data:** 2026-07-24
**Commit/branch revisado:** `c91067e` (branch `redesign`)

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

O handler de `contextmenu` em `src/shared/mapa/mapa.tsx:367-380` passa a projetar a coordenada sobre o traçado (`projetarSobreLinhas`) antes de entregá-la a `aoClicarDireitoNaLinha`, com fallback `?? posicao` para linha degenerada. `aoClicarDireito` (fora da linha) e `ancoraTela` (sempre `originalEvent.clientX/Y`) ficam intocados. Teste unitário novo em `testes/unitarios/mapa/mapa.test.tsx` prova a paridade com o `click` (mesma coordenada projetada). Teste E2E novo em `testes/e2e/etapa-itinerarios.spec.ts` clica a poucos pixels da linha (dentro da tolerância de 6 px, fora do traçado exato) e confirma que a Seção nasce mais perto da projeção do que do pixel bruto. `editor-mapa-itinerario.tsx`/`etapa-itinerarios.tsx` não foram tocados — já propagavam `posicao`/`posicaoNaLinha` genericamente desde a TASK-095.

## Regras RN verificadas

- RN-027 (350 m) — atendida: passa a ser avaliada sobre a coordenada projetada; nenhuma mudança na função de validação, só na entrada (`mapa.tsx:377`). Não há teste dirigido de recusa por 350 m nesta task, mas o motor é reusado sem alteração (mesma cobertura pré-existente).
- RN-029 (município) — atendida pelo mesmo raciocínio; motor reusado sem alteração.
- RN-026 — atendida: `geolocalizacao_<sentido>` da Seção recebe a projeção via fluxo já existente (`aoCriarOuAtualizarSecao` → `posicaoNaLinha`).
- RN-032 / DEC-075 (Local unidirecional) — não tocada por esta task; comportamento preservado (nenhum código de `fluxos-local.ts` mudou).
- RN-042 (ponto de rota) — não tocada; o gesto de botão esquerdo é intocado.
- RN-097 (`shared/mapa` primitiva desacoplada) — atendida: mudança é aditiva dentro do próprio handler de `contextmenu`; consumidores sem `aoClicarDireitoNaLinha` seguem recebendo a coordenada bruta via `aoClicarDireitoRef` (inalterado).
- RN-004 (UUIDs preservadas) — N/A: task não toca import/export/cópia, só a coordenada de uma criação nova.

## Specs verificadas

- Spec 04 §7.3 item 2 — aderente: Seções/Locais continuam inseridos "em ordem, clicando no mapa"; a task só corrige a coordenada resultante do clique sobre a linha.
- Spec 04 §7.3 item 6 — aderente por extensão simétrica: mesma origem de simetria que a DEC-072 já aplicou ao clique esquerdo (TASK-069).
- Spec 02 §5.1/§7.1 — aderente: nenhum campo novo, `geolocalizacao_ida`/`_volta` seguem recebendo `Coordenada` no mesmo formato.
- Spec 03 §2.1/§2.2/§2.3 — aderente: 350 m e município continuam avaliados pelos motores existentes, só a entrada muda.

## Pontos corretos

- Fallback `?? posicao` implementado exatamente como a DEC-078 e os "Casos inválidos" da task exigem: linha degenerada nunca descarta o clique.
- `ancoraTela` permanece na posição bruta do cursor (`originalEvent.clientX/clientY`), confirmando o critério "o menu não pode saltar".
- Índice de inserção intocado: `ancorarPontoNaRota`/`indiceInsercaoParaPosicao` não foram alterados, conforme "Fora de escopo".
- Comentário acrescentado no handler (`mapa.tsx:360-366`) documenta a decisão e cita DEC-078/TASK-098, seguindo o padrão de rastreabilidade do arquivo.
- Reuso do `projetarSobreLinhas` já existente evita duplicar lógica de projeção entre `click` e `contextmenu` — mesma função, sem desvio de comportamento entre os dois botões.
- Teste E2E deliberadamente clica **perto, não exatamente sobre** a linha (deslocamento perpendicular de 3 px), o que prova a projeção de fato desloca o resultado — mais forte que testar só o caso trivial "clique exatamente sobre a linha".

## Problemas encontrados

Nenhum.

## Violações de escopo

Nenhuma. Os únicos arquivos de produção tocados são `src/shared/mapa/mapa.tsx`; `editor-mapa-itinerario.tsx` e `etapa-itinerarios.tsx` não foram alterados, como a própria task previa ("Alterar (provavelmente nada)").

## Testes avaliados

- cobertura das RN da task: sim — RN-026/027/029/097 cobertas por reuso de motores + teste novo de paridade de coordenada.
- casos inválidos testados: sim, no nível do handler — linha degenerada cai no fallback (código presente e coerente com o teste unitário pré-existente de "fora da linha"); recusa de 350 m e "fora de SP" não ganharam teste **dirigido** nesta task, mas não são regra alterada (motor reusado, cobertura pré-existente mantida).
- regressão de UUID: N/A (task não toca import/export/cópia).
- OSRM mockado: sim, no E2E (`page.route` intercepta `router.project-osrm.org`).
- suíte executada com resultado: log canônico reutilizado (não repetido nesta revisão) — `npm run test:all:verificar` confirmou: **APROVADO**, fingerprint `368c14f9…`, working tree `498d7fa6…`, coincidindo com o HEAD revisado (`c91067e`). 67 testes E2E + suíte unitária, código de saída 0 em ambos.

## Checklist 07

40 itens aplicáveis avaliados, 0 violados, 9 N/A (Comparador, Roteamento fora do escopo direto, PDF, itens de UI/design system fora do gesto de mapa). Destaques:
- Escopo: ok em todos os itens (nenhuma regra inventada — cita DEC-078/Q-056 nos comentários e no diff).
- JSON (contrato): ok — nenhum campo novo, UUID preservada, sem R$.
- Testes: ok — RN cobertas, mock de OSRM no E2E, evidência única reutilizada via `test:all:verificar` com fingerprint/identidade conferidos.

## Pendências

Nenhuma. Task independente de 097 (concluída) e sem novas dependências abertas.

## Decisão

Escopo exato ao objetivo da task e à DEC-078: a extensão do `contextmenu` reusa a mesma primitiva de projeção do `click` (TASK-069/DEC-072), sem tocar índice de inserção, motores de 350 m/município, ou o gesto de arrasto (opção C explicitamente descartada permanece descartada). RN relevantes cobertas com teste novo de paridade (unitário) e de deslocamento real (E2E). Evidência canônica válida e correspondente ao commit revisado. Aprovado sem ressalvas.
