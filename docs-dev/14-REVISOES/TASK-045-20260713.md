# Revisão da TASK-045

**Revisor:** Claude (revisão de aderência assistida)
**Data:** 2026-07-13
**Commit/branch revisado:** `546087f` (branch `master`) — "Implementa TASK-045: Fetch-espião ativo na garantia de abertura sem OSRM"

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Correção de qualidade de teste decidida em DEC-042 (Q-023), originada da ressalva
não bloqueante da revisão da TASK-024. Em
[testes/unitarios/formulario/roteamento-recalculo-vivo.test.ts](../../testes/unitarios/formulario/roteamento-recalculo-vivo.test.ts)
o espião decorativo (`const fetchEspiao = vi.fn()`, nunca ligado à unidade) foi
substituído por um **espião ativo** sobre `globalThis.fetch`
(`vi.spyOn(globalThis, "fetch")`), com `afterEach(() => vi.restoreAllMocks())`.
Nenhum código de produção alterado; 1 arquivo tocado.

## Regras RN verificadas

- **RN-052** (Alta) — *atendida, agora com guard real*. A asserção
  `expect(fetchEspiao).not.toHaveBeenCalled()` deixou de ser vacuamente verdadeira:
  o espião agora está ligado ao `fetch` global efetivamente disponível à unidade,
  então passaria a **falhar** se `congelarRotaCarregada` chamasse a rede na
  abertura. A garantia estrutural (função síncrona, sem `Promise`) permanece como
  reforço. `mockImplementation` que rejeita garante que qualquer chamada acidental
  estoure alto, sem tocar a rede real.

## Specs verificadas

- **Spec 04 §3.1 item 6 / Spec 03 §3.6.2** — *aderente*. O caminho de abertura
  continua provado como "0 chamadas OSRM", agora por evidência ativa (DEC-042).

## Pontos corretos

- Escopo exato ao DEC-042/TASK-045: só o teste de abertura foi fortalecido.
- `afterEach(vi.restoreAllMocks())` evita vazar o spy do `fetch` global para outras
  suítes — higiene de isolamento correta.
- `mockImplementation` que rejeita torna qualquer regressão barulhenta em vez de
  silenciosamente ir à rede — coerente com "OSRM sempre mockado" (docs-dev/08).
- Os demais 7 testes da suíte permaneceram intactos e verdes.

## Problemas encontrados

Nenhum.

## Violações de escopo

Nenhuma. `estado-rota-viva.ts` e todo o código de produção permaneceram intocados,
como exige o "Fora de escopo" da task. A assinatura provisória do composer (outro
follow-up da TASK-024) **não** foi tocada — corretamente deferida à TASK-025.

## Testes avaliados

- **cobertura da RN da task:** sim — RN-052 na abertura, agora com espião ativo.
- **casos inválidos testados:** N/A para esta task (é fortalecimento de asserção de
  um caso já existente; os casos inválidos de recálculo seguem cobertos pela suíte).
- **regressão de UUID:** N/A — não toca import/export/cópia.
- **OSRM mockado:** sim — `globalThis.fetch` espionado e mockado; nenhum teste toca
  a rede real.
- **suíte executada com resultado:** **verde** — `npm test` → 39 arquivos / 518
  testes passando (mesma contagem: a task fortalece uma asserção, não adiciona
  teste); `npm run typecheck` e `npm run lint` sem erros (reexecutados nesta revisão).

## Checklist 07

**Resultado: itens aplicáveis OK, demais N/A, 0 violados.**

- *Escopo* — OK: pertence ao ROTA; sem workflow/persistência; só o escopo da task;
  nenhuma regra inventada (segue DEC-042).
- *JSON (contrato)* — N/A (task só de teste, não toca schema nem dados).
- *Domínio* — N/A.
- *Comparador* — N/A (mas DEC-042 registra o padrão reutilizável para a garantia
  RN-080 quando o Comparador for testado).
- *Roteamento* — RN-052 OK (abrir sem OSRM, agora com guard ativo); demais itens
  inalterados/N-A.
- *UI/PDF* — N/A.
- *Testes* — OK: OSRM mockado; verificações executadas e reportadas; fixtures não
  divergentes (reusa as constantes locais já existentes da suíte).

## Pendências

- **Follow-up (TASK-025):** revisar a assinatura de `ComporDescricao` e o call-site
  em `recalcularItinerario` — pendência herdada da TASK-024, fora do escopo desta.
- **Padrão registrado (DEC-042):** aplicar o mesmo espião ativo à garantia RN-080
  (Comparador nunca chama OSRM) quando aquela suíte for escrita/tocada — não agora.

## Decisão

**Aprovado.** A entrega faz exatamente o que DEC-042 determinou e o que a TASK-045
delimitou: transforma uma asserção decorativa em guard de regressão real da RN-052,
sem tocar código de produção nem nada fora de escopo. Checklist 07 sem violações;
nenhuma RN Alta ou NEG-xxx em risco; typecheck, lint e 518 testes verdes. Fecha a
única ressalva de qualidade de teste levantada na revisão da TASK-024. Ciclo
concluído.
