# Revisão da TASK-059

**Revisor:** Claude (revisão automatizada — `/revisar-aderencia`)
**Data:** 2026-07-16
**Commit/branch revisado:** `956a61f` (branch `redesign`) — "Implementa TASK-059: Desenhar a rota ativa no mapa da etapa de itinerários"

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Novo conversor puro `linhaDaGeometria` em [`src/shared/mapa/geometria.ts`](../../src/shared/mapa/geometria.ts#L29-L46), que transforma `Rota.geometria` (GeoJSON `LineString`, Spec 02 §10.2) em `LinhaMapa`. `EtapaItinerarios` computa `linhaRotaAtual` a partir de `estadoAtual` (`congelada`/`recalculada`) e a repassa como nova prop `linhaRota` a `EditorSecoes` e `EditorLocais`, que a incluem no array `linhas` já suportado por `<Mapa>`. Testes novos: unitários do conversor (`testes/unitarios/mapa/geometria.test.ts`) e de encanamento da prop nos dois editores (`testes/unitarios/formulario/editor-linha-rota.test.tsx`, com `<Mapa>` dublê via `vi.mock`).

## Regras RN verificadas

- RN-046 — atendida. A linha desenhada vem de `estadoAtual.rota.geometria` (congelada ou recalculada), nunca recomposta; comentário em [`etapa-itinerarios.tsx:201-207`](../../src/formulario/itinerarios/etapa-itinerarios.tsx#L201-L207) cita a regra corretamente.
- RN-052 — atendida. Nenhuma chamada nova ao OSRM: `linhaDaGeometria` é função pura (`src/shared/mapa/geometria.ts:38-46`) e `linhaRotaAtual` só lê o `EstadoRotaViva` já resolvido (que por sua vez, para o caminho "abrir JSON", vem de `congelarRotaCarregada` — síncrona, sem rede, `estado-rota-viva.ts:53-55`, verificado nesta revisão). Verificado também pelo critério de aceite 2 (abaixo).
- RN-015 — atendida. Nenhum recálculo é introduzido pelos leitores da rota; a task só reformata dado já congelado.
- RN-042 — atendida. `pontos_de_rota` não é tocado por este código; a task desenha só a `LineString` da rota (`geometria.coordinates`), sem misturar com pontos de rota ou paradas — consistente com o "Fora de escopo" da própria task.

## Specs verificadas

- Spec 04 §7.3 — aderente. "A cada parada, a rota é recalculada e desenhada" passa a ser cumprido pela metade que faltava (desenho); o recálculo já existia (TASK-024).
- Spec 02 §10.2 — aderente. `geometria.coordinates` é `[longitude, latitude]`; `dePosicao` (`geometria.ts:29-31`) desestrutura `[lng, lat]` na ordem correta. Teste `"converte coordinates [lon,lat] para pontos {lng,lat} preservando a ordem"` (`testes/unitarios/mapa/geometria.test.ts`) confere a ordem explicitamente.

## Pontos corretos

- Reaproveita o `linhas: LinhaMapa[]` que `<Mapa>` já sabia desenhar (`src/shared/mapa/mapa.tsx:48`), sem tocar o componente de mapa em si — risco baixo, como a task previa.
- Trata corretamente o estado `sem-rota`: `linhaRotaAtual` fica `undefined` e cada editor converte para array vazio (`linhaRota ? [linhaRota] : []`), nunca passando `undefined` para `<Mapa>` — e o bloco `mensagem-sem-rota` ([`etapa-itinerarios.tsx:610-611`](../../src/formulario/itinerarios/etapa-itinerarios.tsx#L610-L611)) não foi alterado.
- Teste de encanamento cobre explicitamente o caso "sem `linhaRota`" (array vazio, não `undefined`) para os dois editores — cobre o critério de aceite 3.
- Segue exatamente os "Arquivos prováveis" listados na task (`docs-dev/06-BACKLOG_INICIAL.md` linhas 600-604): os dois editores, `etapa-itinerarios.tsx`, e o conversor em `src/shared/mapa/`.
- Risco documentado na task ("a rota é passada aos dois mapas, redundante — interino até a TASK-060") reproduzido fielmente pela implementação (mesma `linhaRotaAtual` passada a `EditorSecoes` e `EditorLocais`).

## Problemas encontrados

Nenhum.

## Violações de escopo

Nenhuma. Não unifica os dois mapas (TASK-060), não desenha vértices de pontos de rota, não adiciona chamada ao OSRM, não altera `data-testid`/`aria-*` existentes — todos os itens do "Fora de escopo" da task foram respeitados.

## Testes avaliados

- cobertura das RN da task: sim — RN-046/052/015 cobertas por `geometria.test.ts` (pureza, ordem de coordenadas) e `editor-linha-rota.test.tsx` (encanamento da prop, inclusive quando não há rota).
- casos inválidos testados: sim — caso "sem `linhaRota`" (equivalente a `sem-rota`) testado nos dois editores.
- regressão de UUID: N/A (task não toca import/export/cópia).
- OSRM mockado: N/A (nenhum código novo chama OSRM; nenhum teste novo depende de rede — `<Mapa>` é substituído por dublê via `vi.mock`, evitando WebGL/jsdom sem qualquer chamada de rede).
- suíte executada com resultado: verde. `npx tsc --noEmit -p .` sem erros; `npm test -- --run` → 68 arquivos, 850 testes, todos passando; `npm run lint` sem apontamentos.

## Checklist 07

Percorrido item a item (`docs-dev/07-CHECKLIST_ADERENCIA_SPEC.md`): **11 itens aplicáveis, todos OK; demais N/A** (task não toca JSON/contrato, Domínio, Comparador, Roteamento/OSRM nem PDF).

Aplicáveis e OK:
- Escopo: pertence ao ROTA (não SEI); sem workflow; sem status de pedido; sem persistência transacional; implementa apenas o escopo da task; nenhuma regra inventada (RN-046/052/015/042 citadas corretamente nos comentários).
- Domínio: RN-042 (ponto de rota não confundido com a linha da rota) — OK.
- UI: `data-testid`/`aria-*` existentes preservados — OK (diff confirmado, sem alteração de seletores).
- Testes: RN implementada com teste; casos inválidos testados; fixtures ad hoc simples e coerentes com o schema (`LineString` válido); verificações executadas e reportadas com resultado real acima.

N/A (task explicitamente não toca): JSON (contrato), UUID/import-export, Comparador, Roteamento/OSRM (RN-047/048/050), PDF, design system de componentes novos (nenhum componente novo criado).

## Pendências

Nenhuma. Task pronta para avançar à TASK-060 (unificação dos mapas), que reaproveita `linhaDaGeometria`.

## Decisão

Aprovado sem ressalvas: escopo exatamente o previsto ("Arquivos prováveis" da task batem 1:1 com o diff), as quatro RN envolvidas (RN-046, RN-052, RN-015, RN-042) estão corretamente respeitadas e testadas, os cinco critérios de aceite da task foram verificados individualmente contra o código, nenhum `data-testid`/`aria-*` foi alterado, e a suíte completa (typecheck, lint, 850 testes unitários) está verde.
