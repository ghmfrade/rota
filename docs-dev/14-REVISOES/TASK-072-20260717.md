# Revisão da TASK-072

**Revisor:** Claude (assistido) — invocado por ghmfrade
**Data:** 2026-07-17
**Commit/branch revisado:** `4c1c507` (branch `redesign`)

## Resultado

- [ ] Aprovado
- [x] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Unifica o CRUD da etapa Serviços no modo "novo": [`servicos.tsx`](../../src/formulario/servicos/servicos.tsx)
passa a derivar `completos` de `servicosDaSessao(sessao)` (DEC-053/TASK-061) nos
dois modos em vez de `[]` fixo no modo novo ([`servicos.tsx:135`](../../src/formulario/servicos/servicos.tsx#L135));
`salvar`/`duplicar` sobre um completo passam a gravar via `comServicosDaSessao`
([`servicos.tsx:242`](../../src/formulario/servicos/servicos.tsx#L242),
[`servicos.tsx:278-280`](../../src/formulario/servicos/servicos.tsx#L278-L280)); `remover` bifurca por modo,
usando a nova função pura genérica `removerServicoDeLista`
([`remover.ts:31-49`](../../src/formulario/servicos/remover.ts#L31-L49)) sem trava de mínimo 1 no modo novo
([`servicos.tsx:309-328`](../../src/formulario/servicos/servicos.tsx#L309-L328)); cada linha ganha selo de
estado explícito via `Selo` do `shared/ui` — `servico-completo` para promovidos, mantendo o
`servico-em-construcao` já existente ([`servicos.tsx:381-389`](../../src/formulario/servicos/servicos.tsx#L381-L389)).
6 testes unitários novos de integração da etapa + 5 de `removerServicoDeLista`; suíte completa verde (905).

## Regras RN verificadas

- **RN-001/002** (UUID preservada em edição) — **atendida**. `salvar` sobre completo usa `editarEntidade` mantendo `s.uuid` como chave de match ([`servicos.tsx:233-241`](../../src/formulario/servicos/servicos.tsx#L233-L241)); testado em `servicos-etapa.test.tsx` ("editar um promovido preserva a uuid").
- **RN-007** (duplicar = entidade nova, `secao_uuid` mantida, `local_uuid` re-mapeado) — **atendida**. Reusa `duplicarServico` (não tocado nesta task, conforme "Fora de escopo"); a UI só atribui novo `numero_n` sugerido ([`servicos.tsx:271-280`](../../src/formulario/servicos/servicos.tsx#L271-L280)). Testado ("duplicar um promovido gera UUID nova preservando secao_uuid").
- **RN-006** (`numero_n` não é identidade) — **atendida**. `todosNumeros` (usado só para sugestão) passa a incluir a lista unificada `linhas` ([`servicos.tsx:158-159`](../../src/formulario/servicos/servicos.tsx#L158-L159)); nenhum match por `numero_n`. Testado (numeração contínua `0000-2...`).
- **RN-018** (documento válido exige ≥ 1 Serviço; gate de exportação, não de sessão) — **atendida**. `podeRemover` só aplica a trava no modo carregado ([`servicos.tsx:304-307`](../../src/formulario/servicos/servicos.tsx#L304-L307)); `removerServicoDeLista` nunca lança, `removerServico` (documento) continua lançando ([`remover.ts:57-65`](../../src/formulario/servicos/remover.ts#L57-L65)). Testado nos dois sentidos (remove o único promovido sem trava; modo carregado permanece bloqueado).
- **RN-096 / NEG-004** (sessão efêmera; nada gravado antes da exportação) — **atendida**. Toda escrita do modo novo é sobre `sessao.servicos`/`sessao.secoesEmConstrucao` (campos de sessão já documentados como não-contrato em `sessao.ts:124-132`, TASK-061); nenhuma alteração de `shared/contrato`.

## Specs verificadas

- **Spec 04 §6** (etapa Serviços: criar/editar/duplicar/remover; `numero_n` sugerido) — **aderente**. As quatro operações agora cobrem os dois estados (completo/em construção) nos dois modos.
- **Spec 03 §10.3 regra 5** (`numero_n` sequencial por ordem de cadastro) — **aderente**. `todosNumeros` inclui `completos` (promovidos) + `linhasEmConstrucao`; testado (`0000-1CR` promovido → sugestão `0000-2...`).
- **Spec 02 §14** (Seção com ≥ 1 entrada; documento exportado exige ≥ 1 Serviço) — **aderente** quanto à cascata de Seção órfã (mesma lógica de `removerServico`, generalizada); o gate de "documento exportado" em si permanece — não verificado nesta revisão se a validação de exportação (fora do escopo desta task) já cobre o caso de `sessao.servicos` vazio no modo novo antes de montar o documento final.

## Pontos corretos

- `removerServicoDeLista` extraída de `removerServico` sem duplicar a lógica de cascata — `removerServico` (documento) agora só adiciona a trava de mínimo 1 e delega ([`remover.ts:57-77`](../../src/formulario/servicos/remover.ts#L57-L77)); regressão do modo carregado coberta por teste próprio (`removerServico — cascata de Seção órfã` e o teste de integração "modo carregado não regride").
- Escopo cirúrgico: não tocou `duplicarServico`, `sugerirNumeroN`/`regenerarSufixoNumeroN`, nem o comportamento do modo carregado — reuso conforme "Fora de escopo" da task.
- Selo de estado usa o componente `Selo` de `shared/ui` (DEC-050), sem `style=` inline; `data-testid="servico-em-construcao"` preservado literalmente, `data-testid="servico-completo"` é aditivo (não colide com seletores E2E existentes — conferido em `testes/e2e/servicos.spec.ts`, que só usa `servico-item`/`servico-em-construcao`).
- Atomicidade da remoção no modo novo: `servicos` e `secoesEmConstrucao` gravados no mesmo `aoAtualizarSessao` ([`servicos.tsx:323-327`](../../src/formulario/servicos/servicos.tsx#L323-L327)), sem janela intermediária inconsistente.
- Testes cobrem tanto o caminho feliz quanto a regressão-guarda pedida no risco da task (modo carregado inalterado) e a Seção compartilhada não sendo descartada indevidamente.

## Problemas encontrados

(ordenados por severidade — nenhum viola RN Alta/NEG-xxx)

1. **[Ressalva — cobertura de teste] O cenário "Seção ainda usada por um `ServicoEmConstrucao` permanece" (critério de aceite, `06-BACKLOG_INICIAL.md:859`) não tem teste dedicado no modo novo.** Os testes de cascata cobrem "Seção compartilhada entre dois Serviços completos" (`servicos-remover.test.ts`, via fixture `documentoBidirecionalMultiServico`), mas nenhum teste exercita a remoção de um Serviço promovido enquanto um `ServicoEmConstrucao` ainda referencia a mesma Seção em `secoesEmConstrucao`. Verificado nesta revisão que `ServicoEmConstrucao` ([`sessao.ts:41-53`](../../src/formulario/sessao.ts#L41-L53)) não tem campo de Seção/parada (só ganha `itinerarios`/paradas ao ser promovido) — então, pelo modelo atual, um em-construção não referencia `secao.servicos[]` antes de promovido, o que torna o cenário do critério de aceite estruturalmente inatingível hoje. Isso não é uma falha de código, mas o critério de aceite como redigido não é verificável no estado atual do modelo — vale nota/ajuste de redação, não bloqueio.
2. **[Ressalva menor — verificação] E2E não reexecutado nesta revisão.** Idêntico ao Problema 2 da revisão da TASK-061: apoia-se em não haver `data-testid`/`aria-*` alterado (só um testid novo aditivo, `servico-completo`) e no fato de `testes/e2e/servicos.spec.ts` não referenciar esse testid novo. Estruturalmente verde, mas não executado via Playwright nesta revisão.

## Violações de escopo

Nenhuma. Não tocou `duplicarServico`, `numero-n`, contagens, contrato/schema, nem o caminho do modo carregado além de reusar a função `removerServico` já existente (com a mesma trava, extraída para compartilhar a cascata). Não mexeu em troca de direcionalidade nem na etapa de mapa.

## Testes avaliados

- **cobertura das RN da task:** sim — RN-001/002 (edição), RN-006 (numeração), RN-007 (duplicação), RN-018 (remoção sem trava no modo novo + regressão-guarda do modo carregado), RN-096/NEG-004 (sessão efêmera). Falta apenas o cenário de cascata cruzando completo/em-construção (Problema 1), que hoje é inatingível pelo modelo.
- **casos inválidos testados:** sim — modo carregado com trava de mínimo 1 continua bloqueando o botão Remover.
- **regressão de UUID:** sim — teste direto de preservação na edição e de UUID nova na duplicação.
- **OSRM mockado:** N/A — nenhum teste toca roteamento; a etapa Serviços não chama OSRM.
- **suíte executada com resultado:** **verde** — `npm run typecheck` limpo, `npm run lint` limpo, `npm test` (`vitest run`): 74 arquivos / 905 testes passando (inclui os 11 novos desta task).

## Checklist 07

- **Escopo (6):** todos ok.
- **JSON (9):** ok/N-A — sem campo novo de contrato, UUID preservada (RN-004), entidades novas com UUID nova (RN-002/007), sem R$, sem recálculo por leitor.
- **Domínio (10):** ok/N-A — Serviço mantém identidade por UUID (RN-001), `numero_n` não usado como identidade (RN-006), extremos de itinerário não tocados.
- **Comparador (7):** N/A — não tocado.
- **Roteamento (5):** N/A — etapa Serviços não chama OSRM.
- **UI/PDF (10):** ok — `Selo` de `shared/ui`, sem `style=` inline (DEC-050); `data-testid`/`aria-*` existentes preservados, `servico-completo` é testid aditivo.
- **Testes (6):** 5 ok; 1 ressalva — cenário de cascata cruzando completo/em-construção do critério de aceite não coberto (Problema 1), embora estruturalmente inatingível hoje.

Resultado: itens aplicáveis atendidos, com **2 ressalvas** (cobertura de um critério de aceite estruturalmente inatingível; E2E não reexecutado).

## Pendências

- **Condição de merge:** nenhuma bloqueante.
- **Follow-up:** se o modelo de `ServicoEmConstrucao` ganhar referência a Seção antes da promoção (ex.: pré-visualização de itinerário em construção referenciando Seções), revisitar a cascata de `removerServicoDeLista` para confirmar que ela ainda captura esse caso — hoje a condição opera só sobre `secao.servicos[]`, que reflete exclusivamente Serviços completos.
- **Nota (pointer, verificado):** o E2E ponta a ponta do fluxo unificado (criar → promover → editar/duplicar/remover na mesma lista) é a **TASK-062**, que ainda não roda contra o `servico-completo` novo — vale conferir lá.

## Decisão

**Aprovado com ressalvas.** Escopo exato ao objetivo da task, sem violação de RN Alta nem NEG-xxx; RN-001/002/006/007/018/096 verificadas com teste; regressão-guarda do modo carregado testada; `typecheck`/`lint`/suíte completa verdes (905/905). As duas ressalvas — o critério de aceite sobre Seção compartilhada com `ServicoEmConstrucao` (hoje inatingível pelo modelo, não uma falha de implementação) e o E2E não reexecutado nesta revisão — são desvios menores de verificação, sem impacto de regra crítica, e ficam como acompanhamento pós-merge.
