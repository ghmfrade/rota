# Revisão da TASK-123

**Revisor:** Claude (revisão de aderência, `/revisar-aderencia`)
**Data:** 2026-07-31
**Commit/branch revisado:** `3dec0bf` (branch `redesign`)

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Extrai a superfície `acao-inserir-posterior` (antes só declarada inline no
ramo de célula passante) para a função reaproveitável `superficieInserirPosterior`
em `src/formulario/viagens/etapa-viagens.tsx`, e a renderiza também ancorada na
célula de partida quando `secoesDaGrade.length === 1` (modo compacto,
DEC-086), no mesmo padrão já usado por `superficieHeadway`. Nenhuma lógica de
inserção (`aoInserirViagemPorOffset`) foi tocada — só o call site mudou de
lugar. Testes novos em `testes/e2e/etapa-viagens.spec.ts`,
`testes/unitarios/formulario/etapa-viagens-insercao-relativa.test.tsx` e
`testes/unitarios/formulario/etapa-viagens-modo-compacto.test.tsx`.

## Regras RN verificadas

- RN-061 (Viagem/dia correto) — atendida; `superficieInserirPosterior` recebe
  `viagemUuid`/`dia`/`indiceBloco` do mesmo `alvoFoco` usado pela ação de
  headway e pela ação anterior, sem cruzar Viagens
  (`etapa-viagens.tsx:1365-1374`, `:1444-1453`).
- RN-063 (`horarios_paradas` íntegro) — atendida; o motor de inserção não foi
  alterado (confirmado por `git diff` isolando o commit: a única mudança em
  torno de `aoInserirViagemPorOffset` é a linha do `onClick` mudando de
  posição no arquivo, não de corpo) e o teste unitário "insere Viagem no modo
  compacto com o mesmo resultado do modo completo, exceto a UUID" compara
  `horarios_paradas` das duas Viagens criadas e as verifica iguais.
- RN-067 (sem offset exposto ao usuário) — atendida; o campo exibido é
  "Deslocamento posterior" em `HH:MM`, idêntico ao já existente no modo
  completo, sem novo campo.
- RN-096 (estado efêmero) — atendida; teste unitário confere
  `aoAtualizarSessao` não chamado e documento inalterado só por exibir o
  controle no hover/alternar modo.
- RN-004/RN-007 (UUID nova na Viagem criada) — atendida; teste unitário
  compara `criadaCompacto.uuid !== criadaCompleto.uuid` e
  `criadaCompacto.uuid !== viagensCompacto[0].uuid`.

## Specs verificadas

- Spec 04 §8.1 (modo de apresentação) — aderente: o compacto continua sendo
  puramente um modo de exibição, sem novo dado, e agora expõe a mesma
  capacidade de inserção do modo completo, como a DEC-086 exige.
- Spec 04 §8.3 (inserir viagem entre viagens) — aderente: reusa o mesmo botão
  `↓`/`data-testid="inserir-viagem-posterior"` e o mesmo motor de offset.
- Spec 02 §11/§12 (Viagem e identidade) — aderente: nova Viagem recebe UUID
  nova, mesma estratificação por dia.
- `docs-dev/18-DESIGN_SYSTEM.md` §3/§5/§6 (flutuantes/reposicionamento) —
  aderente: reusa `SuperficieFlutuante` com `limiteRef`/`ladoPreferido`, sem
  `style=` inline novo.

## Pontos corretos

- A extração de `superficieInserirPosterior` elimina a duplicação de JSX que
  já existia entre o ramo de partida (headway) e o de célula passante,
  seguindo o padrão que a própria `superficieHeadway` já demonstrava.
- `data-testid`/`aria-label` do controle são exatamente os mesmos do modo
  completo (`acao-inserir-posterior`, `inserir-viagem-posterior`,
  `erro-insercao-relativa`) — sem duplicar seletores, conforme o critério de
  aceite 2.
- A exclusão mútua com o headway (`!modoHeadway` / `modoHeadway`) é a mesma
  condição já usada pela DEC-100 no modo completo — nenhuma lógica nova de
  alternância foi criada, só espelhada para o ramo de partida.
- `corpoGrade` é compartilhado pelas três grades (comum, feriados,
  excepcional), então a cobertura das três grades exigida no critério de
  aceite 8 vem "de graça" da reutilização da função, sem código condicional
  por grade.
- Testes cobrem caminho feliz, alternância de modo, exclusão com headway e o
  caso inválido de deslocamento vazio.

## Problemas encontrados

Nenhum.

## Violações de escopo

Nenhuma. O diff se restringe a `etapa-viagens.tsx` (extração de função +
condição de renderização) e aos três arquivos de teste listados nos "Arquivos
prováveis" da task. Não há alteração em `aoInserirViagemPorOffset`, no
formulário de headway, na composição/ordem/largura das superfícies (matéria
das TASK-124/125), no contrato JSON, no PDF ou no Comparador — todos citados
no "Fora de escopo" da task e intocados.

## Testes avaliados

- cobertura das RN da task: sim
- casos inválidos testados: sim (deslocamento vazio mantém recusa e mensagem, sem criar Viagem)
- regressão de UUID (se aplicável): sim (UUID nova, distinta da Viagem original e da criada no modo completo)
- OSRM mockado: sim (`chamouOsrm` fica `false` nos dois testes E2E novos)
- suíte executada com resultado: log canônico reutilizado (não foi necessário reexecutar) — `npm run test:all:verificar` reportou "Log canônico válido. Executor: Claude. Fingerprint: 7f5c87e9…5546. Working tree: 498d7fa6…9176."

## Checklist 07

25 itens aplicáveis avaliados, todos ok; demais N/A (Comparador, Roteamento,
maioria de Domínio/JSON — task é puramente de composição de UI/tela, sem
tocar contrato, roteamento ou entidades de domínio). Nenhum item aplicável
violado.

## Pendências

Nenhuma.

## Decisão

Aprovado sem ressalvas: escopo exatamente o declarado no objetivo da task, as
cinco RN listadas estão cobertas por teste direcionado, o "Fora de escopo" foi
respeitado integralmente (confirmado isolando o diff do motor de inserção), os
oito critérios de aceite têm evidência de teste correspondente, e a evidência
canônica da suíte foi validada por `test:all:verificar` sem necessidade de
reexecução.
