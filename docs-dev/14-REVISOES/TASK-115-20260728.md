# Revisão da TASK-115

**Revisor:** Codex
**Data:** 2026-07-28
**Commit/branch revisado:** `61bf31d` / `redesign`

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

A entrega identifica a célula focada pela grade, UUID da Viagem, Seção e dia,
restaurando o foco depois que criação ou redistribuição atualiza a sessão e
reordena/remonta a grade. As chaves que remontavam campos existentes foram
removidas e o rascunho sincroniza mudanças externas sem substituir o `input`.

Arquivos revisados:

- `src/formulario/viagens/campo-horario-grade.tsx`
- `src/formulario/viagens/etapa-viagens.tsx`
- `src/formulario/viagens/index.ts`
- `src/formulario/viagens/montagem-grade.ts`
- `testes/unitarios/formulario/campo-horario-grade.test.tsx`
- `testes/unitarios/formulario/etapa-viagens-foco.test.tsx`
- `testes/unitarios/formulario/viagens-montagem-grade.test.ts`
- `testes/e2e/etapa-viagens.spec.ts`

## Regras RN verificadas

- **RN-062 — atendida.** O alvo usa a UUID da Viagem, não horário nem posição
  ordinal; reforços no mesmo dia/horário geram seletores distintos
  (`src/formulario/viagens/montagem-grade.ts:28-47`;
  `testes/unitarios/formulario/viagens-montagem-grade.test.ts:223-235`).
- **RN-063 — atendida.** A mudança não altera `horarios_paradas`; criação e
  redistribuição continuam encontrando as Seções e preservando a ordem
  (`testes/unitarios/formulario/etapa-viagens-foco.test.tsx:97-124`).
- **RN-064 — atendida.** A criação continua no motor existente e o E2E confirma
  horários derivados e foco na partida reordenada
  (`src/formulario/viagens/etapa-viagens.tsx:207-224`;
  `testes/e2e/etapa-viagens.spec.ts:215-231`).
- **RN-065 — atendida.** O alvo só é armado após resultado válido; fora de
  ordem continua recusado, em erro e focado
  (`src/formulario/viagens/etapa-viagens.tsx:293-334`;
  `testes/e2e/etapa-viagens.spec.ts:245-255`).
- **RN-067 — atendida.** O campo mostra `07:00`, conserva o foco e põe o cursor
  no fim (`testes/e2e/etapa-viagens.spec.ts:215-231`).
- **RN-068 — atendida no alcance renderizado.** Feriados usam o mesmo corpo e
  o E2E confirma foco após criação
  (`src/formulario/viagens/etapa-viagens.tsx:452-456,844-849`;
  `testes/e2e/etapa-viagens.spec.ts:264-267`).
- **RN-095/RN-096 — atendidas.** Não há workflow, estado de pedido,
  persistência ou servidor; o foco é estado efêmero client-side.

## Specs verificadas

- **Spec 04 §2.4–§2.5 — aderente.** O controle recebe horário de relógio e não
  expõe `offset_horario`.
- **Spec 04 §8.1 — aderente.** A UUID faz o foco acompanhar a reordenação por
  `horario_saida` sem depender do bloco ordinal
  (`src/formulario/viagens/montagem-grade.ts:28-47`).
- **Spec 04 §8.2 — aderente.** Criação, sugestão, redistribuição e bloqueio fora
  de ordem conservam seus motores; só o destino de foco foi acrescentado.
- **Spec 04 §8.4 — aderente.** Feriados reutilizam o mecanismo, com regressões
  unitária e E2E.
- **Spec 04 §8.5 — aderência estrutural; execução futura.** O alvo aceita
  identificador genérico de grade e `CampoHorarioGrade` independe do tipo da
  grade. A superfície excepcional ainda não existe; sua implementação e E2E
  pertencem à TASK-105 (`docs-dev/06-BACKLOG_INICIAL.md:5202-5266`).
- **Spec 03 §8.1/§8.2 — aderente.** Baseline, âncoras, interpolação, tail e
  monotonicidade não foram alterados.
- **Design system §3/§6 — aderente.** Os campos continuam usando `Campo`, não há
  `style=` inline ou valor visual avulso e os atributos existentes permanecem.

## Pontos corretos

- `useLayoutEffect` restaura antes da pintura e só após confirmação válida
  (`src/formulario/viagens/etapa-viagens.tsx:119-134`).
- O seletor combina grade, UUID, Seção e dia, evitando ambiguidade
  (`src/formulario/viagens/montagem-grade.ts:28-47`).
- O cursor vai ao fim do valor confirmado
  (`src/formulario/viagens/etapa-viagens.tsx:127-133`).
- A criação registra a UUID nova antes de atualizar a sessão
  (`src/formulario/viagens/etapa-viagens.tsx:207-224`).
- Partida e passante não usam mais `key` baseada no horário; a sincronização
  mantém o mesmo `input`
  (`src/formulario/viagens/campo-horario-grade.tsx:37-44`;
  `testes/unitarios/formulario/campo-horario-grade.test.tsx:139-156`).
- Enter segue para a Seção seguinte da mesma UUID e Tab mantém o mapa existente
  (`testes/e2e/etapa-viagens.spec.ts:233-243`).
- Horário impossível não cria Viagem; fora de ordem conserva erro, valor e foco
  (`testes/e2e/etapa-viagens.spec.ts:245-262`).
- O E2E intercepta o OSRM e confirma zero chamadas
  (`testes/e2e/etapa-viagens.spec.ts:209-212,268`).

## Problemas encontrados

- Nenhum problema bloqueante ou ressalva funcional.

## Violações de escopo

- Nenhuma. Não foram alteradas regras de criação/redistribuição, Enter/Tab,
  ações flutuantes, contrato JSON, contagens, PDF ou Comparador.

## Consequências sobre outras tasks

- **TASK-105:** a grade excepcional ainda será construída e tem E2E previsto
  (`docs-dev/06-BACKLOG_INICIAL.md:5236-5254`). Esse E2E deverá incluir a
  preservação de foco; o comportamento runtime nessa grade está **não
  verificado nesta revisão**, pois ela ainda não é renderizada.
- **TASK-106:** o arquivo real confirma que a navegação continua em
  `destinoNavegacaoGrade`, sem mudança do algoritmo
  (`src/formulario/viagens/etapa-viagens.tsx:433-449,516-521`), e o novo E2E
  exerce Enter e Tab após restaurar o foco.
- Não mudou a fronteira Formulário/Comparador/`shared` nem o contrato JSON.

## Testes avaliados

- **cobertura das RN da task:** sim — RN-062/063/064/065/067/068 cobertas pelos
  novos testes e regressões existentes.
- **casos inválidos testados:** sim — horário impossível e passante fora de
  ordem não confirmam e mantêm o campo focado.
- **regressão de UUID:** N/A para importação/exportação/cópia; UUIDs distintas
  são testadas no caso de reforço.
- **OSRM mockado:** sim — endpoint interceptado e ausência de chamadas afirmada.
- **fixture canônica:** sim — `carregar-multi-servico.json`.
- **verificações leves:** `git diff 61bf31d^ 61bf31d --check`,
  `npm run typecheck`, `npm run lint` e `npm run build`, todos verdes. O
  primeiro build compilou, mas o sandbox bloqueou subprocesso com `spawn
  EPERM`; repetido fora desse bloqueio, terminou verde.
- **suíte executada com resultado:** verde. `npm run test:all:verificar`
  confirmou executor `Codex`, 96 arquivos/1.289 unitários, 82 E2E, códigos 0,
  fingerprint
  `eb8e55d667ce66d74e1a0c1b1b095d72f9e5385af2cac093dcfcf86642ab9435`
  e identidade
  `498d7fa6397724af2122bdced82f79ddd9749373d9277b227d3e2a3884f91763`.
- **reutilização da evidência:** log válido reutilizado; suíte não repetida.

## Checklist 07

Resultado: **24 itens OK, 29 N/A, 0 violados**.

### Escopo

| Item | Resultado | Evidência |
|---|---|---|
| Alteração pertence ao ROTA, não ao SEI (RN-095) | OK | Correção da grade de Viagens. |
| Não introduz workflow (RN-095) | OK | Nenhum estado administrativo. |
| Não cria status de pedido (RN-011) | OK | Contrato intocado. |
| Não cria persistência transacional (RN-096) | OK | Refs client-side. |
| Implementa apenas o escopo da task | OK | Diff restrito ao foco e testes. |
| Nenhuma regra nova inventada | OK | TASK-115 e Spec 04 §8.1–§8.2. |

### JSON (contrato)

| Item | Resultado | Evidência |
|---|---|---|
| JSON só com dados de operação (RN-010) | OK | Nenhum campo novo. |
| JSON autossuficiente (RN-009) | OK | Nenhum recurso externo novo. |
| Nenhum campo sem Spec 02 | OK | Schema intocado. |
| UUIDs existentes preservadas (RN-004) | OK | Edição/foco usam a UUID existente. |
| Entidades novas recebem UUID nova (RN-002/007) | OK | Criação continua em `criarViagemNaCelula`. |
| XOR de Parada (RN-033) | N/A | Paradas intocadas. |
| Sem R$ (RN-013) | OK | Nenhum valor monetário. |
| Unidades/arredondamento (RN-014/050) | N/A | Fora do escopo. |
| Congelados não recalculados por leitores (RN-015) | N/A | Leitores intocados. |

### Domínio

| Item | Resultado | Evidência |
|---|---|---|
| Seção e Local separados (RN-025/031) | N/A | Entidades intocadas. |
| Ponto de rota não vira Parada (RN-042) | N/A | Fora do diff. |
| Serviço mantém UUID (RN-001) | N/A | Serviço intocado. |
| `numero_n` não é identidade (RN-006) | N/A | Nenhuma lógica por número. |
| Extremos continuam Seções (RN-035) | N/A | Estrutura intocada. |
| Ida/Volta compartilham Seções (RN-030) | N/A | Estrutura intocada. |
| Tipificação respeitada (RN-019..022) | N/A | Fora do escopo. |
| Viagem estratificada (RN-061) | OK | Grade/dia/UUID compõem o alvo. |
| Feriado fora das contagens (RN-069) | N/A | Contagens intocadas. |

### Comparador

| Item | Resultado | Evidência |
|---|---|---|
| UUID como base (RN-081) | N/A | Comparador fora do diff. |
| Taxonomia de diff (RN-081..083) | N/A | Comparador fora do diff. |
| Comparativo não gravado (RN-080/097) | N/A | Comparador fora do diff. |
| Status/datas fora do diff (RN-012) | N/A | Comparador fora do diff. |
| Autos diferentes bloqueados (RN-084) | N/A | Comparador fora do diff. |
| Rota por sinais estáveis (RN-087) | N/A | Comparador fora do diff. |
| Sem OSRM/persistência no Comparador (RN-080) | N/A | Comparador fora do diff. |

### Roteamento

| Item | Resultado | Evidência |
|---|---|---|
| OSRM indisponível bloqueia rota inválida (RN-048) | N/A | Roteamento intocado. |
| Distância vem do OSRM (RN-047/050) | N/A | Distâncias intocadas. |
| Pontos de rota só condicionam traçado (RN-041..043) | N/A | Fora do diff. |
| Abrir JSON não chama OSRM (RN-052) | N/A | Importação intocada. |
| Mensagens §14 sem tarifa (RN-049) | N/A | Mensagens intocadas. |

### UI/PDF

| Item | Resultado | Evidência |
|---|---|---|
| Nome `Cidade - Nome da Seção` (RN-076) | N/A | Rótulos intocados. |
| Horário de relógio; offsets ocultos (RN-067/076) | OK | Campo confirma HH:MM. |
| Matrizes triangulares sem R$ (RN-076) | N/A | Matrizes fora do diff. |
| Locais fora da grade principal (RN-031/044/076) | N/A | Linhas intocadas. |
| Aviso SEI no PDF (RN-077) | N/A | PDF fora do diff. |
| Gate de exportação (RN-078) | N/A | Exportação fora do diff. |
| Design system (DEC-050) | OK | `Campo` compartilhado; sem `style=`. |
| `data-testid`/`aria-*` preservados (DEC-050) | OK | Atributos existentes intactos. |

### Testes

| Item | Resultado | Evidência |
|---|---|---|
| Toda RN/regra implementada possui teste | OK | Foco, reordenação, redistribuição, navegação e feriado. |
| Casos inválidos testados | OK | Horário impossível e fora de ordem. |
| Regressão de UUID quando aplicável | N/A | Não toca import/export/cópia. |
| OSRM mockado | OK | Endpoint interceptado, zero chamadas. |
| Fixtures canônicas reutilizadas | OK | `carregar-multi-servico.json`. |
| Direcionados/typecheck/lint/build reportados | OK | Todos verdes. |
| Evidência final canônica única e válida | OK | Marcador, códigos, fingerprint e identidade confirmados. |
| Revisão reutilizou log ou executou uma vez | OK | Log reutilizado. |
| Porta/PIDs isolados e encerrados | OK | Log registra PID próprio encerrado. |

## Pendências

- Nenhuma condição funcional ou de merge para a TASK-115.
- Na TASK-105, incluir no E2E da grade excepcional a preservação de foco após
  criação/reordenação e redistribuição; até a superfície existir, seu
  comportamento runtime permanece não verificável.

## Decisão

**Aprovado.** A perda de foco foi corrigida pela identidade lógica da Viagem,
com cobertura de reordenação, cursor, Enter/Tab, redistribuição, recusas e
feriados, sem alterar regra de negócio, contrato ou seletores existentes. A
comprovação runtime na grade excepcional fica vinculada ao E2E já exigido pela
TASK-105, ainda não implementada, e não é condição de merge desta task.
