# Revisão da TASK-081

**Revisor:** Claude Sonnet 5 (revisão assistida)
**Data:** 2026-07-17
**Commit/branch revisado:** `06a7ed4` (branch `redesign`) — "Implementa TASK-081: alerta agregado "tabela de feriados vazia" na Revisão"

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

`coletarPendencias` (`src/formulario/pendencias/pendencias.ts:155-179`) passa a emitir no máximo **um** alerta `id: "tabela-feriados-vazia"` quando algum Serviço não tem nenhuma Viagem de feriado em sentido algum, listando os `numero_n` afetados por vírgula na ordem do documento. `severidade: "alerta"`, `etapaAlvo: "viagens-horarios"`. Testes novos em `testes/unitarios/formulario/pendencias.test.ts:352-436` (7 casos). Nenhum outro arquivo tocado.

## Regras RN verificadas

- **RN-071** (grade de feriados vazia é válida, alerta) — atendida: nenhuma Viagem de feriado só gera alerta (nunca bloqueante), confirmado no código (`severidade: "alerta"`, `pendencias.ts:174`) e no teste "alerta de feriado nunca é bloqueante" (`pendencias.test.ts:420-430`).
- **RN-078** (exportação bloqueada só por bloqueantes) — atendida: `avaliarGateExportacao` (`src/formulario/exportacao/gate-exportacao.ts:60-62`) filtra `pendenciasBloqueantes` por `severidade === "bloqueante"`, inalterado por esta task; como o novo item é `"alerta"`, fica automaticamente fora do gate sem precisar tocar `gate-exportacao.ts`. Verificado por leitura do arquivo, não só por inferência.
- **RN-006** (`numero_n` não é chave) — atendida: `numero_n` só é usado como texto de exibição na mensagem (`.map((servico) => servico.numero_n)` para compor a lista), nunca como chave de busca/comparação/agrupamento.
- **RN-096/NEG-004** (sem persistência, pendência efêmera) — atendida: função pura, recomputada a cada chamada sobre `servicosDaSessao(sessao)`, nenhum campo novo de estado nem de contrato.

## Specs verificadas

- **Spec 04 §11** (`docs/specs/04-formulario-ux-pdf.md:323-325`) — lista "tabela de feriados vazia (nenhuma Viagem de feriado — pode ser intencional)" entre os alertas não bloqueantes. Aderente: implementado como alerta, texto singular da spec preservado na decisão de agregação (documentada na TASK-081 do backlog, decisão do responsável em 2026-07-17).
- **Spec 02 §11 v0.6** (`src/shared/contrato/esquema.ts:229-235`) — `viagem_feriado: z.boolean()` por Viagem, dentro de `itinerarios[].viagens[]`. A checagem `servico.itinerarios.every(it => it.viagens.every(v => !v.viagem_feriado))` usa exatamente esse campo, sem inventar estrutura nova.

## Pontos corretos

- Alerta único e agregado, exatamente como decidido pelo responsável (não um item por Serviço/sentido) — evita o ruído identificado no contexto da task.
- Assimetria por sentido tratada como a própria task propôs como candidato (linha entra na lista só se **nenhum** sentido tem feriado) — inferência controlada corretamente marcada como tal na Análise da Task, sem virar Q-xxx desnecessária.
- Painel de pendências (`src/formulario/layout/painel-pendencias.tsx:107-110`) renderiza `Pendencia[]` genericamente via `.map`, sem lista hard-coded — confirmado por leitura do arquivo: o novo alerta aparece na UI sem qualquer alteração de wiring, `data-testid` ou `aria-*`.
- Testes cobrem os três cenários dos "Casos válidos" da task (nenhum com feriado, parcial, todos com feriado) mais dois "inválidos" adicionais (assimetria por sentido, modo "novo" sem Serviços) e uma regressão-guarda contra a fixture `sessaoCarregado` já usada por outros testes do arquivo.
- Escopo respeitado: nenhuma mudança em `gate-exportacao.ts`, RN-071 em si, contadores (TASK-031) ou contrato JSON — confirmado pelo diff (`git show --stat`): só `pendencias.ts` e seu teste foram tocados.
- Verificações reais executadas e reportadas: `typecheck`, `lint` e suíte completa (`956/956` testes, `81` arquivos) rodados nesta revisão, resultado igual ao relatado na entrega.

## Problemas encontrados

Nenhum bloqueante. Um ponto de baixa severidade, sem impacto em RN/NEG:

- **Nomenclatura de teste levemente otimista** (`pendencias.test.ts:420`): o teste `"[inválido] alerta de feriado nunca é bloqueante — não entra em pendenciasBloqueantes do gate"` filtra `coletarPendencias(...)` diretamente por `severidade === "bloqueante"`, mas **não chama** `avaliarGateExportacao` — a função citada no nome do teste e no critério de aceite da task. A equivalência é real (confirmada por leitura de `gate-exportacao.ts:60-62`, que usa o mesmo filtro), mas o teste não exercita `avaliarGateExportacao` diretamente. Não é lacuna de cobertura da RN-078 (já coberta por `gate-exportacao.test.ts` para o filtro em si), só uma imprecisão de nome/profundidade neste teste específico. Não impede aprovação — registrado como follow-up opcional, não condição de merge.

## Violações de escopo

Nenhuma. Diff limitado a `src/formulario/pendencias/pendencias.ts` e `testes/unitarios/formulario/pendencias.test.ts`, exatamente os "Arquivos prováveis" da task. Nenhum código dentro do "Fora de escopo" (RN-071 em si, contadores TASK-031, contrato JSON) foi tocado.

## Testes avaliados

- cobertura das RN da task: sim — RN-071 (alerta agregado, presença/ausência) e RN-078 (não bloqueante) cobertas.
- casos inválidos testados: sim — assimetria por sentido, todos com feriado (nenhum alerta), modo "novo" sem Serviços, regressão contra fixture com feriado preenchido.
- regressão de UUID (se aplicável): N/A — task não toca import/export/cópia.
- OSRM mockado: N/A — task não toca roteamento.
- suíte executada com resultado: verde — `npm run typecheck` (sem erros), `npm run lint` (sem erros), `npm test` → **Test Files 81 passed (81) / Tests 956 passed (956)**, reexecutado nesta revisão com o mesmo resultado.

## Checklist 07

Percorrido item a item (`docs-dev/07-CHECKLIST_ADERENCIA_SPEC.md`):

**Escopo** — 6/6 ok (task é ROTA puro, sem workflow, sem status novo, sem persistência, escopo exato, nenhuma regra inventada — a única inferência controlada foi marcada na Análise da Task).

**JSON (contrato)** — todos **N/A**: task não toca schema, campos, UUIDs ou dados congelados (leitura pura de `viagem_feriado`, campo já existente).

**Domínio** — aplicáveis: "Viagem continua estratificada" (ok, campo lido sem alteração), "`numero_n` não usado como identidade" (ok, só exibição); demais itens do grupo (Seção/Local, ponto de rota, tipificação) **N/A** — task não toca essas entidades.

**Comparador** — todos **N/A**: task é só Formulário.

**Roteamento** — todos **N/A**: task não toca OSRM/roteamento.

**UI/PDF** — aplicáveis: "`data-testid`/`aria-*` preservados" (ok, confirmado por leitura de `painel-pendencias.tsx`, nenhum seletor tocado), "exportação bloqueada com pendências bloqueantes" (ok, RN-078 intocada — o novo item é alerta, não bloqueante); demais itens (nomes de Seção, offsets, matrizes, PDF, design system) **N/A** — task não toca UI/PDF além do dado que já flui pelo painel genérico.

**Testes** — 6/6 ok: RN implementada tem teste, casos inválidos testados, regressão de UUID N/A, OSRM mockado N/A, fixtures canônicas reutilizadas (`documentoBidirecionalMultiServico`, `documentoExemploMinimo` — nenhum JSON ad hoc), verificações executadas e reportadas honestamente.

**Total:** 0 itens violados; itens aplicáveis todos ok; demais marcados N/A por não se aplicarem ao escopo da task.

## Pendências

- Nenhuma bloqueante para o merge.
- Follow-up opcional, não condição de merge: se desejado, trocar o teste de `pendencias.test.ts:420-430` por uma chamada direta a `avaliarGateExportacao` (ou duplicar o caso em `gate-exportacao.test.ts`) para que o nome do teste corresponda exatamente ao que é exercitado.

## Decisão

**Aprovado.** Escopo exato (só os dois arquivos previstos na Análise da Task), RN-071/078 atendidas com testes cobrindo casos válidos e inválidos, checklist 07 sem violações, verificações (typecheck/lint/teste) verdes e reproduzidas nesta revisão com o mesmo resultado relatado na entrega. A única observação (nome de teste um pouco mais amplo do que o que ele exercita) é estritamente cosmética — a equivalência lógica foi verificada por leitura direta de `gate-exportacao.ts` — e não configura desvio de RN, NEG ou escopo; não é condição de merge.
