# Revisão da TASK-069

**Revisor:** Codex
**Data:** 2026-07-21
**Commit/branch revisado:** `f995680` / `redesign` (definição da TASK-069 e DEC-072 presentes no working tree)

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

A entrega adiciona ao `Mapa` um callback opt-in de hover sobre a linha, projeta hover e clique na mesma coordenada do traçado e compõe no editor um marcador fantasma efêmero. A aparência usa o token ciano/tamanho do ponto de rota com opacidade própria, sem mudança no contrato JSON, no ancorador lógico ou no motor OSRM.

## Regras RN verificadas

- RN-042 — **atendida**: o fantasma é apenas um `MarcadorMapa`, sem `uuid`, `apos_parada_ordem` ou entrada no modelo; somente o clique confirmado chega ao fluxo que cria `PontoDeRota` (`src/formulario/itinerarios/editor-mapa-itinerario.tsx:198`, `src/formulario/itinerarios/etapa-itinerarios.tsx:612`).
- RN-052 — **atendida**: `mousemove` apenas consulta a camada, projeta a coordenada e notifica a UI; não chama o roteador. O clique continua sendo a edição que chega a `aplicarNovasParadas` e recalcula (`src/shared/mapa/mapa.tsx:314`, `src/formulario/itinerarios/etapa-itinerarios.tsx:630`).
- RN-096 — **atendida**: a pré-visualização fica em `useState` local e desaparece por callback `null`; não há backend, storage ou escrita no JSON (`src/formulario/itinerarios/editor-mapa-itinerario.tsx:136`, `src/shared/mapa/mapa.tsx:324`).
- RN-097 — **atendida**: a extensão vive em `shared/mapa`, é opcional e só o editor do Formulário a conecta; nenhum acoplamento entre Formulário e Comparador foi introduzido (`src/shared/mapa/mapa.tsx:66`, `src/formulario/itinerarios/editor-mapa-itinerario.tsx:379`).

## Specs verificadas

- Spec 03 §3.6 — **aderente**: a projeção retorna uma coordenada pertencente ao traçado, e a coordenada confirmada continua sendo persistida apenas como ponto de rota que condiciona a rota (`src/shared/mapa/ancoragem.ts:37`, `src/formulario/itinerarios/etapa-itinerarios.tsx:630`).
- Spec 04 §7.3 item 6 — **aderente**: o clique esquerdo sobre a linha continua criando o vértice, agora na mesma projeção antecipada pelo fantasma (`src/shared/mapa/mapa.tsx:299`).
- Spec 04 §7.3, regra visual — **aderente**: o fantasma é círculo ciano pequeno, sem rótulo, translúcido e sem capturar eventos (`src/formulario/itinerarios/editor-mapa-itinerario.tsx:198`, `src/app/globals.css:105`).

## Pontos corretos

- `projetarNaLinha` expõe de forma aditiva a coordenada mais próxima sem alterar as distâncias já consumidas pelo ancorador (`src/shared/mapa/ancoragem.ts:18`, `testes/unitarios/mapa/ancoragem.test.ts:36`).
- Hover e clique reutilizam `projetarSobreLinhas`, garantindo a coincidência exigida pela DEC-072 (`src/shared/mapa/mapa.tsx:278`, `testes/unitarios/mapa/mapa.test.tsx:161`).
- Fora da linha e no `mouseleave`, cursor e pré-visualização são limpos; sem callback novo, o comportamento anterior do consumidor é preservado (`src/shared/mapa/mapa.tsx:314`, `testes/unitarios/mapa/mapa.test.tsx:188`).
- Sem `linhaRota`, o editor não habilita o hover nem rende o fantasma (`src/formulario/itinerarios/editor-mapa-itinerario.tsx:198`, `testes/unitarios/formulario/editor-mapa-itinerario.test.tsx:389`).
- O E2E prova aparição, distinção visual, limpeza fora/ao sair e contador OSRM igual a zero, com OSRM e tiles mockados (`testes/e2e/etapa-itinerarios.spec.ts:388`).
- A alteração da reconciliação de classes preserva as classes estruturais acrescentadas pelo MapLibre, evitando regressão dos marcadores existentes (`src/shared/mapa/mapa.tsx:141`, `testes/unitarios/mapa/mapa.test.tsx:294`).

## Problemas encontrados

- Nenhum problema impeditivo ou desvio menor encontrado.

## Violações de escopo

- Nenhuma. A entrega não implementa remoção por clique (TASK-070), affordance de clique direito (TASK-065), mudança de cor/tamanho do vértice real (TASK-068), contrato JSON, ancorador lógico ou motor OSRM. A pequena refatoração do hit-test de `contextmenu` apenas reutiliza a consulta já existente e tem regressão unitária verde (`src/shared/mapa/mapa.tsx:331`, `testes/unitarios/mapa/mapa.test.tsx:128`).

## Testes avaliados

- cobertura das RN da task: **sim** — projeção, igualdade hover/clique, estado efêmero, ausência sem linha, callback opt-in, limpeza e zero OSRM.
- casos inválidos testados: **sim** — fora da linha, `mouseleave`, linha ausente e consumidor sem callback.
- regressão de UUID: **N/A** — importação, exportação, cópia e identidade não foram tocadas.
- OSRM mockado: **sim** — o E2E intercepta o endpoint público e exige zero chamadas durante o hover (`testes/e2e/etapa-itinerarios.spec.ts:392`).
- verificações complementares desta revisão: `npm run typecheck`, `npm run lint`, `npm run build` e `git diff --check HEAD^ HEAD` — **código 0**.
- suíte canônica: o log válido existente foi reutilizado, conforme o protocolo. Executor `Codex`; fingerprint `95bba59e3179149a453a7b8df3c87559b3f6de76c074da0b5a163bd7d53d844b`; working tree `00c21aa501d19b4a9ade6b12db96274b9a6725e300e14f1fda33b4dc1336f57e`. Resultado: **90 arquivos / 1.090 testes unitários aprovados; 59 E2E aprovados; códigos 0; resultado geral APROVADO** (`ultimo-test-all.log:1147`, `ultimo-test-all.log:1278`, `ultimo-test-all.log:1299`).

## Checklist 07

Resultado: **23 itens atendidos, 30 N/A, 0 violados**.

### Escopo

- [x] A alteração pertence ao ROTA, não ao SEI (RN-095).
- [x] Não introduz workflow (RN-095).
- [x] Não cria status de pedido (RN-011).
- [x] Não cria persistência transacional (RN-096).
- [x] Implementa apenas o escopo da TASK-069.
- [x] Não inventa regra; comportamento fundamentado nas Specs 03/04, RN e DEC-057/072.

### JSON (contrato)

- [x] JSON continua contendo apenas dados de operação (RN-010).
- [x] JSON continua autossuficiente (RN-009).
- [x] Nenhum campo novo foi adicionado.
- [N/A] Preservação de UUID na importação/edição (RN-004) — importador e identidade não tocados.
- [N/A] UUID nova para entidade nova/cópia (RN-002/007) — factory e cópias não tocadas.
- [N/A] XOR de Parada (RN-033) — schema/modelo de Parada não tocados.
- [x] Nenhum valor monetário entrou no JSON (RN-013).
- [N/A] Unidades/arredondamento (RN-014/050) — cálculo de rota não tocado.
- [N/A] Dados congelados em leitores (RN-015) — leitores não tocados.

### Domínio

- [N/A] Separação entre Seção e Local (RN-025/031) — entidades não tocadas.
- [x] Ponto de rota não virou Parada/Seção/Local (RN-042).
- [N/A] Identidade UUID de Serviço (RN-001) — Serviço não tocado.
- [N/A] `numero_n` como identidade (RN-006) — lógica de identidade não tocada.
- [N/A] Extremos como Seções (RN-035) — lista de Paradas não tocada.
- [N/A] Conjunto de Seções Ida/Volta (RN-030) — sentidos não tocados.
- [N/A] Tipificação (RN-019..022) — não tocada.
- [N/A] Estratificação de Viagem (RN-061) — não tocada.
- [N/A] Feriado/contagens (RN-069) — não tocados.

### Comparador

- [N/A] Comparação por UUID (RN-081).
- [N/A] Taxonomia adicionada/removida/alterada/inalterada (RN-081..083).
- [N/A] Comparativo fora do JSON e sem mutação (RN-080/097).
- [N/A] `status`/datas fora do diff (RN-012).
- [N/A] Bloqueio de Autos diferentes (RN-084).
- [N/A] Comparação estável de rota (RN-087).
- [N/A] Comparador sem OSRM/persistência (RN-080).

### Roteamento

- [N/A] Falha OSRM sem fallback inválido (RN-048) — tratamento de falha não tocado.
- [N/A] Distância exclusivamente do OSRM (RN-047/050) — cálculo não tocado.
- [x] Pontos de rota apenas condicionam o traçado; invariante de trechos preservado (RN-041..043).
- [x] Abrir JSON/hover não chama OSRM; clicar continua recalculando (RN-052).
- [N/A] Mensagens de erro (RN-049) — nenhuma mensagem alterada.

### UI/PDF

- [N/A] Padrão de nomes de Seção (RN-076) — rótulos não tocados.
- [N/A] Horários/offsets (RN-067/076).
- [N/A] Matrizes triangulares/sem R$ (RN-076).
- [N/A] Locais fora da grade/descrição/PDF (RN-031/044/076).
- [N/A] Aviso de fronteira com o SEI no PDF (RN-077).
- [N/A] Gate de exportação (RN-078) — não tocado.
- [x] UI aderente ao design system, com token e classe semântica (DEC-050).
- [x] `data-testid`/`aria-*` existentes preservados (DEC-050).

### Testes

- [x] RN implementadas possuem testes conforme a matriz 03.
- [x] Casos inválidos foram testados.
- [N/A] Regressão específica de UUID — task não toca import/export/cópia.
- [x] Testes de roteamento usam OSRM mockado.
- [x] Fixtures canônicas são reutilizadas no E2E.
- [x] Verificações direcionadas, typecheck/lint e build foram executados e reportados.
- [x] Evidência final canônica tem marcador, códigos 0, fingerprint e identidade válidos.
- [x] A revisão reutilizou o log válido sem repetir a suíte.
- [x] Executor registrou limpeza do PID próprio e nenhum servidor alheio foi reutilizado/finalizado.

## Pendências

- Nenhuma condição de merge para a TASK-069.
- TASK-070 pode seguir na ordem recomendada; TASK-079/064 continuam com as dependências já registradas no status de execução.

## Decisão

**Aprovado.** A entrega satisfaz os critérios da TASK-069, preserva o escopo e o contrato, cobre as RN de criticidade Alta afetadas e deixa o checklist sem item aplicável violado. Todas as verificações estão verdes; o ciclo da TASK-069 está concluído e ela pode sair da lista de pendentes.
