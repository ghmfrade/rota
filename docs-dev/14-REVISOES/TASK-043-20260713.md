# Revisão da TASK-043

**Revisor:** Claude (Sonnet 5), apoiando revisão humana
**Data:** 2026-07-13
**Commit/branch revisado:** `6fabe27` sobre `master` — "Implementa TASK-043: Consolidar arredondamento half-up num único primitivo em shared/"

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Consolidação das duas implementações duplicadas de arredondamento half-up (`arredondar2` em `validacoes-estruturais.ts` e `arredondaHalfUp` em `extrair-rota.ts`) num único primitivo `arredondaHalfUp` em `src/shared/calculo/arredondamento.ts` (novo módulo, com barrel `index.ts`). Os dois pontos de consumo originais passaram a importar do shared; `extrair-rota.ts` reexporta o símbolo para preservar o caminho de import `@/formulario/roteamento`; `duracao_s` passou a usar o helper (`arredondaHalfUp(leg.duration, 0)`) em vez de `Math.round` cru. Suíte de testes nova em `testes/unitarios/calculo/arredondamento.test.ts`, com o `describe` duplicado removido de `roteamento-extrair-rota.test.ts`. Origem: ressalva não bloqueante da revisão da TASK-021.

## Regras RN verificadas

- **RN-050** — **atendida**. [arredondamento.ts:7-9](../../src/shared/calculo/arredondamento.ts#L7-L9) preserva bit-a-bit a fórmula original (`Math.round((valor + Number.EPSILON) * fator) / fator`); os dois pontos de consumo ([extrair-rota.ts:82-83](../../src/formulario/roteamento/extrair-rota.ts#L82-L83), [validacoes-estruturais.ts:297-298](../../src/shared/contrato/validacoes-estruturais.ts#L297-L298)) chamam o mesmo primitivo. `duracao_s` passou a usar `arredondaHalfUp(leg.duration, 0)` ([extrair-rota.ts:83](../../src/formulario/roteamento/extrair-rota.ts#L83)), eliminando a inconsistência estilística apontada na revisão da TASK-021. Confirmado por teste de caso de fechamento (1,005+1,005→2,02) em [arredondamento.test.ts:27-33](../../testes/unitarios/calculo/arredondamento.test.ts#L27-L33).
- **RN-040** — **atendida**. A checagem estrutural de soma (`rota.distancia_km == Σ trechos`) em [validacoes-estruturais.ts:294-303](../../src/shared/contrato/validacoes-estruturais.ts#L294-L303) usa o mesmo primitivo consolidado com a mesma tolerância (`1e-9`), sem alteração de resultado — confirmado pela suíte `rota-trechos-descricao.test.ts` permanecendo verde sem mudança de expectativa.
- **RN-014** — **não infringida**. Nenhuma nova conversão de unidade foi introduzida; a fronteira m→km continua única em `extrairRota`. O primitivo consolidado é puramente numérico (não conhece unidade).

## Specs verificadas

- **Spec 03 §3.4** — aderente: a regra (`distancia_km` half-up 2 casas, `duracao_s` inteiro, totais por soma dos trechos já arredondados) permanece literalmente a mesma; a task não altera a spec nem sua leitura, só a localização do código que a implementa.
- **docs-dev/13-ARCHITECTURE_GUARDRAILS.md** — aderente: `shared/calculo/` é primitivo puro, sem dependência de `contrato/` nem de UI (confirmado por leitura de [arredondamento.ts](../../src/shared/calculo/arredondamento.ts) — só `Math.round`/`Number.EPSILON`, nenhum import). `formulario/roteamento` segue dependendo apenas de `shared/` (import de `@/shared/calculo` é a direção permitida). Confirmado por `grep` nesta revisão: `comparador/` não importa `formulario/` nem `shared/calculo` (ainda não usa o módulo — esperado, a task não tocou o Comparador).

## Pontos corretos

- **Fórmula preservada bit-a-bit**: a nova implementação em `shared/calculo/arredondamento.ts` é uma cópia literal do corpo de `arredondaHalfUp` original (mesmo uso de `Number.EPSILON`, mesmo `10 ** casas`), eliminando risco de drift de comportamento durante a consolidação.
- **Compatibilidade de import preservada**: `roteamento/index.ts` continua exportando `arredondaHalfUp` (agora reexportado de `@/shared/calculo`), então nenhum consumidor existente de `@/formulario/roteamento` quebra — verificado por `npm run typecheck` limpo e pela suíte de `extrairRota` intacta.
- **Localização justificada e não arbitrária**: o novo módulo `shared/calculo/` (em vez de `shared/geo/`) tem justificativa registrada na Análise da Task (cálculo numérico puro, não geoespacial) e é literalmente uma das duas opções que a própria task sugeria — inferência controlada corretamente marcada, não decisão silenciosa.
- **Teste do caso mais sensível migrado, não descartado**: o caso de fechamento 1,005+1,005→2,02 (o que expõe erro de arredondamento se a soma fosse feita antes de arredondar) foi preservado na nova suíte ([arredondamento.test.ts:27-33](../../testes/unitarios/calculo/arredondamento.test.ts#L27-L33)), e o teste equivalente em `extrairRota` ([roteamento-extrair-rota.test.ts:81-100](../../testes/unitarios/formulario/roteamento-extrair-rota.test.ts#L81-L100)) permanece intocado.
- **Sem duplicação residual**: `grep -rn "function arredonda"` nesta revisão confirma exatamente uma definição no repositório; `grep -rn "arredondar2"` confirma zero referências (exceto um comentário histórico no novo teste, citando o nome antigo para rastreabilidade).

## Problemas encontrados

Nenhum.

## Violações de escopo

Nenhuma. O diff toca exatamente os dois arquivos citados na ressalva de origem (`validacoes-estruturais.ts`, `extrair-rota.ts`), mais o novo módulo `shared/calculo/` e os testes correspondentes. Nenhuma mudança na regra dos 350 m, na regra de arredondamento em si, no schema do contrato ou em qualquer outro cálculo (confirmado por leitura integral do diff — `git show --stat` lista só os 7 arquivos esperados). Nenhuma outra duplicação foi tocada além da citada na origem.

## Testes avaliados

- **Cobertura das RN da task:** sim — RN-050 com os 4 casos originais (borda 0,005→0,01, limiar 4m/5m, caso genérico, duração 0 casas) mais o caso de fechamento e um caso de não-corrupção de valor exato por `Number.EPSILON`, ambos novos face à suíte original mas dentro do escopo de "mesmos casos já cobertos" (o de fechamento já existia na suíte de `extrairRota`; o de valor exato é uma reformulação defensiva do mesmo comportamento, não regra nova).
- **Casos inválidos testados:** N/A — o primitivo não tem caminho de erro (função numérica total, sem lançar exceção); a task não pede caso inválido para ele. Os casos inválidos de `extrairRota` (legs.length divergente, routes[0] ausente) permanecem intactos e verdes.
- **Regressão de UUID:** N/A — task não toca import/export/cópia de entidades.
- **OSRM mockado:** N/A para o teste do primitivo (função pura, sem I/O); os testes de `extrairRota` que exercitam o primitivo indiretamente já usam fixtures locais, sem chamada de rede.
- **Suíte executada com resultado:** **verde**, resultados obtidos nesta sessão de revisão: `npm run typecheck` sem erros; `npm run lint` sem avisos; `npm test` **480/480** (36 arquivos) — nenhuma expectativa de teste pré-existente foi alterada (confirmado por diff: as remoções em `roteamento-extrair-rota.test.ts` são exclusivamente do `describe` migrado, sem tocar os `expect` de `extrairRota`).

## Checklist 07

- **Escopo (6 itens):** 6 OK — refator puro pertence ao ROTA (qualidade interna); nenhum workflow/status/persistência introduzido; escopo exatamente o da ressalva de origem; nenhuma regra nova inventada (RN-050 citada, decisão de localização marcada como inferência controlada).
- **JSON — contrato (9 itens):** N/A em todos — task não altera nenhum campo do JSON, não toca UUID, não introduz valor monetário; os dois itens tecnicamente aplicáveis ("arredondamento half-up 2 casas onde aplicável" RN-014/050 e "dados congelados não recalculados" RN-015) permanecem **OK** por não haver mudança de comportamento observável nem novo ponto de recálculo.
- **Domínio (9 itens):** N/A — nenhuma entidade de domínio tocada.
- **Comparador (7 itens):** N/A — módulo não tocado; confirmado nesta revisão que `comparador/` não referencia `shared/calculo` nem `formulario/`.
- **Roteamento (5 itens):** 2 aplicáveis e **OK** — "distância roteada vem exclusivamente do OSRM, convertida na fronteira" (RN-047/050): a fronteira de conversão não mudou, só a implementação do arredondamento aplicado nela; "abrir JSON continua sem chamar OSRM" (RN-052): N/A, task não toca esse fluxo. Os demais 3 itens são N/A (falha de OSRM, pontos de rota, mensagens de erro — não tocados).
- **UI/PDF (6 itens):** N/A — task não toca UI nem PDF.
- **Testes (6 itens):** 5 OK, 1 N/A — toda RN implementada tem teste; casos inválidos N/A (função pura sem caminho de erro, justificado); regressão de UUID N/A (justificada); OSRM mockado N/A (função pura, sem I/O, justificado); fixtures canônicas N/A (não há JSON de operação envolvido, são só valores numéricos); verificações executadas e reportadas com resultado real.
- **Resultado:** **0 itens violados**, todos os aplicáveis OK, demais N/A justificados por escopo.

## Pendências

- **Critérios de aceite da task, verificados nesta revisão:**
  1. "existe exatamente um primitivo de arredondamento half-up no repositório" — **confirmado** por `grep -rn "function arredonda" src/` retornando uma única ocorrência.
  2. "`validacoes-estruturais.ts` e `extrair-rota.ts` (incluindo `duracao_s`) o consomem" — **confirmado** por leitura direta dos dois arquivos.
  3. "nenhum teste existente muda de resultado" — **confirmado**: `npm test` 480/480 verde, e o diff de `roteamento-extrair-rota.test.ts` mostra apenas remoção do `describe` migrado, nenhum `expect` alterado.
- **Q-xxx:** nenhuma aberta por esta task.
- **Depende desta task:** nenhuma task futura depende diretamente desta consolidação (é encerramento de um follow-up, não abre novo).

## Decisão

**Aprovado.** Refator puro, exatamente no escopo da ressalva de origem da TASK-021: um único primitivo `arredondaHalfUp` em `shared/calculo/`, consumido pelos dois pontos citados, com `duracao_s` uniformizado como pedido. Fórmula preservada bit-a-bit (nenhuma mudança de comportamento observável), os três critérios de aceite resumidos da task foram verificados diretamente nesta revisão (não por inferência), checklist sem itens violados, typecheck/lint/testes verdes (480/480, sem alteração de expectativa em nenhum teste pré-existente). Próxima task pode prosseguir (`docs-dev/00` §3).
