# Revisão da TASK-048

**Revisor:** Claude (Opus 4.8), apoiado pelo prompt `/revisar-aderencia`
**Data:** 2026-07-14
**Commit/branch revisado:** `0ff749c` (branch `master`) — "Implementa TASK-048: Display read-only da matriz de distâncias na etapa Matrizes"

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Acopla à etapa Matrizes (shell da TASK-027) a apresentação **somente-leitura** da matriz de distâncias do Serviço selecionado (Spec 04 §9.1). Um módulo de derivação pura (`apresentacao-matriz-distancias.ts`: `celulaDistancia` + `formatarKm`) mapeia o `matriz_distancias` congelado em células, e uma nova `<section data-testid="matriz-distancias">` em `etapa-matrizes.tsx` renderiza a triangular inferior (X na diagonal, `Cidade - Nome da Seção`, km) com detalhe Ida/Volta expansível quando bidirecional. Testes: 9 unitários novos + 2 E2E novos. Sem toque em contrato, cálculo, seccionamento ou rede.

## Regras RN verificadas

- **RN-054** (Alta) — *atendida*. Células habitadas derivam do `matriz_distancias` por par não-direcional (`chaveParNaoDirecional`, reusado do cálculo da TASK-026 — `apresentacao-matriz-distancias.ts:39`); ordem de linhas/colunas vem de `secoesAtendidas` (`etapa-matrizes.tsx:64`), mesma da grade de seccionamento. Checado em `matrizes-display-distancias.test.ts` (insensibilidade à ordem dos argumentos e de armazenamento).
- **RN-056** (Alta) — *atendida*. Exibe `valor_adotado_de_distancia`; `bidirecional` é derivado da presença simultânea de `distancia_trecho_ida` e `distancia_trecho_volta` na própria célula congelada (`apresentacao-matriz-distancias.ts:47-49`), não de contagem de itinerarios. Detalhe Ida/Volta só em bidirecional; unidirecional mostra só o valor único. Testes cobrem ambos os ramos.
- **RN-076** (Alta) — *atendida*. Triangular inferior com "X" na diagonal (`celula-distancia-diagonal`), cabeçalhos `Cidade - Nome da Seção` via `nomeExibicaoSecao` (reuso), valores em km via `formatarKm`. Sem offsets (não há horário nesta etapa).
- **RN-013** (Alta) — *atendida*. `formatarKm` emite apenas distância + " km"; grep confirma nenhum "R$"/"reais" novo no código; E2E assere `body` sem "R$".
- **RN-055 / RN-057** (contexto) — *não implementadas nesta task* (cálculo/mínimo de Seções são da TASK-026); a task só lê o congelado. Sem regressão.
- **Leitura do congelado** (Spec 03 §12; NEG-015 espião de rede) — *atendida*. Nenhuma chamada OSRM/rede; E2E com `page.route` de abort assere zero chamadas.

## Specs verificadas

- **Spec 04 §9 / §9.1** — *aderente*. Formato triangular, X na diagonal, `Cidade - Nome da Seção`, km, sem R$; célula = `valor_adotado_de_distancia`; detalhe Ida/Volta expansível (hover/clique) em bidirecional; somente leitura. Evidência: `etapa-matrizes.tsx:154-227`.
- **Spec 04 §2.8** — *aderente*. Sem valores monetários.
- **Spec 02 §8** — *aderente*. Lê `ParDistancia` (`distancia_trecho_ida?`, `distancia_trecho_volta?`, `valor_adotado_de_distancia`) tal como definido no schema; nenhum campo novo.

## Pontos corretos

- Derivação separada em módulo puro e testável, sem estado React, facilitando a cobertura unitária das RN.
- Reuso de `chaveParNaoDirecional`, `secoesAtendidas` e `nomeExibicaoSecao` — nenhuma duplicação da lógica de ordenação/nomenclatura da TASK-026/027.
- `data-testid` distintos (`matriz-distancias`, `celula-distancia`, `celula-distancia-diagonal`) e seletores E2E escopados a `getByTestId("matriz-distancias")` — não colidem com as contagens da matriz de seccionamento; os 5 E2E pré-existentes da TASK-027 seguem verdes.
- Detalhe Ida/Volta via `<details>` nativo, marcado como inferência controlada de UX no código e na entrega.
- Duas seções mantidas separadas (distância read-only × seccionamento editável) — nada de unificação de grade fora de escopo.

## Problemas encontrados

Nenhum bloqueante.

- **(Informativo, não impeditivo)** A TASK-048 no backlog cita "Spec 02 §9" como fonte dos dados de distância; a fonte correta é a **Spec 02 §8** (§9 é seccionamento), como confirmam Spec 04 §9.1 e o schema (`esquema.ts:123-129`). A implementação seguiu §8. Lapso de redação do backlog, sem efeito no código — candidato a correção editorial do `06-BACKLOG_INICIAL.md`.

## Violações de escopo

Nenhuma. O diff toca apenas: novo módulo de apresentação, a nova seção read-only em `etapa-matrizes.tsx`, o export em `index.ts` e os testes. Cálculo (`calculo-matriz-distancias.ts`), seccionamento (`edicao-seccionamento.ts`), contrato e OSRM não foram alterados — todos no "Fora de escopo".

## Testes avaliados

- **Cobertura das RN da task:** sim. RN-054 (mapeamento/ordem de pares), RN-056 (bidirecional vs unidirecional), RN-013/076 (km, sem R$) têm asserção direta.
- **Casos inválidos testados:** sim — par sem entrada → `undefined` (não inventa valor), matriz vazia → sem célula, `formatarKm` sem "R$".
- **Regressão de UUID:** N/A (task não toca import/export/cópia; só lê o documento carregado).
- **OSRM mockado:** N/A no sentido de dependência — a etapa não chama OSRM; o E2E usa `page.route` de abort só para **provar** que nenhuma chamada ocorre.
- **Fixtures canônicas:** sim — `documentoBidirecionalMultiServico`, `documentoExemploMinimo`, `documentoUnidirecional` e o fixture E2E `carregar-multi-servico.json`; nenhum JSON ad hoc divergente do schema.
- **Suíte executada:** `npm run typecheck` OK; `npm run lint` OK; `npm test` → **670 passed (48 arquivos)**; `npx playwright test etapa-matrizes` → **7 passed** (5 pré-existentes + 2 novos).

## Checklist 07

- **Escopo (6 itens):** 6 ok — pertence ao ROTA (não SEI), sem workflow/status, sem persistência de servidor, só o escopo da task, sem regra inventada (inferência de UX marcada).
- **JSON (9 itens):** RN-013 ok (sem R$), RN-015 ok (lê congelado, não recalcula), RN-014 ok (km); demais (campo novo, UUID, RN-033, import) N/A — task não escreve no JSON.
- **Domínio (10 itens):** RN-006 ok (`numero_n` só como rótulo do `<option>`, identidade por `uuid`); Seção/Local separados (grade só de `Secao[]`); demais N/A.
- **Comparador (7 itens):** N/A.
- **Roteamento (5 itens):** RN-052 ok (abrir/navegar sem OSRM); demais N/A.
- **UI/PDF (6 itens):** RN-076 ok (nomes `Cidade - Nome`, triangular, km, sem R$), Locais fora da grade ok; offsets N/A, SEI/PDF N/A, exportação N/A.
- **Testes (6 itens):** RN com teste, casos inválidos, sem dependência de OSRM real, fixtures canônicas, verificações reportadas — ok; regressão UUID N/A.

**Resultado: todos os itens aplicáveis ok; nenhum violado.**

## Pendências

- Nenhuma condição de merge. Follow-up editorial opcional: corrigir "Spec 02 §9" → "Spec 02 §8" na descrição da TASK-048 no backlog.
- Q-xxx abertas: nenhuma.

## Decisão

**Aprovado.** Escopo exato (só o display read-only da matriz de distâncias sobre o shell da TASK-027), RN de criticidade Alta (RN-054, RN-056, RN-076, RN-013) atendidas e cobertas por testes válidos e inválidos, leitura do congelado sem tocar OSRM comprovada por E2E, checklist 07 limpo e verificações todas verdes. A única observação (referência "§9" no backlog) é editorial e não afeta o código.
