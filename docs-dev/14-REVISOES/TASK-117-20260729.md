# Revisão da TASK-117

**Revisor:** Codex
**Data:** 2026-07-29
**Commit/branch revisado:** branch `redesign`, `f14988a` + diff não commitado do working tree

## Resultado

- [ ] Aprovado
- [x] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

As operações de copiar/apagar um dia foram movidas para botões nos cabeçalhos
`SEG…DOM`, reutilizando os motores existentes, e o `X` da superfície ficou
restrito à Viagem. O modo headway ganhou variante azul-clara inativa, linha
própria com os campos `a cada`/`até`, botão lateral único e máscara de quatro
algarismos. A entrega altera principalmente
`src/formulario/viagens/etapa-viagens.tsx`,
`src/formulario/viagens/horario-relogio.ts` e `src/shared/ui/botao.tsx`.

## Regras RN verificadas

- RN-004 — atendida: nenhuma identidade importada é regenerada; os motores de
  cópia continuam isolados e cobertos pelas regressões existentes.
- RN-007 — atendida: `copiarDiaParaDiasComGuarda` e
  `gerarViagensPorHeadway` continuam criando Viagens por `criarViagem`, com
  UUID nova (`src/formulario/viagens/copias-grade.ts:152` e
  `src/formulario/viagens/acoes-grade.ts:101`).
- RN-061 — atendida: menu e geração mantêm o dia e a grade explícitos; apagar
  filtra `dia_semana`, `viagem_feriado` e `tabela_excepcional_uuid`
  (`src/formulario/viagens/etapa-viagens.tsx:597`).
- RN-062 — atendida: o motor de cópia de dia preserva reforços no destino vazio
  e limita a guarda ao gesto (`src/formulario/viagens/copias-grade.ts:152`);
  há testes unitário e E2E.
- RN-063 — atendida: cópias e headway clonam `horarios_paradas` sem recalcular
  offsets (`src/formulario/viagens/copias-grade.ts:182` e
  `src/formulario/viagens/acoes-grade.ts:128`).
- RN-067 — atendida: os campos exibem `HH:MM`, a máscara não corrige valores
  impossíveis e a validação temporal continua anterior à geração
  (`src/formulario/viagens/etapa-viagens.tsx:88` e
  `src/formulario/viagens/horario-relogio.ts:15`).
- RN-096 — atendida: menu, modo e rascunhos permanecem em estado React local;
  não há persistência ou chamada de escrita.

## Specs verificadas

- Spec 04 §8.1 — aderente: cabeçalhos continuam em `<th scope="col">` e as
  colunas `SEG…DOM` recebem botões acessíveis
  (`src/formulario/viagens/etapa-viagens.tsx:1139`).
- Spec 04 §8.2 — aderente: criação, edição, âncoras, redistribuição e
  “Restaurar sugestão” não foram alteradas; headway continua recebendo horário
  de relógio.
- Spec 04 §8.3 — aderente: cópia unitária segue por arrasto e
  `Ctrl+←`/`Ctrl+→`; a regressão permanece em
  `testes/e2e/etapa-viagens.spec.ts:940`.
- Spec 02 §11/§12 — aderente: Viagem continua estratificada e as cópias
  continuam recebendo UUID nova, preservando offsets.

## Pontos corretos

- O menu de cabeçalho contém exatamente “Copiar para outro dia” e “Apagar o
  dia”, carregando dia e grade no estado
  (`src/formulario/viagens/etapa-viagens.tsx:1281`).
- O botão de cópia de dia foi removido do hover e o `X` chama somente a remoção
  da Viagem (`src/formulario/viagens/etapa-viagens.tsx:840`).
- A variante `alternador` foi criada por prop, documentada no design system e
  testada, sem sobreposição de cores por `className`
  (`src/shared/ui/botao.tsx:13`).
- A máscara cobre `1 → 00:01`, `123 → 01:23`, `9875 → 98:75`, exclusão e
  limite de quatro algarismos
  (`testes/unitarios/formulario/viagens-horario-relogio.test.ts:34`).
- O botão de geração nasce desabilitado e só habilita quando headway e limite
  são temporalmente válidos; o E2E prova que a entrada inválida é atômica
  (`testes/e2e/etapa-viagens.spec.ts:482`).
- `MenuFlutuante` já prova foco inicial, setas, `Esc` e clique fora em teste
  próprio (`testes/unitarios/shared-ui/menu-flutuante.test.tsx:33`).

## Problemas encontrados

1. **Média — seletor estável de diálogo removido (DEC-050; critério da
   TASK-117).** O diff remove `data-testid="dialogo-opcoes-apagar"` e adapta os
   E2E para não atravessarem mais o botão “Apagar esta Viagem”. O código atual
   liga `apagar-viagem` diretamente a `aoApagarViagem`
   (`src/formulario/viagens/etapa-viagens.tsx:852`), embora o critério em
   `docs-dev/06-BACKLOG_INICIAL.md:6598` determine preservar os `data-testid`
   existentes das ações, campos e diálogos. Isso viola também
   `docs-dev/18-DESIGN_SYSTEM.md` §6.5. Condição de merge: manter o `X`
   semanticamente restrito à Viagem, mas restaurar um fluxo de confirmação
   acessível que preserve o seletor existente e sua regressão.
2. **Baixa — cobertura E2E incompleta frente aos testes esperados da task.**
   O fluxo de cabeçalho foi exercido somente na grade comum
   (`testes/e2e/etapa-viagens.spec.ts:862` e `:920`); não há cenário que abra o
   cabeçalho da grade de feriados e prove que apagar atua somente nela. O novo
   cenário de headway verifica `aria-pressed`, máscara e geração, mas não afere
   as classes de repouso/ativo nem a geometria de duas linhas com o botão
   abrangendo ambas (`testes/e2e/etapa-viagens.spec.ts:482`). Condição de merge:
   adicionar essas regressões explícitas, sem depender de inspeção visual.

## Violações de escopo

- Nenhuma alteração funcional além do objetivo foi encontrada. O desvio de
  seletor é violação de critério de aceite/contrato de UI, não ampliação de
  regra de negócio.

## Testes avaliados

- cobertura das RN da task: sim — RN-004/007/061/062/063/067/096 têm cobertura
  direta ou regressão existente; permanecem as lacunas de UI descritas acima
- casos inválidos testados: sim — máscara impossível, botão desabilitado,
  cópia sem destino, cancelamento e guardas dos motores
- regressão de UUID (se aplicável): sim — testes unitários de cópia/headway e
  regressões canônicas existentes
- OSRM mockado: sim — o E2E da etapa intercepta o endpoint e comprova zero
  chamada no cenário de headway
- suíte executada com resultado: verde — `npm run test:all:verificar`
  reutilizou o log canônico do executor `humano`; fingerprint
  `781394e9c5359e26040cc628b8d122be084d425e72b1fd1a4f92f99a3e098367`;
  identidade do working tree
  `498d7fa6397724af2122bdced82f79ddd9749373d9277b227d3e2a3884f91763`;
  1.323 unitários e 92 E2E, códigos 0, porta/árvore do PID próprio limpas

## Checklist 07

Resultado: **52 itens conformes ou N/A, 1 violado**.

### Escopo

- [x] Pertence ao ROTA, não ao SEI (RN-095).
- [x] Não introduz workflow (RN-095).
- [x] Não cria status de pedido (RN-011).
- [x] Não cria persistência transacional (RN-096).
- [x] Implementa somente o objetivo da task.
- [x] Não inventa regra; a composição vem da DEC-093.

### JSON (contrato)

- [x] Continua contendo apenas dados de operação (RN-010).
- [x] Continua autossuficiente (RN-009).
- [x] Nenhum campo novo foi adicionado.
- [x] UUIDs existentes permanecem preservadas (RN-004).
- [x] Cópias/headway recebem UUID nova (RN-002/RN-007).
- [N/A] XOR de Parada não foi tocado (RN-033).
- [x] Nenhum valor monetário foi introduzido (RN-013).
- [N/A] Unidades e arredondamento não foram tocados (RN-014/RN-050).
- [N/A] Dados congelados/leitores não foram tocados (RN-015).

### Domínio

- [N/A] Separação Seção/Local não foi tocada (RN-025/RN-031).
- [N/A] Ponto de rota não foi tocado (RN-042).
- [N/A] Identidade de Serviço não foi tocada (RN-001).
- [N/A] `numero_n` não participa da alteração (RN-006).
- [N/A] Extremos de itinerário não foram tocados (RN-035).
- [N/A] Conjunto Ida/Volta não foi tocado (RN-030).
- [N/A] Tipificação não foi tocada (RN-019..022).
- [x] Viagem permanece estratificada por dia/grade (RN-061).
- [N/A] Contagens de feriado não foram alteradas (RN-069).

### Comparador

- [N/A] Base de comparação por UUID não foi tocada (RN-081).
- [N/A] Taxonomia de diff não foi tocada (RN-081..083).
- [N/A] Imutabilidade do Comparador não foi tocada (RN-080/RN-097).
- [N/A] Exclusão de status/datas do diff não foi tocada (RN-012).
- [N/A] Bloqueio de Autos distintos não foi tocado (RN-084).
- [N/A] Comparação estável de rota não foi tocada (RN-087).
- [N/A] Comparador/OSRM/persistência não foram tocados (RN-080).

### Roteamento

- [N/A] Bloqueio por OSRM indisponível não foi tocado (RN-048).
- [N/A] Fronteira de distância roteada não foi tocada (RN-047/RN-050).
- [N/A] Invariante de pontos de rota não foi tocada (RN-041..043).
- [N/A] Abertura de JSON não foi tocada (RN-052).
- [N/A] Mensagens de roteamento não foram tocadas (RN-049).

### UI/PDF

- [N/A] Nomes de Seção não foram alterados (RN-076).
- [x] Usuário continua vendo/digitando horários de relógio (RN-067/RN-076).
- [N/A] Matrizes não foram tocadas (RN-076).
- [N/A] Exibição de Locais não foi tocada (RN-031/RN-044/RN-076).
- [N/A] PDF não foi tocado (RN-077).
- [N/A] Gate de exportação não foi tocado (RN-078).
- [x] Componentes/tokens/variante explícita aderem ao design system (DEC-050).
- [ ] `data-testid` existentes não foram todos preservados: foi removido
  `dialogo-opcoes-apagar` (DEC-050).

### Testes

- [x] RN alteradas/exercidas possuem regressões.
- [x] Há casos inválidos.
- [x] Há regressão de UUID aplicável (RN-004/RN-007).
- [x] OSRM é interceptado e não é acessado no cenário alterado.
- [x] Fixtures canônicas foram reutilizadas.
- [x] Verificações canônicas foram reportadas honestamente.
- [x] A evidência final veio de uma única execução canônica válida.
- [x] A revisão reutilizou o log válido sem repetir a suíte.
- [x] Porta e PIDs próprios foram limpos; nenhum servidor alheio foi usado.

## Pendências

- Restaurar `data-testid="dialogo-opcoes-apagar"` num fluxo de confirmação que
  ofereça apenas apagar a Viagem e atualizar a regressão sem trocar o seletor.
- Acrescentar E2E do menu na grade de feriados e das classes/geometria do modo
  headway.
- Criar o commit próprio da implementação antes do commit documental de revisão;
  o working tree revisado está sem commit de implementação.
- Nenhuma Q-xxx aberta.

## Decisão

**Aprovado com ressalvas impeditivas de merge.** A lógica de negócio, as RN de
identidade/estratificação/offsets, a composição principal e a suíte canônica
estão verdes. O merge depende de preservar o seletor de diálogo explicitamente
protegido pela task/DEC-050 e de fechar as regressões E2E enumeradas. Como essas
condições mantêm a entrega em aberto, a TASK-117 não é movida para executadas e
deve passar por nova revisão após a correção.
