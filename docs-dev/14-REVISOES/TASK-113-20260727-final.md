# Revisão da TASK-113 — reavaliação final

**Revisor:** Codex
**Data:** 2026-07-27
**Commit/branch revisado:** `ccc2572` + correção `1c70bb3` / `redesign`

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

A entrega mantém, na instância aberta de `EtapaViagens`, dois últimos
deslocamentos válidos independentes para as ações anterior e posterior. A
correção `1c70bb3` ajusta o E2E que havia causado a reprovação inicial,
selecionando novamente o sentido após a troca de Serviço e consultando a ação
posterior na última Seção.

Arquivos de código e teste revisados:

- `src/formulario/viagens/etapa-viagens.tsx`
- `testes/e2e/etapa-viagens.spec.ts`

## Regras RN verificadas

- **RN-004 — atendida no aspecto aplicável.** A TASK-113 não altera o motor de
  criação; `inserirViagemPorOffsetRelativo` continua criando outra Viagem por
  `criarViagem` (`src/formulario/viagens/acoes-grade.ts:66-82`), com teste de
  UUID nova e diferente da origem
  (`testes/unitarios/formulario/viagens-acoes-grade.test.ts:82-103`).
- **RN-061 — atendida.** A inserção preserva `dia_semana`,
  `viagem_feriado` e `tabela_excepcional_uuid`
  (`src/formulario/viagens/acoes-grade.ts:76-81`).
- **RN-063 — atendida.** `horarios_paradas` é copiado integralmente, sem
  compartilhar os objetos mutáveis com a origem
  (`src/formulario/viagens/acoes-grade.ts:81`;
  `testes/unitarios/formulario/viagens-acoes-grade.test.ts:99-103`).
- **RN-067 — atendida.** Os controles recebem `HH:MM`, são normalizados na
  fronteira e nunca expõem `offset_horario`
  (`src/formulario/viagens/etapa-viagens.tsx:214-236`).
- **RN-096 — atendida.** Os valores e suas referências de último valor válido
  vivem somente no estado local da etapa
  (`src/formulario/viagens/etapa-viagens.tsx:101-104`); não entram na
  `SessaoFormulario`, em storage, servidor ou contrato JSON.

## Specs verificadas

- **Spec 04 §8.2 — aderente.** A UI continua recebendo horários de relógio e
  a mudança limita-se ao ciclo de vida dos valores dos controles.
- **Spec 04 §8.3 — aderente.** O cálculo e a criação unitária permanecem no
  motor existente; a TASK-113 não altera ações ou entidades da grade.
- **DEC-091 — aderente.** Há dois valores e duas referências independentes no
  alcance da instância aberta da etapa
  (`src/formulario/viagens/etapa-viagens.tsx:101-104`). Entrada malformada
  restaura somente a direção correspondente
  (`src/formulario/viagens/etapa-viagens.tsx:214-230`), e o desmonte reinicia
  naturalmente o estado.

## Pontos corretos

- O estado é compartilhado entre Viagens, grades, Serviços e sentidos sem ser
  elevado à sessão.
- As direções anterior e posterior permanecem independentes.
- Entrada malformada não substitui o último valor válido e mantém a mensagem
  amigável.
- O E2E corrigido seleciona o sentido depois da troca de Serviço
  (`testes/e2e/etapa-viagens.spec.ts:369-373`) e consulta a ação posterior na
  última Seção (`testes/e2e/etapa-viagens.spec.ts:375-388`).
- O cenário cobre valores distintos, criação às 08:25, grade de feriados,
  troca de Serviço e sentido, entrada inválida, reset no desmonte e ausência
  de chamada ao OSRM (`testes/e2e/etapa-viagens.spec.ts:326-421`).
- As invariantes herdadas de dia, grade, UUID, offsets e limites
  00:00–23:59 permanecem cobertas pelos testes do motor
  (`testes/unitarios/formulario/viagens-acoes-grade.test.ts:82-121`) e pelo E2E
  da TASK-107 (`testes/e2e/etapa-viagens.spec.ts:424-470`).
- O diff não toca schema, contrato JSON, PDF, Comparador, contagens ou
  roteamento.
- `Campo` e `Botao` continuam vindo de `shared/ui`; não há `style=` inline,
  token visual novo nem alteração de `data-testid`/`aria-*` existente.

## Problemas encontrados

- Nenhum problema bloqueante ou ressalva funcional.

## Violações de escopo

- Nenhuma. Os commits revisados se limitam ao estado dos dois controles e ao
  E2E correspondente; não entram nas superfícies declaradas fora de escopo.

## Consequências sobre outras tasks

- O motor herdado da TASK-107 foi aberto nesta revisão e permanece em
  `src/formulario/viagens/acoes-grade.ts:66-82`; a TASK-113 não altera cálculo,
  UUID, dia, grade ou offsets.
- TASK-108..112 permanecem apenas como ponteiros de trabalho futuro; o
  comportamento de seus códigos não foi verificado nesta revisão.
- Não houve mudança nas fronteiras entre Formulário, Comparador e `shared`,
  nem no contrato JSON.

## Testes avaliados

- **cobertura das RN da task:** sim — o E2E da DEC-091 e as regressões
  herdadas da TASK-107 estão verdes.
- **casos inválidos testados:** sim — entrada malformada sem criação nem
  contaminação entre direções; resultado fora do dia coberto no motor e no
  E2E herdado.
- **regressão de UUID:** sim — a nova Viagem recebe UUID diferente da origem;
  a TASK-113 não altera importação.
- **OSRM mockado:** sim — a rota pública é interceptada e o cenário confirma
  que não houve chamada.
- **fixture canônica:** sim — `carregar-multi-servico.json`.
- **verificações leves:** `npm run typecheck`, `npm run lint` e
  `npm run build` executados nesta revisão, todos verdes.
- **suíte executada com resultado:** verde. O verificador canônico confirmou
  log do executor `Codex`, 95 arquivos/1.282 testes unitários e 80 E2E
  aprovados, fingerprint
  `9ab972fc5f238fce0e6db78c445707c3eccac79878c8799ac5a1ab9f122d37d8` e
  identidade do working tree
  `498d7fa6397724af2122bdced82f79ddd9749373d9277b227d3e2a3884f91763`.
- **reutilização da evidência:** `npm run test:all:verificar` passou nesta
  revisão; conforme o protocolo, a suíte completa não foi repetida.

## Checklist 07

Resultado: **24 itens OK, 29 N/A, 0 violados**.

### Escopo

| Item | Resultado | Evidência |
|---|---|---|
| Alteração pertence ao ROTA, não ao SEI (RN-095) | OK | Conveniência de edição de Viagem. |
| Não introduz workflow (RN-095) | OK | Nenhum estado administrativo. |
| Não cria status de pedido (RN-011) | OK | Contrato intocado. |
| Não cria persistência transacional (RN-096) | OK | Estado local da etapa. |
| Implementa apenas o escopo da task | OK | Diff restrito aos dois valores e ao E2E. |
| Nenhuma regra nova inventada | OK | Comportamento fundamentado na DEC-091. |

### JSON (contrato)

| Item | Resultado | Evidência |
|---|---|---|
| JSON só com dados de operação (RN-010) | OK | Nenhum campo novo. |
| JSON autossuficiente (RN-009) | OK | Nenhum recurso externo novo. |
| Nenhum campo sem Spec 02 | OK | Schema não foi alterado. |
| UUIDs existentes preservadas (RN-004) | OK | Motor preservado e regressão verde. |
| Entidades novas recebem UUID nova (RN-002/007) | OK | `criarViagem` e teste herdados. |
| XOR de Parada (RN-033) | N/A | Referências de Parada intocadas. |
| Sem R$ (RN-013) | OK | Nenhum valor monetário. |
| Unidades/arredondamento (RN-014/050) | N/A | Fora do escopo. |
| Congelados não recalculados por leitores (RN-015) | N/A | Nenhum leitor alterado. |

### Domínio

| Item | Resultado | Evidência |
|---|---|---|
| Seção e Local separados (RN-025/031) | N/A | Entidades intocadas. |
| Ponto de rota não vira Parada (RN-042) | N/A | Fora do diff. |
| Serviço mantém UUID (RN-001) | N/A | Serviço não é alterado. |
| `numero_n` não é identidade (RN-006) | N/A | Nenhuma lógica por número. |
| Extremos continuam Seções (RN-035) | N/A | Itinerário intocado. |
| Ida/Volta compartilham Seções (RN-030) | N/A | Estrutura intocada. |
| Tipificação respeitada (RN-019..022) | N/A | Fora do escopo. |
| Viagem estratificada (RN-061) | OK | Motor preserva dia e grade. |
| Feriado fora das contagens (RN-069) | N/A | Contagens intocadas. |

### Comparador

| Item | Resultado | Evidência |
|---|---|---|
| UUID como base (RN-081) | N/A | Comparador fora do diff. |
| Taxonomia de diff (RN-081..083) | N/A | Comparador fora do diff. |
| Comparativo não gravado (RN-080/097) | N/A | Comparador fora do diff. |
| Status/datas fora do diff (RN-012) | N/A | Comparador fora do diff. |
| Autos diferentes bloqueados (RN-084) | N/A | Comparador fora do diff. |
| Rota por sinais estáveis (RN-087) | N/A | Comparador fora do diff. |
| Sem OSRM/persistência no Comparador (RN-080) | N/A | Comparador fora do diff. |

### Roteamento

| Item | Resultado | Evidência |
|---|---|---|
| OSRM indisponível bloqueia rota inválida (RN-048) | N/A | Roteamento intocado. |
| Distância vem do OSRM (RN-047/050) | N/A | Distâncias intocadas. |
| Pontos de rota só condicionam traçado (RN-041..043) | N/A | Fora do diff. |
| Abrir JSON não chama OSRM (RN-052) | N/A | Importação/roteamento intocados. |
| Mensagens §14 sem tarifa (RN-049) | N/A | Mensagens de roteamento intocadas. |

### UI/PDF

| Item | Resultado | Evidência |
|---|---|---|
| Nome `Cidade - Nome da Seção` (RN-076) | N/A | Rótulos de Seção intocados. |
| Horário de relógio; offsets ocultos (RN-067/076) | OK | Controles em HH:MM. |
| Matrizes triangulares sem R$ (RN-076) | N/A | Matrizes fora do diff. |
| Locais fora da grade principal (RN-031/044/076) | N/A | Montagem de linhas intocada. |
| Aviso SEI no PDF (RN-077) | N/A | PDF fora do diff. |
| Gate de exportação (RN-078) | N/A | Exportação fora do diff. |
| Design system (DEC-050) | OK | Componentes e tokens oficiais preservados. |
| `data-testid`/`aria-*` preservados (DEC-050) | OK | Nenhum seletor existente alterado. |

### Testes

| Item | Resultado | Evidência |
|---|---|---|
| Toda RN/regra implementada possui teste | OK | E2E da DEC-091 e regressões herdadas verdes. |
| Casos inválidos testados | OK | Malformado e fora do dia cobertos. |
| Regressão de UUID quando aplicável | N/A | Motor não foi alterado; regressão herdada permanece verde. |
| OSRM mockado | OK | Endpoint público interceptado. |
| Fixtures canônicas reutilizadas | OK | `carregar-multi-servico.json`. |
| Direcionados/typecheck/lint/build reportados | OK | Typecheck, lint e build verdes nesta revisão; suíte canônica verde. |
| Evidência final canônica única e válida | OK | Verificador confirmou log verde e fingerprint atual. |
| Revisão reutilizou log ou executou uma vez | OK | Log válido reutilizado; suíte não repetida. |
| Porta/PIDs isolados e encerrados | OK | Log registra árvore do PID próprio encerrada. |

## Pendências

- Nenhuma condição funcional ou de merge remanescente para a TASK-113. A
  TASK-113, a Q-069 e a DEC-091 são versionadas no mesmo fechamento desta
  revisão para preservar a rastreabilidade do ciclo.

## Decisão

**Aprovado.** A correção `1c70bb3` elimina o defeito de preparação do E2E que
motivou a reprovação inicial. Código, escopo, RN, requisitos negativos,
contrato JSON e design system permanecem aderentes; os casos válidos e
inválidos estão verdes na evidência canônica do mesmo working tree. A
TASK-113 pode sair da lista de pendentes.
