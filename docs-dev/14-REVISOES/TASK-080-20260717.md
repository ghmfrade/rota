# Revisão da TASK-080

**Revisor:** Claude Sonnet 5 (revisão de aderência assistida)
**Data:** 2026-07-17
**Commit/branch revisado:** `656f221` — "Implementa TASK-080: promove ServicoEmConstrucao a Servico completo também no modo carregado" (branch `redesign`)

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Remove o gate `atual.modo === "novo"` do ramo de promoção `ServicoEmConstrucao → Servico` em [etapa-itinerarios.tsx](../../src/formulario/itinerarios/etapa-itinerarios.tsx#L366-L373), que passa a valer nos dois modos. A composição de sessão (achar o Serviço em construção, chamar `promoverServico`, remover de `servicosEmConstrucao`, anexar via `comServicosDaSessao`) foi extraída para a função pura `promoverServicoNaSessao` em [promocao-servico.ts](../../src/formulario/itinerarios/promocao-servico.ts#L120-L149), junto dos helpers `paradasPorSentidoDoServico`/`estadosPorSentidoDoServico` movidos do componente. Novo arquivo de teste [promocao-servico-sessao.test.ts](../../testes/unitarios/formulario/promocao-servico-sessao.test.ts) cobre a composição nos dois modos.

## Regras RN verificadas

- **RN-001/002/004** (Alta) — atendida. `promoverServicoNaSessao` preserva `servico.uuid` (herdado de `promoverServico`, inalterado); teste `"Serviço em construção 'ida' ... uuid preservada, pré-existente intacto"` prova round-trip do promovido **e** do Serviço pré-existente no mesmo documento.
- **RN-018** — atendida. No modo carregado, `comServicosDaSessao` anexa o promovido a `documento.autos.servicos` (código lido em `sessao.ts:158-169`, inalterado); testado.
- **RN-034** — atendida (via `promoverServico`, inalterado): `paradas.length < 2` bloqueia a promoção.
- **RN-038** — atendida. Testado o caso "ambos" com os dois sentidos e o caso inválido "só a Ida pronta → não promove" no modo carregado.
- **RN-039** — atendida (inalterado): Serviço promovido nasce com `viagens: []`; comentário do arquivo já documenta que a validade de schema plena é gate de exportação, não da promoção.
- **RN-048** — atendida. Testado: estado `sem-rota` (OSRM mockado) não promove, Serviço permanece em `servicosEmConstrucao`.
- **RN-054..057** — atendida (via `matrizDistanciasDoServico` dentro de `promoverServico`, inalterado): reconciliada no mesmo commit.
- **RN-096 / NEG-004** — atendida: nenhuma persistência tocada; a função opera só sobre `SessaoFormulario` em memória.

## Specs verificadas

- **Spec 04 §8** — aderente: o Serviço promovido no carregado passa a ser elegível para a etapa Viagens (mesmo caminho de `servicosDaSessao` que já alimenta Viagens/Matrizes, `sessao.ts:149-152`, inalterado).
- **Spec 04 §9.1** — aderente: matriz reconciliada no mesmo commit da rota, herdado de `promoverServico`.
- **Spec 02 §6** — aderente: o `Servico` produzido tem `itinerarios` ≥ 1 e `matriz_distancias` — estrutura inalterada de `promoverServico`.
- **Spec 02 §10** — aderente: "ambos" só produz 2 itinerários com os dois sentidos prontos, herdado e testado.

## Pontos corretos

- Extração para função pura (`promoverServicoNaSessao`) preservou EXATAMENTE a semântica do código original: verifiquei que `proxima` (passada ao helper) já carrega `estadosRotaViva` mesclado com o resultado do recálculo corrente antes da chamada — equivalente ao antigo uso ad-hoc de `{ ...atual, estadosRotaViva: estadosMapa }` só para os estados, mantendo `atual` para as paradas. Como `proxima.paradasEmEdicao === atual.paradasEmEdicao` neste ponto do fluxo, o comportamento é idêntico ao pré-TASK-080.
- O commit da task foi isolado corretamente do trabalho pré-existente e não relacionado que já estava no working tree (integração do `PainelReusoSecao`/`altura-mapa.ts` em `etapa-itinerarios.tsx`, `editor-mapa-itinerario.tsx` e `index.ts`) — usando `git add -p` para separar hunks, o commit `656f221` contém só os 4 arquivos do escopo da TASK-080 (`git show --stat` confirma).
- Teste de regressão explícito para o caminho "Serviço já completo não é tocado pela promoção" (`toBe(sessao)` por identidade quando não há `ServicoEmConstrucao` com o uuid buscado).
- Fixture canônica reutilizada (`documentoExemploMinimo()`), sem JSON ad hoc divergente do schema.

## Problemas encontrados

Nenhum bloqueante. Um ponto de manutenção documental, não impeditivo:

- **Doc staleness (não impeditiva):** `docs-dev/03-TRACEABILITY_MATRIX.md` linhas 11 e 25 (RN-004, RN-018) ainda descrevem a promoção como exclusiva do "fluxo novo" (redação herdada da TASK-061), sem refletir a generalização desta task. Não é uma spec nem uma RN — é um documento derivado — mas vale atualizar num follow-up para não desinformar leitura futura.

## Violações de escopo

Nenhuma. O diff toca apenas: o ramo de promoção em `etapa-itinerarios.tsx` (remoção do gate + extração dos helpers), o novo `promoverServicoNaSessao` em `promocao-servico.ts`, o export em `index.ts`, e o teste novo. Nenhum CRUD de Serviços, contrato/schema ou mecânica de `promoverServico` foi tocado — exatamente o que o "Fora de escopo" da task delimitou.

## Testes avaliados

- **Cobertura das RN da task:** sim — RN-001/002/004/018/034/038/048/054-057/096 todas exercitadas (diretamente nesta task ou por herança comprovada de `promoverServico`, inalterado e já coberto por `promocao-servico.test.ts`).
- **Casos inválidos testados:** sim — "ambos" com só um sentido pronto, `sem-rota`, uuid sem `ServicoEmConstrucao` correspondente (regressão-guarda).
- **Regressão de UUID:** sim — round-trip do promovido e do Serviço pré-existente no mesmo documento carregado.
- **OSRM mockado:** sim — `EstadoRotaViva` construído diretamente em memória, nenhuma chamada de rede em nenhum teste.
- **Suíte executada com resultado:**
  - `npm run typecheck` → **sem erros**.
  - `npm run lint` → **sem erros/warnings**.
  - `npm test` (Vitest) → **81 arquivos, 949 testes, todos verdes**.
  - `npx playwright test testes/e2e/etapa-itinerarios.spec.ts --workers=1` → **8/8 verdes** (a primeira tentativa com workers paralelos teve 8 timeouts de `page.goto` por contenção no boot do `webServer` compartilhado do Playwright — não uma regressão de código; `npm run dev` isolado sobe em <1s e a suíte roda limpa com um worker). Confirma que o caminho "Serviço já completo, modo carregado" (`servicosComItinerarioAtualizado`, intocado por esta task) segue funcionando ponta a ponta.

## Checklist 07

47 itens aplicáveis avaliados, todos atendidos; demais marcados N/A (task não toca JSON/contrato, Comparador, Roteamento em si, UI/PDF). Nenhum item aplicável violado.

- **Escopo:** 6/6 ok.
- **JSON (contrato):** 4 atendidos (UUIDs preservadas, nenhum campo novo, entidades novas — N/A pois a promoção herda a UUID existente, não cria cópia —, dados congelados não recalculados), 5 N/A.
- **Domínio:** Serviço mantém identidade por UUID (ok), `numero_n` não usado como chave (ok — código usa `s.uuid ===`), demais N/A.
- **Comparador:** todos N/A.
- **Roteamento:** OSRM indisponível bloqueia (ok, herdado+testado), demais N/A.
- **UI/PDF:** `data-testid`/`aria-*` preservados (ok — nenhum JSX tocado por esta task; E2E confirma), demais N/A.
- **Testes:** 6/6 ok (RN com teste, casos inválidos, regressão UUID, OSRM mockado, fixtures canônicas, verificações executadas e reportadas).

## Pendências

- Follow-up não bloqueante: atualizar `docs-dev/03-TRACEABILITY_MATRIX.md` (linhas 11 e 25) para refletir que a promoção `ServicoEmConstrucao → Servico` vale nos dois modos desde a TASK-080, não só no "fluxo novo".
- O working tree deste repositório contém trabalho não commitado e não relacionado (painel de reuso de Seção — `painel-reuso-secao.tsx`, `altura-mapa.ts`) que precede esta task; não foi tocado nem avaliado por esta revisão (fora de escopo) — sinalizado ao responsável para retomada/commit à parte.

## Decisão

**Aprovado.** Escopo exato (gate de modo removido, sem tocar mecânica de promoção/contrato), todas as RN citadas na task atendidas com teste (unitário direto na composição de sessão + regressão E2E confirmando que o caminho pré-existente do Serviço já completo continua intacto), checklist 07 sem violações, verificações (typecheck/lint/unitários/E2E) todas verdes. A única observação (matriz de rastreabilidade desatualizada) é documental e não bloqueia o merge.
