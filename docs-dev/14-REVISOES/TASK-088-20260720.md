# Revisão da TASK-088

**Revisor:** Claude (revisão de aderência, conversa separada da implementação)
**Data:** 2026-07-20
**Commit/branch revisado:** `2b27c9a` — "Implementa TASK-088: reconcilia matriz de seccionamento" (branch `redesign`)

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Função pura nova `reconciliarMatrizSeccionamento` em
[reconciliar-matriz-seccionamento.ts](src/formulario/matrizes/reconciliar-matriz-seccionamento.ts),
exportada em [matrizes/index.ts:26](src/formulario/matrizes/index.ts#L26) e aplicada no
write-back consolidado `servicosComItinerarioAtualizado`
([etapa-itinerarios.tsx:314-323](src/formulario/itinerarios/etapa-itinerarios.tsx#L314-L323)),
no mesmo update que recompõe `matriz_distancias`. Testes: 8 unitários novos em
[matrizes-reconciliacao-seccionamento.test.ts](testes/unitarios/formulario/matrizes-reconciliacao-seccionamento.test.ts)
e asserções de integração acrescidas em
[etapa-itinerarios.test.tsx:193-217](testes/unitarios/formulario/etapa-itinerarios.test.tsx#L193-L217).
Nenhuma alteração de contrato JSON, schema, UI ou de outras etapas.

## Regras RN verificadas

- **RN-059** (par de seccionamento íntegro, presente em `matriz_distancias`, não-direcional) — **atendida**. A filtragem usa `chaveParNaoDirecional` ([calculo-matriz-distancias.ts:139](src/formulario/matrizes/calculo-matriz-distancias.ts#L139)), a mesma primitiva do validador estrutural; o teste `[inválido] par obsoleto produz RN-059; reconciliado elimina a violação` prova o antes/depois contra `coletarViolacoesEstruturais`, e o teste "preserva ordem, orientação…" prova o caso `{b,a}`.
- **RN-058** (`distancia_km` é valor confirmado pelo usuário) — **atendida**. A reconciliação só filtra: os objetos sobreviventes são devolvidos por referência, sem reescrever `distancia_km` (teste `expect(resultado[0]).toBe(cb)` e a asserção de integração que preserva 6/8 km, valores da fixture, não os `valor_adotado_de_distancia` recomputados).
- **RN-054/RN-055/RN-056** (cálculo de `matriz_distancias`) — **atendidas por não-alteração**. `matrizDistanciasDoServico` só passou a ser guardada em variável local antes do spread; a chamada e o resultado são idênticos ao pré-existente.
- **RN-015** (dados congelados, reconciliação no Formulário) — **atendida**. Toda a lógica vive em `src/formulario/`; nada em `shared/contrato`, Comparador ou PDF recalcula.
- **RN-004/RN-007** (UUIDs) — **atendidas**. Nenhuma UUID é criada, reescrita ou reordenada; a função só remove elementos inteiros do array.
- **RN-013** (sem R$) — **atendida**. Só `distancia_km` circula.

## Specs verificadas

- **Spec 02 §9** — aderente: "o par deve corresponder a uma combinação presente em `matriz_distancias` deste mesmo Serviço"; "par não-direcional"; "o que o JSON guarda é sempre o valor final confirmado/editado pelo usuário". A implementação faz exatamente isso e nada além.
- **Spec 02 §8** — aderente: a matriz de distâncias segue sendo recomposta como antes, como fonte de verdade dos pares válidos.
- **Spec 02 §14** — aderente: o documento resultante passa em `coletarViolacoesEstruturais` sem RN-059 e no `esquemaDocumentoOperacao` strict (asserção na integração).

## Pontos corretos

- Reuso de `chaveParNaoDirecional` em vez de nova primitiva — elimina o risco nº 1 listado na task (filtrar por par ordenado).
- Reconciliação no **mesmo** `aoAtualizarSessao` da rota, matriz de distâncias, horários (TASK-046) e limpeza de Seções (TASK-084 — `limparSecoesAposEdicaoDeParadas` chamado em [etapa-itinerarios.tsx:449](src/formulario/itinerarios/etapa-itinerarios.tsx#L449), no mesmo bloco `if (linhaAtual.completo && …)`); não há janela de documento inválido.
- Função pura, sem UI, sem mutação: retorna array novo e não toca as entradas (teste dedicado com `structuredClone`).
- O teste de integração parte da fixture canônica com 3 pares de seccionamento e verifica que sobra exatamente 1, com o `distancia_km` original — cobre simultaneamente "remove todos e somente os obsoletos" e "preserva o valor confirmado".
- OSRM mockado por `vi.stubGlobal("fetch", …)`; nenhum teste toca a rede.

## Problemas encontrados

Nenhum.

Observação cosmética, sem efeito normativo: o comentário inline em
[etapa-itinerarios.tsx:414-416](src/formulario/itinerarios/etapa-itinerarios.tsx#L414-L416)
ainda fala em "reconcilia a matriz no mesmo commit (RN-054..057)", no singular; o
docblock da função logo acima já foi atualizado para as duas matrizes. Não é
condição de nada.

## Violações de escopo

Nenhuma. O diff toca só os três arquivos previstos em "Arquivos prováveis" da task
mais os dois de teste. Não altera o cálculo de `matriz_distancias`, o editor de
seccionamento, as sugestões (RN-060), o gate de exportação, o contrato, o
Comparador nem o PDF — todos itens do "Fora de escopo".

## Testes avaliados

- cobertura das RN da task: **sim** — RN-059 (chave não-direcional, remoção de obsoletos, regressão estrutural), RN-058 (preservação de `distancia_km`/ordem/objeto), RN-054 (matriz recomposta intocada), mais os casos de borda "ambas vazias" e "todos os pares obsoletos".
- casos inválidos testados: **sim** — dois testes marcados `[inválido]` (mutação das entradas; documento com par obsoleto violando RN-059), além do cenário "inserir Seção não habilita automaticamente os novos pares".
- regressão de UUID (se aplicável): **N/A** — a task não toca import/export/cópia; as UUIDs dos pares sobreviventes são preservadas por identidade de objeto, o que os testes já asseguram.
- OSRM mockado: **sim** — `respostaOsrmMock()` em [etapa-itinerarios.test.tsx:30](testes/unitarios/formulario/etapa-itinerarios.test.tsx#L30); `fetch` chamado 1×, contra o mock.
- suíte executada com resultado: **verde**.
  - `npm run typecheck` → `tsc --noEmit` sem erros.
  - `npm run lint` → `eslint .` sem erros.
  - `npm test` → `Test Files 86 passed (86) | Tests 1025 passed (1025)` em 40,28 s.
  - E2E não executado nesta revisão (a própria task marca o E2E como opcional).

## Checklist 07

**Resultado: 21 itens ok, 25 N/A, 0 violados.**

- Escopo: 6/6 ok (nada de workflow, status, persistência; escopo exato; toda decisão citando Spec 02 §9/§14 e RN-058/059).
- JSON: RN-010, RN-009, "sem campo novo", RN-004, RN-002/007, RN-013, RN-015 ok; RN-033 e RN-014/050 N/A (a task não toca Paradas nem unidades).
- Domínio: RN-025/031, RN-001, RN-006, RN-035 ok por não-alteração; RN-042, RN-030, RN-019..022, RN-061, RN-069 N/A.
- Comparador: todos N/A (nenhum arquivo do Comparador tocado).
- Roteamento: todos N/A — o recálculo de rota não foi alterado; a única interação é ler a matriz já recomposta.
- UI/PDF: todos N/A — nenhuma alteração de componente, `data-testid` ou `aria-*` (o diff de UI é exclusivamente lógica de estado dentro de `servicosComItinerarioAtualizado`).
- Testes: 6/6 ok — fixture canônica `documentoExemploMinimo()` reutilizada, casos inválidos presentes, OSRM mockado, verificações rodadas e reportadas acima.

## Pendências

- Nenhuma condição de merge.
- Follow-up já registrado no próprio backlog (§ ordem de execução): a **TASK-077** (Volta espelhada) deve continuar passando pelo write-back consolidado `servicosComItinerarioAtualizado` para não recriar a janela de documento inválido. O comportamento da TASK-077 **não foi verificado nesta revisão** (task não implementada).
- Nenhuma Q-xxx aberta ou tocada.

## Decisão

**Aprovado.** Escopo exato, aderência literal à Spec 02 §9 (par não-direcional,
presença obrigatória em `matriz_distancias`, valor confirmado pelo usuário
preservado), RN-058/RN-059 cobertas por testes unitários e de integração, e as três
verificações (typecheck, lint, 1025 testes) verdes. A única observação é um
comentário inline desatualizado no plural das matrizes, que não é condição de merge
nem gera ressalva.
