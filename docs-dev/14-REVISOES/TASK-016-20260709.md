# Revisão da TASK-016

**Revisor:** Claude (Opus 4.8), apoiando revisão humana
**Data:** 2026-07-09
**Commit/branch revisado:** working tree sobre `master`, implementação da TASK-016

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

> As duas ressalvas levantadas na primeira passada (cobertura E2E do modo
> carregado; cópia rasa no `duplicarServico`) foram **corrigidas dentro da
> própria task** antes do fechamento — histórico em "Decisão".

## Resumo da entrega

Etapa Serviços do Formulário (Spec 04 §6): criar/editar/remover/duplicar Serviço em memória (sem banco — RN-096/NEG-009). Núcleo puro em `src/formulario/servicos/` (`numero-n.ts`, `duplicar.ts`, `remover.ts`) + UI (`servicos.tsx`); sessão estendida com `ServicoEmConstrucao`/direcionalidade (DEC-035/036); reconversão passa a regenerar o sufixo do `numero_n` (DEC-037) e a atuar no modo novo (RN-023). Testes: 3 suítes unitárias novas + 1 atualizada, E2E dos modos novo e carregado.

## Regras RN verificadas

- **RN-006** — atendida. `numero_n` nunca é chave (duplicar/remover/cascata casam por `uuid`); sufixo regenerado preservando o sequencial (`numero-n.ts`); testes diretos + DEC-037.
- **RN-007 / RN-002 / RN-005** — atendida. `duplicarServico` faz cópia **profunda** (`structuredClone`) e re-cunha `uuid` novas para Serviço/Locais/Viagens via fábricas, sem colisão; `secao_uuid` preservada, `local_uuid` re-mapeado. Testes de colisão e de independência (mutação da cópia não afeta o original).
- **RN-003** — atendida. Edição via `editarEntidade` (UUID reafirmada) no caminho completo e na reconversão.
- **RN-018** — atendida. `removerServico` guarda ≥ 1 Serviço e remove Seção órfã em cascata; testes de cascata e de "remover o último lança".
- **RN-019/020/021/022** — atendida. Dropdown filtrado por `caracteristicasPermitidas(tipo)`; E2E prova ausência de SU/SUL no Rodoviário; reutiliza o módulo de tipificação.
- **RN-024** — atendida. `carater` explícito nos três valores; exercido nos E2E (novo e carregado).
- **RN-023 / DEC-034 / DEC-037** — atendida. `aplicarTrocaDeTipoEmConstrucao` reconverte no modo novo e regenera o sufixo; unit dedicado.

## Specs verificadas

- **Spec 04 §6** — aderente nas quatro ações e nos campos. A UX "apenas filtra os dropdowns e exibe o erro quando um documento carregado viola a tabela": a filtragem é desta etapa (`caracteristicasPermitidas(tipo)`) e a **exibição do erro de tipificação em doc carregado já é atendida pela TASK-012** — `checagens-leitor.ts` emite o alerta técnico `tipificacao` (RN-019..022), propagado a `sessao.alertasImportacao` e exibido no aviso de importação (`layout-formulario.tsx`). Logo, não é lacuna desta task; a §6 não pede selo inline por linha. Divergência menor documentada: "contadores (viagens semanais)" não exibidos — dependem da §10 (TASK-031); omissão deliberada para não inventar regra de contagem (NEG-018).
- **Spec 02 §6/§12/§14** — aderente: entidade nova recebe UUID; `secao_uuid` compartilhada preservada; cascata respeita `secao.servicos.min(1)`; nenhum campo novo no JSON.
- **Spec 03 §10.2/§10.3** — aderente: tabela de tipificação e numeração sequencial.

## Pontos corretos

- Fronteira de contrato preservada: `ServicoEmConstrucao`/direcionalidade são estado de sessão efêmero, nunca gravados no JSON (DEC-035/036) — sem campo de fluxo (NEG-004/007).
- Cópia de Serviço agora totalmente independente (deep copy) além das UUIDs novas.
- Cascata de Seção órfã correta e não-mutante; guarda dupla de RN-018 (UI desabilita + função lança).
- Ponta solta da TASK-015 fechada (reconversão regenera `numero_n`); teste antes mascarado por `"N01"` agora usa rótulo realista.
- Nenhum toque em OSRM; nenhum teste depende de rede.

## Problemas encontrados

Nenhum aberto. Os dois da primeira passada foram corrigidos (ver Decisão).

## Violações de escopo

Nenhuma. O toque em `reconversao.ts`/`identificacao.tsx` é consequência declarada de DEC-037/RN-023. Nada do "Fora de escopo" (itinerários, matrizes, viagens, contadores da §10) foi implementado.

## Testes avaliados

- **Cobertura das RN da task:** sim — RN-005/006/007/018/024 + RN-019..022 (E2E) + RN-003/023 (unit).
- **Casos inválidos testados:** sim — remover o último (lança), rótulo sem sequencial, rótulo sem sufixo conhecido (não lança), lista vazia na reconversão.
- **Regressão de UUID (cópia):** sim — `servicos-duplicar.test.ts` (UUIDs novas, sem colisão, cópia profunda, não-mutação).
- **OSRM mockado:** N/A — a etapa não chama OSRM; nenhum teste toca rede.
- **Suíte executada:** **verde** — `typecheck` OK, `lint` OK, `npm test` 440/440, `npx playwright test servicos` 7/7 (4 modo novo + 3 modo carregado).

## Checklist 07

- **Escopo (6):** 6 OK.
- **JSON (9):** 6 OK, 3 N/A (Parada XOR, km/half-up, autossuficiência sem mudança).
- **Domínio (9):** 4 OK (Seção/Local separados, identidade UUID, `numero_n` não-chave, tipificação), 5 N/A.
- **Comparador (7) / Roteamento (5) / UI-PDF (6):** N/A (não tocados).
- **Testes (6):** 5 OK, 1 N/A (OSRM).
- **Resultado:** **0 violados**, ~21 OK, ~29 N/A.

## Pendências

- **Futuro (outra task):** contadores de viagens semanais na lista (TASK-031); promoção de `ServicoEmConstrucao` a `Servico` completo (TASK-017+ mapa/viagens).
- Nenhuma Q-xxx aberta (Q-016/017/018 decididas em DEC-035/036/037).

## Decisão

**Aprovado.** Escopo exato, todas as RN da task atendidas com teste, contrato JSON e regra de UUID preservados, checklist sem itens violados, suíte verde (440 unit + 7 E2E).

Histórico das ressalvas (corrigidas nesta task, antes do fechamento — não eram Q nem exigiam refazer a task, pois `duplicarServico` é novo e sem consumidores):
1. **Cópia rasa no `duplicarServico`** → passou a `structuredClone` os dados e re-cunhar só as UUIDs via fábricas; +2 testes de independência (referências distintas; mutação da cópia não afeta o original).
2. **E2E só do modo novo** → adicionado fixture carregável `carregar-multi-servico.json` (identidade válida, 2 Serviços) e 3 testes E2E do modo carregado (listar completos, editar com direcionalidade somente-leitura, duplicar/remover).

Próxima task pode prosseguir (`docs-dev/00` §3).
