# Revisão da TASK-107

**Revisor:** Codex  
**Data:** 2026-07-27  
**Commit/branch revisado:** `1ce9b0a` / `redesign`

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

A entrega acrescenta a criação de uma Viagem por deslocamento relativo de
`horario_saida`, com controles ±X editáveis no hover da célula de partida. O
motor em `src/formulario/viagens/acoes-grade.ts` cria UUID nova, preserva dia,
grade e uma cópia profunda de `horarios_paradas`; a UI em
`src/formulario/viagens/etapa-viagens.tsx` aplica o default de 10 minutos,
recusa formato/resultado inválido e mantém as âncoras da origem. Testes
unitários e E2E cobrem criação, limites, herança e ausência de chamada ao OSRM.

## Regras RN verificadas

- **RN-004 — atendida.** A nova Viagem nasce por `criarViagem`, portanto recebe
  UUID nova; o teste também prova que ela difere da UUID da origem
  (`src/formulario/viagens/acoes-grade.ts:75`;
  `testes/unitarios/formulario/viagens-acoes-grade.test.ts:94`).
- **RN-061 — atendida.** `dia_semana`, `viagem_feriado` e
  `tabela_excepcional_uuid` são copiados da origem, mantendo a nova Viagem na
  mesma grade e no mesmo dia (`src/formulario/viagens/acoes-grade.ts:77`).
- **RN-063 — atendida.** `horarios_paradas` é copiado integralmente, elemento a
  elemento, sem alterar ordem ou offsets; a prova unitária inclui offset manual
  e cópia profunda (`src/formulario/viagens/acoes-grade.ts:80`;
  `testes/unitarios/formulario/viagens-acoes-grade.test.ts:84`).
- **RN-067 — atendida.** A UI recebe X como `HH:MM`, nunca expõe
  `offset_horario`, e apresenta a nova partida como horário de relógio
  (`src/formulario/viagens/etapa-viagens.tsx:180`;
  `src/formulario/viagens/etapa-viagens.tsx:471`).
- **RN-002/RN-007 — atendidas no aspecto aplicável.** A operação cria entidade
  nova com UUID nova e mantém a origem intacta.
- **RN-062 — preservada.** Deslocamento zero é aceito e pode criar reforço no
  mesmo horário; não foi introduzida validação de unicidade
  (`testes/unitarios/formulario/viagens-acoes-grade.test.ts:108`).
- **RN-095/RN-096 — atendidas.** É edição operacional client-side, sem workflow
  ou persistência transacional.

## Specs verificadas

- **Spec 04 §8.1 — aderente.** A nova Viagem é anexada ao itinerário e a
  montagem existente a reposiciona temporalmente; o E2E prova 08:00 seguido de
  09:10 (`testes/e2e/etapa-viagens.spec.ts:222`).
- **Spec 04 §8.2 — aderente.** A ação afeta somente a Viagem/dia de origem e
  horários continuam sendo apresentados como relógio.
- **Spec 04 §8.3 — aderente.** A entrega acrescenta o gesto de inserção
  autorizado pela DEC-082, sem implementar geração em lote nem outros gestos.
- **Spec 02 §11/§11.1 — aderente.** A entidade criada conserva a
  estratificação e a lista completa/monotônica de horários da origem.
- **Spec 02 §12 — aderente.** A cópia é entidade nova e recebe UUID nova.
- **DEC-082 — aderente.** Default 10 minutos, X editável, translação rígida,
  mesmo dia/grade e recusa fora de 00:00–23:59 estão implementados e testados.

## Pontos corretos

- O motor puro rejeita deslocamento fracionário e qualquer transbordamento de
  dia, sem aplicar módulo de 24 horas
  (`src/formulario/viagens/acoes-grade.ts:67`).
- Os offsets e objetos internos não compartilham referências mutáveis com a
  origem (`testes/unitarios/formulario/viagens-acoes-grade.test.ts:103`).
- As âncoras manuais da sessão são copiadas para a UUID nova
  (`src/formulario/viagens/etapa-viagens.tsx:208`).
- Os controles usam `Campo` e `Botao` de `shared/ui`, tokens do design system,
  rótulos acessíveis e nenhum `style=` inline.
- O E2E prova o default, X editável, ordenação, translação rígida, herança de
  âncora, recusa fora do dia, visibilidade por hover e zero chamadas ao OSRM
  (`testes/e2e/etapa-viagens.spec.ts:197`).

## Problemas encontrados

- Nenhum.

## Violações de escopo

- Nenhuma. O diff está restrito ao motor/UI da grade, exportação do helper e
  testes. Geração por headway, gestos das TASK-109..112, contrato, PDF,
  Comparador e contagens não foram alterados.

## Consequências sobre outras tasks

- A TASK-107 depende da fundação da TASK-106, registrada como aprovada em
  `docs-dev/19-STATUS_EXECUCAO.md:350`; a implementação atual reutiliza os
  estados de seleção/hover existentes em
  `src/formulario/viagens/etapa-viagens.tsx:96`.
- TASK-108..112 permanecem ponteiros de escopo futuro; seus comportamentos não
  foram verificados nesta revisão.
- Não houve alteração do contrato JSON nem das fronteiras entre Formulário,
  Comparador e `shared/`.

## Testes avaliados

- **cobertura das RN da task:** sim — RN-004/061/063/067 e preservação de
  RN-062 têm prova unitária e/ou E2E.
- **casos inválidos testados:** sim — resultado abaixo de 00:00/acima de 23:59
  e deslocamento fracionário no motor.
- **regressão de UUID (se aplicável):** sim — UUID nova e diferente da origem.
- **OSRM mockado:** sim — os dois E2E interceptam o endpoint público e exigem
  zero chamadas.
- **suíte executada com resultado:** verde. `npm run test:all:verificar`
  confirmou log canônico válido produzido por `Codex`, fingerprint
  `326276ad72c660ab2a2be6c7ecea270d4a5664ce95e0433ad68fc4b5ca6f736b` e
  identidade do mesmo working tree
  `498d7fa6397724af2122bdced82f79ddd9749373d9277b227d3e2a3884f91763`.
  A revisão reutilizou o log e não repetiu a suíte.

## Checklist 07

Resultado: **29 itens OK, 24 N/A, 0 violados**.

### Escopo

| Item | Resultado | Evidência |
|---|---|---|
| Alteração pertence ao ROTA, não ao SEI (RN-095) | OK | Edição operacional de Viagem. |
| Não introduz workflow (RN-095) | OK | Nenhum estado administrativo. |
| Não cria status de pedido (RN-011) | OK | Contrato intocado. |
| Não cria persistência transacional (RN-096) | OK | Estado somente client-side. |
| Implementa apenas o escopo da task | OK | Diff restrito ao gesto relativo e testes. |
| Nenhuma regra nova inventada | OK | DEC-082, Specs 02/04 e RN citadas. |

### JSON (contrato)

| Item | Resultado | Evidência |
|---|---|---|
| JSON só com dados de operação (RN-010) | OK | Só cria Viagem operacional existente no contrato. |
| JSON autossuficiente (RN-009) | OK | Cópia não cria dependência externa. |
| Nenhum campo sem Spec 02 | OK | Zero campo novo. |
| UUIDs existentes preservadas (RN-004) | OK | Origem não é mutada. |
| Entidades novas recebem UUID nova (RN-002/007) | OK | `criarViagem` + teste explícito. |
| XOR de Parada (RN-033) | N/A | Referências de Parada não são alteradas. |
| Sem R$ (RN-013) | OK | Nenhum valor monetário. |
| Unidades/arredondamento (RN-014/050) | N/A | Distâncias/durações não são alteradas. |
| Congelados não recalculados por leitores (RN-015) | N/A | Nenhum leitor ou dado congelado alterado. |

### Domínio

| Item | Resultado | Evidência |
|---|---|---|
| Seção e Local separados (RN-025/031) | N/A | Entidades não alteradas. |
| Ponto de rota não vira Parada (RN-042) | N/A | Fora do diff. |
| Serviço mantém UUID (RN-001) | N/A | Serviço não alterado. |
| `numero_n` não é identidade (RN-006) | N/A | Não usado no gesto. |
| Extremos continuam Seções (RN-035) | N/A | Itinerário não alterado. |
| Ida/Volta compartilham Seções (RN-030) | N/A | Conjunto de Seções não alterado. |
| Tipificação respeitada (RN-019..022) | N/A | Fora do escopo. |
| Viagem estratificada (RN-061) | OK | Mesmo dia e mesma grade da origem. |
| Feriado fora das contagens (RN-069) | OK | Contagens intocadas; grade preservada. |

### Comparador

| Item | Resultado | Evidência |
|---|---|---|
| UUID como base (RN-081) | N/A | Comparador fora do diff. |
| Taxonomia de diff (RN-081..083) | N/A | Comparador fora do diff. |
| Comparativo não gravado (RN-080/097) | N/A | Comparador fora do diff. |
| Status/datas fora do diff (RN-012) | N/A | Comparador fora do diff. |
| Autos diferentes bloqueados (RN-084) | N/A | Comparador fora do diff. |
| Rota por sinais estáveis (RN-087) | N/A | Comparador fora do diff. |
| Sem OSRM/persistência no Comparador (RN-080) | N/A | Comparador fora do diff. |

### Roteamento

| Item | Resultado | Evidência |
|---|---|---|
| OSRM indisponível bloqueia rota inválida (RN-048) | N/A | Roteamento não alterado. |
| Distância vem do OSRM (RN-047/050) | N/A | Distâncias não alteradas. |
| Pontos de rota só condicionam traçado (RN-041..043) | N/A | Fora do diff. |
| Abrir JSON não chama OSRM (RN-052) | OK | E2E registra zero chamada. |
| Mensagens §14 sem tarifa (RN-049) | N/A | Mensagens de roteamento não alteradas. |

### UI/PDF

| Item | Resultado | Evidência |
|---|---|---|
| Nome `Cidade - Nome da Seção` (RN-076) | OK | Grade existente e rótulos preservados. |
| Horário de relógio; offsets ocultos (RN-067/076) | OK | X em HH:MM e partida exibida em relógio. |
| Matrizes triangulares sem R$ (RN-076) | N/A | Matrizes fora do diff. |
| Locais fora da grade principal (RN-031/044/076) | OK | Montagem de linhas não alterada. |
| Aviso SEI no PDF (RN-077) | N/A | PDF fora do diff. |
| Gate de exportação (RN-078) | N/A | Exportação fora do diff. |
| Design system (DEC-050) | OK | `Campo`/`Botao`, tokens, sem estilo inline. |
| `data-testid`/`aria-*` preservados (DEC-050) | OK | Seletores existentes preservados; novos controles acessíveis. |

### Testes

| Item | Resultado | Evidência |
|---|---|---|
| Toda RN alterada/implementada possui teste | OK | Unitários + E2E para RN da task. |
| Casos inválidos testados | OK | Limites inferior/superior e fração. |
| Regressão de UUID quando aplicável | OK | UUID nova e origem preservada. |
| OSRM mockado | OK | Rotas E2E interceptadas; zero chamadas. |
| Fixtures canônicas reutilizadas | OK | `documentoExemploMinimo` e `multiServico`. |
| Direcionados/typecheck/lint/build reportados | OK | Evidência canônica válida para o conteúdo atual. |
| Evidência final canônica única e válida | OK | Fingerprint e identidade confirmados. |
| Revisão reutilizou log válido | OK | Suíte pesada não repetida. |
| Porta/PIDs isolados e encerrados | OK | Verificador aceitou o marcador final canônico. |

## Pendências

- Nenhuma condição de merge.
- Nenhuma Q-xxx aberta para a TASK-107.

## Decisão

**Aprovado.** O commit `1ce9b0a` cumpre os quatro critérios de aceite da
TASK-107 e a DEC-082: cria outra Viagem com UUID nova, mesmo dia/grade,
translação rígida dos offsets, X editável com default de 10 minutos, ordenação
temporal e recusa fora do dia. O escopo está exato, os casos inválidos estão
cobertos, o OSRM não é chamado e o checklist não contém item violado.
