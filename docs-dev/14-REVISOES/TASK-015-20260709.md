# Revisão da TASK-015

**Revisor:** Claude (Opus 4.8), apoiando revisão humana
**Data:** 2026-07-09
**Commit/branch revisado:** working tree sobre `master`, implementação da TASK-015

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Etapa Identificação do Formulário (Spec 04 §5): seleção de Autos/empresa/tipo das listas estáticas no modo novo; `codigo`/`empresa` somente-leitura; `tipo` editável com reconversão automática dos Serviços incompatíveis à forma convencional (aviso, sem bloqueio); selo de status. Entregue em `src/formulario/identificacao/{identificacao.tsx,reconversao.ts,index.ts}`, com a sessão estendida (`IdentidadeAutos`, `identidadeDaSessao`) e o layout/container religados para tornar a identidade editável.

## Regras RN verificadas

- **RN-016** — atendida. `SeletorDeAutos` popula `codigo`/`empresa`/`tipo` de um registro das listas via `carregarListasAutosEmpresas` + `nomeDaEmpresa`; nenhum Autos é criado. Coberta por E2E (modo novo).
- **RN-023 / DEC-034** — atendida. `aplicarTrocaDeTipoNoDocumento` delega a `reconverterServicosParaTipo` (TASK-008) e reescreve só os incompatíveis ao padrão do tipo, com aviso por `numero_n`, **sem bloquear**. Unit (4 cenários) + E2E.
- **RN-003** — atendida. Reconversão via `editarEntidade` reafirma a `uuid`; teste dedicado prova UUID preservada, objeto novo só onde muda, entrada não mutada.
- **RN-011** — atendida. Documento novo nasce `proposta`; selo só exibe (E2E confere `proposta`).
- **RN-019..022** — atendida por construção: destino é sempre `padrao` ∈ `permitidas` do tipo; reutiliza o módulo de tipificação, não reimplementa.
- **RN-006** — atendida. `numero_n` usado só como rótulo no aviso; identidade permanece a `uuid`.

## Specs verificadas

- **Spec 04 §5** — aderente: Código/Empresa não editáveis (renderizados como texto, sem `textbox` — provado por `getByRole("textbox")` toHaveCount(0)); Tipo editável com reconversão+aviso; Status como selo; Data somente-leitura quando presente.
- **Spec 04 §3.2** — aderente: modo novo seleciona Autos das listas, não cria linha.
- **Spec 03 §10.2/§10.4** — aderente: reconversão sempre ao padrão (não remapeia por código); nunca há caso de bloqueio.
- **Spec 02 §4/§4.1** — aderente: `IdentidadeAutos` é subconjunto estrito de `autos`; não introduz campo de contrato.

## Pontos corretos

- Estado de identidade elevado ao container (não persiste — NEG-004/RN-096); sessão do modo novo intencionalmente **não** monta um `DocumentoOperacao` inválido (respeita RN-018 adiando a montagem).
- `aplicarTrocaDeTipoNoDocumento` preserva todos os demais campos de `autos` (spread) e mantém a referência dos Serviços não alterados — imutabilidade limpa.
- Reconversão toca **apenas** `caracteristica_veiculo`; não recalcula rota/matriz (correto: o veículo não afeta distância), logo não introduz dado congelado stale (RN-015).
- Nenhum acesso ao OSRM na etapa; único recurso de rede é o bundle estático (RN-052 preservada).

## Problemas encontrados

Nenhum de severidade que afete RN Alta, NEG-xxx ou escopo. Duas observações menores (não bloqueiam merge):

- **`numero_n` pode ficar visualmente inconsistente após reconversão** (ex.: `0001-1SU` com `caracteristica_veiculo` já `CR`). Não é defeito: RN-006/Spec 02 §6 fazem de `numero_n` rótulo livre, sem regra que o vincule à característica; a numeração é da TASK-016. Registrado como follow-up de UX, não como violação.
- Discrepância de contagem no texto da entrega original ("6 testes novos" vs. os 5 efetivamente escritos em `identificacao-reconversao.test.ts`) — erro de relato, não de código; corrigido nesta revisão.

## Violações de escopo

Nenhuma. A entrega fica dentro do objetivo (etapa Identificação); Serviços (TASK-016), mapa (TASK-020) e exportação/gate (TASK-032/007) não foram antecipados. A extensão do modo novo da sessão para carregar `identidade` foi previamente aprovada na análise.

## Testes avaliados

- cobertura das RN da task: sim — RN-016 (E2E), RN-023/DEC-034 (unit 4 cenários + E2E), RN-003 (unit), RN-011 (E2E).
- casos inválidos testados: sim — troca para tipo já compatível (nenhuma alteração, referências preservadas), não-mutação da entrada, ausência de `textbox` editável de identidade, ausência de aviso quando nada reconverte.
- regressão de UUID (se aplicável): sim — teste dedicado de preservação de `uuid` na reconversão (RN-003/RN-004).
- OSRM mockado: N/A — etapa não roteia.
- suíte executada com resultado: verde — `typecheck` sem erros; `npm test` 25 arquivos / **416 testes** passando; E2E `identificacao.spec.ts` **4/4**; regressão `formulario-layout.spec.ts` + `tela-inicial.spec.ts` **9/9**.

## Checklist 07

**21 itens ok · 30 N/A · 0 violados.**

- Escopo (6/6 ok). JSON: ok — só dados de operação, sem campo novo, UUID preservada em edição, sem R$, congelados intactos; Parada/km/half-up N/A. Domínio: Serviço por UUID, `numero_n` não-identidade, tipificação respeitada — ok; Seção/Local/ponto de rota/extremos/Ida=Volta/Viagem/feriado N/A. Comparador/Roteamento/PDF: N/A (nenhum tocado; sem OSRM). Testes: RN com teste, casos inválidos, regressão de UUID, fixtures canônicas reutilizadas, verificações reportadas — ok.

## Pendências

Nenhuma condição de merge. Follow-ups (fora desta task):

- Atribuição de `data_criacao` na exportação (**TASK-007**).
- Geração/atualização de `numero_n` e reconversão de Serviços do modo novo quando existirem (**TASK-016**).

Nenhuma Q-xxx aberta.

## Decisão

Aprovado. Escopo exato, todas as RN da task atendidas e cobertas por testes (unit + E2E), contrato JSON e UUIDs preservados (RN-003/004 provados), sem workflow nem campo de fluxo (NEG-001..007 respeitados), verificações verdes. As duas observações são cosméticas/de outra task e não constituem ressalva. Pode seguir para a próxima task.
