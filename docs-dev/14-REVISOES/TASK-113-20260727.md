# Revisão da TASK-113

**Revisor:** Codex
**Data:** 2026-07-27
**Commit/branch revisado:** `ccc2572` / `redesign`

## Resultado

- [ ] Aprovado
- [ ] Aprovado com ressalvas
- [x] Reprovado

## Resumo da entrega

A entrega substitui os mapas de deslocamento por UUID de Viagem por dois
valores escalares independentes, mantidos no estado local da instância de
`EtapaViagens`. O E2E acrescentado tenta provar compartilhamento entre hovers,
grades, Serviços e sentidos, rejeição de entrada inválida e reset no desmonte.

Arquivos do commit revisado:

- `src/formulario/viagens/etapa-viagens.tsx`
- `testes/e2e/etapa-viagens.spec.ts`

## Regras RN verificadas

- **RN-004 — atendida no aspecto aplicável.** A TASK-113 não altera o motor de
  criação; `inserirViagemPorOffsetRelativo` continua criando uma nova Viagem
  sem mutar a origem (`src/formulario/viagens/acoes-grade.ts:66-82`), com
  regressão unitária de UUID diferente da origem
  (`testes/unitarios/formulario/viagens-acoes-grade.test.ts:83-103`).
- **RN-061 — atendida no código.** O motor preservado mantém `dia_semana`,
  `viagem_feriado` e `tabela_excepcional_uuid`
  (`src/formulario/viagens/acoes-grade.ts:76-81`).
- **RN-063 — atendida no código.** `horarios_paradas` continua copiado
  integralmente e sem compartilhar os objetos mutáveis com a origem
  (`src/formulario/viagens/acoes-grade.ts:81`;
  `testes/unitarios/formulario/viagens-acoes-grade.test.ts:101-103`).
- **RN-067 — atendida no código.** Os controles recebem `HH:MM`, normalizam
  pela fronteira de horário e não exibem `offset_horario`
  (`src/formulario/viagens/etapa-viagens.tsx:214-236`).
- **RN-096 — atendida no código.** Os valores vivem apenas em `useState`/`useRef`
  da etapa e não entram na `SessaoFormulario`, em storage, servidor ou contrato
  JSON (`src/formulario/viagens/etapa-viagens.tsx:99-105`).

## Specs verificadas

- **Spec 04 §8.2 — aderente no código.** A mudança é estado de conveniência da
  grade; o usuário continua trabalhando com horários de relógio e a alteração
  não amplia o escopo de edição da Viagem.
- **Spec 04 §8.3 — aderente no código.** O cálculo e a criação unitária da
  inserção relativa permanecem delegados ao motor existente; a TASK-113 só
  muda o ciclo de vida dos valores dos controles.
- **DEC-091 — aderente no código, mas sem prova E2E verde.** Há dois valores e
  duas referências de último valor válido independentes, no alcance da
  instância da etapa (`src/formulario/viagens/etapa-viagens.tsx:99-105`).
  Entrada malformada restaura somente a direção correspondente
  (`src/formulario/viagens/etapa-viagens.tsx:214-230`), e o estado local é
  reiniciado naturalmente no desmonte.

## Pontos corretos

- O alcance do estado é exatamente a instância de `EtapaViagens`: trocar
  Serviço, sentido ou grade não recria os quatro estados da DEC-091.
- As direções anterior e posterior não compartilham estado nem referência de
  último valor válido.
- Uma entrada malformada não substitui a referência válida e é restaurada antes
  de retornar com a mensagem amigável.
- O diff não toca schema, contrato JSON, PDF, Comparador, contagens ou
  roteamento.
- `Campo` e `Botao` continuam vindo de `shared/ui`; não há `style=` inline,
  alteração de `data-testid`/`aria-*` existente nem token visual novo.
- `npm run typecheck` e `npm run lint` passaram nesta revisão.
- A observação manual do responsável é coerente com o mecanismo encontrado no
  código: os valores persistem durante a instância e reiniciam ao trocar de
  etapa.

## Problemas encontrados

1. **Bloqueante — a evidência canônica está vermelha por defeito no E2E da
   própria TASK-113.** Ao selecionar outro Serviço, a UI limpa deliberadamente
   o sentido (`src/formulario/viagens/etapa-viagens.tsx:728-731`). O teste troca
   para `0001-2SU`, mas tenta localizar uma célula antes de selecionar `Ida`
   (`testes/e2e/etapa-viagens.spec.ts:369-371`). O snapshot da falha confirma
   “Sentido — selecione —”; sem sentido, a grade não é montada e
   `locator.hover()` expira. A execução humana registrou 1.282 unitários
   aprovados e 79 E2E aprovados, mas esse caso falhou nas três tentativas.
   Pelo critério do template, verificação falhando reprova a entrega.
2. **Alta — critérios de aceite ficam sem teste executável aprovado.** Como o
   teste para no primeiro acesso após a troca de Serviço, ele não prova em
   verde o compartilhamento entre Serviços e sentidos, a entrada inválida nem
   o reset ao desmontar (`testes/e2e/etapa-viagens.spec.ts:369-410`). A leitura
   do código e o teste manual são evidências complementares, não substitutos
   da cobertura automatizada exigida pela task.
3. **Média — a fundamentação da entrega ainda não está versionada.** No
   working tree revisado, TASK-113, Q-069 e DEC-091 aparecem como alterações
   não commitadas em `docs-dev/06-BACKLOG_INICIAL.md`,
   `docs-dev/16-OPEN_QUESTIONS.md` e `docs-dev/10-DECISION_LOG.md`. O commit de
   implementação referencia DEC-091, mas não contém seu registro persistido.

## Violações de escopo

- Nenhuma violação no código do commit `ccc2572`. A reprovação decorre da
  evidência automatizada inválida e da rastreabilidade ainda não versionada,
  não de ampliação funcional da implementação.

## Consequências sobre outras tasks

- O motor herdado da TASK-107 foi aberto nesta revisão e permanece em
  `src/formulario/viagens/acoes-grade.ts:66-82`; a TASK-113 não altera seu
  cálculo, UUID, grade ou offsets.
- TASK-108..112 permanecem apenas como ponteiros de trabalho futuro; o
  comportamento de seus códigos não foi verificado nesta revisão.
- Não houve mudança nas fronteiras entre Formulário, Comparador e `shared`, nem
  no contrato JSON.

## Testes avaliados

- **cobertura das RN da task:** parcial — as invariantes herdadas têm unitários
  verdes; os comportamentos novos da DEC-091 estão num único E2E vermelho.
- **casos inválidos testados:** não de forma aprovada — o cenário existe após a
  linha que falha e não é alcançado.
- **regressão de UUID:** sim — unitário herdado aprovado; a TASK-113 não altera
  o motor.
- **OSRM mockado:** sim — o endpoint público é interceptado; não há dependência
  do serviço real.
- **fixture canônica:** sim — `carregar-multi-servico.json`.
- **verificações direcionadas:** `npm run typecheck` e `npm run lint` verdes.
- **suíte executada com resultado:** vermelho. A execução humana, fingerprint
  `d5b4f3a6a375c3379c7b1b80f45cb2231bd9a4d6a513e397aa9d56c1e11f2667`,
  identidade
  `498d7fa6397724af2122bdced82f79ddd9749373d9277b227d3e2a3884f91763`,
  registrou 1.282 unitários verdes e 79/80 E2E verdes; o caso TASK-113 falhou
  por timeout.
- **execução única da revisão:** o comando
  `npm run test:all:log -- --executor=Codex-revisao` foi executado uma vez, mas
  o ambiente recusou o primeiro subprocesso com `spawn EPERM`; o verificador
  permaneceu vermelho. A suíte não foi repetida.

## Checklist 07

Resultado: **21 itens OK, 29 N/A, 3 violados**.

### Escopo

| Item | Resultado | Evidência |
|---|---|---|
| Alteração pertence ao ROTA, não ao SEI (RN-095) | OK | Conveniência de edição de Viagem. |
| Não introduz workflow (RN-095) | OK | Nenhum estado administrativo. |
| Não cria status de pedido (RN-011) | OK | Contrato intocado. |
| Não cria persistência transacional (RN-096) | OK | Estado local da etapa. |
| Implementa apenas o escopo da task | OK | Diff restrito aos dois valores e ao E2E. |
| Nenhuma regra nova inventada | OK | Comportamento fundamentado na DEC-091. |

### JSON (contrato)

| Item | Resultado | Evidência |
|---|---|---|
| JSON só com dados de operação (RN-010) | OK | Nenhum campo novo. |
| JSON autossuficiente (RN-009) | OK | Nenhum recurso externo novo. |
| Nenhum campo sem Spec 02 | OK | Schema não foi alterado. |
| UUIDs existentes preservadas (RN-004) | OK | Motor preservado e teste unitário. |
| Entidades novas recebem UUID nova (RN-002/007) | OK | `criarViagem` permanece no motor. |
| XOR de Parada (RN-033) | N/A | Referências de Parada intocadas. |
| Sem R$ (RN-013) | OK | Nenhum valor monetário. |
| Unidades/arredondamento (RN-014/050) | N/A | Fora do escopo. |
| Congelados não recalculados por leitores (RN-015) | N/A | Nenhum leitor alterado. |

### Domínio

| Item | Resultado | Evidência |
|---|---|---|
| Seção e Local separados (RN-025/031) | N/A | Entidades intocadas. |
| Ponto de rota não vira Parada (RN-042) | N/A | Fora do diff. |
| Serviço mantém UUID (RN-001) | N/A | Serviço não é alterado. |
| `numero_n` não é identidade (RN-006) | N/A | Nenhuma lógica nova por número. |
| Extremos continuam Seções (RN-035) | N/A | Itinerário intocado. |
| Ida/Volta compartilham Seções (RN-030) | N/A | Estrutura intocada. |
| Tipificação respeitada (RN-019..022) | N/A | Fora do escopo. |
| Viagem estratificada (RN-061) | OK | Motor preserva dia e grade. |
| Feriado fora das contagens (RN-069) | N/A | Contagens intocadas. |

### Comparador

| Item | Resultado | Evidência |
|---|---|---|
| UUID como base (RN-081) | N/A | Comparador fora do diff. |
| Taxonomia de diff (RN-081..083) | N/A | Comparador fora do diff. |
| Comparativo não gravado (RN-080/097) | N/A | Comparador fora do diff. |
| Status/datas fora do diff (RN-012) | N/A | Comparador fora do diff. |
| Autos diferentes bloqueados (RN-084) | N/A | Comparador fora do diff. |
| Rota por sinais estáveis (RN-087) | N/A | Comparador fora do diff. |
| Sem OSRM/persistência no Comparador (RN-080) | N/A | Comparador fora do diff. |

### Roteamento

| Item | Resultado | Evidência |
|---|---|---|
| OSRM indisponível bloqueia rota inválida (RN-048) | N/A | Roteamento intocado. |
| Distância vem do OSRM (RN-047/050) | N/A | Distâncias intocadas. |
| Pontos de rota só condicionam traçado (RN-041..043) | N/A | Fora do diff. |
| Abrir JSON não chama OSRM (RN-052) | N/A | A task não altera importação/roteamento. |
| Mensagens §14 sem tarifa (RN-049) | N/A | Mensagens de roteamento intocadas. |

### UI/PDF

| Item | Resultado | Evidência |
|---|---|---|
| Nome `Cidade - Nome da Seção` (RN-076) | N/A | Rótulos de Seção intocados. |
| Horário de relógio; offsets ocultos (RN-067/076) | OK | Controles em HH:MM. |
| Matrizes triangulares sem R$ (RN-076) | N/A | Matrizes fora do diff. |
| Locais fora da grade principal (RN-031/044/076) | N/A | Montagem de linhas intocada. |
| Aviso SEI no PDF (RN-077) | N/A | PDF fora do diff. |
| Gate de exportação (RN-078) | N/A | Exportação fora do diff. |
| Design system (DEC-050) | OK | Componentes e tokens oficiais. |
| `data-testid`/`aria-*` preservados (DEC-050) | OK | Nenhum seletor existente alterado. |

### Testes

| Item | Resultado | Evidência |
|---|---|---|
| Toda RN/regra implementada possui teste | VIOLADO | E2E da DEC-091 falha antes das asserções principais. |
| Casos inválidos testados | VIOLADO | Bloco inválido não é alcançado na execução. |
| Regressão de UUID quando aplicável | N/A | Motor não foi alterado; regressão herdada permanece verde. |
| OSRM mockado | OK | Rota pública interceptada. |
| Fixtures canônicas reutilizadas | OK | `carregar-multi-servico.json`. |
| Direcionados/typecheck/lint/build reportados | OK | Typecheck/lint verdes; build humano chegou aos E2E. |
| Evidência final canônica única e válida | VIOLADO | Resultado geral vermelho. |
| Revisão reutilizou log ou executou uma vez | OK | Log vermelho detectado; uma execução da revisão, sem repetição. |
| Porta/PIDs isolados e encerrados | OK | Execução humana limpou PID próprio; revisão não iniciou subprocessos. |

## Pendências

- Corrigir o E2E para selecionar explicitamente um sentido após trocar o
  Serviço, antes de buscar a grade. O mesmo cuidado já existe no trecho de
  reabertura (`testes/e2e/etapa-viagens.spec.ts:400-401`).
- Executar novamente a implementação/correção em conversa própria, obter uma
  única evidência canônica verde e então solicitar nova revisão de aderência.
- Versionar TASK-113, Q-069 e DEC-091 junto ao ciclo, preservando a
  rastreabilidade da decisão humana.

## Decisão

**Reprovado.** A leitura do código e a validação manual indicam que o
comportamento de produção está alinhado à DEC-091; não foi encontrada violação
de RN Alta, NEG-xxx, contrato JSON ou escopo no código. Entretanto, o E2E
obrigatório contém um erro de preparação de contexto, a suíte canônica está
vermelha e três critérios novos ficam sem teste aprovado. Pelo critério
vinculante do template, a entrega não pode avançar até corrigir o teste,
versionar a fundamentação e produzir evidência canônica verde.
