# Revisão da TASK-111

**Revisor:** Codex  
**Data:** 2026-07-30  
**Commit/branch revisado:** `4dbf068` / `redesign`

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

A entrega acrescenta o modo compacto da grade de horários como preferência
visual efêmera por Serviço × sentido. O filtro de
`src/formulario/viagens/montagem-grade.ts:90` mantém somente a primeira Seção
visível, e o toggle acessível em
`src/formulario/viagens/etapa-viagens.tsx:1418` alterna entre a grade completa e
as linhas de partida sem escrever na sessão nem no contrato JSON.

Foram acrescentadas regressões unitárias do filtro, isolamento entre sentidos e
integridade dos dados, além de um cenário E2E que cobre alternância, cópia,
inserção por digitação, Enter entre partidas, restauração das passantes e
ausência de chamada ao OSRM.

## Regras RN verificadas

- **RN-063 — atendida.** `linhasSecoes` copia, ordena e filtra as Paradas antes
  de aplicar `slice(0, 1)`; não altera `itinerario.paradas` nem
  `horarios_paradas` (`src/formulario/viagens/montagem-grade.ts:90`). O teste de
  componente compara o documento inteiro antes/depois e confirma que
  `aoAtualizarSessao` não é chamado
  (`testes/unitarios/formulario/etapa-viagens-modo-compacto.test.tsx:65`).
- **RN-067 — atendida.** O modo compacto reutiliza as mesmas células de partida
  e o mesmo fluxo de horário absoluto da grade
  (`src/formulario/viagens/etapa-viagens.tsx:869`). O E2E digita `09:10`,
  confirma esse valor em HH:MM e, ao reabrir a grade, encontra a passante
  preservada (`testes/e2e/etapa-viagens.spec.ts:1157`).
- **RN-061 — preservada.** O filtro atua somente sobre as linhas de Seção; os
  blocos comum, de feriado e excepcional continuam sendo montados das Viagens
  originais (`src/formulario/viagens/etapa-viagens.tsx:820`). O teste de
  componente exerce as três grades
  (`testes/unitarios/formulario/etapa-viagens-modo-compacto.test.tsx:65`).
- **RN-095/RN-096 — atendidas.** O estado novo é React local e explicitamente
  não integra a sessão nem o JSON
  (`src/formulario/viagens/etapa-viagens.tsx:176`). Não há workflow, backend ou
  persistência transacional no diff.

## Specs verificadas

- **Spec 04 §2.4–§2.5 — aderente.** A interface continua recebendo horários de
  relógio e não expõe `offset_horario`.
- **Spec 04 §8.1 — aderente.** A grade completa mantém as Seções na ordem de
  travessia; o modo decidido na DEC-086 deriva somente a primeira Seção e
  continua excluindo Locais
  (`src/formulario/viagens/montagem-grade.ts:84`).
- **Spec 04 §8.2 — aderente.** Criação e edição continuam passando pelo corpo
  compartilhado da grade, sem caminho alternativo de dados
  (`src/formulario/viagens/etapa-viagens.tsx:869`).
- **DEC-086 — aderente.** O toggle oculta as Seções intermediárias/final, mantém
  as demais Paradas intactas e faz o Enter usar a quantidade de Seções
  atualmente visíveis (`src/formulario/viagens/etapa-viagens.tsx:827`).
- **DEC-050 / Design System §3 e §6 — aderente.** O novo controle usa `Botao`
  com variantes públicas, `aria-pressed` e `data-testid`; não introduz
  `style=`, cor, raio, sombra ou dimensão avulsa
  (`src/formulario/viagens/etapa-viagens.tsx:1418`).

## Pontos corretos

- O estado usa chave composta de UUID do Serviço e sentido, evitando vazamento
  visual entre grades (`src/formulario/viagens/etapa-viagens.tsx:294`).
- O filtro tem valor padrão `false`, preservando todos os chamadores e o
  comportamento completo (`src/formulario/viagens/montagem-grade.ts:90`).
- Enter recebe `secoesDaGrade.length`; com uma linha visível, avança para a
  partida do bloco seguinte (`src/formulario/viagens/etapa-viagens.tsx:827` e
  `src/formulario/viagens/montagem-grade.ts:67`).
- Cópia e inserção continuam no corpo compartilhado, e o E2E comprova criação de
  UUID distinta na cópia, nova partida por digitação e restauração do horário
  passante (`testes/e2e/etapa-viagens.spec.ts:1157`).
- O teste inválido prova que o modo de Ida não é aplicado à Volta
  (`testes/unitarios/formulario/etapa-viagens-modo-compacto.test.tsx:123`).
- O cenário E2E aborta qualquer chamada ao OSRM e confirma contador zero
  (`testes/e2e/etapa-viagens.spec.ts:1157`).

## Problemas encontrados

- Nenhum.

## Violações de escopo

- Nenhuma. O diff não toca PDF, contrato JSON, `horarios_paradas`, exportação,
  Comparador ou dados persistidos.

## Testes avaliados

- **cobertura das RN da task:** sim — RN-063 e RN-067 têm provas unitárias/de
  componente e E2E; RN-061, RN-095 e RN-096 são preservadas no recorte.
- **casos inválidos testados:** sim — ausência de Seção não inventa linha e o
  estado compacto não vaza entre sentidos.
- **regressão de UUID (se aplicável):** N/A — a task não toca importação,
  exportação nem motores de cópia; o E2E confirma que a cópia existente continua
  criando Viagem distinta.
- **OSRM mockado:** sim — o cenário da task intercepta e aborta o endpoint
  público, com zero chamadas observadas.
- **suíte executada com resultado:** verde — a revisão reutilizou, sem repetir, o
  log canônico validado por `npm run test:all:verificar`: executor `Codex`,
  fingerprint
  `8c2b90046c5766bd56458505f84479e2d1289eb320b9ea15eb7e69b9f74228d7` e
  identidade do working tree
  `498d7fa6397724af2122bdced82f79ddd9749373d9277b227d3e2a3884f91763`.
  Resultado: 100 arquivos / 1.358 testes unitários e 101 E2E aprovados; build,
  TypeScript e limpeza do servidor próprio aprovados. Na revisão,
  `npm run lint` e `npm run typecheck` também passaram.

## Checklist 07

Resultado: **27 itens OK, 26 N/A, 0 violados**.

### Escopo

- [x] Pertence ao ROTA, não ao SEI (RN-095).
- [x] Não introduz workflow (RN-095).
- [x] Não cria status de pedido (RN-011).
- [x] Não cria persistência transacional (RN-096).
- [x] Implementa somente o objetivo da TASK-111.
- [x] A affordance está autorizada pela DEC-086; nenhuma regra foi inventada.

### JSON (contrato)

- [x] O JSON continua contendo somente dados de operação (RN-010).
- [x] O JSON continua autossuficiente (RN-009).
- [x] Nenhum campo novo foi adicionado.
- [x] UUIDs existentes permanecem preservadas (RN-004).
- [N/A] A task não cria entidade nem cópia em seu código (RN-002/RN-007).
- [N/A] A estrutura XOR de Parada não foi alterada (RN-033).
- [x] Nenhum valor monetário entrou no diff (RN-013).
- [N/A] Distâncias, durações e arredondamento não foram alterados
  (RN-014/RN-050).
- [N/A] Dados congelados e seus leitores não foram alterados (RN-015).

### Domínio

- [x] Seção e Local continuam separados; o filtro seleciona somente Seções
  (RN-025/RN-031).
- [N/A] Ponto de rota não foi alterado (RN-042).
- [N/A] Identidade de Serviço não foi alterada (RN-001).
- [N/A] `numero_n` não participa da lógica nova (RN-006).
- [N/A] Extremos do itinerário não foram alterados (RN-035).
- [N/A] Relação Ida/Volta não foi alterada (RN-030).
- [N/A] Tipificação não foi alterada (RN-019..022).
- [x] Viagem continua estratificada; apenas a apresentação muda (RN-061).
- [N/A] Contagens e rótulo de semana padrão não foram alterados (RN-069).

### Comparador

- [N/A] Casamento por UUID não foi alterado (RN-081).
- [N/A] Taxonomia do diff não foi alterada (RN-081..083).
- [N/A] Comparativo/arquivos não foram alterados (RN-080/RN-097).
- [N/A] Exclusão de `status`/datas do diff não foi alterada (RN-012).
- [N/A] Bloqueio por Autos diferentes não foi alterado (RN-084).
- [N/A] Comparação estável da rota não foi alterada (RN-087).
- [N/A] Arquitetura offline do Comparador não foi alterada (RN-080).

### Roteamento

- [N/A] Tratamento de OSRM indisponível não foi alterado (RN-048).
- [N/A] Origem e conversão de distância não foram alteradas (RN-047/RN-050).
- [N/A] Pontos de rota e invariante de trechos não foram alterados
  (RN-041..043).
- [x] Abrir o JSON e usar o modo compacto não chama OSRM; E2E confirma zero
  chamadas (RN-052).
- [N/A] Mensagens de roteamento não foram alteradas (RN-049).

### UI/PDF

- [x] Os nomes continuam vindo de `nomeExibicaoSecao`, no padrão oficial
  (RN-076).
- [x] Os controles continuam em HH:MM, sem offsets expostos
  (RN-067/RN-076).
- [N/A] Matrizes não foram alteradas (RN-076).
- [x] Locais continuam fora da grade principal (RN-031/RN-044/RN-076).
- [N/A] PDF e aviso de fronteira com o SEI não foram alterados (RN-077).
- [N/A] Gate de exportação não foi alterado (RN-078).
- [x] UI aderente ao design system: `Botao`, variantes e tokens existentes
  (DEC-050).
- [x] Seletores e atributos existentes foram preservados; o novo toggle nasce
  com `aria-pressed` e teste estável (DEC-050).

### Testes

- [x] As RN implementadas/preservadas no recorte possuem teste.
- [x] Há casos inválidos, não apenas caminho feliz.
- [N/A] Regressão específica de UUID não é exigida para este filtro visual
  (RN-004/RN-007).
- [x] O cenário de navegador usa mock/abort do OSRM.
- [x] A fixture canônica `carregar-multi-servico.json` é reutilizada.
- [x] Testes, lint, typecheck e build aplicável foram executados e ficaram
  verdes.
- [x] A evidência final é uma única execução canônica validada para o conteúdo e
  working tree atuais.
- [x] A revisão reutilizou o log válido e não repetiu a suíte.
- [x] O executor canônico registrou a porta 3100 livre ao fim e encerrou somente
  a árvore do PID próprio.

## Consequências sobre outras tasks

- **TASK-106:** a dependência declarada está satisfeita pelo uso da primitiva
  atual de navegação em `src/formulario/viagens/montagem-grade.ts:67`; a
  TASK-111 apenas fornece a quantidade de linhas visíveis.
- **TASK-034:** o PDF permanece fora deste diff e continua como ponteiro de
  sequenciamento para a versão simples da Spec 04 §13.2; não verificado nesta
  revisão.
- **TASK-112:** não é desbloqueada, absorvida nem substituída pela TASK-111; sua
  execução permanece independente.

## Pendências

- Nenhuma condição de merge e nenhuma Q-xxx aberta para esta entrega.

## Decisão

**Aprovado.** O commit implementa exatamente o toggle autorizado pela DEC-086,
mantém RN-063/RN-067 e o contrato intactos, cobre os três critérios de aceite,
passa todo o checklist aplicável e reutiliza evidência canônica válida e verde.
