# Reavaliação da TASK-117

**Revisor:** Codex
**Data:** 2026-07-30
**Commit/branch revisado:** `feb82b2` / `redesign`

## Resultado

- [ ] Aprovado
- [x] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

O commit `feb82b2` reancora as operações de dia inteiro nos cabeçalhos
`SEG…DOM`, mantém o `X` restrito à confirmação de remoção da Viagem e recompõe
o headway em duas linhas com máscara temporal e botão lateral. A entrega altera
`src/formulario/viagens/etapa-viagens.tsx`,
`src/formulario/viagens/horario-relogio.ts`, `src/shared/ui/botao.tsx` e seus
testes. As duas condições técnicas do parecer de 2026-07-29 foram fechadas.

## Regras RN verificadas

- RN-004 — atendida: a task não altera importação/edição de identidade; a suíte
  canônica mantém as regressões de preservação de UUID.
- RN-007 — atendida: a UI continua delegando a cópia de dia ao motor
  `copiarDiaParaDiasComGuarda`, que cria cada cópia por `criarViagem`
  (`src/formulario/viagens/copias-grade.ts:173`).
- RN-061 — atendida: dia e grade são transportados separadamente pelo estado do
  menu, e a remoção/cópia recebem os discriminadores da grade corrente
  (`src/formulario/viagens/etapa-viagens.tsx:1292`).
- RN-062 — atendida: a task não cria validação estrutural de unicidade; o E2E
  conserva a regressão de cópia de todos os reforços da origem
  (`testes/e2e/etapa-viagens.spec.ts:1020`).
- RN-063 — atendida: os motores de cópia permanecem inalterados e preservam
  `horarios_paradas`; a task só muda a afordância que os aciona.
- RN-067 — atendida: os campos recebem horário de relógio, apresentam a máscara
  `HH:MM` e só entregam valores temporais válidos ao motor
  (`src/formulario/viagens/etapa-viagens.tsx:1066`).
- RN-096 — atendida: menu, confirmação, modo e rascunhos são estados React
  efêmeros; nenhum backend, endpoint de escrita ou campo JSON foi criado.

## Specs verificadas

- Spec 04 §8.1 — aderente: cada grade conserva sete colunas semânticas e cada
  cabeçalho contém um botão acessível para o respectivo dia
  (`src/formulario/viagens/etapa-viagens.tsx:1150`).
- Spec 04 §8.2 — aderente: “Restaurar sugestão”, edição de horários e ações
  próprias da Viagem não tiveram seus motores alterados.
- Spec 04 §8.3 — aderente: o hover não contém cópia unitária visível, e as
  regressões de arrasto/atalho permanecem na suíte
  (`testes/e2e/etapa-viagens.spec.ts:1071`).
- Spec 02 §11 — aderente: Viagem continua pertencendo a um único dia e uma única
  grade, com `horarios_paradas` próprios.
- Spec 02 §12 — aderente: as cópias continuam sendo entidades novas com UUID
  nova, sem regenerar a identidade da origem.

## Pontos corretos

- Cada cabeçalho expõe exatamente “Copiar para outro dia” e “Apagar o dia” por
  `MenuFlutuante`, mantendo dia e grade no estado
  (`src/formulario/viagens/etapa-viagens.tsx:1292`).
- O `X` abre confirmação restrita à Viagem, restaura
  `data-testid="dialogo-opcoes-apagar"` e não oferece apagar o dia
  (`src/formulario/viagens/etapa-viagens.tsx:1319`;
  `testes/e2e/etapa-viagens.spec.ts:940`).
- A grade de feriados tem regressão própria que prova remoção somente na grade e
  no dia escolhidos (`testes/e2e/etapa-viagens.spec.ts:965`).
- O alternador inativo usa a variante explícita `alternador`; o ativo usa
  `primario`, com teste de componente e E2E das classes de estado
  (`src/shared/ui/botao.tsx:13`; `testes/e2e/etapa-viagens.spec.ts:498`).
- A composição em duas linhas e o botão abrangendo ambas são aferidos por
  geometria no navegador (`testes/e2e/etapa-viagens.spec.ts:528`).
- A máscara preserva `1 → 00:01`, `123 → 01:23` e `9875 → 98:75`, limita quatro
  algarismos e mantém a geração desabilitada para valor impossível
  (`src/formulario/viagens/horario-relogio.ts:15`;
  `testes/e2e/etapa-viagens.spec.ts:542`).
- O `MenuFlutuante` efetivamente cobre foco inicial, setas, `Esc`, clique fora e
  restauração de foco (`src/shared/ui/menu-flutuante.tsx:53`;
  `testes/unitarios/shared-ui/menu-flutuante.test.tsx:33`).

## Problemas encontrados

1. **Média — artefatos normativos da TASK-117 continuam fora do histórico Git
   (protocolo `docs-dev/04`, DEC-050).** No commit revisado, `git show
   HEAD:docs-dev/06-BACKLOG_INICIAL.md`, `10-DECISION_LOG.md` e
   `16-OPEN_QUESTIONS.md` não encontra TASK-117, DEC-093 nem Q-071. Esses
   artefatos existem apenas nas alterações não commitadas e estão misturados a
   TASK-118/TASK-119. A documentação da variante `alternador` também permanece
   não commitada em `docs-dev/18-DESIGN_SYSTEM.md:59`, embora o código já
   publique a variante em `src/shared/ui/botao.tsx:13`. Isso impede que o commit
   `feb82b2` seja auditável apenas por artefatos persistidos e descumpre a
   exigência de documentar a variante explícita. **Condição de merge:** versionar
   TASK-117, Q-071/DEC-093 e a alteração do design system em commit documental
   próprio, sem arrastar as definições ainda não encerradas de outras tasks;
   depois reavaliar somente essa condição.

## Violações de escopo

- Nenhuma alteração funcional além do objetivo foi encontrada. O commit
  `feb82b2` contém somente os sete arquivos de código/testes previstos para a
  TASK-117; não altera contrato JSON, PDF, Comparador, contagens, arrasto ou
  atalhos.

## Testes avaliados

- cobertura das RN da task: sim — identidade/cópia/grade/offsets permanecem
  cobertos pelas regressões dos motores, e a nova composição tem testes
  unitários e E2E;
- casos inválidos testados: sim — diálogo cancelado, destino vazio, headway
  impossível/zero/limite anterior, ausência da ação no hover;
- regressão de UUID: sim — regressões canônicas de RN-004/RN-007 e cópia de
  reforços permaneceram verdes;
- OSRM mockado: sim — o E2E alterado intercepta o endpoint e prova que não houve
  chamada (`testes/e2e/etapa-viagens.spec.ts:482`);
- suíte executada com resultado: verde — log canônico reutilizado, executor
  `Codex`, 97 arquivos/1.326 testes unitários e 93 E2E aprovados; um E2E fora do
  escopo precisou de retry e terminou aprovado; códigos Vitest/Playwright `0`;
- evidência: fingerprint
  `de9a38e30fd3fcb8a74128acd56c1e9e23a33ba99c4c6a29c212ed00644e36b9`,
  working tree
  `498d7fa6397724af2122bdced82f79ddd9749373d9277b227d3e2a3884f91763`;
  `npm run test:all:verificar` confirmou o log e a revisão não repetiu a suíte;
- infraestrutura: servidor dedicado encerrado com sua árvore de PID; nenhum
  servidor alheio foi reutilizado ou finalizado.

## Checklist 07

**Resultado:** 24 itens atendidos, 29 N/A, 0 violados no código revisado. A
ressalva é de persistência documental/protocolo e está registrada separadamente.

### Escopo

- [x] Pertence ao ROTA, não ao SEI (RN-095).
- [x] Não introduz workflow (RN-095).
- [x] Não cria status de pedido (RN-011).
- [x] Não cria persistência transacional (RN-096).
- [x] Implementa apenas o escopo da TASK-117.
- [x] Não inventa regra: a interação deriva da DEC-093.

### JSON (contrato)

- [x] Continua contendo apenas dados de operação (RN-010).
- [x] Continua autossuficiente (RN-009).
- [x] Nenhum campo novo foi adicionado.
- [x] UUIDs existentes permanecem preservadas (RN-004).
- [x] Cópias continuam recebendo UUID nova (RN-002/RN-007).
- [N/A] Referência Seção/Local de Paradas não foi alterada (RN-033).
- [N/A] Valores monetários não pertencem à superfície alterada (RN-013).
- [N/A] Distâncias/durações/arredondamento não foram alterados (RN-014/RN-050).
- [N/A] Dados congelados não são lidos/recalculados nesta task (RN-015).

### Domínio

- [N/A] Separação Seção/Local não foi alterada (RN-025/RN-031).
- [N/A] Pontos de rota não foram alterados (RN-042).
- [N/A] Identidade de Serviço não foi alterada (RN-001).
- [N/A] `numero_n` não participa da alteração (RN-006).
- [N/A] Extremos de itinerário não foram alterados (RN-035).
- [N/A] Relação de Seções entre Ida/Volta não foi alterada (RN-030).
- [N/A] Tipo/característica não foram alterados (RN-019..022).
- [x] Viagem continua estratificada por dia e grade (RN-061).
- [N/A] Contagens de feriado não foram alteradas (RN-069).

### Comparador

- [N/A] Base de comparação por UUID (RN-081).
- [N/A] Taxonomia do diff (RN-081..083).
- [N/A] Comparativo fora do JSON (RN-080/RN-097).
- [N/A] `status`/datas fora do diff (RN-012).
- [N/A] Bloqueio por Autos diferente (RN-084).
- [N/A] Comparação estável de rota (RN-087).
- [N/A] Ausência de OSRM/persistência no Comparador (RN-080).

### Roteamento

- [N/A] Tratamento de OSRM indisponível (RN-048).
- [N/A] Origem/conversão de distância roteada (RN-047/RN-050).
- [N/A] Invariante de pontos de rota/trechos (RN-041..043).
- [N/A] Abertura sem OSRM (RN-052).
- [N/A] Mensagens de roteamento (RN-049).

### UI/PDF

- [N/A] Padrão de nome de Seção não foi alterado (RN-076).
- [x] Usuário digita horários de relógio, não offsets (RN-067/RN-076).
- [N/A] Matrizes não foram alteradas (RN-076).
- [N/A] Exibição de Locais não foi alterada (RN-031/RN-044/RN-076).
- [N/A] PDF não foi alterado (RN-077).
- [N/A] Exportação não foi alterada (RN-078).
- [x] UI usa `shared/ui`, tokens e a variante por prop, sem `style=` local
  indevido (DEC-050); a falta de versionamento da documentação é a ressalva.
- [x] `data-testid`/`aria-*` existentes foram preservados (DEC-050).

### Testes

- [x] Regras RN aplicáveis possuem regressão.
- [x] Há casos inválidos.
- [x] Há regressão de UUID aplicável (RN-004/RN-007).
- [x] OSRM é interceptado e não é acessado no cenário alterado.
- [x] Fixtures canônicas foram reutilizadas.
- [x] Verificações foram reportadas honestamente.
- [x] A evidência final veio de uma única execução canônica válida.
- [x] A revisão reutilizou o log válido sem repetir a suíte.
- [x] Porta e PIDs próprios foram limpos.

## Pendências

- Versionar isoladamente a definição da TASK-117, Q-071/DEC-093 e a
  documentação da variante `alternador`, hoje misturadas no working tree com
  documentos de TASK-118/TASK-119.
- Reavaliar a condição documental; não é necessário repetir a análise técnica
  nem a suíte se o verificador continuar aceitando o mesmo conteúdo.
- Nenhuma Q-xxx funcional permanece aberta para a TASK-117.

## Decisão

**Aprovado com ressalva impeditiva de merge.** O commit `feb82b2` fecha as duas
ressalvas técnicas do parecer anterior e atende specs, RN, escopo e testes. A
TASK-117 ainda não pode ser marcada como concluída porque sua própria definição,
a decisão que autoriza a interação e a documentação visual correspondente não
existem no histórico da branch. Após versionar esses artefatos sem misturar
outras tasks, basta reavaliar essa condição documental.
