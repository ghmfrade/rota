# Revisão da TASK-013

**Revisor:** Claude (Opus/Haiku), apoiando revisão humana
**Data:** 2026-07-09
**Commit revisado:** 70d7806 (Implementa TASK-013)

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Tela Inicial do Formulário (Spec 04 §3): duas ações — "Carregar JSON existente" (marcada como recomendada) e "Criar Autos do zero" (com aviso obrigatório §3.2). Componente client consome a função pura `importarDocumento` (TASK-006) sem reimplementar validação. E2E cobre os dois fluxos e as mensagens de erro; 6 testes passam.

## Regras RN verificadas

- **RN-004 (Alta) — atendida.** "Carregar" reusa `importarDocumento` sem fábricas; mensagem de preservação de UUID (Spec 04 §3.1 item 5) só aparece em `resultado.ok`. Round-trip regressão já existente cobre o caminho.
- **RN-016 (Média) — atendida.** "Criar do zero" não monta Autos administrativo; só exibe aviso e sinaliza entrada em modo novo. Seleção nas listas é TASK-015 (deferida).
- **RN-017 (Alta) — atendida.** Bloqueio por identidade obsoleta com mensagem literal da Spec 04 §14; E2E verifica com documento schema-válido de identidade desconhecida.
- **RN-052, RN-015 — atendidas.** Abrir não chama OSRM nem recalcula; smoke "nenhuma requisição externa" passa.
- **RN-096 (Alta) — atendida.** Sem persistência de servidor; guard de varredura intacto.

## Specs verificadas

- **Spec 04 §3.1 — aderente.** Fluxo de carregar completo; mensagem item 5 idêntica.
- **Spec 04 §3.2 — aderente.** Aviso obrigatório com texto exato; confirmação explícita.
- **Spec 04 §14 — aderente.** Categorias "JSON inválido" e "identidade inexistente" com mensagens operacionais literais.
- **Spec 04 §2.2/§3 — aderente com inferência controlada.** "Carregar" destacado como recomendado sempre (infeasible to detect "existe documento anterior" client-side sem documento já carregado).

## Pontos corretos

- Reuso de função pura de TASK-006; fronteira `formulario/ → shared/` respeitada.
- Correção de bug real de corrida (input `disabled` vs. `setInputFiles`): handler agora aguarda promessa memoizada — melhora UX sem comprometer semântica de validação.
- Mensagens 100% fiéis ao texto da spec.

## Problemas encontrados

Nenhum impeditivo. Observações menores:

1. Destaque visual é textual ("— Recomendado") sem ênfase forte; refinamento de UX cabe a TASK-014.
2. Fixture E2E fora do índice canônico (justificada: identidade real exigida para caminho feliz).

## Violações de escopo

Nenhuma. Stepper (TASK-014), seleção de Autos/tipo e reforço §3.2 por-Autos `operante` (TASK-015) deferidos corretamente.

## Testes avaliados

- Cobertura RN: sim — RN-004 (msg), RN-016 (aviso), RN-017 (bloqueio) via E2E.
- Casos inválidos: sim — JSON malformado e identidade inexistente.
- Regressão UUID: N/A (código novo).
- OSRM: N/A.
- Suíte: typecheck ✓, lint ✓, 391 unit ✓, 6 E2E ✓.

## Checklist 07

**20 ok · 33 N/A · 0 violados.**
- Escopo: 6/6 ok.
- JSON: RN-010✓, campo novo✓, RN-004✓, RN-013✓, RN-015✓; RN-009/002/007/033/014 N/A.
- Domínio: N/A.
- Comparador: N/A.
- Roteamento: RN-052✓, RN-049✓; RN-048/047/041 N/A.
- UI/PDF: N/A.
- Testes: 5 ok.

## Pendências

- Nenhuma bloqueante.
- Follow-ups: ênfase visual do recomendado (TASK-014), reforço §3.2 por-Autos `operante` (TASK-015).

## Decisão

**Aprovado.** Escopo exato, mensagens fiéis à Spec 04, RN-004/016/017 atendidas com testes E2E (válidos e inválidos), nenhuma violação de RN Alta nem NEG-xxx, verificações verdes. Segue para próxima task.
