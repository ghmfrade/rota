# Revisão da TASK-021

**Revisor:** Claude (Sonnet 5), apoiando revisão humana
**Data:** 2026-07-10
**Commit/branch revisado:** `709b75e` sobre `master` — "Implementa TASK-021: Cliente OSRM (requisição, extração, conversão)"

## Resultado

- [x] Aprovado
- [ ] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Cliente OSRM do Formulário em `src/formulario/roteamento/`: `montarUrlOsrm`/`urlBaseOsrm` (URL de requisição, base configurável via `NEXT_PUBLIC_OSRM_BASE_URL`), `extrairRota`/`arredondaHalfUp` (mapeamento 1:1 `legs[]→trechos[]`, conversão m→km half-up, totais por soma dos trechos) e `solicitarRota` (fetch fino + guarda mínima de `code`, sem retry/mensagens específicas — reservado para a TASK-022). Três suítes novas em `testes/unitarios/formulario/roteamento-*.test.ts` (23 casos). Nenhum arquivo pré-existente foi tocado.

## Regras RN verificadas

- **RN-047** — **atendida**. [url-osrm.ts:39-49](../../src/formulario/roteamento/url-osrm.ts#L39-L49) monta exatamente `overview=full&geometries=geojson&steps=true&annotations=false&continue_straight=false`, coordenadas `lon,lat` na ordem das paradas separadas por `;` — string literal idêntica à da Spec 03 §3.2. Testada em [roteamento-url-osrm.test.ts](../../testes/unitarios/formulario/roteamento-url-osrm.test.ts) (parâmetros, ordem das coordenadas) e reforçada por igualdade de string completa em [roteamento-cliente-osrm.test.ts:52-59](../../testes/unitarios/formulario/roteamento-cliente-osrm.test.ts#L52-L59).
- **RN-050** — **atendida**. [extrair-rota.ts:47-52](../../src/formulario/roteamento/extrair-rota.ts#L47-L52) (`arredondaHalfUp`) e seu uso em `extrairRota` (linhas 88-96) implementam half-up a 2 casas para `distancia_km` e arredondamento ao inteiro para `duracao_s`, com os **totais somados a partir dos trechos já arredondados**. Teste do caso de fechamento (1005 m + 1005 m → 1,01 + 1,01 = 2,02, não 2,01 do bruto) prova exatamente essa regra — [roteamento-extrair-rota.test.ts:78-97](../../testes/unitarios/formulario/roteamento-extrair-rota.test.ts#L78-L97).
- **RN-014** — **atendida**. A conversão m→km ocorre uma única vez, na fronteira (`extrairRota`); `duracao_s` permanece em segundos; nenhum outro ponto do módulo mexe em unidade.
- **RN-041** — **atendida para o caso sem pontos de rota** (escopo desta task). `extrairRota` gera `trechos.length == numeroParadas - 1` com `parada_destino_ordem == parada_origem_ordem + 1` e **rejeita explicitamente** `legs.length` divergente ([extrair-rota.ts:78-85](../../src/formulario/roteamento/extrair-rota.ts#L78-L85)), evitando que a TASK-023 herde uma função silenciosamente incorreta fora da premissa. Testada com caso inválido dedicado.
- **RN-040** — **atendida** (parcial, coerente com o escopo): `distancia_km`/`duracao_s` do resultado somam exatamente os trechos.
- **RN-053** — **não infringida**: `steps=true` é pedido na URL (exigência da RN-047) mas `steps[].name` não é consumido nesta task — correto, é insumo da TASK-025. Nenhum código aqui lê `steps`.
- **RN-048/049** — **não implementadas nesta task, por decisão de escopo explícita e aprovada na Análise da Task**. `solicitarRota` só tem uma guarda mínima (`code !== "Ok"` → erro genérico, sem retry, mensagem que não menciona tarifa) — consistente com o plano aprovado, que atribuiu a política completa (retry, mensagens por código, bloqueio de exportação) à TASK-022. Verificado no próprio arquivo desta task ([cliente-osrm.ts:29-36](../../src/formulario/roteamento/cliente-osrm.ts#L29-L36)); **não verificado nesta revisão** se a TASK-022 já existe implementada (não faz parte deste diff).

## Specs verificadas

- **Spec 03 §3.1** — aderente: entrada é a sequência ordenada de paradas com coordenada no sentido do itinerário; pontos de rota tratados como fora de escopo, não como ausência de requisito.
- **Spec 03 §3.2** — aderente: string de query idêntica à especificada, geometria sempre de `overview` (nunca dos steps).
- **Spec 03 §3.3** — aderente: tabela de extração implementada (`.geometry`→`geometria` cópia direta; `.legs[i].distance/duration`→`trechos[i]`); caso sem pontos de rota com `legs.length == paradas.length - 1` respeitado e validado.
- **Spec 03 §3.4** — aderente: half-up 2 casas, duração inteira, totais por soma dos trechos já arredondados — literalmente o exemplo de fechamento da spec foi replicado no teste.
- **docs-dev/13** — aderente: cliente HTTP fino sem SDK; `formulario/roteamento` não é importado por `comparador/` (confirmado por `grep` — nenhuma referência); URL base configurável via env var, espelhando o padrão de `NEXT_PUBLIC_TILES_URL` já usado em `src/shared/mapa/config.ts`, ancorado em **DEC-029** ("mantém a URL configurável... para permitir migração futura sem refatoração").

## Pontos corretos

- **Fronteira de escopo bem demarcada e testada, não só declarada**: o "fora de escopo" (pontos de rota, retry/erro, descrição textual) aparece como comentário **e** como guarda de execução que lança erro identificável (`legs.length` divergente), reduzindo o risco de a TASK-023 herdar uma função que mascara silenciosamente uma premissa quebrada.
- **Reuso do contrato em vez de duplicação de forma**: `Trecho` e a forma de `geometria` foram importados de `@/shared/contrato` (`esquemaGeometriaLineString`) em vez de redefinidos — evita drift entre o cliente OSRM e o schema Spec 02 se este mudar.
- **Teste do caso de fechamento é o correto, não um teste feliz genérico**: a suíte de `extrairRota` inclui exatamente o cenário que separa "soma dos trechos arredondados" de "total bruto arredondado" (RN-040/050), que é o ponto mais fácil de errar nesta regra.
- **Nenhum arquivo pré-existente tocado** — confirmado por `git show --stat` do commit: só arquivos novos.
- Nomenclatura oficial preservada (parada, itinerário, trecho, rota); nenhum termo traduzido ou inventado.

## Problemas encontrados

Nenhum bloqueante. Duas observações menores, sem violar RN Alta nem NEG-xxx:

1. **Duplicação de arredondamento half-up.** `arredondaHalfUp` (novo, `extrair-rota.ts:47-52`) e `arredondar2` (já existente em [validacoes-estruturais.ts:22-25](../../src/shared/contrato/validacoes-estruturais.ts#L22-L25)) implementam a mesma semântica half-up com o mesmo truque de `Number.EPSILON`, em módulos diferentes. Consolidar seria refator de um arquivo existente fora do escopo desta task — corretamente não feito aqui, mas a duplicação persiste no repositório. **Follow-up**, não condição de merge.
2. **Inconsistência estilística menor entre `arredondaHalfUp(distancia, 2)` e `Math.round(leg.duration)` direto** ([extrair-rota.ts:91](../../src/formulario/roteamento/extrair-rota.ts#L91) vs. linha 94) — funcionalmente equivalentes (o parâmetro `casas=0` de `arredondaHalfUp` reduz ao mesmo `Math.round`), mas usar o helper para os dois campos teria sido mais uniforme. Cosmético, sem impacto de comportamento.

## Violações de escopo

Nenhuma. O diff contém apenas o módulo `formulario/roteamento/` e seus testes; nenhuma alteração em `docs/specs/**`, no schema do contrato, em outros módulos de `formulario/` ou em `shared/`. Retry/erro (TASK-022), pontos de rota/`waypoints` (TASK-023) e descrição textual (TASK-025) foram deliberadamente deixados de fora, com a guarda de `code` explicitamente marcada como transitória.

## Testes avaliados

- **Cobertura das RN da task:** sim — RN-047 (URL), RN-050 (arredondamento/totais), RN-041 (mapeamento 1:1 + invariante), RN-014 (unidade) todas com teste dedicado.
- **Casos inválidos testados:** sim — menos de 2 paradas (RN-047/034); `legs.length` divergente (RN-041); resposta sem `routes[0]`; `code` `NoRoute`/`NoSegment` sem retry; variável de ambiente em branco ignorada.
- **Regressão de UUID:** N/A — task não toca import/export/cópia de entidades.
- **OSRM mockado:** sim — todas as três suítes usam fixtures locais de resposta OSRM ou `fetch` injetado via `vi.fn()`; nenhuma chamada de rede real. `roteamento-cliente-osrm.test.ts` inclusive verifica `toHaveBeenCalledTimes(1)` para provar ausência de retry.
- **Fixtures canônicas:** N/A para esta task — os mocks são do envelope de resposta do OSRM (formato externo), não do JSON de operação (Spec 02); não há fixture canônica de resposta OSRM no repositório para reutilizar, e inventar uma ad hoc aqui é o esperado (categoria 4 de `08-TEST_STRATEGY`, que não exige fixtures canônicas para este tipo de teste).
- **Suíte executada:** **verde**, resultados obtidos nesta mesma sessão de trabalho (antes desta revisão, sem alterações no working tree desde então): `npm run typecheck` sem erros; `npm run lint` sem avisos; `npm test` **478/478** (35 arquivos), incluindo os 23 casos novos desta task rodados isoladamente antes da suíte completa.

## Checklist 07

- **Escopo (6 itens):** 6 OK — pertence ao ROTA; nenhum workflow/status/persistência introduzido; escopo exato (nada "aproveitado"); toda decisão cita `RN-xxx`/`Spec XX §YY` ou está marcada como inferência controlada (URL base configurável, tipo de retorno parcial, divisão com a TASK-022).
- **JSON — contrato (9 itens):** N/A em quase todos (task não produz `rota` congelada completa nem toca o schema); os aplicáveis — "distâncias em km, durações em segundos, arredondamento half-up 2 casas" (RN-014/050) e "dados congelados não recalculados por leitores" (RN-015, N/A — este módulo é a própria fonte do cálculo, não um leitor) — **OK**.
- **Domínio (9 itens):** N/A — nenhuma entidade de domínio (Seção/Local/Serviço/Viagem) tocada.
- **Comparador (7 itens):** N/A — módulo não é acessível pelo Comparador (confirmado: nenhuma referência de `comparador/` a `formulario/roteamento`).
- **Roteamento (5 itens):** 2 aplicáveis e atendidos — "distância roteada vem exclusivamente do OSRM, convertida na fronteira" (RN-047/050) **OK**; "pontos de rota apenas condicionam o traçado, invariante preservado" (RN-041..043) **OK para o caso sem pontos de rota** (o único coberto por esta task). Os demais 3 itens ("OSRM indisponível bloqueia", "abrir JSON não chama OSRM", "mensagens não mencionam tarifa") são **N/A** — pertencem à TASK-022/024, não implementados aqui por escopo.
- **UI/PDF (6 itens):** N/A — task não toca UI nem PDF.
- **Testes (6 itens):** 6 OK — toda RN implementada tem teste; casos inválidos cobertos; regressão de UUID N/A (justificado); OSRM mockado; fixtures ad hoc justificadas (N/A canônicas); verificações executadas e reportadas com resultado real.
- **Resultado:** **0 itens violados**, 14 OK, ~29 N/A (todos justificados por escopo).

## Pendências

- **Follow-up (sem task própria ainda):** consolidar `arredondaHalfUp` (`extrair-rota.ts`) e `arredondar2` (`validacoes-estruturais.ts`) num único helper compartilhado, quando alguma task futura tocar um dos dois arquivos — não vale abrir refator isolado só para isso agora.
- **Depende desta task:** TASK-022 (tratamento de falha) substituirá a guarda mínima de `cliente-osrm.ts`; TASK-023 (pontos de rota) estenderá `extrairRota` além do mapeamento 1:1; TASK-024/025 completarão `ResultadoRotaOsrm` até o objeto `rota` congelado final (`descricao_itinerario`, `pontos_de_rota`). Nenhuma dessas foi verificada nesta revisão (fora do diff).
- **Q-xxx:** nenhuma aberta por esta task.

## Decisão

**Aprovado.** Escopo exatamente o da task (URL, extração, conversão — nada de retry, pontos de rota ou descrição), as três RN centrais (RN-047, RN-050, RN-041) atendidas com teste incluindo o caso de fechamento mais sensível da regra de arredondamento, contrato JSON e UUIDs intocados (N/A, nenhuma entidade de domínio envolvida), OSRM sempre mockado, checklist sem itens violados, verificações (typecheck/lint/478 testes) verdes. As duas observações registradas (duplicação de `arredondaHalfUp`/`arredondar2`; uso não uniforme do helper para duração) são cosméticas, não violam nenhuma RN de criticidade Alta nem NEG-xxx, e ficam como follow-up — não são condição de merge. Próxima task pode prosseguir (`docs-dev/00` §3).
