# Revisão da TASK-061

**Revisor:** Claude (assistido) — invocado por ghmfrade
**Data:** 2026-07-17
**Commit/branch revisado:** `4126530` (branch `redesign`)

## Resultado

- [ ] Aprovado
- [x] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Promove `ServicoEmConstrucao → Servico` completo no fluxo "novo" ao concluir a edição
do itinerário (DEC-053/opção A da Q-034). Introduz o par unificado
`servicosDaSessao`/`comServicosDaSessao` ([`sessao.ts:110-134`](../../src/formulario/sessao.ts#L110-L134))
como caminho único de leitura/escrita de Serviços completos nos dois modos, com novo campo
de sessão efêmero `servicos?: Servico[]` no modo "novo". A função pura de promoção fica em
[`promocao-servico.ts`](../../src/formulario/itinerarios/promocao-servico.ts); o gatilho é fiado em
[`etapa-itinerarios.tsx`](../../src/formulario/itinerarios/etapa-itinerarios.tsx). Viagens, Matrizes,
pendências e `itinerariosAoVivoDaSessao` passam a ler pelo caminho unificado (deixam de receber `[]`
no modo novo). 13 testes unitários novos; suíte completa verde (895).

## Regras RN verificadas

- **RN-001/002/004** (UUID preservada na promoção) — **atendida**. `promoverServico` herda `servico.uuid` sem regeneração ([`promocao-servico.ts:54`](../../src/formulario/itinerarios/promocao-servico.ts#L54)); testado em `promocao-servico.test.ts` (assert `uuid` = SERVICO_UUID) e round-trip em `sessao.test.ts:47-51`. Regra crítica nº 1 provada por teste.
- **RN-054..057** (matriz reconciliada no mesmo commit da rota) — **atendida**. Promoção chama `matrizDistanciasDoServico` no mesmo objeto ([`promocao-servico.ts:63`](../../src/formulario/itinerarios/promocao-servico.ts#L63)); o caminho do Serviço já completo usa `servicosComItinerarioAtualizado`, que reconcilia junto ([`etapa-itinerarios.tsx:305-312`](../../src/formulario/itinerarios/etapa-itinerarios.tsx#L305-L312)). Matriz conferida no teste.
- **RN-048** (OSRM falho/`sem-rota` não promove Serviço incompleto) — **atendida**. `promoverServico` retorna `null` quando o estado do sentido não é `congelada`/`recalculada`; testado (`[inválido] estado sem-rota → não promove`).
- **RN-034** (mín. 2 paradas) — **atendida**. Guarda `paradas.length < 2 → null`; testado.
- **RN-038** (1 itinerário para ida/volta, 2 para "ambos"; "ambos" só com os dois prontos) — **atendida**. Testado nos dois cenários "ambos".
- **RN-018** (documento válido exige ≥ 1 Serviço) — **atendida na intenção da task**: a promoção move o Serviço para `sessao.servicos`, então Viagens/Matrizes voltam a enxergá-lo; o gate de exportação em si é de outra task.
- **RN-096 / NEG-004** (nada persistido antes da exportação; sessão efêmera) — **atendida**. `servicos?` é campo de sessão, documentado como não-contrato ([`sessao.ts:124-132`](../../src/formulario/sessao.ts#L124-L132)); nenhuma alteração de `shared/contrato`/schema.

## Specs verificadas

- **Spec 02 §6 / §10** (`Servico`: `itinerarios` ≥ 1, `matriz_distancias`; itinerário: paradas ≥ 2) — **aderente**. O `Servico` promovido monta `itinerarios[]` com paradas+rota e `matriz_distancias` reconciliada. `viagens: []` é deliberado (a preencher na etapa Viagens, Spec 04 §8) — logo o Serviço é estruturalmente completo mas só passa no `esquemaServico` strict após ≥ 1 viagem (RN-039). Distinção sessão × exportação corretamente documentada e testada.
- **Spec 04 §7/§8/§9.1** (montagem do itinerário; recálculo automático ao concluir) — **aderente**. Promoção/reconciliação ocorre no handler de conclusão do recálculo.

## Pontos corretos

- Refatoração para caminho único (`servicosDaSessao`/`comServicosDaSessao`) elimina o padrão duplicado `sessao.modo === "carregado" ? … : []` em 4 pontos, sem alterar o comportamento do modo carregado (round-trip testado nos dois modos).
- Promoção é função **pura** e testável isolada de OSRM (recebe `estados` já resolvidos) — não há dependência de serviço real (mock N/A por design).
- Atomicidade: mover de `servicosEmConstrucao` para `servicos` acontece num único `comServicosDaSessao`, sem janela de duplicidade.
- Sem alteração de contrato/schema, `data-testid`/`aria-*` ou JSX (todas as edições `.tsx` são fluxo de dados) — DEC-050 preservado.
- Escopo cirúrgico: **não** tocou o CRUD de `servicos.tsx` (corretamente deixado para a TASK-072) nem o contrato.

## Problemas encontrados

(ordenados por severidade — nenhum viola RN Alta/NEG-xxx)

1. **[Ressalva — cobertura de teste] A fiação da promoção em `etapa-itinerarios.tsx` não tem teste direto.** As funções puras (`promoverServico`, `servicosDaSessao`, `comServicosDaSessao`) estão bem cobertas, mas o *glue* que as conecta no handler de conclusão — `paradasPorSentidoDoServico`/`estadosPorSentidoDoServico` ([`etapa-itinerarios.tsx:98-126`](../../src/formulario/itinerarios/etapa-itinerarios.tsx#L98-L126)) e o ramo que promove, filtra `servicosEmConstrucao` e anexa a `servicos` ([`etapa-itinerarios.tsx:381-398`](../../src/formulario/itinerarios/etapa-itinerarios.tsx#L381-L398)) — é exercitado só estruturalmente. O item "Integração" de *Testes esperados* da task ("após concluir itinerário no modo novo, `servicosDaSessao`/leitura de Viagens e Matrizes retorna o Serviço") não foi entregue como teste; fica coberto de fato apenas pelo E2E da **TASK-062**.
2. **[Ressalva menor — verificação] E2E não reexecutado nesta revisão.** O critério "E2E existentes verdes" não foi validado por execução Playwright aqui; apoia-se em: nenhum seletor `data-testid`/`aria-*` alterado e nenhuma mudança de JSX. Estruturalmente verde, mas não executado.

## Violações de escopo

Nenhuma. A entrega implementa exatamente a promoção + caminho unificado de leitura/escrita. Não introduz workflow, campo de contrato, R$, backend nem persistência. Não invade o CRUD de Serviços (TASK-072) nem o E2E (TASK-062).

## Testes avaliados

- **cobertura das RN da task:** sim para as regras da promoção (RN-001/002/004, 034, 038, 048, 054..057 via matriz, 096/NEG-004). Falta: teste de **integração** da fiação em `etapa-itinerarios.tsx` (ver Problema 1).
- **casos inválidos testados:** sim — `sem-rota`, < 2 paradas, estado ausente, "ambos" com só ida pronta.
- **regressão de UUID:** sim — assert direto + round-trip nos dois modos (`sessao.test.ts`).
- **OSRM mockado:** N/A — a promoção é pura e recebe estados já resolvidos; nenhum teste toca o serviço real.
- **suíte executada com resultado:** **verde** — `typecheck` limpo, `lint` limpo, `vitest run` 73 arquivos / 895 testes passando (inclui os 13 novos).

## Checklist 07

- **Escopo (6):** todos ok.
- **JSON (9):** ok/N-A — sem campo novo de contrato (`servicos?` é sessão), UUID preservada, sem R$, sem recálculo por leitor.
- **Domínio (10):** ok/N-A — identidade por UUID mantida (RN-001), `numero_n` não usado como identidade (RN-006), extremos continuam Seções.
- **Comparador (7):** N/A — não tocado.
- **Roteamento (5):** ok/N-A — RN-048 (sem-rota não promove) atendida; abrir JSON segue sem OSRM.
- **UI/PDF (10):** N/A — sem mudança de JSX/PDF; `data-testid`/`aria-*` intactos (DEC-050).
- **Testes (6):** 5 ok; 1 ressalva — regra da fiação de integração sem teste dedicado (Problema 1).

Resultado: itens aplicáveis atendidos, com **1 ressalva** de cobertura no bloco Testes.

## Pendências

- **Condição de merge:** nenhuma bloqueante. As ressalvas são follow-ups aceitáveis dado que a **TASK-062** (E2E ponta a ponta, já planejada e dependente desta) cobrirá a fiação de integração e revalidará os E2E existentes.
- **Follow-up:** garantir que a TASK-062 exercite explicitamente o ramo de promoção do handler (`etapa-itinerarios.tsx`), fechando a lacuna do Problema 1; ou, se preferível, adicionar um teste de integração leve do handler antes dela.
- **Nota (pointer, verificado):** o Serviço promovido nasce com `viagens: []`; a pendência bloqueante "itinerário sem viagem" é TODO explícito da **TASK-028** ([`pendencias.ts:157`](../../src/formulario/pendencias/pendencias.ts#L157)) — não é regressão desta task, e o Serviço incompleto vive só em sessão (não exportável). A **TASK-072** cobrirá o CRUD da etapa Serviços sobre os promovidos.

## Decisão

**Aprovado com ressalvas.** Escopo exato, sem violação de RN Alta nem NEG-xxx; a regra crítica nº 1 (UUID, RN-004) está provada por teste de round-trip; contrato e design system intactos; suíte verde. As duas ressalvas — ausência do teste de integração da fiação em `etapa-itinerarios.tsx` e E2E não reexecutado nesta revisão — são desvios menores de verificação, endereçados pela TASK-062 (dependente desta e já planejada). Ficam como condições explícitas de acompanhamento, não de bloqueio de merge.
