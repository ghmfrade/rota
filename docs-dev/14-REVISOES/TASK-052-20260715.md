# Revisão da TASK-052

**Revisor:** Claude (revisão de aderência assistida — `/revisar-aderencia`)
**Data:** 2026-07-15
**Commit/branch revisado:** `34cf0e1` — "Implementa TASK-052: Tela inicial full-screen com cartões-carimbo" (branch `redesign`)

## Resultado

- [ ] Aprovado
- [x] Aprovado com ressalvas
- [ ] Reprovado

## Resumo da entrega

Restilização da tela inicial (Spec 04 §3, doc 18 §5) em dois arquivos, sem tocar em testes:

- `src/app/page.tsx` — hero full-screen centrado (`flex`/`justify-center`), `<h1>` e parágrafo com textos **intocados**.
- `src/formulario/tela-inicial/tela-inicial.tsx` — os dois cartões de entrada passam a usar `Painel` de `shared/ui`, com ícones-carimbo (`CarimboCarregar`/`CarimboCriar`) em moldura circular decorativa (`MolduraIcone`, local, `aria-hidden`); "Carregar" destacado como recomendado (`ring-2 ring-azul-600` + `Selo tom="azul"`); `alertdialog` e mensagens de erro restilizados via `Painel`/`Botao`; `style=` inline removido.

Nenhuma mudança de fluxo, validação, mensagem ou contrato JSON.

## Regras RN verificadas

- **RN-004** (UUIDs preservadas na importação) — **atendida / não afetada.** A task não toca `importarDocumento` nem qualquer caminho de import/export; o diff é exclusivamente de apresentação. E2E `tela-inicial.spec.ts:10` ("carregamento válido preserva UUIDs e exibe a mensagem obrigatória") passa. Texto "preserva as UUIDs das entidades existentes" mantido no cartão.
- **RN-016** (seleção de identidade Autos/empresa/tipo) — **atendida / não afetada.** Comportamento vive fora desta tela; E2E de identificação (4 testes) verdes.
- **RN-017** (identidade obsoleta bloqueia carregamento) — **atendida.** E2E `tela-inicial.spec.ts:44` ("identidade inexistente na lista estática bloqueia o carregamento (RN-017)") passa sem alteração de seletor.
- **RN-095/096** (sem workflow, sem servidor; exportar é o salvar) — **atendida.** A frase "Todo o processamento ocorre no navegador; nada é salvo no servidor — exportar o JSON de operação é o salvar" permanece byte-a-byte no `page.tsx`; smoke `smoke-pagina-inicial.spec.ts:6` ("carrega sem requisições externas") verde.
- **RN-013** (sem R$) — **atendida.** `grep 'R\$'` nos dois arquivos: nenhum achado.

## Specs verificadas

- **Spec 04 §3** ("Duas ações, lado a lado, com a primeira visualmente destacada como recomendada") — **aderente.** Os dois `Painel` ficam lado a lado (`flex flex-wrap`); o destaque do caminho "Carregar" é real e verificado no navegador: `acao-carregar` tem anel `rgb(37, 99, 235)` (= `azul-600`) 2px + `Selo "Recomendado"`, e `acao-criar-zero` não tem anel. Ver ressalva 1: o destaque funciona pelo *ring*, não pela borda pretendida.
- **Spec 04 §3.1** (fluxo de carregar; abrir não chama OSRM) — **aderente.** Fluxo intocado; smoke confirma ausência de requisição externa.
- **Spec 04 §3.2** (aviso obrigatório antes de criar do zero) — **aderente.** Texto do `alertdialog` conferido **caractere a caractere** contra o bloco de citação da spec: idêntico. E2E `tela-inicial.spec.ts:69` verde.
- **doc 18 §5** (tela inicial: hero centrado + dois cartões `Painel` + `Carimbo` pasta/folha, "Carregar" destacado) — **aderente com desvio justificado** (ver ressalva 2).
- **doc 18 §6.1/§6.3** (sem `style=`, sem hex/px avulso) — **aderente.** `grep` de `style=`, `#rrggbb` e `[Npx]` nos dois arquivos: nenhum achado.

## Pontos corretos

- **Escopo cirúrgico:** exatamente os dois arquivos previstos pela task; zero arquivo de teste tocado.
- **`data-testid` e `aria-*` intocáveis (doc 18 §1.2/§6.4) — comprovado por diff:** os 7 `data-testid` antes/depois são idênticos; `role="alert"` ×2, `role="alertdialog"` e `aria-labelledby` preservados. A única adição é `aria-hidden="true"` na moldura decorativa — correta, pois o ícone é redundante com o rótulo textual.
- **Decisão acertada de não usar `Carimbo` nos cartões:** o `Carimbo` de `shared/ui` é um `<button>` com `rotulo` obrigatório (→ `aria-label`). Usá-lo decorativamente injetaria um segundo botão dentro de `acao-criar-zero`, o que poderia quebrar a semântica do cartão e o E2E de "Criar do zero". A escolha está documentada no comentário do código (`tela-inicial.tsx:55-58`) e respeita doc 18 §1.2 ("comportamento prevalece").
- Tons de erro via token `text-erro`; `Selo`/`Botao`/`Painel` com as variantes do doc 18 §3.

## Problemas encontrados

(ordenados por severidade)

1. **[Média — ressalva] `border-azul-600` no cartão recomendado é código morto** (`tela-inicial.tsx:122`).
   `Painel` já fixa `border border-cinza-200` em `CLASSES_SUPERFICIE` (`painel.tsx:23`), e o `className` da chamada acrescenta `border-azul-600`. Em Tailwind, utilitários conflitantes de mesma especificidade resolvem-se pela **ordem de emissão no CSS**, não pela ordem na string de classes. No CSS compilado, `.border-azul-600` sai no byte 9665 e `.border-cinza-200` no byte 9717 — **`cinza-200` vence**.
   Verificado no navegador: `getComputedStyle(acao-carregar).borderColor` = `rgb(226, 232, 240)` (= `#e2e8f0`, `cinza-200`), e **não** `rgb(37, 99, 235)` (= `azul-600`).
   **Não viola RN de criticidade Alta nem NEG-xxx**, e o critério de aceite da Spec 04 §3 continua atendido porque o `ring-2 ring-azul-600` (propriedade distinta, não conflitante) renderiza e sustenta o destaque sozinho. O defeito é a classe morta, que declara uma intenção não realizada e engana o próximo leitor.
   *Correção sugerida:* remover `border-azul-600`, ou (melhor) dar ao `Painel` uma prop de destaque (ex.: `destacado`) que troque a classe de borda internamente em vez de sobrepor por `className`.

2. **[Baixa — observação] `MolduraIcone` duplica localmente o contrato visual do `Carimbo`.**
   Doc 18 §5 nomeia literalmente `Painel` + `Carimbo` para os cartões, e `carimbo.tsx:4-5` afirma ser "usado na sidebar de etapas (stepper) **e nos cartões da tela inicial**" — premissa da TASK-050 que não se sustentou, justamente porque `Carimbo` é interativo. O desvio é defensável (ver "Pontos corretos") e **não viola doc 18 §6.2**, cuja enumeração vinculante é "botão/campo/select/painel/selo/tabela/tooltip" — `Carimbo` não está na lista; os ícones (§4) vêm de `shared/ui`. Ainda assim, a moldura circular (`rounded-full border-2`) passa a existir em dois lugares → risco de drift visual.
   *Follow-up sugerido:* extrair a moldura para `shared/ui` (variante não-interativa do `Carimbo`, ou `MolduraCarimbo`), e corrigir o comentário de `carimbo.tsx`.

3. **[Baixa — observação] Divergência de tom na moldura neutra.** `MolduraIcone` tom `cinza` usa `bg-cinza-100` (`tela-inicial.tsx:63`), enquanto doc 18 §3 descreve o repouso do `Carimbo` como "fundo **transparente**". Decorativo e sem impacto funcional; só vira problema se a moldura for unificada com `Carimbo` (follow-up 2).

4. **[Informativo] `shadow-sombra-3` no `alertdialog` funciona, mas pela mesma mecânica frágil.** `Painel` fixa `shadow-sombra-2`; o `className` acrescenta `shadow-sombra-3` (`tela-inicial.tsx:185`). Aqui a ordem favorece o pretendido (`.shadow-sombra-2` byte 13201 < `.shadow-sombra-3` byte 13407) e o resultado está **correto** — verificado no navegador: `boxShadow` = `rgba(15, 23, 42, 0.16) 0px 8px 24px 0px` (= `sombra-3`, doc 18 §2). Mas o acerto depende da ordem de declaração dos tokens no `@theme`: reordenar `globals.css` quebraria isso **silenciosamente**. Mesma raiz do problema 1.

## Violações de escopo

**Nenhuma.** O diff toca apenas `src/app/page.tsx` e `src/formulario/tela-inicial/tela-inicial.tsx`, ambos no objetivo da task. Nada do "Fora de escopo" foi tocado: fluxos de import/criação, mensagens e validações intactos (`importarDocumento` não aparece no diff); shell do formulário (TASK-051) não alterado.

## Testes avaliados

- **cobertura das RN da task:** sim. A task é de apresentação e explicitamente "não reimplementar" comportamento; as RN citadas (RN-004/016/017) são cobertas pelos E2E preexistentes, todos verdes sem alteração de seletor. O destaque de recomendação (Spec 04 §3) tem asserção própria em `tela-inicial.spec.ts:86`.
- **casos inválidos testados:** sim (preexistentes, verdes): JSON malformado (`json_invalido`, Spec 04 §14), identidade inexistente na lista estática (RN-017), erro de listas estáticas.
- **regressão de UUID:** N/A — a task não toca import/export/cópia; os E2E de UUID preexistentes seguem verdes.
- **OSRM mockado:** N/A — nenhum caminho desta tela chama OSRM; o smoke confirma ausência de requisição externa.
- **suíte executada com resultado (verde):**
  - `npm run typecheck` → limpo, sem erros.
  - `npm run lint` → limpo, sem achados.
  - `npm test` (Vitest) → **65 arquivos, 827 testes, 827 passed** (32.5s).
  - `npm run build` → export estático OK (8 páginas geradas).
  - `npx playwright test` (suíte E2E completa) → **47 passed** (1.5m). Inclui `tela-inicial` (5) + `smoke-pagina-inicial` (1), confirmando a alegação do commit ("5/5 + smoke").
- **Nota de ambiente:** o ambiente estava sem os binários nativos (`@tailwindcss/oxide-linux-x64-gnu`, `@rolldown/binding-linux-x64-gnu`) e sem o Chromium do Playwright — `npm test` e `npm run build` falhavam por `MODULE_NOT_FOUND`, **sem relação com o commit**. Instalados (`--no-save` + `npx playwright install chromium`) para permitir a verificação real. A modificação pendente em `package-lock.json` (linha `"dev": true` em `fsevents`) é **anterior a esta revisão** e não faz parte do commit da task.

## Checklist 07

**Resultado: 18 itens ok, 28 N/A, 0 violados** (1 item atendido *com ressalva*).

- **Escopo (6):** todos ok. Não é SEI, sem workflow, sem status de pedido, sem persistência transacional, escopo exato, nenhuma regra inventada.
- **JSON (10):** N/A — a task não toca o contrato. (`Nenhum valor monetário` conferido mesmo assim: ok.)
- **Domínio (9):** N/A.
- **Comparador (7):** N/A.
- **Roteamento (5):** N/A.
- **UI/PDF (8):**
  - Nomes `Cidade - Nome da Seção` (RN-076) — N/A (não há Seção nesta tela).
  - Offsets não aparecem (RN-067) — N/A.
  - Matrizes km sem R$ — N/A.
  - Locais fora da grade — N/A.
  - Aviso de fronteira com o SEI nos PDFs (RN-077) — N/A.
  - Exportação bloqueada com pendências (RN-078) — N/A.
  - **UI aderente ao design system (DEC-050) — ok com ressalva:** componentes de `shared/ui`, tokens do `@theme`, zero `style=` inline, zero hex/px avulso. Ressalva: a classe `border-azul-600` não surte efeito (problema 1) e `MolduraIcone` é local (problema 2, desvio justificado).
  - **`data-testid`/`aria-*` preservados — ok:** comprovado por diff; E2E verdes sem alterar seletores.
- **Testes (6):** todos ok (ver seção acima); "regressão de UUID" e "OSRM mockado" marcados N/A por não aplicabilidade.

## Pendências

1. **Condição de merge:** resolver o problema 1 — remover a classe morta `border-azul-600` **ou** implementar destaque no `Painel` por prop. Correção de uma linha; o destaque visual já está garantido pelo `ring`, então não há risco de regressão do critério de aceite.
2. **Follow-up (não bloqueia):** unificar a moldura circular em `shared/ui` e corrigir o comentário de `carimbo.tsx:4-5` (problema 2). Como a decisão envolve a API de um componente compartilhado, cabe registrar como Q-xxx se houver dúvida real; caso contrário, vira escopo natural da varredura de consistência da **TASK-056**.
3. **Consequência sobre outras tasks (atenção real):** a mecânica de sobrescrever por `className` as classes fixas do `Painel` (`border-cinza-200`, `shadow-sombra-2`) é uma armadilha latente para **TASK-053, TASK-054, TASK-055 e TASK-056**, que vão restilizar as demais telas com o mesmo `Painel`. Qualquer sobrescrita de borda/sombra via `className` pode falhar silenciosamente conforme a ordem dos tokens no `@theme`. Recomenda-se que o `Painel` ganhe variantes explícitas antes dessas tasks. *(Afirmação restrita ao que foi lido nesta revisão: `painel.tsx` e o CSS compilado; não inspecionei o código das TASK-053..056 — elas ainda não foram implementadas.)*
4. Nenhuma Q-xxx aberta bloqueia esta task.

## Decisão

**Aprovado com ressalvas.**

A entrega cumpre o objetivo com escopo exato e disciplina exemplar quanto ao que o design system chama de intocável: os 7 `data-testid`, os `role`/`aria-*` e todos os textos obrigatórios da Spec 04 §3/§3.2 estão preservados — o aviso obrigatório foi conferido caractere a caractere contra a spec. A suíte completa (typecheck, lint, 827 unitários, build e 47 E2E) foi executada de verdade nesta revisão e está verde; a alegação do commit ("E2E tela-inicial 5/5 + smoke verdes") confere.

Nenhum achado viola RN de criticidade Alta nem NEG-xxx, e o critério de aceite da Spec 04 §3 (caminho "carregar" visualmente recomendado) está **de fato** atendido — verificado no navegador via anel `azul-600` + `Selo`, não apenas por inspeção de código. Por isso não é caso de reprovação.

A ressalva que vira **condição de merge** é o problema 1: `border-azul-600` é uma classe morta, sobrescrita por `border-cinza-200` do `Painel` por ordem de emissão no CSS. Hoje é cosmético (o `ring` sustenta o destaque), mas é uma afirmação falsa no código e o sintoma de um padrão de composição frágil que se propagaria pelo resto do bloco de redesign — daí a pendência 3, que considero o achado mais valioso desta revisão para as próximas tasks.
