# Revisão da TASK-090

**Revisor:** Codex
**Data:** 2026-07-21
**Commit/branch revisado:** `735c992` / `redesign`

## Resultado

- [ ] Aprovado
- [ ] Aprovado com ressalvas
- [x] Reprovado

## Resumo da entrega

A entrega cria o executor canônico da suíte completa, o servidor Next controlado em porta dedicada, o log efêmero com fingerprint e o comando de verificação. Também alinha `package.json`, Playwright, Next, as skills e os documentos operacionais ao novo protocolo.

Os arquivos centrais são `scripts/executar-test-all-log.mjs`, `scripts/executar-e2e-controlado.mjs`, `scripts/infraestrutura-test-all.mjs`, `scripts/verificar-log-test-all.mjs` e `testes/unitarios/scripts/infraestrutura-test-all.test.ts`.

## Regras RN verificadas

- RN-004 — não alterada e protegida pela regressão completa: a execução canônica registrou 1.065 testes unitários e 58 E2E verdes.
- RN-010/RN-013 — atendidas: nenhum campo, workflow ou valor monetário foi acrescentado ao contrato JSON; o diff se limita à infraestrutura de testes e documentação operacional.
- RN-035 — não alterada e coberta pela suíte completa reutilizada.
- RN-041/RN-042 — não alteradas e cobertas pela suíte completa reutilizada.
- RN-048 — atendida no recorte da task: os E2E continuam determinísticos e não houve introdução de fallback ou dependência do OSRM real.
- RN-052 — não alterada e coberta pela suíte completa reutilizada.
- RN-069 — não alterada e coberta pela suíte completa reutilizada.
- RN-078 — não alterada e coberta pela suíte completa reutilizada.
- RN-080 — atendida: o Comparador não foi alterado e a infraestrutura não introduz OSRM nem persistência.

## Specs verificadas

- Spec 01 §5/§6 — aderente: contrato client-side e identidade preservada não foram alterados; a regressão completa permaneceu verde.
- Spec 02 §10.1/§12 — aderente: extremos-Seção e UUIDs não foram alterados; a task apenas preserva sua evidência automatizada.
- Spec 03 §3.5/§3.6.2 — aderente: os cenários de falha do OSRM e abertura sem recálculo continuam na suíte E2E determinística.
- Spec 04 §11/§12/§14 — aderente: pendências, gate de exportação e mensagens não foram modificados e suas regressões ficaram verdes.

## Pontos corretos

- `npm run test:all:log` executa Vitest e E2E sequencialmente, devolvendo falha se qualquer código não for zero (`scripts/executar-test-all-log.mjs:178`, `scripts/executar-test-all-log.mjs:186`, `scripts/executar-test-all-log.mjs:208`).
- O caminho canônico desativa o `webServer` do Playwright e usa servidor próprio na porta 3100; a porta ocupada é recusada e o PID do Next é explícito (`playwright.config.ts:21`, `scripts/executar-e2e-controlado.mjs:124`, `scripts/executar-e2e-controlado.mjs:173`).
- Timeout, health check e encerramento somente da árvore do PID próprio foram implementados e possuem testes com processo testemunha (`scripts/infraestrutura-test-all.mjs:115`, `scripts/infraestrutura-test-all.mjs:132`, `scripts/infraestrutura-test-all.mjs:189`, `testes/unitarios/scripts/infraestrutura-test-all.test.ts:112`, `testes/unitarios/scripts/infraestrutura-test-all.test.ts:154`).
- O log final é promovido por `rename` somente depois do resumo e marcador final (`scripts/executar-test-all-log.mjs:211`, `scripts/executar-test-all-log.mjs:220`, `scripts/executar-test-all-log.mjs:222`).
- O protocolo ficou alinhado em `CLAUDE.md`, nas duas skills e em `docs-dev/04`, `07` e `08`.
- A evidência canônica foi reutilizada sem repetir a suíte: executor `Codex`, fingerprint `bb48af62511f099399f488b6ff5caa231542c6b67f17e0431fe35e6ac59c7774`, 1.065 unitários e 58 E2E verdes. O log registra o encerramento do PID 26164; na revisão, a porta 3100 estava livre e o PID ausente.

## Problemas encontrados

1. **Alta — o verificador aceita evidência produzida em outro working tree, contrariando um caso inválido explícito da TASK-090.** O gerador registra branch, commit e apenas o booleano de working tree sujo (`scripts/executar-test-all-log.mjs:158`), mas não registra a identidade/caminho do working tree. O verificador lê somente formato, executor, fingerprint, códigos e resultado (`scripts/verificar-log-test-all.mjs:17`) e, ao validar, calcula apenas o fingerprint do conteúdo atual (`scripts/verificar-log-test-all.mjs:59`). Portanto, copiar um log verde para outro working tree com o mesmo conteúdo produz `valido: true`, embora a task determine que isso não é evidência reutilizável (`docs-dev/06-BACKLOG_INICIAL.md:3635`). O defeito atinge o objetivo central de impedir reaproveitamento falso da evidência; não viola uma RN de negócio, mas reprova um critério funcional da própria task.
2. **Alta — faltam testes dos caminhos negativos do orquestrador real.** A task exige testar composição do resumo, códigos zero/não zero, timeout, interrupção e log truncado (`docs-dev/06-BACKLOG_INICIAL.md:3640`). Os testes atuais exercitam o analisador com strings e helpers isolados (`testes/unitarios/scripts/infraestrutura-test-all.test.ts:75`, `testes/unitarios/scripts/infraestrutura-test-all.test.ts:111`), mas nunca executam `executar-test-all-log.mjs` nem `executar-e2e-controlado.mjs` sob Vitest/Playwright vermelho, `SIGINT`, `SIGTERM` ou exceção. Assim, não provam que o arquivo temporário deixa de ser promovido, que o resultado geral fica vermelho e que a limpeza ocorre nesses caminhos reais. Também não existe teste que rejeite o caso de outro working tree do problema 1.

## Violações de escopo

- Nenhuma. As alterações em configuração do Next/Playwright, scripts, testes e documentos operacionais pertencem à infraestrutura transversal prevista. Não houve mudança em componentes de produção, contrato JSON, fixtures ou asserções funcionais existentes.

## Testes avaliados

- cobertura das RN da task: N/A para comportamento novo; as RN citadas são invariantes de regressão e a suíte completa ficou verde.
- casos inválidos testados: parcial — porta ocupada, health check, timeout de helper, log truncado/vermelho e cleanup por PID; faltam outro working tree, falha real de cada etapa, sinais e exceção no orquestrador.
- regressão de UUID (se aplicável): sim, pela suíte completa reutilizada (RN-004).
- OSRM mockado: sim; a suíte não depende do serviço público.
- suíte executada com resultado: log canônico válido reutilizado, executor `Codex`, fingerprint `bb48af62511f099399f488b6ff5caa231542c6b67f17e0431fe35e6ac59c7774`, 1.065 unitários e 58 E2E verdes; `npm run test:all:verificar`, lint e typecheck verdes. O build controlado também terminou verde dentro da etapa E2E.

## Checklist 07

Resultado: **15 itens ok, 37 N/A, 1 violado**.

### Escopo

- [x] A alteração pertence ao ROTA, e não ao SEI (RN-095).
- [x] Não introduz workflow (RN-095).
- [x] Não cria status de pedido além de `autos.status` (RN-011).
- [x] Não cria persistência transacional (RN-096).
- [x] Implementa somente o escopo da task.
- [x] Não inventa regra de negócio.

### JSON (contrato)

- [x] O JSON continua contendo apenas dados de operação (RN-010).
- [x] O JSON continua autossuficiente (RN-009).
- [x] Nenhum campo novo foi adicionado.
- [N/A] Preservação de UUID na importação — importação não alterada (RN-004).
- [N/A] UUID nova para entidades novas/cópias — criação não alterada (RN-002/007).
- [N/A] XOR de Parada — contrato não alterado (RN-033).
- [x] Nenhum valor monetário entrou no JSON (RN-013).
- [N/A] Unidades/arredondamento — cálculo não alterado (RN-014/050).
- [N/A] Dados congelados — leitores não alterados (RN-015).

### Domínio

- [N/A] Seção e Local separados (RN-025/031).
- [N/A] Ponto de rota não tratado como Parada (RN-042).
- [N/A] Identidade de Serviço (RN-001).
- [N/A] `numero_n` como identidade (RN-006).
- [N/A] Extremos-Seção (RN-035).
- [N/A] Conjunto de Seções Ida/Volta (RN-030).
- [N/A] Tipificação (RN-019..022).
- [N/A] Estratificação de Viagem (RN-061).
- [N/A] Contagens de feriado (RN-069).

### Comparador

- [N/A] Comparação por UUID (RN-081).
- [N/A] Classificação de entidades (RN-081..083).
- [N/A] Imutabilidade do comparativo (RN-080/097).
- [N/A] `status`/datas fora do diff (RN-012).
- [N/A] Bloqueio de Autos diferentes (RN-084).
- [N/A] Comparação estável de rota (RN-087).
- [N/A] Comparador sem OSRM/persistência (RN-080).

### Roteamento

- [N/A] Falha do OSRM bloqueante — código de produção não alterado (RN-048).
- [N/A] Distância exclusivamente do OSRM (RN-047/050).
- [N/A] Pontos de rota e invariante de trechos (RN-041..043).
- [N/A] Abertura sem OSRM (RN-052).
- [N/A] Mensagens de erro (RN-049).

### UI/PDF

- [N/A] Nomes de Seção (RN-076).
- [N/A] Horários/offsets (RN-067/076).
- [N/A] Matrizes (RN-076).
- [N/A] Locais no PDF/grade/descrição (RN-031/044/076).
- [N/A] Aviso de fronteira com SEI no PDF (RN-077).
- [N/A] Gate de exportação (RN-078).
- [N/A] Design system (DEC-050).
- [N/A] Preservação de `data-testid`/`aria-*` (DEC-050).

### Testes

- [N/A] Toda RN alterada possui teste — nenhuma RN ganhou comportamento nesta task.
- [ ] Casos inválidos foram testados — cobertura incompleta nos dois problemas acima.
- [N/A] Regressão UUID específica — a task não toca import/export/cópia (RN-004/007).
- [x] Testes de roteamento usam OSRM mockado.
- [N/A] Fixtures canônicas — a infraestrutura usa processos HTTP descartáveis, não JSON de operação.
- [x] Verificações direcionadas, lint, typecheck e build aplicável foram executados e reportados honestamente.
- [x] A evidência final veio de uma execução canônica e o verificador confirmou marcador, códigos e fingerprint atual.
- [x] A revisão reutilizou o log válido sem repetir a suíte.
- [x] A porta 3100 ficou livre, o PID próprio foi encerrado e nenhum processo alheio foi reutilizado ou finalizado.

## Pendências

- Vincular a evidência a uma identidade verificável do working tree e fazer o verificador rejeitar log copiado de outro working tree, sem impedir o caso válido de commit posterior com o mesmo conteúdo.
- Acrescentar teste negativo que reproduza a cópia entre working trees.
- Testar os scripts de orquestração reais sob Vitest vermelho, Playwright vermelho, timeout, `SIGINT`, `SIGTERM` e exceção, comprovando código não zero, não promoção do log temporário e limpeza dos PIDs próprios.
- Nenhuma Q-xxx nova: o comportamento esperado está fechado na própria TASK-090.

## Decisão

**Reprovado.** A execução feliz real está verde e o controle de porta/PID resolveu o teardown no baseline observado. Contudo, o verificador ainda aceita uma evidência que a task declara explicitamente inválida, e os caminhos negativos centrais do orquestrador não estão cobertos. Como o objetivo da TASK-090 é tornar a evidência confiável e o template reprova ausência de testes exigidos, a task permanece pendente até corrigir os dois achados e passar por nova revisão.
